---
type: runbook
project: agent-studio
status: active
updated: 2026-05-24
owners:
  - codex
  - interactive-note-taking-agent
  - product-manager
confidence: high
source_notes:
  - [[codex-obsidian-working-memory]]
  - [[autonomous-obsidian-ingestion-flow]]
  - [[../concepts/three-tier-agent-memory]]
  - [[../../raw/articles/2026-05-17-mitcham-ai-agent-operating-system]]
  - [[../../raw/articles/2026-05-17-ziwen-codex-knowledge-vault]]
  - [[../concepts/production-agent-system-design-canon]]
---

# Active Codex Context

## Purpose

This is the compact working-memory note Codex should read first when a future turn depends on project context. It prevents broad chat replay and keeps the vault as the source of truth.

## Current Project Shape

- Product: local-first realtime multi-agent content studio.
- Planning memory: this Obsidian vault.
- Deep system-design research memory: separate `system_design_vault/` Obsidian workspace at the project root.
- Product store: Postgres + pgvector only. No SQLite.
- Product interface: creator-facing Next.js app with voice/text input, draft preview, sources, artifacts, feedback, run activity, and memory policy. Cockpit/planning surfaces are internal/operator surfaces, not the main product app.
- Planning interface: Obsidian notes first; generated JSON/HTML viewers are secondary inspection outputs.
- Planning board: [[../../01-work-tracking/Agent Studio Kanban]] is the vault-only Kanban tracker for backlog, ready, in-progress, blocked, done, and review-watch items. The creator app must not render this board or become a project-management dashboard.
- Model policy: OpenRouter is now the default realtime provider. Cursor-agent live-voice status and the product proof packets route live dialogue through OpenRouter text reasoning (`deepseek/deepseek-v4-flash`) over LiveKit plus Kokoro spoken output; raw microphone PCM is not sent to OpenRouter, and the product path uses the Live Voice panel text-turn composer for provider-backed dialogue. Source: `agent_progress_vault/06-live-voice/status-snapshot-2026-05-23.md`, `agent_progress_vault/06-live-voice/openrouter-livekit-live-dialogue.md`, and the accepted provider-backed live-voice proof record for run `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`.
- Routing boundary: Do not use Hugging Face or Gamma/Gemma as the active realtime default. Treat local MLX on `:8080`, the MLX proxy on `:8090`, and `GEMMA4_MULTIMODAL_ENDPOINT_URL` as no longer required for the current OpenRouter live-dialogue path; live voice now has product-owned accepted proof, while overall completion still requires accepted external publication proof and reviewed closure.
- Current credential snapshot: the UUID proof workspace now has configured OpenRouter and LiveKit file/URL inputs for live voice (`OPENROUTER_API_KEY_FILE`, `OPENROUTER_LIVEKIT_URL`, `LIVEKIT_API_KEY_FILE`, `LIVEKIT_API_SECRET_FILE`). The provider-backed live-voice proof record is now accepted for run `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`; Publication inputs remain blocked on LinkedIn credential, policy acknowledgement, durable destination, and rollback/postcondition evidence. Do not treat the overall objective as complete until external publication proof is also accepted and completion status is rechecked.
- Current proof-plan closeout path: `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now expose `closeout_commands` with the same post-capture sequence: completion status, closure-review template, closure-review validation, closure-review recording, closure-review status, and reviewed blocker-state update note. The update note remains `goal_completion_claimed=false`; it does not complete the active objective.
- Current system-design proof-plan sequence handoff: the generated system-design viewer now carries `proof_plan_operator_sequence` and `proof_plan_closeout_commands_by_proof` from the UUID proof plan and renders them in the Objective Completion Audit detail. Architecture reviewers can inspect the ordered operator path plus closeout commands without leaving the system-design projection; this remains no-secret handoff visibility only.
- Current system-design completion recovery handoff: the generated system-design viewer's top-level Current proof gate now carries `completion_next_action` and `completion_next_action_commands` from the current blocker matrix. Architecture reviewers can see the recovery route without drilling into packet-local gates or raw matrix JSON.
- Current system-design aggregate diagnostics handoff: the generated system-design viewer's Operator input readiness diagnostics now carry matrix-owned `required_fields`, `field_contracts`, and `field_statuses`, not only blocked/configured/grouped fields. Architecture reviewers can inspect the aggregate no-secret input contract from the system-design projection before drilling into per-proof routes.
- Current system-design readiness-status handoff: the same diagnostics now include matrix-owned `status=blocked_by_operator_inputs`, so architecture reviewers can see the aggregate readiness state alongside checked-at, exit policy, fields, contracts, and field statuses.
- Current system-design route evidence handoff: each Objective Completion Audit per-proof route now carries `required_evidence_after_unblock` from the current blocker matrix. Route-first architecture reviewers can see the live voice and publication post-unblock evidence gates without returning to the top-level blocker map.
- Current current-state completion recovery handoff: the UUID `current-blocker-matrix.json` now exposes `completion.next_action` and top-level `completion.next_action_commands`, and `current-proof-status.md` plus `operator-unblocker-checklist.md` render the same recovery chain inside `## Current Gate` with one completion-status recheck. This is status-first recovery routing only; provider-backed OpenRouter LiveKit live voice proof is accepted, and external publication proof remains blocked.
- Current completion-status JSON recovery handoff: when `external-publication-proof` is the latest failed proof and its LinkedIn/publication operator inputs are still blocked, `provider-proof-completion-status` now emits the strict `provider-proof-operator-input-readiness --fail-on-blocked` gate, credential snapshot, proof-plan, matrix, status, checklist refresh, and one completion-status recheck. It must not jump straight to product/publish capture until operator-input readiness is clear. Accepted `provider-backed-live-voice-proof` commands stay excluded.
- Current completion-status custom workspace handoff: `provider-proof-completion-status --output-dir <provider-proof-workspace>` is now supported by the CLI and preserves the same strict operator-input recovery route for non-default proof workspaces. The final completion-status recheck command also carries the same `--output-dir`, including in current-gate packet exports, so agents do not fall back to the default UUID path after a custom-workspace retry.
- Current external-publication recovery chain correction: matrix-derived current-gate completion commands now use the same strict operator-input gate while publication inputs are blocked; only after readiness is clear should operators continue to product/publish preflight capture, preflight validation, `build-distribution-package > distribution-package.json`, proof-record template, record validation, record audit, and completion-status recheck. Accepted `provider-backed-live-voice-proof` commands remain excluded from current-gate recovery.
- Current packet-level completion recovery handoff: matrix-derived `operator_proof_packets[*].current_gate` now also carries `completion_next_action` plus nested `completion_next_action_commands`, and both `current-proof-status.md` and `operator-unblocker-checklist.md` render those commands inside each packet block. Packet-first A2A/review agents can follow the safe recovery route without separately opening top-level `completion-status.json`.
- Current browser-visible packet recovery handoff: proof-readiness, OpenRouter voice boundary, publication boundary, and the generated system-design viewer now embed the refreshed operator proof packets and visibly render packet current-gate completion recovery. Browser-first agents can inspect the same recovery commands that JSON/Markdown packet consumers receive.
- Current proof-plan packet recovery authority: compact proof-plan and README operator packets now carry `current_gate_recovery_authority`, pointing operators to `current_matrix_packet_ref` plus `current_matrix_operator_packet_ref` for matrix-owned current gate, completion next action, and recovery commands. This avoids copying live current-gate state into plan-first packets while making the route explicit.
- Current browser-visible proof-plan recovery authority: proof-readiness, OpenRouter voice boundary, publication boundary, and generated system-design viewer detail panes now render `current_gate_recovery_authority` inside the visible proof-plan operator packet section, so browser-first reviewers see the same matrix-authority routing that JSON and README consumers receive.
- Current proof-plan blocker summary: proof-plan entries now expose `blocking_reasons`, so placeholder run ids and placeholder-only credential configuration are visible at the same time instead of being hidden behind one primary status.
- Current credential setup handoff: proof-plan entries, proof-readiness exports, voice/publication boundary maps, and proof-workspace READMEs expose no-secret setup/refresh commands. For live voice, the current configured inputs are OpenRouter + LiveKit file/URL names only; do not add Hugging Face or Gamma/Gemma as realtime defaults. Publication setup still uses file-backed LinkedIn and other social token alternatives without printing values, and literal `<...>` direct/file placeholders do not count as configured.
- Current repo hygiene handoff: the root `/repo_check_temp/` scratch checkout is explicitly ignored as local scratch/course work because it may contain PDFs, snapshots, and nested repository state; the rule is anchored so nested product paths with the same name are not hidden. CI dependency sync is guarded to use the committed `uv.lock`, CI now runs `uv lock --check` before Python dependency sync, and `uv.lock` is tracked/not ignored. Keep scratch research checkouts, local command logs, and bulky generated artifacts out of the PR surface; use lightweight notes or source-attributed summaries in the vault instead.
- Current app credential setup path: `/api/local-secret-files` can write local `0600` publication token files for Instagram, LinkedIn, X, and Substack as well as HF/LiveKit/Tavily. `/api/runs/{run_id}/publish-readiness` receives the current `Settings` credential map, so app-written publication files can satisfy non-live credential presence checks without direct token env values. This is not accepted external publication proof.
- Current Cursor live-dialogue coordination snapshot: `agent_progress_vault/06-live-voice/status-snapshot-2026-05-23.md` reports Docker Postgres and LiveKit are up, `.secrets/openrouter_api_key`, `.secrets/livekit_api_key`, and `.secrets/livekit_api_secret` are present, `.env` has OpenRouter plus LiveKit configured, and `openrouter-livekit` is the selected ready realtime provider. The UUID operator-input packet now reflects `OPENROUTER_API_KEY_FILE`, `OPENROUTER_LIVEKIT_URL`, `LIVEKIT_API_KEY_FILE`, and `LIVEKIT_API_SECRET_FILE` as configured; the provider-backed live-voice proof is accepted for run `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`. Do not restart or modify these processes from the vault-sync subagent; use this only as coordination input.
- Current OpenRouter live-dialogue readiness implementation: `/api/voice-runtime-readiness` now has regression coverage for OpenRouter text-turn dialogue as the distinct `openrouter-live-dialogue-reasoning` ready path when OpenRouter key-file routing and chat completions are configured, without requiring `GEMMA4_MULTIMODAL_ENDPOINT_URL`. Hugging Face and Gamma/Gemma endpoint checks are legacy/non-default for this flow; the live-voice acceptance path is OpenRouter + LiveKit + Kokoro.
- Current proof-workspace recovery snapshot: refreshed UUID preflight captures show `voice-runtime-readiness.preflight.json` overall `ready` for OpenRouter text-turn dialogue plus LiveKit/Kokoro/Rust/agent checks, and `provider-backed-live-voice-proof.preflight-validation.json` validates the current required runtime checks. The live-voice proof has since been captured and recorded as accepted; regenerated current packets should treat external publication as the remaining proof blocker.
- Current live-smoke evidence snapshot: `provider-smoke-ledger.live-openrouter.json` for run `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e` passes with `execute_live_calls=true`, ledger artifact `94857bb9-c5eb-4174-8bc5-6687bd8befbe`, events `105510` through `105516`, selected realtime session `ebd43531-86e3-4af1-ade0-15ac8d7184bf`, OpenRouter `deepseek/deepseek-v4-flash` first text delta at 1111.171 ms, Kokoro `hexgrad/Kokoro-82M` first audio chunk at 4946.351 ms, and first-audio end-to-end latency at 6057.54 ms. The fresh headless LiveKit capture recorded 25 agent events, and the current realtime voice timing ledger is `ready` with 8/8 stages measured, ledger artifact `7e932381-4bf4-4206-a490-58d6a4ca7880`, including audio bridge, speech start, turn correlation, first text/audio, barge-in, cancellation acknowledgement, and cancelled-turn proof. `provider-backed-live-voice-proof` is accepted in all configured audit targets; `external-publication-proof` is still the remaining completion blocker.
- Current frontend live-smoke proof display: the `openrouter-livekit` smoke step carries `reasoning_model_id` and `reasoning_ttft_ms`, not legacy `gemma_model_id` / `gemma_ttft_ms`. The frontend proof formatter now renders current accepted smoke evidence as `OpenRouter/Kokoro transport`, `OpenRouter TTFT`, and `Reasoning: deepseek/deepseek-v4-flash`; legacy Gemma labels remain only for explicit legacy smoke steps.
- Current project-memory and publish-readiness durable event boundary: `project_memory_recorded`, publish-readiness `feedback_recorded`, `human_feedback_gate_opened`, and `publish_readiness_checked` event payloads now use `safe_realtime_metadata` before persistence. Private `AgentMemory`, `FeedbackItem`, and publish-readiness API responses intentionally retain trusted local content, while durable events redact Bearer/HF/Tavily-shaped strings, including lower-case bearer variants created by normalized publish-channel names, and drop sensitive metadata keys such as `authorization` and `hfToken`.
- Current feedback and memory durable event boundary: `feedback_recorded`, `feedback_routed`, and `memory_recorded` event payloads emitted by `FeedbackRoutingWorkflow`, `RevisionWorkflow`, and the direct `/api/memories` record path now use `safe_realtime_metadata` before persistence. Stored `FeedbackItem` records, routed public A2A messages, and private `AgentMemory` records intentionally retain original human feedback or memory text, while durable feedback/memory events redact Bearer/HF/Tavily-shaped strings and drop sensitive metadata keys such as `authorization` and `hfToken`.
- Current conversation-turn durable event boundary: `conversation_turn_recorded` event payloads from direct run turn recording, content workflow capture, and conversation router user/assistant replies now use `_safe_realtime_metadata` or `safe_realtime_metadata` before persistence. Private stored `ConversationTurn` records and API responses intentionally retain original transcript content, while durable event payloads redact Bearer/HF/Tavily-shaped strings and drop sensitive metadata keys such as `authorization`, `clientSecret`, `hfToken`, and `sessionToken`.
- Current operator sequence handoff: proof-plan entries now expose `operator_sequence`, so operators see the ordered path from credential setup and safe run-id replacement through capture, validation, audit recording, completion status, closure review, and blocker-state update after approval.
- Current proof-capture after-unblock handoff: `proof_capture_commands_after_unblock` includes the evidence-producing proof execution commands between preflight validation and proof-record template generation. Live voice routed through managed voice-agent process capture, runtime-health ledger, OpenRouter LiveKit provider-smoke ledger, and realtime voice timing ledger and is now accepted. External publication still routes through `build-distribution-package` after explicit action-time approval, now saving the output as `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/distribution-package.json` before proof-record validation/recording. The raw long-running `run-voice-agent --dev` command must not appear as an inline sequential proof-capture step.
- Current live voice proof-record schema: accepted provider-backed live voice records now require `voice_agent_process_start_artifact_id` in addition to preflight, runtime-health, provider-smoke, realtime timing, LiveKit, participant, and workspace-validation artifact ids. Validation rejects an accepted voice record that omits the managed process-start artifact, so proof-record acceptance cannot skip the supervised voice-agent start capture.
- Current proof workspace README schema handoff: generated proof workspace READMEs now include `proof_record_schema` metadata plus `proof_record_required_fields` under each proof section, so the command-source workspace has the accepted-record type, allowed outcomes, state field, and required fields without requiring a separate matrix/status lookup. For non-UUID workspace labels, the README keeps the product-run gate first and uses `<run-id>` paths for proof preflight/record handoff until a durable product UUID exists.
- Current operator proof packet schema handoff: `provider-proof-current-blocker-matrix` now includes full `proof_record_schema` metadata inside each `operator_proof_packets` entry, including `artifact_type`, `allowed_outcomes`, `state_field`, and `required_fields`, while preserving `proof_record_required_fields` for compact field-list consumers. Packet-first A2A/review agents can validate the accepted proof-record contract without separately reading `proof-plan.json`.
- Current operator proof packet reciprocal proof-plan refs: `operator_proof_packets` in the concrete UUID `current-blocker-matrix.json` now point back to `proof-plan.json` with `proof_plan_packet`, `proof_plan_packet_ref`, `proof_plan_packet_command`, and `proof_plan_operator_packet_ref`. `current-proof-status.md`, `operator-unblocker-checklist.md`, proof-readiness, OpenRouter voice boundary, publication boundary, and the system-design viewer carry the same reciprocal routing where applicable. This is packet navigation and regeneration metadata only; it does not execute or accept provider/publication proof.
- Current Markdown operator proof packet identity: `current-proof-status.md`, `operator-unblocker-checklist.md`, and generated proof workspace READMEs now render each packet's `proof_id`, `matrix_parity_ref`, and `proof_capture_matrix_ref`, so text-first operators can line up the Markdown packet with `/operator_input_readiness/proofs/<proof>` and `/proofs/<proof>/proof_capture_commands_after_unblock` without reopening JSON. This is packet identity/parity metadata only; it does not execute or accept provider/publication proof.
- Current Markdown operator proof packet capture chain: those same Markdown `operator_proof_packet` sections now render `proof_capture_commands_after_unblock` inside the packet body, matching the current-matrix packet-owned command list instead of requiring text-first operators to find a later proof-level section.
- Current Markdown operator proof packet closeout refs: those same Markdown packet sections now render `completion_evidence_ref` and `closure_evidence_refs`, matching the current-matrix packet-owned completion and closure handoff path.
- Current Markdown operator proof packet gate state: matrix-derived `current-proof-status.md` and `operator-unblocker-checklist.md` packet sections now render `current_gate`, including completion status, closure statuses, state-change flag, goal-completion flag, and completion issue codes. Proof-plan README packets do not render an empty gate because they are not current-matrix derived.
- Current Markdown operator proof packet current-state refs: matrix-derived `current-proof-status.md` and `operator-unblocker-checklist.md` packet sections now render `current_state_packets` plus `current_state_packet_commands`, so the packet block itself carries the matrix/status/checklist refresh route.
- Current Markdown operator proof packet schema: generated proof workspace READMEs, `current-proof-status.md`, and `operator-unblocker-checklist.md` now render `proof_record_schema` plus `proof_record_required_fields` inside each `operator_proof_packet`, keeping record validation shape inside the packet block as well as in the surrounding human section.
- Current Markdown operator proof packet input readiness: matrix-derived `current-proof-status.md` and `operator-unblocker-checklist.md` packet sections now render `operator_input_readiness`, including blocked fields, issue codes, next action, strict readiness command, and retry command lists.
- Current operator proof packet input contracts: generated proof workspace READMEs plus matrix-derived status/checklist packet sections now render `operator_input_field_contracts`, so packet-first operators see the exact no-secret validation contract for `OPENROUTER_API_KEY_FILE`, `OPENROUTER_LIVEKIT_URL`, `LIVEKIT_API_KEY_FILE`, `LIVEKIT_API_SECRET_FILE`, `LINKEDIN_ACCESS_TOKEN_FILE`, `LINKEDIN_POLICY_ACKNOWLEDGEMENT_ARTIFACT_ID`, `PUBLICATION_DURABLE_PLATFORM_ID_OR_URL`, and `PUBLICATION_ROLLBACK_OR_POSTCONDITION_ARTIFACT_ID`.
- Current operator-input readiness contract: `operator-input-readiness.json` now carries the same per-proof field contracts under `proofs[*].field_contracts`, and the current blocker matrix preserves them in `operator_input_readiness.proofs[*]`. Automation can now consume the no-secret input contract directly from JSON before refreshing credential snapshots.
- Current operator-input field statuses: `operator-input-readiness.json` now also carries `proofs[*].field_statuses`, giving every required field a no-secret state, issue code, value source, next action, and contract. The current blocker matrix preserves this map for packet consumers, so automation can tell whether to write a secret file, replace a placeholder, correct an invalid value, or refresh credential snapshots without deriving state from grouped lists.
- Current operator-input aggregate contract: `operator-input-readiness.json` now carries top-level `required_fields`, `blocked_fields`, `field_contracts`, and `field_statuses` in required operator-input field order. `current-blocker-matrix.json`, `proof-plan.json`, proof-readiness, OpenRouter voice boundary, publication boundary, and the system-design viewer preserve that order instead of deriving it from grouped diagnostics.
- Current compact proof-plan packet readiness: `proof-plan.json` now embeds compact `operator_input_readiness` inside each `proof_plan.operator_proof_packet`, including field groups, field contracts, field ownership, field statuses, report-only retry commands, guarded retry commands, blocked fields, and required post-unblock evidence. Proof-readiness, OpenRouter voice boundary, publication boundary, and the system-design viewer mirror this compact readiness contract while the richer current-matrix packet remains authoritative for current state.
- Current Markdown readiness command rendering: generated proof workspace READMEs, `current-proof-status.md`, and `operator-unblocker-checklist.md` now render `operator_input_readiness.next_action_commands` and `guarded_next_action_commands` as nested command bullets inside each `operator_proof_packet`, so text-first agents can copy the report-only and guarded retry chains without parsing inline list text.
- Current Markdown field-status rendering: `current-proof-status.md` and `operator-unblocker-checklist.md` now expand `operator_input_readiness.field_statuses` into nested per-field lines inside each `operator_proof_packet`, so text-first operators can read state, issue code, value source, next action, and contract without parsing an inline dictionary.
- Current Markdown field-ownership rendering: generated proof workspace READMEs, `current-proof-status.md`, and `operator-unblocker-checklist.md` now expand `operator_input_readiness.field_ownership` into nested `proof_id` and `proof_input_role` lines inside each `operator_proof_packet`, so text-first operators can route OpenRouter/LiveKit versus publication inputs without parsing inline dictionaries.
- Current readiness-section ownership rendering: `current-proof-status.md` and `operator-unblocker-checklist.md` now also render aggregate and per-proof input `field_ownership` in their Operator Input Readiness sections, so broad text-first scans can route provider credential, provider endpoint, publisher credential, publication destination, and publication evidence fields without opening raw JSON or packet blocks.
- Current README readiness-section ownership rendering: generated proof workspace READMEs now render the same aggregate and per-proof `field_ownership` maps in their readiness summaries, so workspace-local operators get the proof/role routing map before the proof-specific packet sections.
- Current browser-visible field ownership rendering: proof-readiness, OpenRouter voice boundary, publication boundary, and the system-design viewer now render `Field ownership` in visible detail panes, so browser-first reviewers see provider credential/endpoint and publication destination/evidence ownership without opening JSON exports.
- Current browser-visible proof-plan field ownership rendering: proof-readiness, OpenRouter voice boundary, publication boundary, and the system-design viewer now render compact proof-plan `Proof-plan field ownership` inside visible `Proof-plan operator packet` sections, so plan-first browser reviewers can route provider and publication inputs without opening JSON exports.
- Current operator proof packet provenance: `source_artifacts` inside the concrete current-matrix `operator_proof_packets` now includes `proof_plan: proof-plan.json` in addition to operator-input readiness, current blocker matrix, and operator-input template refs, keeping the packet source list aligned with its proof-plan links.
- Current operator proof packet authority: `operator_proof_packets` in the concrete UUID `current-blocker-matrix.json` now also carry current-state packet filenames, regeneration commands, next status/operator packet refs, packet labels, no-secret handling guidance, must-capture evidence lists, and vault write targets. Proof-readiness, OpenRouter voice boundary, and publication boundary browser exports are guarded by a normalized full-packet parity regression against this matrix packet. This is A2A handoff completeness only; it does not execute or accept provider/publication proof.
- Current human proof-packet schema handoff: `current-proof-status.md` and `operator-unblocker-checklist.md` now render `proof_record_schema` metadata plus `proof_record_required_fields` for both remaining proofs, keeping human operator packets aligned with `current-blocker-matrix.json` for record type, allowed outcome, state field, and required field review.
- Current proof-readiness HTML packet schema handoff: `01-work-tracking/agent-studio-proof-readiness.html` now exposes packet-level `proof_record_schema` metadata plus `proof_record_required_fields` inside each `operator_proof_packet` export/detail, so the central operator HTML surface carries the same accepted-record type, allowed outcomes, state field, and required fields as `current-blocker-matrix.json`.
- Current boundary-map packet schema handoff: `02-research/openrouter-livekit-voice-boundary-map.html` and `03-review-packets/agent-studio-publication-boundary-map.html` now expose packet-level `proof_record_schema` metadata plus `proof_record_required_fields` inside their exported/rendered `operator_proof_packet` blocks, so specialized voice/publication review surfaces match the central proof-readiness and current matrix contracts.
- Current static checked_at sync: proof-readiness, voice boundary, and publication boundary credential snapshots now use the same `checked_at` as the concrete UUID `current-blocker-matrix.json`. `tests/test_blocker_snapshot_consistency.py::test_static_operator_packets_use_current_matrix_checked_at` guards against stale static snapshot dates while preserving the no-secret planning boundary. This is freshness evidence only; it does not supply accepted live-voice or publication records.
- Current system-design viewer proof schema handoff: `output/viewers/agent-studio-system-design.json` and the embedded HTML viewer now expose each Objective Completion Audit route's `proof_record_schema` metadata plus `proof_record_required_fields` from the current matrix, including live voice `provider_backed_live_voice_proof_record`, allowed outcomes, state field, and `voice_agent_process_start_artifact_id`, so architecture reviewers can see the accepted-record contract beside the post-unblock capture commands.
- Current system-design viewer full packet handoff: Objective Completion Audit routes in the JSON and HTML system-design viewer now embed the full authoritative `operator_proof_packet` from the concrete current blocker matrix, and the detail pane renders the packet label, no-secret handling, `must_capture`, vault targets, and next packet refs. `tests/test_system_design_viewer_browser.py` now fails if viewer routes carry only a subset of the matrix packet. This is architecture-review parity only; external proof remains blocked.
- Current product-run bootstrap handoff: proof-plan entries now expose `product_run_bootstrap` with no-secret `POST /api/runs` capture commands, a bootstrap output file at `social_media_optimiser/output/provider-proof/bootstrap/product-run.create.json`, and `init-provider-proof-workspace-from-bootstrap` to validate the captured response as a full `RunState` before initializing the UUID-named proof workspace. `validate-provider-proof-product-run-bootstrap` remains available as a no-write validation check. Run-id-only JSON fails with `product_run_payload_schema_invalid`. Blocked attempt gates list these bootstrap commands instead of leaving run-id replacement implicit.
- Current bootstrap workspace path boundary: provider proof workspace initialization now fails closed before creating directories when `output_dir` or an existing ancestor is a regular file or broken symlink, and catches `OSError` around directory creation as a final no-write backstop. Bootstrap-to-workspace inherits that boundary and returns `invalid_workspace` / `workspace_path_unwritable` with `written_files=[]`.
- Current proof workspace handoff: proof-plan entries now expose `workspace_commands` for `init-provider-proof-workspace --run-id <run-id> --output-dir social_media_optimiser/output/provider-proof/<run-id>`, so the two proof templates and README can be initialized from the same operator surface after the durable product run UUID exists.
- Current proof workspace expected files: proof-plan entries now expose `workspace_expected_files`, naming the live voice proof template, external publication proof template, and `README.md` that should exist after workspace initialization.
- Current blocked workspace behavior: `init-provider-proof-workspace` with a placeholder or unsafe run id now returns `replace_run_id_and_initialize_workspace` plus a sanitized rerun command for the same output directory, not a completion-status command.
- Current proof attempt gate: proof-plan entries now expose `attempt_gate`, which says whether preflight capture can start, whether proof commands are still blocked, the exact blocking reasons, and the next operator action. Credential-blocked gates now list setup-and-recheck commands; ready-for-preflight gates list the first workspace/capture/validation commands only when credentials plus a durable product run UUID make preflight capture ready. A shell-safe label such as `RUN-ID` remains blocked as `run_id_not_product_uuid` after credentials are configured. It never allows blocker state changes by itself.
- Current product-run preflight evidence gate: provider proof preflight now starts with `GET /api/runs/<uuid>` captured as `product-run.preflight.json`. `validate-provider-proof-preflight-artifacts` must see that captured run id match the command run id and that the file matches the real `/api/runs/<uuid>` response shape before provider-readiness, voice-runtime, or publish-readiness evidence can validate. Hand-written run-id-only JSON fails closed with `product_run_payload_schema_invalid`. Accepted proof records and compact audit notes must carry `product_run_preflight_artifact_id` plus `preflight_validation_report_validated_product_run_id`; UUID shape alone is not accepted as proof that a product run exists.
- Current actual UUID proof workspace: `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/` contains a real `POST /api/runs` response captured in `bootstrap/product-run.create.json`, a UUID-named initialized proof workspace, `workspace-validation.json` with `valid_workspace`, current `proof-plan.json`, live `product-run.preflight.json`, `provider-readiness.preflight.json`, `voice-runtime-readiness.preflight.json`, refreshed `publish-readiness.preflight.json`, proof-specific preflight validation reports, `operator-checkpoint.json`, `checkpoints.list.json`, `runtime-health-ledger.json`, `current-proof-status.md`, a local publication-readiness fixture source/claim/artifact/guardrail/source-ledger packet, accepted live-voice proof record, and `completion-status.json`. The product run, workspace, checkpoint, local runtime-health, source-backed local publication fixture, source-ledger, guardrail gates, OpenRouter LiveKit provider readiness, local LiveKit dev transport, local LiveKit agent participant startup construction, local Kokoro package availability, live provider-smoke evidence, realtime timing ledger, and accepted live-voice record are proven for this UUID. Publication preflight has advanced past missing content/source/claim blockers and is now blocked by `missing_publish_channel_credentials` and `publish_channel_policy_review_required` for `linkedin`; this is not external publication proof. Completion status remains `blocked_by_latest_failed_proof_record`.
- Current blocker handoff behavior: `provider-proof-current-blocker-matrix` and `provider-proof-operator-unblocker-checklist` now clear proof-level blocker prompts when accepted proof records exist for both required proofs. Future accepted proof handoffs should show `accepted_proof_record_available`, no remaining proof-level blockers, and closure-review guidance instead of stale provider/publication input prompts. The current UUID workspace still has the external-publication latest failed proof record, so its concrete checklist remains blocked on LinkedIn publication inputs and external publication proof capture/recheck while provider-backed live voice stays accepted.
- Current blocker accepted-evidence behavior: accepted proof entries in `provider-proof-current-blocker-matrix` now cite `completion-status.json` plus `accepted_record_sources`, not failed-record files. Current failed proof entries still cite failed-record evidence because that is the authoritative state.
- Current blocker completion-status linkage: the matrix now trusts per-proof `completion-status.json` proof statuses, not only top-level `accepted_proofs`. If accepted records are present but audit-target coverage is incomplete or an accepted audit note is invalid/secret-shaped, the row stays blocked instead of clearing proof blockers.
- Current checklist audit-coverage repair behavior: when accepted proof records are missing from some configured audit targets, the compact checklist names those missing audit targets and tells operators to record the same validated accepted proof record into them before rerunning completion status. It should not reopen provider credential or preflight instructions for that state.
- Current checklist accepted-audit-note repair behavior: when completion status reports invalid or secret-shaped accepted proof audit notes, the compact checklist points to corrected accepted-proof recording and completion-status recheck, not normal provider credential/preflight retry work.
- Current operator-checklist wording: `operator-unblocker-checklist.md` now reports `Required accepted proof state` from completion status, distinguishing latest failed records, missing accepted records, and accepted records. The current UUID checklist names latest failed records for `external-publication-proof` only; `provider-backed-live-voice-proof` is accepted and must not be reopened as an active blocker.
- Current operator-checklist closeout sequence: the compact checklist now includes closure-review validation and closure-review recording between template generation and closure-review status. Operators should not jump from template to status/update; a filled review record must validate and be recorded before blocker-state update can be checked.
- Current operator-checklist proof-record sequence: after each proof-specific preflight-validation command, the checklist now includes proof-record template, `validate-provider-proof-record`, and `record-provider-proof-record` commands with repo-relative validation-report/workspace-report paths. Operators should not jump from preflight validation to completion status; accepted proof records must be created, validated, and recorded first.
- Current concrete proof workspace: `social_media_optimiser/output/provider-proof/RUN-2026-05-20-NEXT/` is now intentionally blocked because `RUN-2026-05-20-NEXT` is not a durable product run UUID. `README.md` is a blocked warning that now points operators to the product-run bootstrap-to-workspace command before creating a UUID workspace. `proof-plan.json`, `workspace-validation.json`, preflight validation reports, record validation/audit reports, completion status, closure-review reports, and blocker-state update reports all match fresh CLI payloads and fail closed with `run_id_not_product_uuid` or downstream blocked status. Command fields render `<run-id>` until a product UUID is supplied, and current preflight files include the missing `product-run.preflight.json` requirement. This is no-secret planning evidence only; it is not a valid workspace, accepted proof, closure review, blocker update, or objective completion.
- Current review status: the prior UUID-gate findings are closed; Leibniz found and the implementation fixed a product-run preflight run-id-only JSON bypass, then fix-back reported no Critical or Important findings. The product-run bootstrap handoff, bootstrap-validator, and bootstrap-to-workspace slices also received Leibniz review with no Critical/Important findings after the non-directory/broken-symlink workspace path fix. Latest local verification covered provider proof tests `193 passed`, touched browser/export consistency tests `12 passed` outside the macOS sandbox after the expected Chromium Mach-port sandbox failure, foundation `21 passed`, `compileall` over `src tests`, and `git diff --check`. The broader local subset previously reached `406 passed` before sandboxed Chromium hit the same known Mach-port permission failure.
- Current audit write boundary: proof-record, closure-review, and reviewed blocker-state update audit recording now preflight every target before appending notes. If any target is a directory, has a non-directory parent, sits below a non-directory existing ancestor, or already exists but cannot be read as UTF-8, recording returns `audit_target_unwritable`, writes no target, and keeps all state-change or goal-completion flags false.
- Current audit payload redaction boundary: successful proof-record, closure-review, and blocker-state update recorders write to the real operator-supplied audit targets, but payload `written_targets` and blocker-update `existing_targets` redact token-shaped path segments before JSON serialization.
- Current proof command redaction boundary: proof-record validation/recording command builders now redact token-shaped preflight-validation and workspace-validation report path segments before quoting command arguments, while preserving only whitelisted placeholder literals. Workspace/preflight command helpers also redact token-shaped output-dir/report/output-file path arguments before command serialization. Angle-wrapped token-shaped values are not treated as placeholders.
- Current proof status read boundary: `provider-proof-completion-status` and `provider-proof-closure-review-status` now treat non-UTF-8 configured audit targets as invalid audit targets instead of raising decode errors. Accepted proof or approved closure-review coverage is still blocked when any configured target is malformed. Token-shaped audit target path segments are redacted in status arrays and generated closeout command path args. Completion-status default project audit targets, completion per-proof source arrays, and closure-review status default review/proof audit arrays now render as portable `<workspace-root>/...` values while the validators still read the real files.
- Current closure-review CLI file-read boundary: `validate-provider-proof-closure-review` and `record-provider-proof-closure-review` now fail closed for missing, unreadable, non-UTF-8, or invalid-JSON `--record-path` values. The record command reports `audit_recorded=false`, `state_change_allowed=false`, `blocker_state_update_allowed_after_review=false`, and writes no audit targets.
- Current proof-record CLI file-read boundary: `validate-provider-proof-record` and `record-provider-proof-record` now fail closed for missing, unreadable, non-UTF-8, or invalid-JSON `--record-path` values. The record command reports `audit_recorded=false`, `state_change_allowed=false`, and writes no audit targets.
- Current preflight report fail-closed behavior: accepted proof-record validation now treats an unreadable or directory `--preflight-validation-path` as `invalid_record` with `preflight_validation_report_unreadable`, not as a traceback.
- Current workspace-validation evidence link: accepted provider proof records now require `workspace_validation_report_artifact_id`; record validation cross-checks the referenced `workspace-validation.json` for `valid_workspace`, same run id, and proof template plus README coverage. Proof-plan/static exports expose `workspace_validation_report_files` and `workspace_validation_capture_commands` for durable product UUID runs; the old `RUN-2026-05-20-NEXT` README is now a blocked warning because its label is not a product UUID.
- Current proof-record command handoff: both `provider-proof-record-template` next commands and missing-proof `provider-proof-completion-status` next-action commands now point `--preflight-validation-path` at each proof's run-scoped preflight validation report file instead of the placeholder `<preflight-validation.json>`.
- Current accepted-proof audit-note portability boundary: accepted proof audit notes must not emit the local checkout root. `_provider_proof_record_audit_note` now serializes project-root artifact paths as `<workspace-root>/...`, and the accepted live-voice audit note copies in the project and system-design vault handoffs have been corrected after Leibniz flagged the path leak.
- Current live-voice blocker boundary: provider-backed live voice is accepted for the current OpenRouter DeepSeek + LiveKit + Kokoro route. Knowledge-graph/status surfaces and operator templates must not describe live voice as still needing accepted proof capture; the remaining active proof blocker is external publication.
- Current preflight command handoff: proof-plan entries expose runnable no-secret commands for provider readiness, full OpenRouter/Kokoro/LiveKit runtime readiness, and non-mutating publish-readiness policy-channel checks before live proof capture.
- Current preflight evidence-target handoff: proof-plan entries now expose `preflight_output_files` and shell-safe `preflight_capture_commands`, so product-run, provider-readiness, voice-runtime-readiness, and publish-readiness JSON can be saved under `social_media_optimiser/output/provider-proof/<run-id>/` before proof-record validation. `init-provider-proof-workspace` READMEs rewrite those capture targets to the actual initialized `--output-dir`, including shell-safe quoting for paths with spaces.
- Current proof-record schema handoff: accepted proof records now require preflight artifact IDs, so both proofs must link product-run preflight evidence, live voice proof must also link provider-readiness and voice-runtime-readiness preflight evidence, and external publication proof must also link publish-readiness preflight evidence before validation can pass.
- Current preflight validation report handoff: proof-plan entries and proof-workspace READMEs now expose `preflight_validation_report_files` plus `preflight_validation_capture_commands`; validation/record commands point `--preflight-validation-path` at those report files instead of a placeholder path. Captured preflight-artifact validation reports render project-root preflight dirs, expected/validated files, issue fields, and preflight artifact IDs as portable `<workspace-root>/...` paths while still validating the real files. Missing, unreadable, non-UTF-8, invalid JSON, token-shaped, or semantically blocked preflight files return `invalid_preflight_artifacts` without state changes or artifact IDs.
- Current semantic preflight validation handoff: `validate-provider-proof-preflight-artifacts` now requires captured provider-readiness, voice-runtime-readiness, and publish-readiness JSON to match their structured payloads and proof-specific ready-enough semantics before artifact IDs are emitted. Publication preflight must include exactly one normalized, non-empty supported `publish_channel_checks` entry per channel, so duplicate, alias-duplicate, blank, or unsupported platform entries cannot make policy or credential evidence ambiguous.
- Current accepted live-voice proof-record handoff: accepted `provider-backed-live-voice-proof` records must link to a valid preflight-validation report whose `validated_runtime_checks` exactly equals the current generated required runtime-check sequence: `livekit-transport`, `livekit-agent-participant`, `voice-agent-backend-event-sink`, `openrouter-live-dialogue-reasoning`, `kokoro-tts`, and `rust-voice-edge`. Compact proof audit notes preserve `preflight_validation_report_validated_runtime_checks`, so completion status cannot treat hand-edited accepted notes as sufficient when checks are missing, reordered, duplicated, or padded with non-generated names.
- Current accepted publication proof-record handoff: accepted `external-publication-proof` records must link to a valid preflight-validation report whose `validated_publish_channels` includes the normalized `destination_channel`; otherwise record validation emits `destination_channel_missing_from_preflight` and blocks acceptance. Compact proof audit notes now preserve `preflight_validation_report_validated_publish_channels` so completion status cannot treat hand-edited accepted notes as sufficient without the same preflight/channel linkage.
- Current preflight validation requirements handoff: proof-plan entries, proof readiness, OpenRouter voice boundary, publication boundary, and proof-workspace READMEs now expose `preflight_validation_requirements`, so operators can see the semantic rejection rules before running validation.

## Kanban Handoff

Source board: [[../../01-work-tracking/Agent Studio Kanban]].

- Current proof status packet: `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/current-proof-status.md`.
- Current blocker matrix packet: `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/current-blocker-matrix.json`.
- Current operator checklist packet: `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/operator-unblocker-checklist.md`.
- Latest review-watch status: Leibniz latest status: no Critical/Important findings. The latest requested scope was A2A skill-invocation durable event redaction for `agent_skill_invocation_recorded`; no Critical/Important finding or blocker was returned. `public_a2a_skill_invocation_event_payload` preserves skill-use observability while redacting token-shaped task strings; private worker result payloads and provider prompts remain full-fidelity.
- Review-watch surface update: the feedback-loop map now includes the Leibniz review-watch escalation loop, tying standing reviewer `019e3899-5ab3-7171-9d3c-32e7c57bbde7` to the vault-sync rule that material findings surface with severity, files, and next action while no-finding heartbeats stay quiet.
- Recent completed board evidence: A2A skill-invocation event redaction. `tests/test_api_contracts.py::test_agent_skill_invocation_event_redacts_task_type_and_context_summary` first failed on raw token-shaped task type strings inside `agent_skill_invocation_recorded`; `src/all_about_llms/orchestration/a2a_projection.py` now supplies `public_a2a_skill_invocation_event_payload`, preserving skill ids, source paths, outputs, guardrails, workflow/status/notes, and context summary counts while redacting token-shaped task type strings in durable skill-use event payloads.
- Latest completed board item: System-design Objective Completion Audit routes now render required evidence after unblock. Each per-proof route carries matrix-owned `required_evidence_after_unblock`, and the visible route detail shows live voice and publication post-unblock evidence gates. Verification passed focused red/green system-design viewer coverage `2 passed`; this improves architecture-review route handoff only and leaves external proof blockers unchanged.
- Latest completed board item: System-design Objective Completion Audit diagnostics now render operator-input readiness status. The generated viewer carries matrix-owned `status=blocked_by_operator_inputs` inside the visible diagnostics, next to aggregate field contract/status data. Verification passed focused red/green system-design viewer coverage `2 passed`; this improves architecture-review diagnostics visibility only and leaves external proof blockers unchanged.
- Latest completed board item: System-design Objective Completion Audit diagnostics now render aggregate operator-input contracts and statuses. The generated viewer carries `required_fields`, `field_contracts`, and `field_statuses` from the current blocker matrix, and the visible diagnostics expose those fields before per-proof routing. Verification passed focused red/green system-design viewer coverage `2 passed`; this improves architecture-review diagnostics visibility only and leaves external proof blockers unchanged.
- Latest completed board item: System-design Objective Completion Audit Current proof gate now renders completion recovery. The generated viewer carries `completion_next_action` and `completion_next_action_commands` from the current blocker matrix, and the visible gate shows `capture_validate_record_and_recheck` plus the recovery commands. Verification passed focused red/green system-design viewer coverage `2 passed`; this improves architecture-review recovery visibility only and leaves external proof blockers unchanged.
- Latest completed board item: System-design Objective Completion Audit detail now renders proof-plan operator sequence and closeout commands. The generated viewer carries `proof_plan_operator_sequence` and `proof_plan_closeout_commands_by_proof` from the UUID proof plan, and the visible audit panel shows the ordered path plus closeout commands through completion status, closure review, and blocker-state update. Verification passed focused red/green system-design viewer coverage `2 passed`; this improves architecture-review handoff visibility only and leaves external proof blockers unchanged.
- Latest completed board item: System-design Objective Completion Audit detail now renders proof-plan recovery authority. The generated system-design viewer already embedded `current_gate_recovery_authority` in JSON; its visible proof-plan operator packet panel now shows `Proof-plan current gate recovery authority` beside current matrix refs. Verification passed focused browser red/green `1 passed` and system-design viewer browser coverage `2 passed`; this improves architecture-review handoff visibility only and leaves external proof blockers unchanged.
- Latest completed board item: HTML proof-plan packet panels now render recovery authority. Proof-readiness, OpenRouter voice boundary, and publication boundary visible detail panes show `current_gate_recovery_authority` beside current matrix refs. Verification passed focused browser red/green `2 passed`, broader static/browser viewer parity `11 passed`, foundation `21 passed`, and whitespace checks; this improves browser-first plan routing only and leaves external proof blockers unchanged.
- Latest completed board item: README/proof-plan packets now name current-gate recovery authority. Compact proof-plan and README packet sections point to the current matrix packet and operator-packet ref for current gate, completion next action, and recovery commands. Verification passed focused README red/green `2 passed`, provider-proof CLI `223 passed`, static/browser viewer parity `11 passed`, foundation `21 passed`, compileall, and whitespace checks; this improves workspace-local recovery routing only and leaves external proof blockers unchanged.
- Latest completed board item: HTML proof surfaces now render packet completion recovery. Proof-readiness, OpenRouter voice boundary, publication boundary, and the system-design viewer embed packet current gates matching the current matrix, and visible detail panes show completion next action plus recovery command bullets. Verification passed static red/green `1 passed`, focused browser packet checks `2 passed`, broader static/browser viewer checks `11 passed`, foundation `21 passed`, and whitespace checks; this improves browser-first packet recovery only and leaves external proof blockers unchanged.
- Latest completed board item: packet-local current gates now render completion recovery commands. Each matrix-derived operator proof packet carries `completion_next_action` and nested command bullets, and both Markdown current-state packets expose the same route inside the packet block. Verification passed focused red/green `3 passed`, full provider-proof CLI `223 passed`, foundation `21 passed`, compileall, and whitespace checks; this improves packet-first no-secret recovery only and leaves external proof blockers unchanged.
- Latest completed board item: current-state proof handoffs now render completion recovery commands. The current blocker matrix exposes `completion.next_action` and top-level recovery commands, while current proof status and operator unblocker checklist show the same current-gate recovery chain with one completion-status recheck. Verification passed focused red/green `3 passed`, full provider-proof CLI `223 passed`, foundation `21 passed`, compileall, and whitespace checks; this improves no-secret recovery handoff only and leaves external proof blockers unchanged.
- Latest completed board item: completion-status payloads now populate top-level `next_action_commands` when unresolved proof records require capture, validation, recording, and recheck. Verification passed focused red/green provider-proof checks, full provider-proof CLI, foundation, compileall, and whitespace checks; this improves automation recovery handoff only and leaves external proof blockers unchanged.
- Latest completed board item: browser proof-plan panels now render compact `Proof-plan field ownership` in proof-readiness, OpenRouter voice boundary, publication boundary, and the system-design viewer. Verification passed focused browser checks, full static/viewer parity, provider-proof CLI, foundation, compileall, and whitespace checks; this improves plan-first browser routing only and leaves external proof blockers unchanged.
- Latest completed board item: browser-visible proof surfaces now render `Field ownership` in proof-readiness, OpenRouter voice boundary, publication boundary, and system-design viewer detail panes. Verification passed focused browser checks, full static/viewer parity, provider-proof CLI, foundation, compileall, and whitespace checks; this improves browser-first routing only and leaves external proof blockers unchanged.
- Latest completed board item: generated proof workspace READMEs now render aggregate and per-proof readiness `field_ownership` directly. Verification passed provider-proof CLI, foundation, compileall, direct README assertions, and whitespace checks; this improves workspace-local text routing only and leaves external proof blockers unchanged.
- Latest completed board item: aggregate and per-proof readiness sections in `current-proof-status.md` and `operator-unblocker-checklist.md` now render `field_ownership` directly. Verification passed provider-proof CLI, foundation, compileall, direct Markdown assertions, and whitespace checks; this improves no-secret text routing only and leaves external proof blockers unchanged.
- Latest completed board item: Markdown operator proof packets now render `field_ownership` as nested `proof_id` and `proof_input_role` lines in generated READMEs, `current-proof-status.md`, and `operator-unblocker-checklist.md`. Verification passed provider-proof CLI, foundation, compileall, direct Markdown ownership assertions, and whitespace checks; this improves text-first routing only and leaves external proof blockers unchanged.
- Latest completed board item: compact proof-plan operator packets now carry `operator_input_readiness.field_ownership` through `proof-plan.json`, proof-readiness, OpenRouter voice boundary, publication boundary, and the system-design viewer. Verification passed provider-proof CLI, browser/static/viewer parity, foundation, compileall, and whitespace checks; this improves no-secret field routing only and leaves external proof blockers unchanged.
- Current compact proof-plan operator packet: `proof_plan.operator_proof_packet` in proof-readiness/OpenRouter LiveKit voice/publication exports and `proof_plan_operator_packet` in the system-design viewer preserve the compact provider-proof-plan packet beside the richer current-matrix `operator_proof_packet`. The compact packet now also carries `current_matrix_packet`, `current_matrix_packet_ref`, `current_matrix_packet_command`, `current_matrix_operator_packet_ref`, and `source_artifacts` for `proof-plan.json` plus `current-blocker-matrix.json` so plan-first agents can jump back to, refresh, and attribute the authoritative matrix route.
- Latest completed board item: Markdown operator proof packets now render `field_statuses` as nested per-field lines in `current-proof-status.md` and `operator-unblocker-checklist.md`. Focused red/green coverage passed; this improves text-first no-secret unblock routing only and leaves external proof blockers unchanged.
- Latest completed board item: Markdown operator proof packets now render packet-local `proof_record_schema` and `proof_record_required_fields` in generated READMEs, current status, and operator checklist. Focused red/green coverage passed; this improves packet-local validation shape only and leaves external proof blockers unchanged.
- Latest completed board item: Matrix-derived Markdown operator proof packets now render `current_state_packets` and `current_state_packet_commands` in `current-proof-status.md` and `operator-unblocker-checklist.md`. Focused red/green coverage passed; this improves packet-local regeneration routing only and leaves external proof blockers unchanged.
- Latest completed board item: Matrix-derived Markdown operator proof packets now render `current_gate` in `current-proof-status.md` and `operator-unblocker-checklist.md`. Focused red/green coverage passed; this improves packet-local no-completion guard visibility only and leaves external proof blockers unchanged.
- Latest completed board item: Markdown operator proof packets now render `completion_evidence_ref` and `closure_evidence_refs` in `current-proof-status.md`, `operator-unblocker-checklist.md`, and generated proof workspace READMEs. Focused red/green coverage passed; this improves text-first closeout handoff parity only and leaves external proof blockers unchanged.
- Latest completed board item: Markdown operator proof packets now render packet-owned `proof_capture_commands_after_unblock` lists in `current-proof-status.md`, `operator-unblocker-checklist.md`, and generated proof workspace READMEs. Focused red/green coverage passed; this improves text-first A2A proof-capture routing only and leaves external proof blockers unchanged.
- Latest completed board item: Markdown operator proof packets now render `proof_id`, `matrix_parity_ref`, and `proof_capture_matrix_ref` in `current-proof-status.md`, `operator-unblocker-checklist.md`, and generated proof workspace READMEs. Full provider-proof CLI and foundation checks passed; this improves text-first packet parity only and leaves external proof blockers unchanged.
- Latest completed board item: generated proof workspace READMEs now render `current_matrix_packet`, `current_matrix_packet_ref`, `current_matrix_packet_command`, and `current_matrix_operator_packet_ref` inside each `operator_proof_packet` section. Focused README coverage passed; this improves workspace-local matrix navigation only and leaves external proof blockers unchanged.
- Latest completed board item: generated proof workspace READMEs now render `proof_plan_packet`, `proof_plan_packet_ref`, `proof_plan_packet_command`, and `proof_plan_operator_packet_ref` inside each `operator_proof_packet` section. Focused README coverage passed; this improves workspace-local packet navigation only and leaves external proof blockers unchanged.
- Latest completed board item: Markdown operator proof packets now render `source_artifacts` in `current-proof-status.md`, `operator-unblocker-checklist.md`, and generated proof workspace READMEs. Focused provider-proof Markdown coverage passed; this improves reviewer provenance only and leaves external proof blockers unchanged.
- Latest completed board item: proof-readiness, OpenRouter LiveKit voice, and publication boundary proof-plan panels now visibly render the compact `Proof-plan operator packet`, including `current_matrix_packet_ref` and proof-plan source artifacts. Focused proof-packet browser coverage passed; this is visible handoff parity only and leaves external proof blockers unchanged.
- Latest completed board item: Objective Completion Audit viewer detail now renders proof-plan operator packet `current_matrix_packet_ref` and proof-plan source artifacts. Focused system-design browser coverage passed; this improves visible review handoff only and leaves external proof blockers unchanged.
- Latest completed board item: Compact proof-plan operator packets now list `proof_plan` and `current_blocker_matrix` in `source_artifacts`. Focused provider-proof provenance checks passed; this improves compact packet attribution only and leaves external proof blockers unchanged.
- Latest completed board item: Current-matrix operator packets now list `proof_plan` in `source_artifacts`. Focused provider-proof/static/viewer parity checks passed; this improves packet provenance only and leaves external proof blockers unchanged.
- Latest completed board item: Compact proof-plan operator packets now expose direct `current_matrix_packet_command` regeneration metadata. Focused provider-proof and proof-plan propagation checks passed; this improves packet-first matrix refresh routing only and leaves external proof blockers unchanged.
- Latest completed board item: Current-matrix operator packets now expose direct `proof_plan_packet_command` regeneration metadata. Focused provider-proof matrix/status/checklist checks and browser parity checks passed; this improves packet-first refresh routing only and leaves external proof blockers unchanged.
- Latest completed board item: Current-matrix operator packets now reciprocate to the compact proof-plan packet. Provider-proof CLI `223 passed`, blocker snapshot consistency `6 passed`, system-design viewer browser `2 passed`, and foundation `21 passed`; this improves A2A/review navigation only and leaves external proof blockers unchanged.
- Latest completed board item: Proof-plan operator packets now include current-matrix back-references. Provider-proof CLI `223 passed`, static blocker snapshot consistency `6 passed`, and system-design viewer checks `2 passed`; this improves A2A/review routing only and does not clear external proof blockers.
- Latest completed board item: Proof-plan operator packet handoff added. The Kanban now names `proof_plan.operator_proof_packet`, `proof_plan_operator_packet`, and the compact provider-proof-plan packet so continuation agents can distinguish the plan-first packet from the current-matrix operator packet before opening generated viewers.
- Latest completed board item: Kanban blocker tracking links to generated current proof packets. The board now points blocked live-voice and publication items at `current-proof-status.md`, and its done evidence names `current-blocker-matrix.json` plus `operator-unblocker-checklist.md`, so continuation agents start from generated current-state packets instead of mining the sprint log.
- Latest completed board item: Publication preflight validation rejects duplicate, alias-duplicate, blank, or unsupported channel checks. A ready `publish-readiness.preflight.json` with two checks for the same normalized platform, including aliases such as `x` and `twitter`, now emits `publish_channel_duplicate_platform`; a blank platform emits `publish_channel_platform_missing`; an unknown channel such as `mastodon` emits `publish_channel_platform_unsupported`. These cases return no preflight artifact IDs, and proof-plan surfaces require one normalized, non-empty supported `publish_channel_checks` entry per channel.
- Latest completed board item: Live voice preflight validation rejects duplicate required runtime check IDs. A duplicate ready check can no longer mask an earlier blocked check with the same ID; the validator emits `voice_runtime_duplicate_check_id`, returns no preflight artifact IDs, and the proof-plan surfaces now say required runtime checks must be present once and ready. `Leibniz` review found the first duplicate issue path could echo raw `check_id` values; the fix now uses a literal `checks.check_id` path so token-shaped duplicate IDs remain redacted.
- Latest completed board item: Source-panel web-research status now resolves stale blocked messages against newer provider-backed research events. `sourceResearchStatus` parses message/event timestamps, orders research events by valid `created_at` before `event_id`, ignores malformed message timestamps as freshness blockers when a valid newer event exists, and still lets newer active research messages keep `Research queued`, so accepted source coverage is not masked by stale provider-configuration copy. The Source panel also disables `Run web research` while provider-backed research is already running.
- Latest completed board item: Publication proof preflight validation now rejects contradictory policy evidence. `validate-provider-proof-preflight-artifacts` keeps policy-review-only `needs_review` publication preflight evidence valid only when the top-level blockers equal the exact single `publish_channel_policy_review_required` entry, channel blockers are empty or policy-review-only, at least one channel policy remains `needs_review`, and acknowledged channels do not still carry policy-review blockers; a payload that claims overall publish readiness is `ready` must have channel `policy_status=acknowledged` and no top-level or channel blocking issues.
- Latest completed board item: Provider proof setup commands now have an executable setup-plus-recheck regression. Running the generated live voice and publication setup snippets in a temp workspace, then rebuilding `provider-proof-plan` from `.env.example`, moves both proof packets to `ready_for_runtime_attempt` through file/local-provider config while keeping direct configured inputs empty and avoiding token/endpoint value output.
- Latest completed board item: Provider proof live-voice setup now rejects malformed existing `LOCAL_PROVIDER_CONFIG_FILE` content without a Python traceback, including invalid JSON, non-UTF-8 bytes, and directory paths. The command emits only `Invalid existing LOCAL_PROVIDER_CONFIG_FILE`, leaves the file state unchanged, and avoids printing existing config content or newly supplied endpoint/secret values.
- Latest completed board item: Provider proof live-voice setup now preserves unmanaged local provider config keys while updating managed endpoint values. The generated setup command still validates endpoint URLs, writes with `0600` permissions, rejects malformed or credential-bearing values without overwriting, and keeps endpoint values out of output.
- Latest completed board item: Provider proof live-voice setup now validates and writes endpoint URLs into `LOCAL_PROVIDER_CONFIG_FILE` instead of leaving them as transient export-only commands. The generated command writes `.secrets/local_provider_config.json` with `0600` permissions and no stdout/stderr echo, rejects malformed or credential-bearing endpoint strings without overwriting the config file, and the concrete proof workspace README/workspace-validation reports were refreshed.
- Latest completed board item: Provider proof plans now classify endpoint URLs stored in `LOCAL_PROVIDER_CONFIG_FILE` without echoing values. The CLI snapshot and proof-plan packets expose `configured_local_provider_inputs` plus `local_provider_config_loaded`; proof readiness, OpenRouter voice boundary, publication boundary, and the concrete `RUN-2026-05-20-NEXT` artifacts carry the same fields. Current concrete state is still false/empty because no valid local provider config file was loaded.
- Latest completed board item: Provider proof credential setup commands now include explicit local file permissions. CLI proof plans, proof readiness, OpenRouter voice boundary, publication boundary, proof-workspace README, and concrete proof-plan snapshots set `.secrets` to `0700`, use `umask 077` before secret-file writes, and apply `chmod 600` afterward without printing secret values.
- Latest completed board item: Concrete proof-workspace README command paths are now portable. The `RUN-2026-05-20-NEXT/README.md` handoff no longer embeds the local checkout root, states that commands run from the repository root, and validation still confirms the README matches the current proof-plan generator.
- Latest completed board item: Concrete closure-review validation and audit snapshots are now durable. The `RUN-2026-05-20-NEXT` workspace includes `closure-review-validation.json` and `closure-review-audit.json`, both matching fresh CLI payloads and preserving invalid/no-write status while completion is not accepted and no real closure-review record exists.
- Latest completed board item: Provider proof preflight validation now checks semantics. Blocked or malformed readiness payloads no longer produce artifact IDs just because the JSON parses; current live voice requires selected ready OpenRouter live dialogue plus ready LiveKit/Kokoro/Rust/backend-event-sink runtime checks, and publication rejects blocked readiness, missing channel credentials, feedback-gate side effects, and non-policy review states. Earlier Gemma realtime validation language is superseded for the active route.
- Latest completed board item: Provider proof credential-blocked gates now expose no-secret setup-and-recheck commands. The commands require real shell variables before writing ignored secret files, literal `<...>` values are rejected by the credential classifier, and publication setup covers either `X_ACCESS_TOKEN` or `X_API_KEY`; `Leibniz` confirmed no actionable findings remain.
- Latest completed board item: Provider proof credential setup commands are now visible across proof-readiness, OpenRouter voice boundary, publication boundary, and generated proof-workspace README surfaces. Browser parity first failed on missing `credential_setup_commands`; the exports now match the CLI proof-plan packets and `Leibniz` found no actionable findings.
- Historical board item: Concrete proof workspace `RUN-2026-05-20-NEXT` now carries a durable `workspace-validation.json` report. It is currently `blocked_by_run_id` with `run_id_not_product_uuid` because the workspace label is not a durable product run UUID. This non-UUID workspace note is superseded for current status by the UUID run `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`, where provider-backed live voice is accepted and only external publication proof remains blocked.
- Latest completed board item: Provider proof credential snapshot source paths are portable. `provider-proof-plan` and `blocker-credential-snapshot` now emit `<workspace-root>/.env.example` rather than a local `/Users/...` source path, and browser consistency still confirms the static proof-readiness/boundary exports match the CLI no-secret snapshot.
- Latest completed board item: Provider proof completion-status default audit targets are portable. `provider-proof-completion-status --run-id RUN-2026-05-20-NEXT` now fails first with `blocked_by_run_id` / `run_id_not_product_uuid`; accepted live voice and publication records still do not exist.
- Latest completed board item: Provider proof closure-review status default audit targets are portable. `provider-proof-closure-review-status --run-id RUN-2026-05-20-NEXT` emits `<workspace-root>/...` for default review/proof audit arrays and remains blocked by completion status until accepted proof records exist.
- Latest completed board item: Concrete provider proof closeout status reports are now durable. The `RUN-2026-05-20-NEXT` workspace includes `completion-status.json` and `closure-review-status.json`, both matching fresh CLI payloads, preserving portable audit target paths, and keeping blocker-state changes disallowed while accepted proof and closure review are missing.
- Latest completed board item: Concrete provider proof plan snapshot is now durable. The `RUN-2026-05-20-NEXT` workspace includes `proof-plan.json`, matching the fresh CLI payload and preserving the blocked credential attempt gates plus no-secret credential snapshot for review.
- Latest completed board item: Concrete blocker credential snapshot is now durable. The `RUN-2026-05-20-NEXT` workspace includes `credential-snapshot.json`, matching the fresh no-secret classifier payload and preserving placeholder-only credential state without direct or file values.
- Latest completed board item: Concrete closure-review template snapshot is now durable. The `RUN-2026-05-20-NEXT` workspace includes `closure-review-template.json`, matching the fresh CLI payload and preserving `blocked_by_completion_status` with no generated review template while accepted proof records are missing.
- Latest completed board item: Concrete blocker-state update snapshot is now durable and uses the correct blocked boundary. The `RUN-2026-05-20-NEXT` workspace includes `blocker-state-update.json`, matching the fresh CLI payload and preserving the no-state-change, no-write, no-goal-completion boundary while closure review remains blocked.
- Latest completed board item: Concrete proof-record validation reports are now durable. The `RUN-2026-05-20-NEXT` workspace includes proof-specific `*.record-validation.json` reports, both matching fresh CLI validation and preserving `invalid_record` status while templates still contain placeholders and no accepted proof outcome.
- Latest completed board item: Concrete proof-record audit attempt reports are now durable and self-contained. The `RUN-2026-05-20-NEXT` workspace includes proof-specific `*.record-audit.json` reports, both matching fresh CLI recording payloads and preserving `command_run_id`, `proof_outcome`, `audit_recorded=false`, and no written targets while validation remains invalid.
- Latest completed board item: Provider proof status scans now fail closed on malformed audit files. Non-UTF-8 proof or closure-review audit targets are reported as invalid targets, not tracebacks, and accepted proof/review coverage is blocked until every configured target is readable. Review-found token-shaped path segments are redacted from status payload path fields and generated closeout commands.
- Latest completed board item: Provider proof recording success payloads now redact token-shaped target path segments. `written_targets` and blocker-update `existing_targets` no longer echo token-shaped directory names while preserving real filesystem writes.
- Latest completed board item: Provider proof record next commands now redact token-shaped report path segments. Generated validate/record command arguments for preflight-validation and workspace-validation reports use the no-secret path renderer before shell quoting.
- Latest completed board item: Provider proof workspace/preflight generated commands now redact token-shaped output path segments. Workspace init/validation, workspace-validation capture, preflight capture, and preflight-validation command helpers render output-dir/report/output-file path args through `_proof_command_arg`; `Leibniz` found no actionable findings in the narrow review.
- Latest completed board item: Provider proof audit recording now preflights the full write set before touching any target. Directory targets, non-directory parents, nested paths below a regular-file ancestor, and existing unreadable or non-UTF-8 audit files return `audit_target_unwritable`, with `written_targets=[]` and state-change flags false for proof records, closure reviews, and reviewed blocker-state update notes.
- Latest completed board item: Closure-review CLI record-path reads now return no-state-change JSON errors instead of tracebacks. Directory, missing, non-UTF-8, or invalid-JSON closure-review files cannot write closure-review audit notes or allow blocker-state updates.
- Latest completed board item: Provider proof CLI record-path reads now return no-state-change JSON errors instead of tracebacks. Directory, missing, non-UTF-8, or invalid-JSON proof-record files cannot write audit notes or imply state changes.
- Latest completed board item: Provider proof-record preflight report linkage fails closed on unreadable paths. A directory passed as `--preflight-validation-path` now produces a no-state-change invalid record with `preflight_validation_report_unreadable` instead of crashing validation.
- Latest completed board item: Provider proof records now carry workspace-validation report evidence. Accepted records require `workspace_validation_report_artifact_id`; validation/recording commands accept `--workspace-validation-path`, and the validator rejects accepted records unless the linked workspace-validation report proves the current proof template plus README matched the proof plan.
- Latest completed board item: Provider proof plans now render preflight validation requirements. The CLI, proof readiness, OpenRouter voice boundary, publication boundary, and proof-workspace READMEs name the exact semantic readiness rules for live voice and publication preflight files before operators run the validator.
- Latest completed board item: Provider proof plans now render a no-secret proof attempt gate. The gate distinguishes run-id and credential blockers from preflight-capture readiness, keeps proof commands blocked until preflight validation succeeds, and lists the first workspace/capture/validation commands only when it is safe to start preflight capture.
- Latest completed board item: Initialized concrete provider proof workspace `RUN-2026-05-20-NEXT` with both proof templates and operator README. JSON validation passed for both templates; the current attempt gates remain credential-blocked, so no preflight or live proof command has been run.
- Latest completed board item: Proof-record template and completion-status missing-proof next commands now use exact run-scoped preflight validation report paths for live voice and publication proof records, removing the stale placeholder from both operator entry points.
- Latest completed board item: Autonomous Studio passes now include publish-channel credential/policy checks whenever publish readiness runs and pass the Settings-backed publication credential map into publish readiness. A clean Instagram/LinkedIn package now blocks on `missing_publish_channel_credentials` with per-channel missing statuses instead of reporting publish-ready, even if a caller explicitly disables channel checks on the autonomous request; this remains non-live gating evidence, not accepted destination proof.
- Latest completed board item: Provider proof workspaces now have a no-secret freshness validator. `validate-provider-proof-workspace` compares both proof templates plus `README.md` with the current proof plan, fails closed on missing/unreadable/mismatched/token-shaped files, and keeps `state_change_allowed=false`; after `Leibniz` review, token-shaped `--output-dir` values are redacted and rejected before path serialization or writes. Provider proof plans and boundary exports expose `workspace_validation_commands`; the concrete `RUN-2026-05-20-NEXT` workspace now remains blocked until replaced by a durable product run UUID.
- Latest completed board item: Publication credentials can now be supplied through readable non-empty token files. The blocker classifier and non-live publish-readiness check accept the `*_FILE` alternatives for Instagram, LinkedIn, X, and Substack without serializing token contents; current proof state remains blocked because no real credentials or accepted destination proof has been recorded.
- Latest completed board item: App-level local secret-file setup now covers publication credentials. The same endpoint that safely writes HF/LiveKit/Tavily secrets can now write Instagram, LinkedIn, X, and Substack token files, and publish-readiness uses the Settings-backed credential map for channel checks.
- Latest review follow-up: Settings-side secret-file loading now catches malformed non-UTF-8 bytes, so invalid app-local publication token files fail closed as unset credentials instead of crashing settings validation.
- Latest review follow-up: Leibniz found the first file-backed publication credential pass could over-trust unreadable files and crash on malformed token bytes. The CLI now requires `_FILE` paths to be openable before counting them as configured, and publish-readiness treats invalid UTF-8 token files as missing rather than raising.
- Latest completed board item: Provider proof plans now name and write preflight validation reports. The CLI, proof readiness, OpenRouter voice/publication boundary maps, and proof-workspace READMEs expose run-scoped `preflight_validation_report_files` and `preflight_validation_capture_commands`, and record commands use those paths for `--preflight-validation-path`.
- Latest completed board item: Provider proof closure-review recording keeps proof-read and review-write targets separate. Leibniz found `record-provider-proof-closure-review` could fall back to using review `--audit-target` paths as proof evidence when `--proof-audit-target` was omitted; completion-status rechecks now use only `--proof-audit-target` or default proof audit targets, while review `--audit-target` paths only select closure-review write destinations.
- Recent completed board evidence: Blocked proof workspace initialization now points back to workspace setup. Placeholder or unsafe run ids return `replace_run_id_and_initialize_workspace` with a sanitized rerun command and zero writes.
- Recent completed board evidence: Provider proof plans expose runnable no-secret preflight commands. CLI proof plans, proof readiness, and the OpenRouter voice/publication boundary maps now carry/render `preflight_commands`, including explicit `open_feedback_gate=false` and `mark_run_completed_if_ready=false` for publication preflight, and proof-workspace READMEs include them before validation/recording commands.
- Recent completed board evidence: Provider proof plans expose stable preflight evidence targets. CLI proof plans, proof readiness, and the OpenRouter voice/publication boundary maps now carry/render `preflight_output_files` and `preflight_capture_commands`; proof-workspace READMEs list expected JSON outputs and use the actual initialized `--output-dir` for capture commands.
- Recent completed board evidence: Provider proof records require preflight evidence linkage. The proof schemas now require `provider_readiness_preflight_artifact_id`, `voice_runtime_readiness_preflight_artifact_id`, and `publish_readiness_preflight_artifact_id` as appropriate, so captured preflight JSON cannot be skipped in accepted proof records.
- Recent completed board evidence: Provider proof plans expose expected workspace files. CLI proof plans, proof readiness, and the OpenRouter voice/publication boundary maps now carry/render `workspace_expected_files` for `provider-backed-live-voice-proof.template.json`, `external-publication-proof.template.json`, and `README.md`.
- Recent completed board evidence: Provider proof plans expose workspace initialization commands. CLI proof plans, proof readiness, and the OpenRouter voice/publication boundary maps now carry/render `workspace_commands` for creating run-scoped proof templates and README under `social_media_optimiser/output/provider-proof/<run-id>`.
- Recent completed board evidence: Provider proof plans expose ordered operator sequences. CLI proof plans, proof readiness, and the OpenRouter voice/publication boundary maps now carry/render `operator_sequence` so the scattered setup, proof, record, completion, closure-review, and blocker-state update fields have a single ordered handoff.
- Recent completed board evidence: Provider proof plans expose no-secret credential setup requirements. CLI proof plans, proof readiness, and the OpenRouter voice/publication boundary maps now carry/render the required OpenRouter/Kokoro/LiveKit and publication credential setup names, including file-backed HF/LiveKit secret alternatives.
- Recent completed board evidence: Provider proof plans expose composite blocker reasons. CLI proof plans, proof readiness, and the OpenRouter voice/publication boundary maps now carry/render `blocking_reasons` so `run_id_not_concrete` and `blocked_by_placeholder_only_configuration` remain visible together.
- Recent completed board evidence: Provider proof closeout commands propagate through proof-plan surfaces. CLI proof plans, proof readiness, and the OpenRouter voice/publication boundary maps now carry/render `closeout_commands` for completion status, closure-review template, closure-review validation, closure-review recording, closure-review status, and reviewed blocker-state update notes.
- Recent completed board evidence: Provider proof closure review has a no-secret audit bridge. `record-provider-proof-closure-review` now validates filled review records, appends compact redacted audit notes, refuses invalid reviews without writing, preserves separate proof-audit targets for completion-status rechecks, and keeps `state_change_allowed=false`.
- Recent completed board evidence: Provider proof closure review supports rejected reviewer decisions. Leibniz found `valid_rejected_closure_review` was unreachable when a reviewer rejected closure because one requirement failed; rejected closure-review records now allow confirmed/rejected requirement values, require at least one rejected requirement, and keep blocker-state updates disallowed.
- Recent completed board evidence: Provider proof closure review has a no-secret validator. `validate-provider-proof-closure-review` now rechecks accepted completion status, validates reviewer decision, timestamp, accepted proof/source linkage, review requirements, redaction proof, and token-shaped values without changing blocker state. The closure-review template exposes the validation command with active audit-target overrides.
- Recent completed board evidence: Provider proof closure-review command preserves audit-target overrides. Leibniz found accepted completion status could be produced from `--audit-target` overrides but then emit a closure-review template command that rechecked default vault targets; accepted completion status now appends every active audit-target override to the emitted `provider-proof-closure-review-template` command with shell-safe quoting, including paths containing spaces.
- Recent completed board evidence: Provider proof closure review has a no-secret template command. `provider-proof-closure-review-template` now emits a reviewer-fillable record only after `provider-proof-completion-status` is `required_proofs_accepted`, blocks when accepted proof is missing, and keeps `state_change_allowed=false`. Accepted completion status now surfaces this template command as the next no-secret review step while leaving blocker mutation outside the status/template commands.
- Recent completed board evidence: Provider proof completion status exposes closure-review packet. When `provider-proof-completion-status` reaches `required_proofs_accepted`, it now returns top-level `next_action=prepare_blocker_closure_review`, a no-secret template command, and a `closure_review_packet` with accepted proof sources, review requirements, and candidate blocker states after reviewer approval while keeping `blocker_state_change_allowed_by_this_command=false`.
- Recent completed board evidence: Provider proof completion status exposes next action commands. Blocked `provider-proof-completion-status` proof entries now include `next_action`, `next_action_commands`, and `record_proof_in`, so the missing/invalid proof status packet can directly hand operators to template generation, validation, audit recording, and recheck without reopening separate proof-plan surfaces. The `blocked_by_run_id` branch now preserves the same shape with safe `<run-id>` command substitution, secret-shaped run ids are classified unsafe before command generation, and proof workspace initialization also sanitizes token-shaped run ids before output.
- Recent completed board evidence: External publication proof rejects cross-channel destinations. Accepted `external-publication-proof` records now fail validation with `destination_channel_mismatch` when a known platform URL/URN contradicts `destination_channel`, with repo-canonical aliases such as `instagram_post`, `instagram_reel`, and `x_thread` normalized before comparison.
- Recent completed board evidence: External publication proof rejects local/draft destination substitutes. Accepted `external-publication-proof` records now fail validation with `durable_destination_not_external` unless `durable_platform_id_or_url` is an external `http(s)` URL or explicit opaque platform ID such as `urn:li:share:123`; opaque draft/preview/local/internal markers and explicit external `/draft` or `/preview` URL paths are blocked without rejecting normal external slugs.
- Recent completed board evidence: Provider proof plans require runtime-health voice-edge benchmark evidence. `provider-proof-plan`, proof readiness, and the voice boundary map require `build-runtime-health-ledger --run-id <run-id>` before live voice smoke/timing proof, and accepted live voice proof records must keep explicit fields consistent: `voice_edge_benchmark_status=ready`, `execute_live_calls=true`, and `realtime_provider=openrouter_livekit`.
- Recent completed board evidence: Provider proof completion status blocks secret-shaped audit notes. `provider-proof-completion-status` now returns `blocked_by_secret_shaped_audit_note` with `audit_note_secret_shape_detected` if the latest otherwise valid proof note contains token-shaped text, reports only proof IDs plus target paths, and keeps that status ahead of failed/missing proof states while preserving their issue codes.
- Recent completed board evidence: Provider proof completion status requires matching artifact types. `provider-proof-completion-status` ignores accepted/failed audit notes unless `proof_artifact_type` matches the selected proof schema, preventing swapped or hand-edited proof-note types from satisfying completion.
- Recent completed board evidence: Provider proof completion status is self-auditing and status-only. `provider-proof-completion-status` now includes `required_proofs`, `completion_requirements`, `state_change_boundary`, and `blocker_state_change_allowed_by_this_command=false`, so even accepted-proof status cannot be mistaken for a direct blocker-state update.
- Recent completed board evidence: Provider proof completion status uses validation timestamps for latest-record selection. `provider-proof-completion-status` prefers parseable `validation_timestamp` values over file order when choosing the latest accepted/failed proof note per proof/run/readable target, with file order only as a tie/fallback.
- Recent completed board evidence: Provider proof completion status follows latest valid record. `provider-proof-completion-status` now blocks as `blocked_by_latest_failed_proof_record` when a later valid failed proof note supersedes an older accepted note for the same proof/run in a readable audit target, and reports per-proof `failed_source_targets`.
- Recent completed board evidence: Provider proof completion status requires full target coverage. `provider-proof-completion-status` now blocks as `blocked_by_incomplete_audit_target_coverage` when accepted live voice or publication proof notes appear in one readable configured audit target but not another, and reports per-proof `missing_source_targets`.
- Recent completed board evidence: Provider proof plans expose completion-status commands. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now carry `completion_status_commands` with `provider-proof-completion-status --run-id <run-id>` so every proof surface shows the generate, validate, record, and final audit-status check path.
- Recent completed board evidence: Provider proof has a no-secret completion-status checker. `provider-proof-completion-status` scans configured audit targets for exact valid accepted live voice and publication proof-record notes for a durable product UUID run id, reports missing accepted proof records plus missing/invalid audit targets, and changes no blocker state.
- Recent completed board evidence: Provider proof has a no-secret workspace initializer. `init-provider-proof-workspace` creates run-specific voice/publication proof-record template JSON files plus a README with validate/record/completion-status commands, writes nothing for placeholder/unsafe run ids, and fails closed as `workspace_exists` with zero writes if target files already exist.
- Latest provider proof workspace closeout handoff: `init-provider-proof-workspace` README now includes the post-capture closeout chain: `provider-proof-completion-status`, `provider-proof-closure-review-template`, `validate-provider-proof-closure-review`, `record-provider-proof-closure-review`, `provider-proof-closure-review-status`, and `record-provider-proof-blocker-state-update`. It explicitly says the blocker-state update command records `goal_completion_claimed=false` and does not mark the active objective complete.
- Recent completed board evidence: Provider proof records have no-secret templates. `provider-proof-record-template` emits product-UUID-only draft proof JSON with required schema fields and post-capture validation keys, blocks placeholder/unsafe/non-UUID run ids, and validator guardrails reject partially filled template placeholders before proof can be accepted or recorded.
- Recent completed board evidence: Provider proof records have a no-secret audit bridge. `record-provider-proof-record` validates captured accepted/failed proof JSON, appends compact redacted Markdown notes to the configured vault audit targets, refuses invalid records or placeholder/unsafe run ids without writing, and is now surfaced through proof-plan `record_commands`.
- Recent completed board evidence: Provider proof records have a no-secret validator. `validate-provider-proof-record` checks a captured proof JSON against the selected `proof_artifact_schema`, required post-capture validation checks, run id, outcome, and token-shaped value scan without changing blocker state.
- Recent completed board evidence: Provider proof plans carry proof-artifact schemas. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now carry `proof_artifact_schema`, so future accepted or failed proof records have stable artifact types, allowed outcomes, state fields, and required no-secret fields.
- Recent completed board evidence: Provider proof plans carry success-recording requirements. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now carry `success_recording_requirements`, so passing proof must record run/check/validation time, artifact ids, passed validation checks, blocker-state update timing, and follow-up closure/linkage.
- Recent completed board evidence: Provider proof plans carry failure-recording requirements. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now carry `failure_recording_requirements`, so failed live voice/publication attempts must record run/check time, failed step, redacted provider or platform context, blocked-state preservation, and a follow-up task.
- Recent completed board evidence: Provider proof plans carry post-capture validation checks. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now carry `post_capture_validation_checks`, so accepted proof must be compared for live-call flags, provider/session/run linkage, required timing/evidence, destination ID/URL linkage, and no-secret artifacts after capture.
- Recent completed board evidence: Provider proof plans carry proof-linkage requirements. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now carry `proof_linkage_requirements`, so accepted live voice/publication proof must link to the same run/session/destination rather than only naming capture artifacts.
- Recent completed board evidence: Provider proof plans name rejected substitutes. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now carry `rejected_substitutes`, so credentials, rehearsal, HF text routing, non-live smoke, generic policy acknowledgement, and local draft previews cannot be mistaken for accepted proof.
- Recent completed board evidence: Provider proof plans expose safe command run ids. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now carry `command_run_id`, making the accepted or substituted run id explicit for operators and tools.
- Recent completed board evidence: Provider proof plans reject unsafe run ids. `provider-proof-plan` now classifies unsupported run ids as `unsafe_run_id`, blocks runtime attempt, and substitutes `<run-id>` in generated command/preflight text so operator packets cannot carry shell-control syntax.
- Recent completed board evidence: Proof-plan renderers preserve literal placeholders. Proof readiness plus the OpenRouter voice/publication boundary maps now escape proof-plan list text and render command/path strings as code, so visible operator copy preserves `<run-id>` instead of dropping it as an HTML tag.
- Recent completed board evidence: Provider proof plans carry preflight checks. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now include preflight checks for provider readiness, full voice runtime readiness, selected realtime provider confirmation, publish readiness, human approval, policy acknowledgement, and disclosure-bearing artifacts before live calls or destination proof.
- Recent completed board evidence: Provider proof plans fail closed on placeholder run ids. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now carry `run_id_state`; a `<run-id>` placeholder blocks runtime attempt even when credential names are configured.
- Recent completed board evidence: Proof plans carry evidence record targets. `provider-proof-plan`, proof readiness, and the OpenRouter voice/publication boundary maps now include `record_proof_in` targets for the project audit, active context, and system-design audit mirror, so accepted live proof has an explicit no-secret storage path.
- Recent completed board evidence: Wiki index exposes proof-plan packet handoff. `wiki/_index.md` now names the proof_plan proof-plan packet handoff next to the objective-audit tracking links, so the wiki-level Markdown entry point no longer lags the MOCs and browser homes.
- Recent completed board evidence: MOCs expose proof-plan packet handoff. `Agent Studio MOC.md` and `system_design_vault/MOC.md` now both state that proof readiness plus the OpenRouter LiveKit voice and publication boundary maps carry proof_plan proof-plan packets matching `provider-proof-plan`.
- Recent completed board evidence: Project vault home exposes proof-plan packet coverage. `agent-studio-vault-home.html` now tells reviewers that proof readiness plus the voice/publication boundary maps carry `proof_plan` proof-plan packets, so the primary project-vault entry point no longer hides the current proof handoff.
- Recent completed board evidence: Generated system-design viewer carries proof-plan packet state. `output/viewers/agent-studio-system-design.json` and the embedded viewer data now include `proof_plan` / `provider-proof-plan` wording on the Objective Completion Audit component, so the generated inspection surface no longer lags the proof-readiness and boundary-map packets.
- Recent completed board evidence: System-design home exposes proof-plan packet coverage. `system_design_vault/agent-studio-system-design-home.html` now tells architecture reviewers that proof readiness plus the voice/publication boundary maps preserve both credential snapshots and `proof_plan` proof-plan packets.
- Recent completed board evidence: Proof-plan packets propagate to boundary maps. The OpenRouter LiveKit voice and publication boundary maps now render/export `proof_plan` for the remaining blockers, and `tests/test_blocker_snapshot_consistency.py` compares those packets against proof readiness so CLI, proof readiness, and boundary-map operator views cannot drift silently.
- Recent completed board evidence: Proof-readiness proof plan matches CLI output. `agent-studio-proof-readiness.html` now renders/exports `proof_plan` for both remaining blockers, and `tests/test_blocker_snapshot_consistency.py` compares those fields to `_provider_proof_plan_payload` so browser operator packets and CLI output cannot drift silently.
- Recent completed board evidence: Provider proof plan CLI added. `provider-proof-plan` combines the no-secret credential snapshot with the exact live voice and external publication proof commands plus publication policy/disclosure capture requirements, makes no live calls, reads no secret file contents, and keeps `runtime_configuration_present_unverified` separate from actual runtime proof.
- Recent completed board evidence: Secret-file presence recognized in blocker snapshots. `build_blocker_credential_snapshots` now accepts configured file env names, and `blocker-credential-snapshot` detects non-empty `OPENROUTER_API_KEY_FILE`, `LIVEKIT_API_KEY_FILE`, and `LIVEKIT_API_SECRET_FILE` paths by file size without reading contents. File-backed credentials move snapshot state only to `runtime_configuration_present_unverified`; live voice proof still needs runtime evidence.
- Recent completed board evidence: Proof-readiness snapshots match CLI classifier output. `tests/test_blocker_snapshot_consistency.py` now compares proof-readiness `credential_snapshot` exports to `_blocker_credential_snapshot_payload` for the current `.env.example` placeholder state, including `configured_inputs`, `configured_file_inputs`, and `secret_files_loaded`, and still verifies the OpenRouter LiveKit voice and publication boundary maps propagate the same fields.
- Recent completed board evidence: CLI blocker credential snapshot refresh added. Use `PYTHONPATH=src python3 -m all_about_llms.cli blocker-credential-snapshot --checked-at YYYY-MM-DD` from this source checkout, or `all-about-llms-admin blocker-credential-snapshot --checked-at YYYY-MM-DD` from an installed environment, to refresh the remaining blocker credential snapshot without printing values. The command reads placeholder names from `.env.example`, samples only known process env names, and leaves configured credentials in `runtime_configuration_present_unverified` until runtime proof exists.
- Recent completed board evidence: No-secret blocker credential classifier added. `src/all_about_llms/orchestration/blocker_credentials.py` can rebuild the remaining blocker credential snapshots from env-name presence without echoing values; `tests/test_blocker_credential_classifier.py` verifies placeholder-only state, missing required configuration state, configured-input state remains unverified, state-aware note wording, X credential group semantics, and no secret value leakage.
- Recent completed board evidence: Operator proof packets exported for remaining blockers. `tests/test_blocker_proof_packets_browser.py` first failed because the static blocker surfaces lacked concrete proof-packet capture instructions. Proof readiness plus the OpenRouter LiveKit voice and publication boundary maps now render/export `operator_proof_packet`, require runtime capture artifacts, preserve the no-secret-printing rule, and route proof storage back to the project audit, active context, and system-design mirror.
- Recent completed board evidence: Operator proof packets now include operator-input readiness. A red browser regression first showed exported proof packets omitted the strict input gate; proof readiness plus the OpenRouter LiveKit voice and publication boundary maps now render/export value-free input status, next action, strict exit code, blocked fields, strict command, and required evidence after unblock.
- Recent completed board evidence: Static proof-plan exports now include `operator-inputs.template.env` in `workspace_expected_files`, matching the CLI proof-plan workspace contract.
- Recent completed board evidence: Blocker acceptance gates exported across proof surfaces. `tests/test_blocker_acceptance_gates_browser.py` first failed because the static blocker surfaces lacked an explicit acceptance gate. Proof readiness plus the OpenRouter LiveKit voice and publication boundary maps now render/export `proof_acceptance_gate`, state that credential presence is not proof, require same-session runtime evidence for live voice, require exact destination evidence for publication, and compare boundary-map gates to proof readiness.
- Recent completed board evidence: Blocker credential snapshot attribution guarded across exports. `tests/test_blocker_snapshot_consistency.py` first failed because the OpenRouter LiveKit voice and publication boundary exports did not carry the original `non-secret local classifier` source from proof readiness. The boundary exports now retain `source: agent-studio-proof-readiness`, add `source_snapshot: non-secret local classifier`, and the browser regression compares the shared blocker fields against `agent-studio-proof-readiness.html`.
- Recent completed board evidence: System-design home links blocker boundary maps. `system_design_vault/agent-studio-system-design-home.html` now links to the project proof-readiness surface, OpenRouter voice boundary map, and publication boundary map, and preserves credential snapshot export copy with `secret_values_printed=false`. The browser/foundation tests first failed on the missing links/copy, then passed after the home update.
- Recent completed board evidence: Voice/publication boundary maps carry credential snapshot evidence. `openrouter-livekit-voice-boundary-map.html` and `agent-studio-publication-boundary-map.html` now render and export the same non-secret `credential_snapshot` as proof-readiness for the blocked audio proof and external publication proof routes. The browser tests first failed on missing snapshot evidence, then passed after the planning-only maps exported placeholder-only input names and `secret_values_printed=false`.
- Recent completed board evidence: Proof-readiness credential snapshot export. `agent-studio-proof-readiness.html` now renders and exports `credential_snapshot` for the Live voice and Publication blockers, preserving placeholder-only input names, canonical `OPENROUTER_LIVEKIT_URL`, generic `LIVEKIT_URL` absence for voice, no loaded shell values, and `secret_values_printed=false`. `tests/test_proof_readiness_browser.py` first failed on missing snapshot evidence, then passed after the static HTML update.
- Recent completed board evidence: Provider/publication credential blocker snapshot recorded. Non-secret classification found only `.env.example` placeholders for required OpenRouter/Kokoro/LiveKit key material, including `OPENROUTER_LIVEKIT_URL`, and social publish credentials; generic `LIVEKIT_URL` is missing; no secret values were printed. This historical snapshot is superseded by accepted provider-backed live voice proof; external publication proof remains blocked on real configuration and destination evidence.
- Recent completed board evidence: Topbar `Refresh` / `New` source-refresh exclusion proof. `frontend/next-app/tests/browser_single_flight.py` now starts delayed `Run web research`, attempts the exact topbar `Refresh` control while the source-refresh worker cycle is in flight, and proves no manual context-packet refresh starts during that window. It also attempts topbar `New` during the same in-flight window and proves the voice-run starter stays hidden and the active run chip still shows the current run. The fixture scopes manual-refresh counting to that overlap window so normal post-source-refresh context reload remains separate. No product code change was needed because the existing busy state and shared mutation gate already enforced both invariants. Validation passed browser single-flight, foundation (`21 passed`), hygiene checks, and `Leibniz` no-material-findings review.
- Recent completed board evidence: Manual `Continue specialists` source-refresh boundary proof. `frontend/next-app/tests/browser_single_flight.py` now rapid-clicks the Activity rail `Continue specialists` control, waits for the delayed manual specialist worker cycle to start, clicks `Run web research`, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts while manual A2A continuation is in flight. The fixture uses an explicit manual-continuation phase so the later voice follow-up worker-cycle proof remains distinct. No product code change was needed because the existing agent-cycle lock and shared mutation gate already enforced the invariant. Validation passed browser single-flight, foundation (`21 passed`), hygiene checks, and `Leibniz` no-material-findings review.
- Recent completed board evidence: Feedback `Resolve` source-refresh boundary proof. `frontend/next-app/tests/browser_single_flight.py` now seeds an open feedback gate, rapid-clicks the exact `Resolve` button, waits for delayed feedback resolution to start, clicks `Run web research`, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts while feedback resolution is in flight. The same browser proof asserts rapid Resolve clicks create one feedback resolution request with the expected creator-app payload. No product code change was needed because the existing feedback action gate already enforced the invariant. Validation passed browser single-flight, foundation (`21 passed`), hygiene checks, and `Leibniz` no-material-findings review.
- Recent completed board evidence: `Check setup` readiness stale-ownership guards. `frontend/next-app/tests/voiceProviderReadiness.test.ts` now pins `shouldApply: isCurrentSetupCheck` for both setup-check runtime preflight readiness and provider-readiness fanout calls. The red source contract first failed on the missing ownership predicates; `RealtimeVoicePanel.handleSetupCheck` now prevents stale setup-check completions from repainting readiness after a newer run/check owns the panel. Validation passed browser single-flight, frontend race suite (`250 passed`), Next typecheck, Next lint, foundation (`21 passed`), `git diff --check`, touched-file hygiene, and `Leibniz` no-material-findings review.
- Recent completed board evidence: resolver/provider readiness stale-ownership guards. `frontend/next-app/tests/voiceProviderReadiness.test.ts` now pins `shouldApply: isCurrentSetupAction` for resolver provider readiness refresh and `shouldApply: isCurrentProofAction` for live-proof runtime preflight. The red source contract first failed on the missing ownership predicates; `RealtimeVoicePanel.handleResolveVoiceSetup` and `handleLiveVoiceProofPathAction` now prevent stale run/session/action completions from repainting readiness after ownership changes. Validation passed browser single-flight, frontend race suite (`250 passed`), Next typecheck, Next lint, foundation (`21 passed`), `git diff --check`, touched-file hygiene, and `Leibniz` no-material-findings review.
- Recent completed board evidence: resolver `Run preflight` blocks source-refresh overlap for voice setup. `frontend/next-app/tests/browser_single_flight.py` now clicks the `Resolve next -> Run preflight` action, waits for the delayed full runtime preflight request to start, clicks `Run web research`, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts while resolver preflight plus setup-proof recording is in flight. The red browser run first proved source refresh could overlap resolver preflight; `RealtimeVoicePanel.handleResolveVoiceSetup` now wraps `run_preflight` in `runResolverParentMutation("Running setup runtime preflight", ...)`, keeps the parent voice-proof mutation gate through `refreshVoiceReadiness` and `recordResolverAttempt`, and applies `shouldApply: isCurrentSetupAction` for stale run/session completions. Validation passed browser single-flight, frontend race suite (`250 passed`), Next typecheck, Next lint, foundation (`21 passed`), `git diff --check`, touched-file hygiene, and `Leibniz` no-material-findings review.
- Recent completed board evidence: action-row `Stop` blocks source-refresh overlap for live voice sessions. `frontend/next-app/tests/browser_single_flight.py` starts transcript rehearsal, routes a rehearsal turn, starts the delayed session status update via action-row `Stop`, clicks `Run web research`, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. The red browser run first proved source refresh could overlap Stop; `RealtimeVoicePanel.handleStop` now acquires the parent voice-run mutation gate before local stop state changes and releases it after the durable `endRealtimeSession` path completes. Validation passed browser single-flight, frontend race suite (`250 passed`), Next typecheck, Next lint, foundation (`21 passed`), `git diff --check`, touched-file hygiene, and `Leibniz` no-material-findings review.
- Recent completed board evidence: direct secret-file `Save locally` blocks source-refresh overlap. `frontend/next-app/tests/browser_single_flight.py` now renders selected-provider `LIVEKIT_API_KEY_FILE` diagnostics, fills `LIVEKIT_API_KEY`, starts the delayed `/api/local-secret-files` request, clicks `Run web research`, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run first proved source refresh could overlap the secret write; `RealtimeVoicePanel.handleSaveLocalSecretFile` now uses a synchronous in-flight ref, holds the parent voice-proof mutation gate, applies run/token ownership to stale completions, keeps the gate through provider-readiness refresh, and keeps secret-write failure copy separate from provider-readiness refresh failure. Validation passed browser single-flight, frontend race suite (`250 passed`), Next typecheck, Next lint, foundation (`21 passed`), `git diff --check`, touched-file hygiene, and `Leibniz` no-material-findings fix-back review.
- Recent completed board evidence: direct provider endpoint `Save endpoint` blocks source-refresh overlap. `frontend/next-app/tests/browser_single_flight.py` now fills a provider endpoint row, starts the delayed `/api/local-provider-config` request, clicks `Run web research`, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run first proved source refresh could overlap the config write; `RealtimeVoicePanel.handleSaveLocalProviderConfig` now uses a synchronous in-flight ref, holds the parent voice-proof mutation gate, applies run/token ownership to stale completions, and keeps the gate through all-settled provider/runtime readiness refreshes. Validation passed browser single-flight, frontend race suite (`250 passed`), Next typecheck, Next lint, foundation (`21 passed`), `git diff --check`, touched-file hygiene, and `Leibniz` no-material-findings review.
- Recent completed board evidence: direct `Use local LiveKit dev defaults` blocks source-refresh overlap. `frontend/next-app/tests/browser_single_flight.py` now starts the local LiveKit dev config request, clicks `Run web research` while config is delayed, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run first proved source refresh could overlap the config write; `RealtimeVoicePanel.handleConfigureLocalLiveKitDev` now holds the parent voice-proof mutation gate through config plus all-settled provider/runtime readiness refreshes, applies run-owned `shouldApply` predicates, and distinguishes `ready` / `failed` / `skipped` so stale, duplicate, or gate-denied resolver attempts do not write durable proof. Validation passed browser single-flight, frontend race suite (`250 passed`), Next typecheck, Next lint, foundation (`21 passed`), `git diff --check`, touched-file hygiene, and `Leibniz` no-material-findings fix-back review.
- Recent completed board evidence: `Route rehearsal turn` blocks source-refresh overlap. `frontend/next-app/tests/browser_single_flight.py` now starts the transcript rehearsal realtime-turn request, clicks `Run web research` while that request is delayed, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run first proved source refresh could overlap the turn route; `RealtimeVoicePanel.handleRehearsalTurn` now acquires the parent voice-run mutation gate before `sendRealtimeTurn`, keeps it through route plus run refresh, and releases it before `onVoiceFollowupReady` so the follow-up continuation can acquire its own agent-cycle gate. Validation passed browser single-flight, frontend race suite (`250 passed`), Next typecheck, Next lint, foundation (`21 passed`), `git diff --check`, touched-file hygiene, and `Leibniz` no-material-findings review.
- Recent completed board evidence: voice follow-up continuation blocks source-refresh overlap. `frontend/next-app/tests/browser_single_flight.py` now routes a transcript rehearsal turn that creates a durable follow-up task, waits for the follow-up worker cycle to start, clicks `Run web research`, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts while the voice follow-up continuation is in flight. The red frontend contract first proved the shared mutation gate did not model the active agent-continuation lock; `RunMutationGateSnapshot` now includes `agentCycleInFlight`, `app/page.tsx` passes `agentCycleInFlightRef.current` into the shared gate, and `handleVoiceFollowupReady` checks `blockSameRunMutationIfBusy(runId)` before continuing specialist agents. Validation passed browser single-flight, frontend race suite (`250 passed`), Next typecheck, Next lint, foundation (`21 passed`), `git diff --check`, touched-file hygiene, and `Leibniz` no-material-findings review with a residual note that the global lock should become run-scoped if the product later supports parallel worker cycles across runs.
- Recent completed board evidence: direct Runtime preflight blocks duplicate and source-refresh overlap. `frontend/next-app/tests/browser_single_flight.py` now rapidly clicks `Runtime preflight`, proves one full LiveKit/OpenRouter/Kokoro/Rust/agent preflight request, then clicks `Run web research` while the delayed preflight is in flight and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run first proved source refresh could overlap; `RealtimeVoicePanel` now owns direct runtime preflight with `runtimePreflightActionInFlightRef`, holds the parent voice-proof mutation gate while a run exists, and passes `shouldApply: isCurrentRuntimePreflight` into `refreshVoiceReadiness` so stale preflight success/failure events cannot repaint after run changes. `Leibniz` found the stale-completion event gap and verified it closed. Final validation passed browser single-flight, frontend race suite (`249 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and final `Leibniz` review closure.
- Recent completed board evidence: local LiveKit and OpenRouter/Kokoro process controls block duplicate and source-refresh overlap. `frontend/next-app/tests/browser_single_flight.py` rapidly clicks direct `Stop LiveKit` and `Stop agent`, then resolver `Start LiveKit` and `Start agent`, proving one process request for each path and no source-refresh A2A message or worker cycle while process mutation is in flight. The red browser runs first proved duplicate `Stop LiveKit` requests and resolver `Start LiveKit` source-refresh overlap; `RealtimeVoicePanel` owns LiveKit/voice-agent process actions with synchronous refs, direct controls take the parent voice-proof mutation gate when a run exists, and resolver process branches use `runResolverParentMutation` through process mutation plus durable proof write. `Leibniz` found the resolver skip-parent gap and verified it closed. Final validation passed browser single-flight, frontend race suite (`249 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and final `Leibniz` review closure.
- Recent completed board evidence: provider panel Provider readiness blocks duplicate and source-refresh overlap. `frontend/next-app/tests/browser_single_flight.py` rapidly clicks the exact provider panel `Provider readiness` button, proves one provider-readiness request, then clicks `Run web research` during the delayed refresh and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run first exposed ambiguous button matching, then proved source refresh could overlap; `RealtimeVoicePanel` owns manual provider-panel refreshes with `providerReadinessActionInFlightRef`, takes the token before the no-run branch, holds the parent voice-proof mutation gate for run-bound refreshes, and applies provider-readiness results only while `isCurrentProviderRefresh` still owns the request. `Leibniz` verified the previous no-run single-flight minor is closed. Final validation passed browser single-flight, frontend race suite (`249 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and final `Leibniz` review closure.
- Recent completed board evidence: live voice proof path Refresh provider readiness blocks duplicate and source-refresh overlap. `frontend/next-app/tests/browser_single_flight.py` rapidly clicks the live proof path `Refresh provider readiness` button, proves one provider-readiness request, then clicks `Run web research` during the delayed refresh and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run first proved source refresh could overlap; `RealtimeVoicePanel` owns live proof path actions with `liveVoiceProofPathActionInFlightRef`, exposes `liveVoiceProofPathActionLoading`, holds the parent voice-proof mutation gate for provider-readiness/runtime/presence proof-path checks, and applies provider-readiness results only while `isCurrentProofAction` still owns the request. `Leibniz` found the stale-provider-readiness apply gap and verified it closed. Final validation passed browser single-flight, frontend race suite (`249 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and final `Leibniz` review closure.
- Recent completed board evidence: Resolve next Refresh providers blocks duplicate and source-refresh overlap. `frontend/next-app/tests/browser_single_flight.py` now rapidly clicks `Refresh providers`, proves one provider-readiness refresh and one durable `refresh_provider_readiness` voice setup proof, then clicks `Run web research` during the delayed provider refresh and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser runs first proved duplicate provider refreshes and source-refresh overlap; `RealtimeVoicePanel` now owns resolver actions with `voiceSetupActionInFlightRef`, exposes `voiceSetupActionLoading`, holds the parent voice-proof mutation gate through provider readiness refresh plus proof write, and ignores stale resolver proof-failure callbacks. Final validation passed browser single-flight, frontend race suite (`249 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review closure with no material findings.
- Recent completed board evidence: Check setup blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now starts Check setup, waits until the runtime-preflight branch has started but not completed, clicks `Run web research`, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run first proved source refresh could overlap; `RealtimeVoicePanel` now starts the parent voice-proof mutation gate before setup fanout, uses independent run/token ownership for stale setup completions, clears setup in-flight state on run change/unmount, disables `Resolve next` while Check setup is active, and synchronously blocks same-tick Resolve clicks. Final validation passed browser single-flight, frontend race suite (`249 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review closure with no material findings.
- Recent completed board evidence: Timing ledger blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now starts a delayed realtime voice timing-ledger request, clicks `Run web research` while `Timing ledger` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. No product code change was needed because the parent `voiceProofActionGateRef` already gates Runtime smoke and Timing ledger proof writes. Final validation passed browser single-flight, frontend race suite (`249 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Runtime smoke blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now starts a delayed provider-smoke request, clicks `Run web research` while `Runtime smoke` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run first proved source refresh could overlap because voice proof writes only used the child `RealtimeVoicePanel` guard; `app/page.tsx` now owns a `voiceProofActionGateRef`, includes it in the shared run-mutation gate, starts/finishes it around Runtime smoke and Timing ledger proof writes, and invalidates it on `New`. Final validation passed browser single-flight, frontend race suite (`249 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Join voice room blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now starts a delayed provider-backed LiveKit realtime-session grant, clicks `Run web research` while `Join voice room` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run first proved source refresh could overlap because Join state lived only inside `RealtimeVoicePanel`; `app/page.tsx` now owns a `voiceSessionActionGateRef`, includes it in the shared run-mutation gate, starts it before transcript rehearsal / provider-backed voice session work, finishes it from the voice panel finally path, and invalidates it on `New`. Final validation passed browser single-flight, frontend race suite (`249 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: always-on Stop blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now starts the delayed always-on worker-profile stop request, clicks `Run web research` while the rail `Stop` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. `Leibniz` found the first overlap proof depended on timing margin; the final fixture splits runner-stop and always-on-stop request-start/completion events, and final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Stop runner blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now starts the delayed background-runner stop request, clicks `Run web research` while `Stop runner` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The fixture now splits stop request-start/completion events so the overlap proof is event-bound; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Start runner blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now starts the delayed background runner request, clicks `Run web research` while `Start runner` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The focused browser proof passed immediately because the existing runner-start route already exposed request-start/completion and the product gate already blocked overlap; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Media plan blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now starts a delayed media-plan request, clicks `Run web research` while `Media plan` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The focused browser proof passed immediately because the existing media-plan route already exposed request-start/completion and the product gate already blocked overlap; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Growth package blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now starts a delayed growth-package request, clicks `Run web research` while `Growth package` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The focused browser proof passed immediately because the existing growth-package route already exposed request-start/completion and the product gate already blocked overlap; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Generate blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now separates conversation request start from delayed completion, clicks `Run web research` while `Generate` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run timed out on the new conversation-start signal until the fixture split conversation request-start and completion; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Publish check blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now starts a delayed publish-readiness request, clicks `Run web research` while `Publish check` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The focused browser proof passed immediately because the existing publish-readiness route already exposed request-start/completion and the product gate already blocked overlap; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Send revision blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now separates revision request start from delayed completion, clicks `Run web research` while `Send revision` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run timed out on the new revision-start signal until the fixture split revision request-start and completion; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Start always-on blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now separates always-on launch request start from delayed completion, clicks `Run web research` while `Start always-on` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run timed out on the new launch-start signal until the fixture split launch request-start and completion; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: Run pulse blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now separates manual heartbeat request start from delayed completion, clicks `Run web research` while `Run pulse` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run timed out on the new heartbeat-start signal until the fixture split heartbeat request-start and completion; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: Check due work blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now separates manual scheduler request start from delayed completion, clicks `Run web research` while `Check due work` is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The red browser run timed out on the new scheduler-start signal until the fixture split scheduler request-start and completion; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: always-on pulse/scheduler browser proof. `frontend/next-app/tests/browser_single_flight.py` now rapidly double-clicks `Run pulse` after `Start always-on` and proves one worker-profile heartbeat request plus creator-facing `Specialist pulse finished.` copy, then rapidly double-clicks `Check due work` and proves one run-scoped scheduler request with `run_id`, `execution_mode=autonomous_pass`, and `max_profiles=10` plus creator-facing checked-work copy. The red browser run timed out on the new heartbeat signal until the fixture modeled heartbeat and scheduler endpoints; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: long-running stop controls browser proof. `frontend/next-app/tests/browser_single_flight.py` now rapidly double-clicks `Stop runner` after `Start runner` and proves one background-runner stop request plus creator-facing stopped copy, then rapidly double-clicks the always-on rail `Stop` control and proves one worker-profile stop request plus creator-facing stopped copy. The red browser run timed out on the new runner-stop signal until the fixture modeled stop endpoints; final validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: HLD public projection discovery parity. `tests/test_foundation.py::test_obsidian_vault_contains_interactive_design_and_tracking_artifacts` first failed because the project HLD and system-design HLD omitted the well-known Agent Card, `/api/a2a`, `projection=public`, `agent-message-public-projection-v1`, and supported read endpoints; both HLDs now record the shared external-safe A2A task-inspection policy.
- Recent completed board evidence: Well-known Agent Card public projection contract. `tests/test_api_contracts.py::test_public_a2a_agent_card_advertises_public_projection_contract` first failed because `/.well-known/agent-card.json` omitted the task-inspection public projection policy; `x-agentStudio.publicProjection` now matches `/api/a2a` for supported read endpoints, unsupported mutation endpoints, and `agent-message-public-projection-v1`.
- Recent completed board evidence: A2A map public projection parity. `tests/test_a2a_map_browser.py::test_agent_studio_a2a_map_filters_public_boundary_and_exports_context` first failed because the Public boundary detail/export omitted `projection=public`, `agent-message-public-projection-v1`, and supported projection endpoints; `agent-studio-a2a-map.html` now carries those fields in the visible detail and filtered export.
- Recent completed board evidence: A2A discovery public projection contract. `tests/test_api_contracts.py::test_a2a_http_json_interface_advertises_public_projection_contract` first failed because `/api/a2a` listed task routes but did not advertise `projection=public`; it now exposes `publicProjection` with supported read endpoints, unsupported mutation endpoints, and `agent-message-public-projection-v1`.
- Recent completed board evidence: A2A agent inbox public projection. `tests/test_api_contracts.py::test_a2a_agent_inbox_public_projection_redacts_sensitive_fields` first failed because `/api/a2a/agents/{agent_id}/inbox?projection=public` returned private payloads with no projection marker; the route now reuses the single-message public projection helper and redacts sensitive keys plus token-shaped strings across per-agent inbox polling.
- Recent completed board evidence: run-level A2A message list public projection. `tests/test_api_contracts.py::test_a2a_run_message_list_public_projection_redacts_sensitive_fields` first failed because `/api/runs/{run_id}/agent-messages?projection=public` returned private payloads with no projection marker; the route now reuses the single-message public projection helper, redacts sensitive keys and token-shaped strings across message lists, and `/api/a2a` advertises `listRunMessages`.
- Recent completed board evidence: Activity retry blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now marks targeted `Queue and run` retry worker-cycle start before delayed completion, clicks `Run web research` while that retry cycle is in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts during or shortly after the retry. The proof still asserts one retry authorization, one targeted retry worker cycle, and a strictly targeted retry payload for the retried message id. `Leibniz` first found the overlap was anchored to retry authorization rather than worker-cycle start; the final proof adds `retry_cycle_started` and re-review found no actionable findings. Validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), and touched-file hygiene.
- Recent completed board evidence: planned-agent execution blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now separates `Run next steps` planned worker-cycle request start from delayed completion, clicks `Run web research` while the planned worker cycle is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. The existing `Run next steps` proof still asserts one work-plan materialization, one bounded planned worker cycle, and one post-run refresh. Validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: work-plan planning blocks source-refresh browser proof. `frontend/next-app/tests/browser_single_flight.py` now separates `Suggest next step` request start from delayed completion, clicks `Run web research` while work-plan planning is still in flight, and proves neither the source-refresh A2A message nor the source-refresh worker cycle starts. `Leibniz` found the first version only checked the worker cycle; the final proof also snapshots the earlier A2A message count. Validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and final `Leibniz` review with no actionable findings.
- Recent completed board evidence: source-refresh blocks production browser proof. `frontend/next-app/tests/browser_single_flight.py` now separates source-refresh worker-cycle request start from delayed completion, reruns `Run web research`, waits for the worker cycle to start, clicks `Growth package`, and proves no growth-package request is sent while source refresh is in flight. No product code change was needed because the existing source-refresh mutation gate already blocked overlapping same-run production work. Validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: production packaging browser single-flight and cross-action proof. `frontend/next-app/tests/browser_single_flight.py` now rapidly double-clicks `Growth package` and proves one growth-package request with platform/audience/campaign/outreach metadata, then rapidly double-clicks `Media plan` and proves one media-production request with image/audio/video planning options. The smoke separates request-start from delayed response completion and proves `Growth package` blocks `Media plan`, while `Media plan` blocks `Publish check`, during the delayed in-flight window. No product code change was needed because the existing production action gate already blocked duplicate submits and overlap. Validation passed browser single-flight, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: provider-backed Join voice room browser proof and cleanup hardening. `frontend/next-app/tests/browser_single_flight.py` now rapidly double-clicks `Join voice room` after full mocked OpenRouter/Kokoro/LiveKit readiness and proves one provider-backed realtime-session request with `dry_run=false` plus one cleanup status update when the browser LiveKit join is blocked. `RealtimeVoicePanel` now disables Join while a live session is retained, ends stale provider sessions after stale start or stale post-join completions, and only clears local active session state after durable `endRealtimeSession` succeeds; cleanup failures keep Stop available for retry. Validation passed the red/green cleanup source contract, browser single-flight proof, frontend race suite (`248 passed`), Next typecheck, Next lint, foundation (`21 passed`), touched-file hygiene, and final `Leibniz` review with no material findings.
- Recent completed board evidence: live voice Interrupt/Stop single-flight hardening. `frontend/next-app/tests/voiceCancellation.test.ts` first failed because `RealtimeVoicePanel` had no synchronous in-flight guard before live interrupt/stop control calls. The panel now uses interrupt and stop action tokens before async LiveKit/backend control paths, requires ready active-session ownership before publishing interrupt controls, invalidates pending interrupt ownership when Stop starts, and clears both controls on Start, run change, and unmount. `Leibniz` found the same-tick Stop-then-Interrupt stale LiveKit publish edge; the follow-up guard closes it. Validation passed the red/green source contract, full frontend race suite (`247 passed`), Next typecheck, Next lint, touched-file hygiene, and final `Leibniz` review with no material findings.
- Recent completed board evidence: cockpit walkthrough realtime provider-turn ownership hardening. `tests/test_api_contracts.py::test_cockpit_walkthrough_rejects_rehearsal_as_provider_smoke` now proves prior rehearsal turns, ended provider sessions, and active-but-expired provider sessions cannot satisfy selected-provider turn evidence. `_realtime_smoke_step` requires turn events to match active, unexpired selected provider-backed session ids before clearing `provider_backed_realtime_turn:<provider>`, and emits `active_provider_backed_realtime_session:<provider>` or `unexpired_provider_backed_realtime_session:<provider>` for stale session boundaries. Validation passed focused cockpit walkthrough/realtime-dialogue tests, compileall, foundation, touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: cockpit walkthrough feedback-loop gate hardening. `tests/test_api_contracts.py::test_cockpit_walkthrough_feedback_step_holds_routed_demo_gate` first failed because a routed demo feedback gate was marked optional/ready; `_feedback_loop_step` now treats open and routed feedback as unresolved, makes the feedback-loop step required, exposes open/routed/unresolved counts, carries `unresolved_feedback_items`, and links `feedback_routed`. Endpoint proof now also verifies `/api/runs/{run_id}/cockpit-walkthrough-ledger` preserves those routed-gate semantics from store state through the API response. Validation passed focused cockpit walkthrough and feedback-ledger tests, compileall, foundation, touched-file hygiene, and `Leibniz` review with no material findings.
- Recent completed board evidence: live text-turn duplicate-submit and pre-send ownership hardening. `frontend/next-app/tests/voiceLiveTextTurn.test.ts` first failed on the missing synchronous submit gate, then failed on the missing post-interrupt ownership recheck before `sendTranscriptTurn`; `RealtimeVoicePanel` now uses `liveTextTurnInFlightRef`, run/session/token ownership, invalidation on run change/Start/Stop/unmount, and a pre-send current-owner recheck. Validation passed focused frontend race contracts (`246 passed`), typecheck, lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: transcript rehearsal turn browser single-flight proof. After the dry-run rehearsal session exists, `frontend/next-app/tests/browser_single_flight.py` rapidly double-clicks `Route rehearsal turn` and proves one realtime-turn request with `input_surface=voice_runtime_transcript_rehearsal`, `provider_backed_realtime=false`, and `rehearsal_only=true`; `RealtimeVoicePanel` now uses `canRouteRehearsalTurn`, a synchronous `rehearsalTurnInFlightRef`, run/session/token completion ownership, and Start/Stop active-session invalidation before routing the rehearsal turn. Validation passed browser single-flight, focused frontend race contracts (`245 passed`), typecheck, lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: transcript rehearsal start browser single-flight proof. `frontend/next-app/tests/browser_single_flight.py` switches the voice runtime to `Transcript rehearsal`, rapidly double-clicks `Start transcript rehearsal`, and proves exactly one dry-run realtime-session request with `transport_framework=local_rehearsal` and `dry_run=true`; `RealtimeVoicePanel` now uses a synchronous `startActionInFlightRef` before start-session API work while the existing run-bound sequence guard still rejects stale start completions. Validation passed browser single-flight, focused race contracts (`244 passed`), typecheck, lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: voice proof-action browser single-flight proof. `frontend/next-app/tests/browser_single_flight.py` rapidly double-clicks `Runtime smoke` and proves one non-live provider-smoke request with `execute_live_calls=false`, rapidly double-clicks `Timing ledger` and proves one realtime timing-ledger request with `event_limit=500`, and cross-clicks the two proof buttons in both orders to prove one in-flight proof action blocks the other; `RealtimeVoicePanel` now uses a shared synchronous `voiceProofActionInFlightRef` before smoke/timing proof APIs so rapid duplicate proof clicks cannot create duplicate proof writes. Validation passed browser single-flight, focused race contracts (`244 passed`), typecheck, lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: voice Check setup browser single-flight proof. `frontend/next-app/tests/browser_single_flight.py` rapidly double-clicks `Check setup` after a voice-first run exists and proves the browser records exactly one durable voice setup proof after one LiveKit process refresh, one voice-agent process refresh, one provider-readiness refresh, one runtime preflight, and one voice-agent presence check; `RealtimeVoicePanel` now uses a synchronous setup-check ref gate before that multi-call setup/proof fanout, and `settleVoiceSetupFanout` holds the gate until every branch settles even when one branch fails early. Validation passed browser single-flight, focused race contracts (`244 passed`), typecheck, lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- Recent completed board evidence: Runtime preflight browser single-flight proof. `frontend/next-app/tests/browser_single_flight.py` rapidly double-clicks `Runtime preflight` after a voice-first run exists and proves the browser sends exactly one readiness request with `preflight_edge=true`, `preflight_agent=true`, `preflight_livekit=true`, `preflight_gemma=true`, and `preflight_tts=true`; `RealtimeVoicePanel` now uses keyed single-flight readiness promises, preserves stronger full-preflight evidence over weaker same-epoch overlaps, and bumps a readiness epoch after config/process mutations so lower-strength refreshes can still update changed setup state.
- Recent completed board evidence: voice-run create browser single-flight proof. `frontend/next-app/tests/browser_single_flight.py` rapidly double-clicks `Create voice run` on the no-run Live Voice panel and proves the browser creates exactly one voice-mode `POST /api/runs` payload with `live_voice_panel`, `provider_backed_realtime=true`, `google/gemma-4-E4B-it`, and `hexgrad/Kokoro-82M` provenance, refreshes the same run context, hides the no-run starter, and binds the next Generate turn to that run.
- Recent completed board evidence: Activity retry browser single-flight proof. `frontend/next-app/tests/browser_single_flight.py` renders a failed specialist task in `Needs attention`, rapidly double-clicks `Queue and run`, and proves the browser creates exactly one retry authorization plus one targeted retry worker cycle for the retried message id.
- Recent completed board evidence: publication boundary browser proof. `tests/test_publication_boundary_browser.py` opens `03-review-packets/agent-studio-publication-boundary-map.html` in Chromium, filters to `External proof`, `Non-live smoke`, `Policy review`, and `Rollback`, and proves exported JSON preserves `external-publication-proof`, `non-live-channel-smoke`, `publish_channel_checks`, `missing_publish_channel_credentials`, `publish_channel_policy_review_required`, destination credential names, exact policy review, durable platform evidence, rollback proof, and the rule that non-live smoke is not real API publish proof.
- Recent completed board evidence: OpenRouter LiveKit voice boundary browser proof. `tests/test_openrouter_livekit_voice_boundary_browser.py` opens `02-research/openrouter-livekit-voice-boundary-map.html` in Chromium, filters to `Current`, `OpenRouter`, `Kokoro TTS`, `LiveKit`, `Rust edge`, `Proof gate`, and `Legacy`, and proves the exported boundary packet preserves `provider-backed-live-voice-proof`, `openrouter-deepseek-reasoning`, `hexgrad/Kokoro-82M`, current OpenRouter/LiveKit input names, `realtime_voice_timing_ledger`, and legacy/non-default Gemma/HF background copy in `openrouter-livekit-voice-boundary-map.html`.
- Recent completed board evidence: feedback loop map browser proof. `tests/test_feedback_loop_map_browser.py` opens `03-review-packets/agent-studio-feedback-loop-map.html` in Chromium, filters to `Guardrails` and `Publish gate`, and proves the exported packet preserves `feedback_resolution_ledger`, accepted/revised/held/rejected outcomes, failed linked task ids, held-until-review policy, `publish_readiness_checked`, unsupported-claim blockers, and external-publication blocker copy.
- Recent completed board evidence: A2A map browser proof. `tests/test_a2a_map_browser.py` opens `00-system-design/agent-studio-a2a-map.html` in Chromium, filters to `Public boundary` and `Repair`, and proves the exported A2A packet preserves `/.well-known/agent-card.json`, `/api/a2a`, `fullA2AProtocolServer=false`, JSON/text HTTP+JSON scope, retry/dependency-repair routes, and `a2a_graph_blocked` evidence while staying a planning-memory surface.
- Recent completed board evidence: A2A context-packet event redaction. `tests/test_api_contracts.py::test_context_packet_built_event_redacts_task_type_and_memory_query` first failed on raw HF/Bearer-shaped worker task type and project-memory query strings inside `context_packet_built`; Leibniz then found the `/api/runs/{run_id}/context-packet` event writer, and `tests/test_api_contracts.py::test_context_packet_endpoint_event_redacts_project_memory_query` first failed on the endpoint's raw project-memory query. `src/all_about_llms/orchestration/a2a_projection.py` now supplies `public_a2a_context_packet_event_payload` for both writers, preserving context counts, skill ids, project-memory policy, and retrieval summary shape while redacting token-shaped event strings.
- Recent completed board evidence: A2A stale-recovery event redaction. `tests/test_api_contracts.py::test_agent_message_recovered_event_redacts_task_type_and_previous_claimant` first failed on raw HF/Bearer-shaped task type and previous claimant values inside `agent_message_recovered`; `src/all_about_llms/orchestration/a2a_projection.py` now supplies `public_a2a_recovered_event_payload`, preserving message id, transition status, previous timestamp, worker id, and stale threshold while redacting task type and previous claimant strings.
- Recent completed board evidence: A2A retry/dependency event redaction. `tests/test_api_contracts.py::test_a2a_retry_and_dependency_events_redact_task_type_and_reason_without_real_database` first failed on raw HF/Tavily/Bearer-shaped task type and reason values inside `agent_message_retry_authorized` and `agent_message_dependencies_repaired` events; Leibniz then found adjacent worker events, and `tests/test_api_contracts.py::test_worker_control_events_redact_dependency_waiting_and_retry_exhausted_metadata` first failed on raw task metadata in `agent_message_dependency_waiting` and `agent_message_retry_exhausted`. `src/all_about_llms/orchestration/a2a_projection.py` now supplies `public_a2a_retry_event_payload`, `public_a2a_dependency_repair_event_payload`, `public_a2a_dependency_waiting_event_payload`, and `public_a2a_retry_exhausted_event_payload`, preserving retry counts and dependency ids while redacting task type, sender/recipient/worker, notes, and reason strings.
- Recent completed board evidence: A2A status-event redaction. `tests/test_api_contracts.py::test_a2a_message_status_event_redacts_task_type_and_notes_without_real_database` first failed on raw HF/Bearer-shaped strings in `agent_message_status_updated` event notes and task type; `src/all_about_llms/orchestration/a2a_projection.py` now supplies `public_a2a_status_event_payload` backed by `redact_realtime_string` for API and worker status-event writers, preserving message id/status/skill metadata including worker `skill_ids` while redacting task type, sender/recipient, and notes strings.
- Recent completed board evidence: A2A accepted-message event redaction. `tests/test_api_contracts.py::test_a2a_message_accepted_event_uses_public_projection_without_real_database` first failed on a raw `api_key` inside an `agent_message_accepted` event payload, and Leibniz then found token-shaped top-level `task_type` / `claimed_by_agent_id` also needed the same boundary; `src/all_about_llms/orchestration/a2a_projection.py` now supplies the shared public projection for accepted-message event writers across API and orchestration paths, preserving message ids while redacting Bearer/HF/Tavily-shaped task, claimant, payload, result, handoff trace, and error values.
- Recent completed board evidence: A2A collaboration graph trace-note redaction and A2A graph trace identity fix-back. `tests/test_api_contracts.py::test_a2a_collaboration_graph_maps_dependencies_traces_and_ready_work` proves Bearer/HF/Tavily-shaped strings in handoff notes, trace actor/action/status, and task-node `latest_handoff_action` are redacted in both the graph API response and recorded graph artifact.
- Recent completed board evidence: proof readiness browser proof. `tests/test_proof_readiness_browser.py` opens `01-work-tracking/agent-studio-proof-readiness.html` in Chromium, filters to `Live voice` and `Publication`, and proves the exported blocker packet preserves `provider-backed-live-voice-proof`, `external-publication-proof`, blocked status, provider env/input names, timing-ledger evidence, publication credential names, and durable publication proof requirements while not marking the objective complete.
- Recent completed board evidence: skill matrix browser proof. `tests/test_skill_matrix_browser.py` opens `00-system-design/agent-studio-skill-matrix.html` in Chromium, filters to `Guardrails`, and proves the detail pane plus exported JSON preserve `agent-studio-guardrails-review`, `feedback_resolution_ledger`, guardrail/reviewer agents, and the source `SKILL.md` path while staying a planning-memory surface.
- Recent completed board evidence: system-design-vault browser index proof. `tests/test_system_design_vault_home_browser.py` opens `system_design_vault/agent-studio-system-design-home.html` in Chromium and proves it exposes the objective-audit mirror, Gemma/realtime sources, production canon, source map, and live-provider blocker copy while staying a planning-memory surface.
- Recent completed board evidence: system-design source-map viewer browser proof. `tests/test_system_design_source_map_viewer_browser.py` opens `system_design_vault/output/viewers/system-design-source-map.html` in Chromium and proves the generated viewer renders source records, source groups, policy lists, first-slice items, search, kind filter, and coverage-granularity filter, plus design implications, implication search, visible record counts, no-match states, and clickable source-note paths.
- Recent completed board evidence: project vault-home source-map route. `agent-studio-vault-home.html` and `Agent Studio MOC.md` now expose `../system_design_vault/output/viewers/system-design-source-map.html`, and `tests/test_vault_home_browser.py` proves the route reaches `Agent Studio Source Map` with 175 source records and 126 design implications.
- Recent completed board evidence: system-design vault entry links to the HLD/LLD review surface and generated system-design viewer. The vault index and MOC now route reviewers to `Project HLD/LLD Review Surface` for comment export and `Project System Design Viewer` for generated viewer inspection while keeping Obsidian notes as source truth.
- Recent completed board evidence: vault home generated-viewer navigation proof. `tests/test_vault_home_browser.py` opens `agent-studio-vault-home.html`, follows the generated Memory OS and System Design Viewer links, and verifies the destination headings/content.
- Recent completed board evidence: retrieval research browser filter proof. `tests/test_retrieval_research_browser.py` opens `02-research/retrieval-quality-research.html` in Chromium, filters to the Graph research area, and proves only graph cards stay visible with the correct count and summary copy.
- Recent completed board evidence: HLD/LLD design-review browser interaction proof. `tests/test_hld_lld_viewer_browser.py` opens `00-system-design/agent-studio-hld-lld.html` in Chromium, saves a high-priority Retrieval comment from `Codex Browser Proof`, verifies the rendered comment references the Retrieval Intelligence Agent, and proves the exported comment packet preserves `agent-studio-hld-lld`, source, target, priority, author, and routing action.
- Recent completed board evidence: work tracker browser interaction proof. `tests/test_work_tracker_browser.py` opens `01-work-tracking/agent-studio-work-tracker.html` in Chromium, advances `Functional Content Studio Cockpit` from `in_progress` to `blocked`, and proves the exported tracker packet preserves `agent-studio-work-tracker`, the updated item status, and the requested planning action.
- Recent completed board evidence: generated Memory OS viewer browser interaction proof. `tests/test_memory_os_viewer_browser.py` opens `output/viewers/agent-studio-memory-os-viewer.html` in Chromium, filters to the `Output Memory` tier, and proves the visible tier is also promoted into the selected detail pane with the generated-output authority question. The HTML-embedded Memory OS JSON now matches the standalone projection after `Leibniz` found stale embedded data.
- Recent completed board evidence: work-plan execution browser single-flight proof. `frontend/next-app/tests/browser_single_flight.py` rapidly double-clicks `Run next steps` after `Suggest next step` in Chromium and proves the browser creates exactly one work-plan materialization, one bounded planned worker cycle, and one post-run next-action refresh.
- Recent completed board evidence: generated system-design viewer browser interaction proof. `tests/test_system_design_viewer_browser.py` opens `output/viewers/agent-studio-system-design-viewer.html` in Chromium, filters for `external publication proof`, and proves the visible `Objective Completion Audit` card is also promoted into the selected detail pane with `Do not call the goal complete` blocker copy.
- Recent completed board evidence: source-refresh browser single-flight proof. `frontend/next-app/tests/browser_single_flight.py` keeps an unresolved search-seed claim visible, rapidly double-clicks `Run web research` in Chromium, and proves the browser creates exactly one A2A source-refresh message plus one bounded Web Research / Claim Verification worker cycle. `browserSingleFlightContract.test.ts` and the Next app README lock this proof.
- Recent completed board evidence: always-on/background-runner browser status proof. `frontend/next-app/tests/browser_single_flight.py` clicks `Start always-on` and `Start runner` in Chromium against stale backend summary fixtures, then proves the browser-visible status strip shows `Always-on studio launch recorded`, `Creator always-on studio`, and `Background runner is running` while hiding raw `Autopilot` / `local scheduler` wording.
- Recent completed board evidence: objective completion audit projected into the generated system-design viewer. `output/viewers/agent-studio-system-design.json` and the embedded HTML data now include an `Objective Completion Audit` component and list `01-work-tracking/Agent Studio Objective Completion Audit.md` in `canonical_sources`; this viewer note is superseded by accepted provider-backed live voice proof, so the remaining active evidence blocker is external publication proof plus closure review.
- Recent completed board evidence: objective completion audit added. `01-work-tracking/Agent Studio Objective Completion Audit.md` maps the full active objective to current proof, caveats, blockers, and next executable slices, and `system_design_vault/04-agent-studio-implications/agent-studio-objective-completion-audit.md` mirrors the design implications with source attribution back to the project vault.
- Recent completed board evidence: generated system-design viewer projection refresh. `output/viewers/agent-studio-system-design.json` and the embedded HTML viewer data now carry `updated: 2026-05-19` plus an `Always-On Activity Rail` component with `Always-on studio`, `Specialist pulse`, and `Background runner` language. The foundation freshness regression asserts exact standalone/embedded projection equality and guards against stale active-context `Local scheduler` proof wording.
- Recent completed board evidence: LLD background-runner product-language contract synchronization. The planning LLD and system-design LLD now both require creator-visible process control to show `Background runner`, `Always-on studio`, and `Specialist pulse` language while local scheduler/autopilot terms remain internal implementation identifiers; the foundation regression prevents the stale `Local scheduler` status contract from returning.
- Recent completed board evidence: creator-visible always-on/background-runner copy tightening. Page-level busy labels, summaries, fallback errors, raw backend result summaries, and the default profile name now use `Always-on studio`, `Specialist pulse`, and `Background runner` language instead of visible `Autopilot` / `local scheduler` copy, with source/AppShell contracts guarding against regressions.
- Recent completed board evidence: source-refresh same-run mutation gate hardening. `Run web research` now owns `sourceRefreshActionGateRef`, `runMutationGate` treats source refresh as an in-flight same-run mutation, and guarded same-run actions call `blockSameRunMutationIfBusy(...)` before mutating APIs. Focused race/source contracts, typecheck, lint, persistent browser single-flight smoke, source-refresh rapid double-click browser proof, and `Leibniz` review passed.
- Recent completed board evidence: browser-level interaction coverage for the source-contract action single-flight guards. `npm run test:browser-single-flight` starts or reuses a local Next dev server, mocks FastAPI in Chromium, rapidly double-clicks Generate, Run web research, Queue and run, Suggest next step, Run next steps, Publish check, and Send revision while API responses are delayed, then clicks Start always-on and Start runner with stale backend summaries to prove creator-facing Always-on studio / Background runner status copy; each guarded path must create exactly one backend action.
- Next board item: select the next bounded implementation slice from this active goal, current board state, and current review-watch status, with a failing-first test, implementation target, and validation plan before patching.
- Why this is next: provider-backed live voice proof is accepted and external publication proof remains blocked on real credentials, so the next unblocked slice should be a small product/proof hardening patch chosen from current app/backend/vault gaps before scanning the full sprint log; credential-gated publication proof remains blocked until real external evidence exists.
- Current ready fallback: review-watch heartbeat alignment is current; the next review action is the next material code slice review after implementation changes. Material reviewer findings should still surface with severity, files, and next action.
- Current blocked items: external publication proof still needs real destination credentials plus channel policy review, durable destination, and rollback/postcondition evidence. Provider-backed live voice is accepted for the current OpenRouter DeepSeek + LiveKit + Kokoro path.
- Next code slice trigger: if backend, frontend, Rust, project-skill, or release-gate contracts change, send a focused review packet to `Leibniz` before closeout.

## Current Implementation State

- Foundation schemas, agent roster, provider boundaries, Postgres migrations, and app shell exist.
- A2A discovery now has a public, secret-free `/.well-known/agent-card.json` for the Agent Studio orchestrator. It follows the current A2A 0.3 Agent Card shape with `HTTP+JSON` transport pointed at `/api/a2a`, advertises the nine project skills, and exposes the 38 internal specialists under `x-agentStudio` while clearly marking `fullA2AProtocolServer=false`. `/api/a2a` is a compatibility/discovery root that links method-aware durable HTTP routes for messages, read-only task lookup, worker cycles, cards, skills, retries, dependency repair, and collaboration graphs. `/.well-known/agent-card.json`, `/api/a2a`, and the A2A map all advertise `projection=public` under `agent-message-public-projection-v1` for `getTask`, `listRunMessages`, and `agentInbox`; mutation routes remain private. `GET /api/a2a/messages/{message_id}` remains the trusted-local raw `AgentMessage` projection unless public projection is requested.
- Foundation audit now gates project skill source contracts, not just roster coverage. The audit includes `agent-skill-source-contracts`, reuses the skill-usage source-contract parser, fails when advertised `SKILL.md` files, frontmatter ids/descriptions, `agents/openai.yaml` interface manifests, or agent mappings are broken, and routes blocking remediation to Agent Harness Engineer.
- The creator revision loop is now durable and replay-safe. `feedback_revision_v1` feedback, revision A2A tasks, revised artifacts, review decisions, and revision gate events use deterministic ids plus idempotent proof events. Postgres exposes advisory-locked `append_event_if_absent`; live Postgres coverage proves duplicate calls return the same event. Replays after partial row persistence repair missing proof events, explicit target ids are filtered to revision-safe publishable artifact types, legacy feedback metadata cannot revise operational artifacts, repeated legacy replays recover original parent artifacts instead of revising revised leaves, and Gemma revision provider fallback reasons are redacted before persistence.
- The creator Activity panel now has a product-native `Plan next` action. It calls `POST /api/runs/{run_id}/work-plan`, records a durable work-plan artifact, and renders compact next agent actions in the run rail without board-style project-management or planning-cockpit UI. The work-plan selector must handle both stale directions: newer durable `run_work_plan_v1` proof wins once the live result's artifact id is present in the artifact list, while a fresh live result wins if the refreshed artifact list has not yet caught up. Partial historical proof artifacts are parsed defensively so optional missing fields cannot crash the UI.
- The creator Activity panel can now execute the current work plan with `Run plan`. It derives a bounded target agent list from work-plan recommendations plus item owners, rejects zero-item plans, rejects plans from a different run, materializes concrete follow-up A2A tasks with `create_followup_tasks=true`, and sends only those agents into the existing guarded worker-cycle path. `Run plan` must acquire `agentCycleInFlightRef` before materializing tasks and hold it through task materialization, the targeted cycle, and the post-run next-action refresh, so double-clicks or concurrent cycles cannot leave newly created work-plan tasks stranded. `Plan next` and `Run plan` also exclude each other synchronously: `Plan next` blocks while the agent-cycle lock is active, and `Run plan` blocks while the work-plan planning gate is active, preventing stale same-run plan overwrites after rapid cross-clicks. Other run-mutation entry points, including manual continuation, live voice follow-up continuation, source refresh, Activity retry, composer submit, feedback actions, production actions, Autopilot profile controls, and local scheduler controls, must reject while the work-plan planning gate is active so stale next-action plans cannot commit after new same-run mutations. `Plan next` must reject while those mutation gates or scheduler/heartbeat refs are active. The shared `runMutationGate` helper owns this decision shape and has behavior tests for idle, feedback/production/heartbeat, and run-scoped scheduler cases; page-level contracts still prove the critical handlers call it before mutating APIs. After agents run, it rebuilds a post-run work plan with `create_followup_tasks=false` and `refresh_reason=creator_app_after_run_plan` so the rail shows the next state rather than the just-executed pre-run plan. This is the product bridge from planning proof to synchronized specialist execution.
- Work-plan proof in Activity is product-visible and compact. The rail labels the current plan as `manual plan`, `execution plan`, `post-run refresh`, or a formatted fallback reason, and shows created follow-up task count, duplicate-skip count, open feedback, and routed feedback when those counters are nonzero. This evidence should stay compact and should not become a planning dashboard.
- Feedback resolution ledgers now expose loop outcome analytics. Each feedback entry is classified as `accepted`, `revised`, `held`, or `rejected`, with ledger totals mirrored into artifact content and `feedback_resolution_ledger_built` events. Open/routed feedback and resolved feedback with pending, blocked, failed, or canceled linked tasks remain held, so autonomous passes do not treat unfinished or failed specialist work as closed.
- The `agent-studio-guardrails-review` project skill is now tied to that ledger contract. Guardrail reviewers should cite `feedback_resolution_ledger`, accepted/revised/held/rejected counts, failed linked task ids, and the next action for held feedback before approving publish readiness.
- Publish readiness now has an opt-in non-live channel smoke. Product Publish check requests it, and results distinguish missing local credentials from unacknowledged platform-policy review for Instagram, LinkedIn, X, and Substack without posting externally or exposing secret values. Known aliases normalize before credential lookup, unsupported platform ids hard-block, and direct selected `post` / `reel_script` artifacts contribute their platform metadata to the same check. The product UI must render every channel check with env names and recommended actions so late-list failures are not hidden. Residual risk: this is a credential/policy smoke, not live destination API publishability proof.
- Source refresh is a first-class same-run mutation boundary. `Run web research` must acquire `sourceRefreshActionGateRef` before browser-triggered source APIs, include that gate in the shared `runMutationGate` snapshot, and reject other same-run mutation handlers through `blockSameRunMutationIfBusy(...)` before composer, retry, feedback, production, Autopilot, local scheduler, and worker-cycle calls overlap. New/run replacement must invalidate the source-refresh gate.
- Recent specialist results are now product-visible in Activity. Completed A2A messages render in a compact `Recent agent outcomes` strip, sorted by latest update time and capped to four entries. The strip uses explicit result summaries when present, otherwise artifact-count and generation-mode fallback copy, and it must not dump raw nested provider/task JSON into the creator UI. Pending tasks remain in the separate active-task list.
- Recent specialist outcomes now link to produced artifacts when those artifacts are present in the run context. Worker-result artifact ids are deduped across known fields before counting or linking. Activity chips show artifact title/type rather than raw ids, and DraftBoard cards expose stable `artifact-*` targets. Hash navigation switches DraftBoard to the target's content/media/growth/proof filter and scrolls the card into view without changing selected revision targets.
- Failed and blocked specialist tasks are product-visible in Activity. Failed, blocked, and canceled A2A messages render in a compact `Needs attention` strip, sorted by latest update time and capped to four entries. They are deliberately separate from active pending tasks and completed outcomes so the creator can distinguish work in progress, work that produced artifacts, and work needing an operator decision.
- Creator retry from Activity is a guarded `Queue and run` action. It calls the existing A2A retry authorization endpoint, then runs one bounded worker cycle with `message_ids=[retried_message_id]` and the retried recipient agent so the exact accepted retry is processed. The frontend sets both the per-message in-flight guard and the global agent-cycle lock before the POST, disables only that message's retry button while running, clears guards in `finally`, and preserves run-id/run-version ownership checks before refreshing state. Backend targeted mode validates target message run, recipient, and accepted status before claim; skips broad stale/exhausted unrelated inbox mutation; and does not fall back to the default roster when target message ids resolve to no runnable agents.
- Attention summaries must not leak raw provider or runtime secrets. Product rendering prefers structured result reason fields, falls back to redacted error text, and redacts bearer, Hugging Face, and Tavily-shaped strings before display. Backend/file secret scans are not enough because runtime payloads can carry exception text.
- Standalone interactive HTML run notes are shareable companion artifacts, so their embedded `run-note-data` JSON and visible title must be sanitized before file writing. `InteractiveRunNoteWorkflow` applies the shared recursive realtime metadata sanitizer to embedded state and token-redacts request/run-goal-derived titles before rendering, `html_note` artifact persistence, and `artifact_recorded` event payloads. This keeps Obsidian/HTML knowledge surfaces useful without turning provider exceptions or artifact provenance into a secret export path.
- Legacy native-audio note: Gemma/HF endpoint checks still document the old text/native-audio boundary, but the current live-dialogue route is OpenRouter DeepSeek V4 Flash over LiveKit with Kokoro output. Missing `GEMMA4_MULTIMODAL_ENDPOINT_URL`, HF router text routing, Gamma, or MLX must not block the current OpenRouter path.
- Long-running OpenRouter/Kokoro LiveKit sessions must prune the maintained Python session history, not only the one prompt passed to Gemma. After every handled turn, `livekit_app.py` applies `RealtimeContextPruner` to `LiveKitVoiceAgentSessionState.history`, replaces old raw-audio refs with transcript/summary turns, bounds the retained context window, and emits `voice_session_history_pruned` with counts computed from the final retained history.
- Local LiveKit agent jobs may start without run/session metadata. In that unbound state only, `livekit_app.py` can bind the session from the first trusted `agent.voice.control` message. The trust proof must include a non-placeholder sender identity, matching `expected_agent_identity` for the local agent participant, exact room-name match, valid UUID `run_id` plus `realtime_session_id`, and the backend-minted HMAC `control_binding_token` tied to run/session/room/creator/agent identity. The token is exposed only in the immediate transport grant and stripped from durable metadata/events. After binding, all presence probes, interrupts, voice turns, typed transcript turns, and audio-track handling return to strict topic/run/session/sender matching; audio tracks are dropped before binding or from the wrong participant after binding.
- Latest live product proof: validation run `191f525c-4861-4144-afe5-b0a780c4b26f` was created from the actual Next.js creator app against FastAPI/Postgres, streamed run events, started Autopilot, recorded scheduler idle proof and blocked heartbeat proof while waiting for human feedback, then stopped the worker profile with durable `worker_profile_stopped` event `#69730`. Browser evidence is `screenshots/autopilot-live-postgres-proof.png`. For local browser validation on non-default backend ports, use `NEXT_API_PROXY_TARGET` so Next proxies `/api/*`; do not set `NEXT_PUBLIC_API_BASE_URL` unless direct browser CORS is intended.
- Hugging Face Gemma expert calls fail closed at the provider boundary. HTTP status failures, network/request errors, invalid JSON, non-object JSON, and successful objects with no usable content become `ProviderConfigurationError`; malformed raw JSON is not stringified into creator drafts or worker outputs.
- Realtime OpenRouter/Kokoro adapters also fail closed. Streaming Gemma rejects HTTP/request failures, streams with no usable text, and whitespace-only streams; hosted Kokoro rejects HTTP/request failures, invalid JSON, invalid base64 audio, and empty audio bodies before the LiveKit voice loop can publish invalid or empty output.
- OpenRouter/Kokoro voice-turn provider failures are now durable events, not task crashes. The engine emits `gemma_kokoro_voice_turn_failed`, clears Kokoro/output buffers, stops LiveKit response audio, returns a failed result, and the participant records only the user turn with failure metadata instead of adding an unspoken assistant response.
- Failed OpenRouter/Kokoro voice turns are visible in the actual product app. Persisted failed-turn events make the timing ledger `failed`, add failure stage/reason/timing to turn proof, move the live voice state strip to `Failed`, and update live captions with the provider failure reason. Failed events do not materialize fake assistant turns or Realtime Conversation Host follow-up tasks.
- Failed OpenRouter/Kokoro voice events now create an idempotent `review_realtime_provider_failure` A2A task for Inference Systems Engineer with recursively redacted failure metadata and provider/session context. This routes provider recovery into the autonomous specialist loop rather than leaving it as a passive UI warning.
- Inference Systems Engineer now has a concrete worker path for that recovery task. It records a `realtime_provider_failure_recovery` provider-operations ledger, classifies the failed component, blocks unsafe fallbacks such as OpenAI Realtime or fake assistant turns, and fans out deterministic follow-up tasks to Observability Agent for runtime health proof and Agent Harness Engineer for checkpoint/resume planning. Manual recovery tasks are redacted before project-memory fallback queries are persisted into `context_packet_built` or recovery artifacts.
- The recovery path now has an end-to-end worker-cycle regression. A failed provider-backed voice event is posted through the API, then one bounded cycle across Inference Systems Engineer, Observability Agent, and Agent Harness Engineer proves the recovery ledger, runtime health ledger, and checkpointed resume plan all exist; no assistant turn is faked; secret-shaped values remain redacted; and a replayed cycle is idle without duplicate artifacts or messages.
- Provider-smoke configuration blockers now route into the autonomous specialist loop. When required smoke steps fail with `configuration_failed`, the Observability Agent creates one deterministic, secret-safe `review_provider_configuration_blockers` A2A task, usually for Inference Systems Engineer and with Agent Harness/Observability plus web/retrieval collaborators when relevant. The follow-up payload contains non-secret env names, secret-file statuses, blocker text, setup actions, and ledger references only; provider exception text is redacted before response/artifact/message/event persistence. Configuration-blocked smoke ledgers use deterministic artifact ids so replay after an artifact-write failure converges on the same follow-up and a real ledger artifact.
- `review_provider_configuration_blockers` is now executable by the autonomous worker loop. Inference Systems Engineer or Agent Harness Engineer records a deterministic `provider_configuration_recovery` provider-operations ledger, keeps provider operation blocked until configuration is repaired and provider smoke is rerun, and creates deterministic follow-up tasks for Observability Agent runtime health proof plus Agent Harness checkpoint/resume planning. The path redacts snake_case, camelCase, compact, nested, suffix-sensitive, credential, bearer, and authorization metadata keys, splits web-search ownership correctly between Agent Harness configuration repair and Web Research source-grounding recheck, and appends `provider_configuration_review_recorded` with an idempotency key so retry after partial side effects converges without duplicate proof events.
- Provider-configuration recovery proof is now visible in the actual creator app. `provider_configuration_recovery` provider-operations ledgers render in DraftBoard Proofs as compact operational cards with status, provider ids, provider-smoke transport, provider-configuration component, first blocker reason, check/action counts, owner agents, and blocked fallback policy. These proof artifacts stay non-revision targets and do not dump raw workflow JSON into the UI.
- Provider-configuration recovery also blocks the actual voice release gate. A `provider_configuration_recovery` ledger newer than the latest provider-backed live smoke produces a blocked `provider-recovery` check, and only a newer same-session live smoke ledger can clear it. The setup checklist must preserve the release-gate label, so configuration recovery appears as `Provider configuration recovery` and routes to the live-smoke resolver rather than being mislabeled as provider failure recovery.
- The product voice panel now uses backend follow-up hints for failed provider-backed voice events. `provider_failure_recovery` responses carry the three recovery agent ids and `followup_worker_use_gemma=false`, so the UI continues only the deterministic recovery lane instead of waking the full default worker roster after a provider failure. Normal `assistant_response_completed` follow-ups still use default worker routing for content continuation.
- The product provider-backed voice release gate is now recovery-aware. It reads run artifacts, detects the latest `realtime_provider_failure_recovery` ledger, blocks readiness if that ledger is newer than live provider-smoke proof, and clears only after a newer same-session live provider smoke ledger passes. This prevents stale green smoke state from hiding a later OpenRouter/Kokoro failure.
- The product voice launch checklist now consumes that provider release gate. After LiveKit room and participant proof are ready, selected-provider blockers, unresolved provider-failure recovery, or missing live OpenRouter/Kokoro smoke remain first-class setup blockers. `Resolve next` refreshes provider readiness for selected-provider blockers and routes recovery/smoke blockers into the explicit live Runtime smoke flow instead of claiming the voice path is ready.
- The live-smoke resolver proof now uses post-action evidence. When `Resolve next` runs live Runtime smoke, the voice panel persists setup proof from the rebuilt post-smoke release gate and checklist. The just-built provider smoke result is converted into provider-smoke-ledger-shaped evidence for the gate, so provider-failure recovery can clear immediately even before refreshed run artifacts arrive through React props.
- The product voice proof panel now has a separate live voice proof path. It does not treat provider-route readiness as true voice-to-voice proof. The path requires a ready provider release gate, captured microphone audio evidence from Runtime smoke rather than synthetic fallback, passed OpenRouter/Kokoro streaming proof, and a ready realtime timing ledger. Open steps surface the next concrete action, such as speaking in the active LiveKit room, rerunning live Runtime smoke, or building the Timing ledger.
- The live voice proof path is now executable. It selects the first open proof step and routes it through one existing action: refresh provider readiness, run runtime preflight, join the LiveKit room, probe participant presence, run live Runtime smoke, or build the Timing ledger. Participant-presence probes from this proof path must stay guarded by run id, realtime session id, `liveVoiceProofActionSequenceRef`, and `shouldContinue` so stale async results cannot repaint a newer run/session.
- Provider-backed OpenRouter/Kokoro live smoke now has a captured-audio freshness boundary. `ProviderSmokeRunRequest.max_voice_audio_artifact_age_seconds` defaults to 120 seconds. Session-bound live smoke with ready participant presence must reject stale same-session microphone artifacts as `session_audio_artifact_stale` before calling OpenRouter or Kokoro, and the product proof UI must tell the creator to speak again. Residual risk: freshness uses durable `ConversationTurn.created_at`, so manually seeded bad timestamps can misstate audio age.
- The creator Activity panel now shows Autopilot schedule and lease state from durable worker-profile fields. It renders `Due now`, `Scheduled`, `Heartbeat running`, or `Not running` beside the existing heartbeat/scheduler proof, includes next due or lease-until timestamps when present, and uses a 5-second local clock so due/scheduled status changes while the page remains open.
- The creator app now has browser-session Autopilot auto wake. When enabled, the page evaluates the active `autonomous_pass` worker profile every 5 seconds and invokes the existing run-scoped scheduler only when the profile is due. It skips while the app is busy or the scheduler is already in flight, and it remembers the due-window wake key so a failed or blocked scheduler pass does not loop continuously.
- Autopilot frontend state must choose one canonical profile before rendering or waking work. The shared helper selects the newest `autonomous_pass` profile by `created_at` with a deterministic profile-id tiebreaker; Activity, heartbeat evidence, and auto wake all use that boundary. If a newer active profile exists without heartbeat proof, the UI must not show an older profile's scheduler or heartbeat proof. Run-scoped idle scheduler proof that lacks profile ids is valid for the selected profile only when the idle event is newer than the selected profile's `created_at`. Auto wake may still pick an older active profile when that older profile is due and the newest active profile is not due, because the run-scoped scheduler should wake any due active profile.
- The product production panel now has a content-readiness preflight before package/media/publish actions. It derives status from publishable content, accepted context evidence, artifact claim traces, claim support, reviewer decisions, and open feedback gates. Raw `source_ids` are not enough: source-backed content requires accepted evidence by source id, citation id, or source evidence linked to the artifact id. `no_content` disables all production actions; `blocked` disables platform and media packaging but keeps Publish check available so the backend readiness report can explain blockers. `visual_brief` is part of the shared publishable content set.
- The initial content-studio run path now follows the same strict source-grounding rule as worker refresh. If provider-backed web search fails during first-run creation, the failure reason is redacted before persistence, a durable `web_research_blocked` event is recorded, seed sources remain only pending-search placeholders, and claims linked to those seed sources stay `needs_review` instead of `supported`.
- Provider-backed source refresh now reranks before durable source repair. Web Research Agent dedupes provider results with URL canonicalization that strips fragments and tracking parameters while preserving meaningful query parameters, builds `RerankCandidate` records with source quality/freshness metadata, orders recorded `worker_web_search_result` sources by the configured reranker or deterministic fallback, preserves original provider `search_rank`, records source-level `rerank_rank`, `rerank_score`, `reranker`, and `rerank_reason`, and includes rerank proof in `web_research_completed` plus the task result. If a configured reranker fails, source refresh falls back to the deterministic reranker and records a redacted `provider_fallback` event rather than crashing or using Gemma fallback. Search-provider failure reasons are redacted before durable `provider_fallback`, `web_research_blocked`, task result, and summary persistence. The Source panel renders this source-level rerank proof separately from accepted-context evidence.
- Successful provider-backed source refresh now also builds retrieval-quality proof before claim verification. After source repair, Web Research Agent builds a `retrieval_quality_ledger`, records its summary in the task result, and passes `retrieval_quality_status`, `retrieval_quality_ledger_artifact_id`, and accepted retrieval source count into the `verify_source_refresh_claims` handoff. Claim Verification enforces accepted retrieval evidence whenever a retrieval-quality ledger exists, including zero-accepted ledgers. This intentionally turns provider-reference-only or raw-source-only claims unsupported when the latest retrieval pass rejects their evidence. Retrieval-quality fallback is scoped to reranker failures through a wrapper; persistence/build failures become normal worker errors and are not mislabeled as provider fallback.
- Retrieval quality now has diversity recall, contradiction precision review, accepted-evidence graph traversal, and downstream enforcement. If enough sources are accepted but every accepted candidate came from one normalized query and one retriever signal, or if accepted candidates cannot prove query/retriever metadata, the ledger records a `retrieval_diversity` graph gap, marks accepted candidates with `single_query_single_retriever_cluster` or `unproven_query_retriever_diversity`, reports `needs_more_recall`, and recommends independent corroborating / alternate primary-evidence queries. Candidate metadata with `contradiction_status`, explicit truthy `contradicts`, valid UUIDs in `contradicts_claim_ids` / `contradiction_claim_ids`, or `contradiction_review` creates contradiction precision risks; string false values such as `false`, `0`, `no`, and malformed claim ids are ignored. Contradictory candidates are not accepted for context, the ledger moves to `needs_rerank` when enough clean evidence remains, and recommended queries include contradictory-evidence review. Claim and artifact graph nodes verify that their source dependencies overlap accepted retrieval evidence; a supported claim or content artifact wired only to unaccepted evidence becomes a graph gap and keeps the ledger non-ready. Artifact graph traversal must accept `content.claim_dependency_ids`, `content.claim_ids`, and `provenance.claim_ids`, dedupe valid UUIDs, and ignore malformed historical values so current writer/revision/media artifacts do not become false graph gaps. Downstream `accepted_retrieval_evidence_by_source` exports evidence only when source evidence is ready: clean accepted candidates, enough accepted sources, no accepted-candidate precision risks, and no source-evidence retrieval-diversity/source gaps. It may still export source evidence for claim verification when the ledger is non-ready only because claim/artifact graph nodes are waiting for rewrite/verification or because only rejected candidates need rerank review. Publish readiness records `retrieval_quality_not_ready` for non-ready ledgers, Autopilot blocks non-ready retrieval quality, source-ledger snapshots surface the non-ready retrieval status, and initial provider-backed plus worker web-search sources persist `retriever=web_search`. Different accepted queries or multiple retriever signals remain allowed. The provider-free demo seed carries distinct seeded queries/retrievers so it still proves complete review state under the stricter gate.
- Accepted-source independence is now part of source-evidence readiness. If accepted sources all resolve to one normalized registrable domain, retrieval quality records `source_independence:host`, marks accepted candidates with `single_host_source_cluster`, recommends independent source corroboration, and keeps accepted retrieval evidence from exporting until an independently hosted source is added. Host normalization strips repeated `www.`, handles simple ports, and uses a conservative common two-part public-suffix list so sibling subdomains such as `docs.vendor.com` and `blog.vendor.com` remain one source cluster. This closes the gap where query/retriever-diverse evidence from a single vendor domain could pass as ready.
- Growth agents are no longer one reused package path. `platform-optimization-agent` owns the canonical `social_package` distribution artifact. `influencer-strategy-agent` records a non-publishable `growth_strategy` artifact for audience, keyword, hashtag, and creator-packaging decisions. `outreach-agent` records a separate `growth_strategy` artifact for community targets, pitches, engagement prompts, and risk checks. These strategy artifacts use deterministic worker artifact ids plus insert-if-absent writes so retries reuse existing artifacts rather than duplicating them. If influencer/outreach runs before platform, the ensured package keeps `created_by_agent_id=platform-optimization-agent` but records `initiated_by_agent_id` and the build event actor as the initiating specialist. `growth_strategy` artifacts participate in source repair/dependency gates, and the product artifact board has a compact Growth strategy render/filter while keeping these artifacts non-publishable and non-revisable.
- The creator-facing Production panel now uses `Growth package`, not the low-level platform-only package. The product path calls `POST /api/runs/{run_id}/growth-package`, which builds or reuses the canonical distribution package, records product-manager-to-growth-specialist A2A messages, persists influencer and outreach `growth_strategy` artifacts, and returns all strategy artifact ids. Reuse must match source artifact set, normalized platforms, audience, campaign goal, outreach flag, and canonical owner; first-call races converge through a deterministic package artifact id plus `record_artifact_if_absent`. Unknown runs return `404`, and custom initiators must resolve to an agent card before durable A2A messages or events are persisted. The old `/distribution-package` route remains available as the lower-level platform package primitive.
- The local always-on scheduler CLI is now scopeable. Use `PYTHONPATH=src python -m all_about_llms.cli run-worker-scheduler --watch --run-id <run-id> --execution-mode autonomous_pass` to run only one product run's Autopilot profiles, or add `--max-iterations <n>` for finite smoke runs. Omit the filters only when a global scheduler over all due profiles is intended.
- Run-scoped scheduler checks now leave durable idle proof. When a caller supplies `run_id` and no profile is due, the scheduler records `worker_scheduler_pass_completed` with `idle_reason=no_due_profiles`, requested run/mode, zero counters, and empty proof lists; unscoped global no-work checks stay quiet. The product Activity panel matches that event to the current Autopilot run as Scheduler proof.
- The actual creator app now exposes local worker-scheduler process supervision for browser-independent always-on studio work. FastAPI owns `GET/POST /api/worker-scheduler-process*` over `LocalWorkerSchedulerSupervisor`, which runs `run-worker-scheduler --watch` scoped to the current run and `autonomous_pass`, redacts provider/database secrets from logs, and reports pid/status/run/mode/cadence. The Activity rail shows compact `Background runner` proof plus Start/Stop background-runner controls; Start is disabled until an active always-on studio profile exists, uses the newest active run-scoped always-on profile for cadence, accepts the same `0.25s` cadence floor as worker profiles, and guards background status polling with a control token so stale polls cannot overwrite fresh Start/Stop results.
- The Next creator app now consumes live run events from `/api/runs/{run_id}/events/stream`. It uses a fetch-based SSE parser rather than browser `EventSource` so custom backend event names are preserved, merges streamed `RunEvent` records into `context.recent_events`, reconnects after disconnect, and guards stream updates by active run id plus run-version ownership. Activity shows compact `Live events` stream state.
- Streamed high-level durable mutation events now schedule a debounced silent run refresh. The app refreshes `getRunContext` and `listWorkerProfiles` after events that imply artifacts, messages, sources, claims, feedback, or worker profiles changed, while ignoring high-frequency voice text/audio stream events. Silent refresh preserves current busy/error state and remains active-run/version guarded. Non-suffix durable task events such as `agent_message_dependency_waiting`, `agent_message_recovered`, and `agent_message_retry_exhausted` must stay explicitly classified.
- The Gemma/Kokoro LiveKit voice-agent event path no longer depends only on an open browser tab for persistence. Every Python-emitted `RealtimeVoiceAgentEvent` has a stable `voice_agent_event_uid`; the LiveKit data channel still drives live captions, and a best-effort backend HTTP sink can also POST the same event directly to FastAPI. `/api/realtime-sessions/{id}/voice-events` deduplicates agent and browser submissions by that UID before appending `run_events`, materializing conversation turns, or creating follow-up tasks. Duplicate responses must still return existing materialized-turn and follow-up hints, because the browser may be the second reporter while still owning UI continuation.
- Direct voice-event persistence is now part of runtime readiness without becoming a hard launch blocker. `GET /api/voice-runtime-readiness` includes a non-required `voice-agent-backend-event-sink` check, validates safe HTTP(S) backend sink origins, bounds the sink timeout, and can preflight FastAPI `/health` when agent preflight is requested. The runtime adapter enforces the same origin-only URL boundary before POSTing events and rejects credentials, paths, query strings, fragments, invalid ports, and non-HTTP schemes. If optional sink config is invalid, the LiveKit participant falls back to data-channel-only event delivery instead of crashing. Top-level `degraded` readiness means required runtime checks passed but optional checks need review, so the product setup checklist and provider release gate treat it as usable while preserving `readiness_status=degraded` in setup proof metadata.
- The product artifact board now renders `realtime_provider_failure_recovery` provider-operations ledgers as compact Proof cards with blocked status, failed component, provider/transport, redacted failure reason, recovery check/action counts, owner agents, and blocked fallback. These proof artifacts remain copy/export-only and cannot be selected for draft revision.
- Production design canon now lives at [[../concepts/production-agent-system-design-canon]] and maps DDIA, ML-system design, inference engineering, effective-agent, guardrail, reliability, Uber/Netflix platform, A2A, and long-running-agent guidance into Agent Studio design rules.
- The A2A roster now has 38 agents, including Backend Platform Engineer, Frontend Experience Engineer, Scalability/Reliability Engineer, and Inference Systems Engineer. Project-owned skills now total 9 with system architecture, frontend engineering, and inference reliability added.
- Backend Platform Engineer, Frontend Experience Engineer, Scalability/Reliability Engineer, and Inference Systems Engineer now have concrete deterministic worker paths and regression coverage.
- `Feynman` owns the background ingestion lane autonomously and should interrupt only for concrete blockers.
- Ingestion now follows [[autonomous-obsidian-ingestion-flow]]: passive capture, provenance gate, extraction, material reading, original synthesis, linking, indexing, and canon promotion. Do not treat unread captures or TOC-only notes as knowledge.
- `system_design_vault/` now contains source inventories, local book corpus classification, official/open source notes, HLD/LLD notes, a PDF scaffold generator, companion JSON/HTML viewer, chapter-note scaffolds for `AI Engineering`, `Designing Machine Learning Systems`, `Practical MLOps`, `LLM Engineers Handbook`, `Building Machine Learning Powered Applications`, and `Inference Engineering`, plus compact book-synthesis notes for the first five priority books.
- `system_design_vault/03-patterns/system-design/production-agent-studio-canon.md` is the current deep system-design synthesis for product objectives, durable state, retrieval/graph quality, latency classes, agent collaboration, and product/planning boundaries.
- Retrieval quality, accepted evidence promotion, source-ledger/claim gates, claim revision plans, closure ledgers, memory promotion, project memory, project-memory retrieval, memory-policy gates, cockpit memory-policy controls, source-ledger drilldowns, project-memory recording controls, provider-free cockpit demo run, provider-readiness smoke walkthrough, provider-free realtime rehearsal, and durable cockpit walkthrough ledger are implemented.
- Provider operations now record latency class, provider latency, end-to-end latency, fallback reason, and smoke-proof status so realtime interruption, long Gemma/HF synthesis, background research, and rehearsal-only paths are distinguishable in observability.
- The product cockpit Provider Operations panel now renders those latency/smoke fields as chips and recent operation rows. Local FastAPI is runnable at `http://127.0.0.1:8000/cockpit` when Docker Postgres is up and `uv run all-about-llms` is running.
- Provider smoke now has a durable run-level endpoint at `POST /api/runs/{run_id}/provider-smoke` plus a cockpit Provider smoke control. It defaults to non-live proof and blocked-config auditing; the Live smoke checkbox maps to `execute_live_calls=true`.
- Autonomous studio passes now build provider smoke before provider operations by default, and context packets include `provider_smoke_ledger` in `latest_ledgers`.
- Direct CLI provider-smoke execution is validated with `all-about-llms-admin build-provider-smoke-ledger --run-id f3290d85-77f5-42b2-9d60-34c050151f94`; latest artifact `82d4c751-d33f-4b35-bf24-dc6ee735c5f2` is blocked only by missing provider credentials and still records local durable-store, cockpit-demo, deterministic reranker, and imagegen-boundary proof.
- The cockpit Worker Profile panel now reloads saved profiles and has a `Launch autopilot` action that creates or reuses an `autonomous_pass` worker profile, starts it, and runs the first heartbeat. This is the preferred operator path for starting an always-on studio loop from the UI.
- The first creator-facing Next.js product app lives at `frontend/next-app`. It supports text/dictation composition, an OpenRouter DeepSeek + LiveKit + Kokoro voice-runtime panel, prompt starters, session and `?runId=` restore, dialogue history, agent/event activity, automatic bounded A2A worker continuation after submitted turns, manual specialist continuation, compact always-on start/stop controls backed by durable `autonomous_pass` worker profiles, explicit New-session cleanup for run-coupled state and live voice runtime, draft filtering, copy/export/revise controls, platform distribution packaging, media planning, publish-readiness checks, feedback resolution, source/claim evidence, responsive layout, and a same-origin `/api` proxy to FastAPI.
- Latest creator-app validation: browser testing against live FastAPI/Postgres found and fixed a Postgres-only worker-cycle 500 caused by untyped JSONB mutation parameters in agent-message recovery/retry/repair metadata. The endpoint now returns a structured worker-cycle response, the app restores the run without the 500 state, and screenshot evidence lives at `screenshots/next-product-app-agent-continuation.png`.
- `Run agents` now has explicit model-routing control: the Activity panel defaults to `Gemma workers` enabled for the product app, but the API client treats omitted `useGemma` as false so other callers must opt in deliberately. The page also keeps an in-flight guard around worker-cycle execution to avoid overlapping cycles.
- Creator-app async run updates use a run-version guard, and the `New` action is disabled during busy states. This prevents a late worker-cycle refresh from restoring a stale run after the user starts a clean session. Busy cleanup must also be version-scoped, not only run-id-scoped: no-run flows share `runId=undefined`, so stale create/compose/voice completions must pass their captured `RunVersionSnapshot` before clearing a busy label. Same-version no-run composer submits, `Plan next`, feedback actions, production actions, always-on launch/stop controls, and local background-runner process controls also need synchronous action-token gates because React `busy` propagation is not a double-submit barrier.
- Voice production rules are non-negotiable: prune raw audio after 3 turns into transcript plus compact summary; do not use raw browser WebSockets for production transport; on barge-in, Rust must drop outbound audio, send cancellation to Python/Gemma, clear Kokoro buffers, and record interruption events.
- Realtime voice contracts now expose the LiveKit transport grant, room and participant identities, token-presence without durable token persistence, context-window and summarize-after-turns policy, media-only turn commits, OpenRouter/Kokoro response ids, and explicit interrupt controls. Some internal event/control names still carry legacy `gemma` labels; treat those as compatibility aliases unless the selected provider is explicitly a native-Gemma route.
- `openrouter_livekit` is the current realtime provider and can use `LIVEKIT_API_KEY` + `LIVEKIT_API_SECRET` to mint a short-lived LiveKit room-join JWT; only the immediate client response carries the token, while durable events/metadata keep only sanitized transport proof. Older `gemma4_realtime` provider names are legacy aliases during migration.
- Latest research correction for current work: OpenRouter DeepSeek handles text-turn reasoning, LiveKit handles realtime transport, and Kokoro remains the TTS waveform leg. Treat legacy Gemma native audio as optional input-understanding research only; do not describe it as the current speech-to-speech runtime.
- Live voice endpoints should avoid scale-to-zero/cold-start behavior; background content generation can use colder provider routes if latency is not interactive.
- The Next.js voice panel now has a real LiveKit client join path through `frontend/next-app/lib/voice/livekitRuntime.ts`: it joins the backend-issued room grant, enables microphone publishing, attaches remote audio, clears local playback on interrupt, and shows blocked/error states for missing LiveKit URL/token or unsafe persisted-token flags.
- The Python voice-agent runtime now lives under `src/all_about_llms/voice_agent/`. The current runnable participant uses OpenRouter live-dialogue reasoning, Kokoro TTS chunking, LiveKit publisher handoff, durable-safe events, context pruning, and cancellation tokens. Compatibility adapter names may still reference Gemma/HF, but those are not the active default.
- Legacy native-audio streaming via `HuggingFaceGemmaAudioReasoner` is retained as an explicit non-default route. It must not be used as the current blocker while OpenRouter/LiveKit/Kokoro is selected.
- Provider smoke for the current route must use `openrouter_livekit`, recording OpenRouter reasoning, LiveKit transport/session, Kokoro first-audio latency, and end-to-end timing evidence. Legacy `gemma-kokoro-voice-streaming-smoke` is a non-default native-audio check.
- Current voice runtime caveats: OpenRouter/LiveKit/Kokoro live voice is now accepted for the current proof route, the Python LiveKit participant uses the Rust persistent JSONL process by default for frame loops, and the Rust Axum/Tokio HTTP sidecar is opt-in through `RUST_VOICE_EDGE_HTTP_URL` and still request/response transport rather than a direct LiveKit media bridge. The remaining objective blocker is external LinkedIn publication proof plus closure review.
- The first Rust voice-edge contract core lives at `services/voice-edge`. It has typed JSON contracts, explicit VAD backend selection (`deterministic_energy` default, `silero_onnx` real Rust Silero ONNX via the bundled model or configured `vad_model_path`), bounded process-wide Silero session pools keyed by model source and session id, bounded recurrent Silero `StreamState` caches keyed by stream id and sample rate, bounded inbound buffer events, barge-in detection, cancellation acknowledgements for dropping outbound audio, canceling Gemma, clearing Kokoro buffers, and stopping LiveKit audio, plus `--jsonl` mode for long-running callers and an Axum/Tokio HTTP sidecar with `GET /healthz`, `POST /v1/voice-edge`, `POST /v1/voice-edge/analyze`, and `POST /v1/voice-edge/cancel`. Python now has `PersistentRustVoiceEdgeClient`, `RustVoiceEdgeHttpClient`, and `FallbackVoiceEdgeClient` in `src/all_about_llms/voice_agent/edge.py`; `_build_voice_edge_client` selects HTTP when `RUST_VOICE_EDGE_HTTP_URL` is configured, wraps it with JSONL fallback when `RUST_VOICE_EDGE_BINARY_PATH` points to an executable file, otherwise uses JSONL directly. HTTP mode runs startup `/healthz` preflight before voice-agent readiness and falls back to JSONL if available; JSONL mode also preflights by starting the persistent process and sending a harmless cancellation contract. Recent-frame windows and cancelled-response ids are bounded, HTTP reuses one async client, local binary paths resolve from the project root, fallback wrappers preserve the requested VAD backend/model/fallback policy, HTTP health must prove the `voice-edge` service/transport/contract/state-model before it is accepted, readiness now separates runtime transport from preflight-selected transport, and the benchmark now emits concrete pool/cache tuning recommendations from steady-state latency. The remaining Rust voice-edge gap is the direct LiveKit-side Rust media bridge plus provider-backed load validation.
- `GET /api/voice-runtime-readiness` now returns a non-secret readiness packet for the actual voice stack: LiveKit, backend agent participant, OpenRouter live-dialogue reasoning, Kokoro, Rust voice edge, backend event sink, and context pruning. `preflight_edge=true` runs Rust edge preflight and `preflight_agent=true` constructs the LiveKit agent server without starting it; the Next product voice panel Runtime preflight requests both. The panel must not block current LiveKit join on missing HF/Gemma/Gamma/MLX configuration while OpenRouter is selected.
- Durable participant presence is now separate from construction readiness. `GET /api/runs/{run_id}/voice-agent-presence` reports whether a fresh persisted `gemma_kokoro_voice_agent_ready` event exists for the run/session, with `ready`, `stale`, or `missing` status, event age, room, expected agent identity, actual LiveKit sender identity, and optional `probe_id` challenge-response binding. The Next LiveKit client sends a `voice_agent_presence_probe` after room join; the Python LiveKit agent replies with a ready event; the frontend persists it through `/api/realtime-sessions/{id}/voice-events`. In an unconfigured local workspace the correct product state is `missing`, not assumed ready.
- Presence probes and transcript turns now also cover the local-development path where LiveKit does not dispatch the Python participant with job metadata. The first trusted control message can initialize run/session and creator identity before emitting `gemma_kokoro_voice_agent_ready` or scheduling OpenRouter/Kokoro; malformed UUIDs, wrong room, wrong expected agent identity, wrong topic, placeholder sender identity, missing/incorrect binding token, pre-bind audio, or wrong-participant audio are ignored without mutating state.
- Local voice-agent process supervision is now product-visible but not proof of live voice. `GET /api/voice-agent-process`, `POST /api/voice-agent-process/start`, and `POST /api/voice-agent-process/stop` control the local `all-about-llms-admin run-voice-agent` process and expose pid/status/return code/log tail with known provider secrets redacted. Stop/exit races return stable status envelopes with typed `last_error` instead of API 500s, and the product row surfaces that control error directly. The Next voice panel shows Start/Stop controls, but real voice availability still requires readiness checks plus durable participant presence for the run/session.
- The Next product voice panel now includes creator-facing proof controls. `Runtime smoke` calls `POST /api/runs/{run_id}/provider-smoke` with the OpenRouter/Kokoro realtime path only, live calls off by default, and web/reranker/image/imagegen checks excluded so it proves the voice route. `Live smoke` is an explicit checkbox plus browser confirmation before external endpoints can be called. `Timing ledger` calls `POST /api/runs/{run_id}/realtime-voice-timing-ledger` and shows measured versus missing LiveKit/OpenRouter/Kokoro stages.
- `Leibniz` remains the standing reviewer and the 5-minute `vault-sync-subagent` heartbeat is active. Latest voice-readiness review found no blockers after fixes for hidden checks, unsupported provider UI, mount-time readiness errors, preflight labeling, status severity, and Stop state.
- Local voice-edge proof now exists at `all-about-llms-admin benchmark-voice-edge` or `PYTHONPATH=src python3 -m all_about_llms.cli benchmark-voice-edge`. It warms the selected transport before measuring steady-state frames, reports bridge latency, false-positive / missed-detection counts for silence/speech/barge-in cases, VAD backend/pool config, speech stimulus metadata, concurrent stream probe, and a `tuning` block with realtime p95 targets plus recommended session-pool and stream-state-cache sizes. It can benchmark the default persistent JSONL bridge or an already-running HTTP sidecar via `--http-url`, can use one or more local 16-bit PCM WAV speech fixtures via repeated `--speech-wav` or recursive case-insensitive `--speech-wav-dir`, and can run Silero probability threshold sweeps with `--vad-probability-threshold-sweep`. Bad corpus/threshold inputs and synthetic-only sweeps fail fast instead of falling back to synthetic speech silently. For HTTP-primary plus JSONL-fallback runs, tuning uses measured fallback events rather than the wrapper's conservative serialization flag, so a slow HTTP sidecar cannot hide concurrency p95 misses just because a serial JSONL fallback exists.
- Runtime health now includes the optional local voice-edge benchmark. It records binary availability, process mode, p50/p95/max latency, false-positive count, missed speech-start count, missed cancellation count, concurrency probe streams/iterations/latency, fallback-used count, measured serialization, tuning status, recommended pool/cache sizes, and emits `voice_edge_benchmark_passed` or `voice_edge_benchmark_needs_attention`. Persistent JSONL request serialization is a recommendation to use the supervised HTTP sidecar for concurrent LiveKit sessions, not by itself a degraded local-health result; HTTP-primary latency misses still degrade even when JSONL fallback is configured but unused. This is operator proof for the Rust/Python edge contract only; LiveKit/OpenRouter/Kokoro timing proof is handled by the separate realtime voice timing ledger.
- Current realtime provider metadata must distinguish current implementation from target architecture: current Rust edge is `persistent_jsonl_subprocess_or_http_sidecar` over `stdin_stdout_jsonl` or `http_json`; real Silero ONNX and recurrent stream state are implemented inside the long-running Rust process, while gRPC/direct LiveKit media remains target architecture, not current production proof.
- LiveKit/OpenRouter/Kokoro timing proof now has a durable ledger path. The LiveKit data channel topic `agent.voice.event` is normalized in the Next client and persisted through `/api/realtime-sessions/{realtime_session_id}/voice-events`; `/api/runs/{run_id}/realtime-voice-timing-ledger` and `all-about-llms-admin build-realtime-voice-timing-ledger --run-id <run-id>` build `realtime_voice_timing_ledger` artifacts.
- LiveKit voice-agent events now also materialize run dialogue turns: `voice_user_turn_committed` records a user voice `ConversationTurn` with pending transcript status when needed, `assistant_response_completed` records the assistant voice text, newly materialized assistant completions enqueue a Realtime Conversation Host `summarize_realtime_turn_context` task, and the Next voice panel refreshes the run context when the event API returns a materialized turn id. A later final transcript for the same user turn must promote the pending turn under the Postgres voice-turn advisory lock, patch existing follow-up payloads to `final_user_transcript` without rewriting execution state, and avoid duplicate promotion events on UID or final-payload replay. Because the agent and browser may both report the same event, materialization/follow-up creation must remain UID-idempotent as well as turn/response-idempotent.
- The Next voice panel now uses `followup_task_message_id` from persisted assistant voice completions to continue the bounded A2A worker-cycle automatically. This continuation is message-lineage targeted: the frontend sends `message_ids=[followup_task_message_id]` with `continue_message_lineage=true`; the worker starts from that exact accepted message, then appends only accepted child messages whose `parent_message_id` matches processed lineage tasks. This prevents older same-recipient realtime host or provider-recovery tasks from stealing a live voice continuation while preserving Realtime Host -> Intent Router -> Content Strategist -> downstream content workers and Inference -> Observability/Harness recovery. External cycle requests cap `message_ids` at `100`; internal worker-run lineage expansion is not capped by that API request limit.
- Realtime Conversation Host briefs now materialize their recommended handoffs as deterministic insert-if-absent A2A child tasks. Voice/context briefs can wake the Intent Router, Audio Producer, Product Manager, Forward Deployed Engineer, or other recommended specialists without waiting for manual operator routing, and duplicate host retries skip duplicate handoff tasks.
- Regression proof now covers the voice-to-content worker chain: Realtime Conversation Host can process a finalized voice transcript, create an Intent Router handoff, Intent Router can route the transcript to content/research/reel specialists, and Content Strategist can fan out to ELI5, Substack, Script Doctor, Audio Producer, Video/Reel Producer, platform, influencer, outreach, editor, critic, and artifact tasks in the same worker cycle. Content Strategist creates deterministic seed post/reel/Substack drafts scoped to that content-strategy handoff; seed reload after insert conflicts prevents partial target sets under concurrent/replay races, seeds inherit source/claim context only from explicit source/claim/target-artifact lineage, and downstream child tasks receive `target_artifact_ids` for the seed artifacts so stale run-level content cannot bleed into the turn. Direct existing-content strategy tasks pass explicit existing artifact ids instead of relying on writer fallback selection, and ambiguous same-format existing drafts fail fast until explicit targets are supplied. The chain regression now executes downstream writer, data, audio, video, and artifact workers and proves durable outputs. Intent Router route-plan artifacts and child tasks use deterministic insert-if-absent writes, Content Strategist child task creation uses deterministic insert-if-absent ids, and the chain regression replays both stages to prove duplicate plans, duplicate seeds, and downstream tasks are not created.
- The timing ledger only reports `ready` for the latest/current LiveKit session with a correlated media/session/turn/response chain: `gemma_kokoro_voice_agent_ready`, `voice_agent_media_bridge_ready`, `voice_user_speech_started`, `voice_user_turn_committed`, `gemma_kokoro_voice_turn_started`, `gemma_generation_started`, `assistant_text_delta`, `assistant_audio_chunk_published`, `voice_edge_cancellation_acknowledged`, and `gemma_kokoro_voice_turn_cancelled`. `voice_agent_media_bridge_ready` must match the same session and precede the speech/turn evidence, so older complete sessions or late bridge events cannot make a current room look ready.
- Product voice proof UI must not expose raw runtime event names as the primary creator instruction. Missing `livekit_audio_track_bridge` is shown as a backend microphone-track bridge blocker with a concrete speak-and-rebuild timing action. If the ledger is failed, failed-stage provider recovery wins over media-bridge retry copy even when backend recommended actions list the media-bridge gap first.
- Live voice state projection treats `voice_agent_media_bridge_ready` as a readiness confirmation only from `connected` to `listening`; if the UI is already `thinking`, `speaking`, or `interrupting`, late media-bridge events must preserve the active state.
- `vault-sync-subagent` runs every 5 minutes to sync learnings between `social_media_optimiser/` and `system_design_vault/`. Main-thread implementation should update the most relevant vault notes; the sync subagent owns cross-vault propagation and conflict surfacing.
- Current automation evidence: `/Users/saumyamehta/.codex/automations/vault-sync-subagent/automation.toml` is ACTIVE with `FREQ=MINUTELY;INTERVAL=5` and includes Leibniz review-watch instructions.
- Keep `Leibniz` open as the standing Spark code-quality reviewer. After each meaningful implementation slice, send it a concise read-only review request focused on correctness, bloat, cleanliness, tests, and out-of-box behavior before closeout.
- Main thread should also poll `Leibniz` opportunistically during long work and rely on the 5-minute `vault-sync-subagent` heartbeat to surface any new `Leibniz` review status or requests.
- Public A2A discovery is a compatibility adapter, not a full public protocol server. `/.well-known/agent-card.json` must only advertise JSON/text HTTP+JSON capabilities currently backed by `/api/a2a`; internal realtime audio/image modes belong under `x-agentStudio` until public A2A-compatible streaming/media routes exist. `/api/a2a` endpoint records must include method plus path and match OpenAPI.
- Local provider credentials should use ignored secret files by default. `Settings` defaults to `.secrets/hf_token` and `.secrets/tavily_api_key`; `.env.example` points to those paths; provider readiness reports only non-secret file status such as `loaded`, `missing`, `empty`, `unreadable`, or `direct_env`; and direct env values override file contents. `.secrets/README.md` is allowlisted in git, while actual token/key files stay ignored. Backend provider `next_actions` should prefer matching secret-file diagnostics for missing HF/Tavily secrets and fall back to generic env guidance only for missing non-secret env values. The product voice setup surface derives non-secret `secretFileGuidance` from selected-provider diagnostics so creators see concrete setup actions without token values or planning UI.
- Voice research/design note: [[../../02-research/Realtime Voice Architecture - Gemma Kokoro LiveKit Rust Python]].
- The Rust retrieval-ranker lives at `services/retrieval-ranker`. Python can select it with `RERANKER_PROVIDER=rust` and `RUST_RERANKER_BINARY_PATH`; deterministic Python reranking remains the default fallback.
- Retrieval research direction: use query rewriting, dense plus lexical plus web plus graph candidate fanout, rank fusion, top-K reranking, source acceptance, contradiction/freshness checks, and FP/FN evaluation. Postgres + pgvector remains the canonical v1 store; specialized indexes are provider-backed later additions only if benchmarks justify them.
- Retrieval-quality source independence uses a conservative normalized source identity, not raw hostname text. It strips repeated `www.`, removes simple ports, canonicalizes IPv4/IPv6 literals, groups sibling subdomains by registrable-domain-like keys, and treats common country-code second-level labels such as `co`, `com`, `org`, `net`, `gov`, `ac`, and `edu` under two-letter TLDs as public-suffix components. This avoids treating `docs.vendor.com` and `blog.vendor.com` as independent sources while keeping unrelated domains such as `vendor-a.co.nz` and `vendor-b.co.nz` independent. Residual risk: this is not a full Public Suffix List parser; revisit with a PSL dependency if source-host precision becomes a recurring issue.
- Realtime rehearsal uses `dry_run = true` and provider `local_realtime_rehearsal`; it is useful for browser voice UX and durable route testing, but it does not count as provider-backed smoke.
- The product voice panel must keep the live interaction controls before diagnostics. In `RealtimeVoicePanel`, the primary actions `Join voice room`, mic toggle, `Interrupt`, and `Stop` belong immediately after `Live voice state` and before live captions, runtime readiness, and proof ledgers. Diagnostics can be dense; the first-screen creator path must still read as back-and-forth voice interaction.
- The product app is now voice-first in primary layout. `RealtimeVoicePanel` renders before the text composer in `app/page.tsx`. When no run exists, the voice panel owns the first durable-entry action with `Voice run goal` and `Create voice run`; this calls `POST /api/runs` through the typed Next client, creates a voice-mode run, and records initial context for `live_voice_panel`, selected voice provider, and `voice_first=true`. The active provider-backed mode is OpenRouter `deepseek/deepseek-v4-flash` dialogue over LiveKit with Kokoro output; transcript rehearsal records `provider_backed_realtime=false`, `rehearsal_only=true`, and no model claims. Keep this path distinct from browser dictation, which remains a transcript-composer fallback below Live Voice. The create path has parent and child ref-level duplicate-submit guards plus a child mounted guard because successful creation remounts the voice panel under the new run id.
- The no-run voice-first screen should show starter, state/action controls, captions, and a compact setup summary rather than full diagnostic proof panels. The starter uses a 3-row `Voice run brief` textarea. Runtime readiness, setup proof, and voice proof live inside `voice-diagnostics-disclosure`: closed with a `Setup details` summary when no run exists and open automatically once a durable run id exists.
- The right rail should read as creator studio flow, not an operations dashboard. `ActivityPanel` keeps the same durable worker-plan, always-on, retry, scheduler, and outcome mechanics, but visible copy should use product language: `Studio flow`, `Continue specialists`, `Suggest next step`, `Gemma experts`, `Always-on studio`, `Background runner`, `Next studio steps`, `Live updates`, and `Recent specialist outputs`. Scheduler and always-on proof rows should expose creator summaries such as `Background check`, `Specialist pulse`, specialist update counts, pulse counts, and cadence summaries as the primary text; raw audit details such as event ids, ledger ids, profile counts, heartbeat counts, process pid, and autonomous-pass mode belong behind `technical-proof-disclosure` / `Technical proof`. Parent-provided stream, auto-continue summaries, busy labels, action-complete summaries, fallback errors, default profile names, and raw backend result summaries must also stay creator-facing, using labels such as `Live update: background check #...`, `Auto continue checked this run`, `Always-on studio started`, `Specialist pulse finished`, `Creator always-on studio`, and `Starting background runner` instead of raw underscored event names or visible `Autopilot` / `local scheduler` copy. Do not reintroduce first-class labels such as `Agents and events`, `Run agents`, `Plan next`, `Gemma workers`, `Autopilot`, or `Local scheduler` in the creator surface.
- Shared provider-readiness `next_actions` are product-facing when consumed by the Next voice setup/release-gate path, so they should use creator-app language such as product voice panel. Cockpit/demo wording belongs in explicit `/cockpit` smoke-plan fields such as `cockpit_action`, not in provider item next actions.
- Live Docker Postgres + pgvector was started and run `f34c732f-1168-4582-86f7-8bd6efcd0b02` produced DB-backed cockpit walkthrough ledgers. The current realtime direction is OpenRouter `deepseek/deepseek-v4-flash` through LiveKit with Kokoro output; older `gemma4_realtime` session names are treated as legacy aliases rather than a reason to reopen Gemma/HF setup.
- Current implementation focus is hardening the actual creator-facing app and backend/Rust orchestration slices while preserving the product/planning/cockpit boundary. Real provider-backed smoke now requires OpenRouter, LiveKit, Kokoro, and web-search credentials plus same-session accepted proof capture; Gemma/HF/Gamma/MLX are no longer the active setup path.
- Latest provider-smoke control-binding update: smoke-created OpenRouter/LiveKit sessions preallocate the durable `realtime_session_id` before provider creation, pass that id into realtime metadata for HMAC proof minting, verify the raw returned control-binding token before durable recording, persist the same id on `RealtimeSessionRecord`, and recursively strip normalized binding-token, token, secret, key, signed URL, authorization, and raw-response variants from durable payloads while redacting token-shaped strings inside nested lists and keeping non-secret booleans such as `control_binding_token_issued`, `control_binding_required`, `has_token`, and `has_transport_token`. Existing/manual legacy realtime sessions without `metadata.control_binding_token_issued=true` still block selected realtime smoke and session-bound voice smoke. `Leibniz` found the self-reported-proof trust bug, exact-key redaction gap, and list-string redaction gap; all are fixed with regressions, and final verification found no actionable findings.
- Latest realtime API redaction update: the main product `/api/runs/{run_id}/realtime-session` and `/api/realtime-sessions/{realtime_session_id}/control` paths now use the normalized recursive metadata redaction boundary, not exact-key stripping. Durable session/event/control-task payloads strip camelCase/separator/suffix credential keys such as `controlBindingToken`, `clientSecret`, `apiKey`, `session_token`, and `signedUrl`, preserve non-secret proof booleans and `token: None`, and redact Bearer/HF/Tavily-shaped strings inside nested lists. Provider-configuration failure reasons now pass through the same realtime string redactor before durable `realtime_session_configuration_failed` events or HTTP 503 details, after `Leibniz` flagged raw exception persistence. This keeps normal product voice setup, interrupt/resume/stop-output, provider errors, and provider-smoke on the same durable secret-safety contract. Validation evidence: focused realtime session/control/provider-smoke tests `28 passed`, default backend regression `470 passed, 11 explicit infra skips`, live Postgres outside sandbox `10 passed`, real Rust voice-edge HTTP smoke outside sandbox `1 passed`, compileall, and clean hygiene scans. `Leibniz` fix verification found no actionable findings; residual risk is non-token-shaped opaque secrets embedded in exception text, which current policy treats as out of scope unless carried under sensitive metadata keys.
- Latest source-side voice-event redaction update: the shared realtime redaction boundary now also runs before voice-worker event payloads leave the runtime, not only at backend persistence. `LiveKitDataEventSink`, `BackendRealtimeVoiceEventSink`, and forwarded Rust voice-edge events use `realtime_safety.safe_realtime_metadata` so LiveKit data-channel broadcasts, direct backend event POSTs, and intermediate sink emissions strip normalized sensitive keys and redact Bearer/HF/Tavily-shaped strings recursively. Failing-first regressions cover backend posting, LiveKit broadcast, and voice-edge forwarding. Validation evidence: redaction tests `3 passed`, broader voice/API subset `6 passed`, voice-agent suite `69 passed`, compileall, focused API redaction subset `2 passed`, no-SQLite boundary tests `3 passed`, full backend regression `477 passed, 11 explicit infra skips`, and live Postgres outside sandbox `10 passed`. `Leibniz` found no actionable issues; residual risk is unchanged project policy for opaque non-pattern secrets in non-sensitive field names.
- Latest local provider setup update: the actual creator voice panel now treats `OPENROUTER_API_KEY`, `OPENROUTER_LIVEKIT_URL`, `LIVEKIT_API_KEY`, `LIVEKIT_API_SECRET`, and Kokoro configuration as the active live-dialogue setup path. `POST /api/local-livekit-dev-config` writes `OPENROUTER_LIVEKIT_URL=ws://127.0.0.1:7880` plus LiveKit local dev key/secret to ignored local files without returning credential values. Legacy Hugging Face/Gemma endpoint fields are compatibility/background fields only and must not be used as current live-dialogue blockers.
- Latest Kokoro readiness update: Kokoro TTS remains the active spoken-output lane for the OpenRouter/LiveKit path. Runtime preflight requires proof metadata for TTS output before product readiness can be claimed; local Kokoro package readiness is allowed without a hosted provider call. This proves the preflight contract and failure routing, not the availability of a real configured hosted Kokoro endpoint in this local environment.
- Latest OpenRouter dialogue readiness update: `/api/voice-runtime-readiness` now treats OpenRouter chat-completions routing as `openrouter-live-dialogue-reasoning`, with raw microphone PCM kept out of OpenRouter and LiveKit carrying transport. The legacy native-audio Gemma check is optional/non-default and must not block the current OpenRouter DeepSeek + LiveKit + Kokoro path.
- Latest voice panel preflight wiring update: `RealtimeVoicePanel` no longer dispatches runtime readiness with positional boolean tuples. Use `RUNTIME_PREFLIGHT_READINESS_OPTIONS` for full setup/check/start preflight, including both hosted Kokoro and hosted Gemma probes; use `LIVEKIT_TRANSPORT_PREFLIGHT_OPTIONS` only for LiveKit transport refresh; use `refreshVoiceReadiness()` for passive/background refreshes. The constants and `VoiceReadinessRefreshOptions` live in `lib/voice/runtimeReadiness`, so tests can assert actual option values instead of parsing component constant formatting. The frontend regression directly checks the exported option objects and still rejects future `refreshVoiceReadiness(true, ...)` / `false` calls. Evidence: failing-first frontend race command passed with `217 passed`, follow-up direct-option race command passed with `217 passed`, Next typecheck, Next lint, `git diff --check`, and touched-file trailing-whitespace scan. `Leibniz` found no actionable findings; residual risk is minor because the remaining source guard intentionally depends on the protected helper name `refreshVoiceReadiness`.
- Latest provider proof completion-status update: hand-edited accepted audit notes are no longer trusted if their explicit accepted-proof fields contradict the validators. A failing-first CLI regression covered an accepted `external-publication-proof` audit note with LinkedIn channel and Instagram URL; `provider-proof-completion-status` now reports `blocked_by_invalid_accepted_audit_note`, `accepted_audit_note_invalid_fields`, `invalid_accepted_audit_note_proofs`, proof-level `latest_record_has_invalid_fields`, and `invalid_source_targets`. Leibniz then found duplicate Markdown keys could hide later contradictory or secret-shaped values behind the first field; ordered audit-field entries now feed the secret scan, duplicate accepted-note fields are invalid evidence, and duplicate secret-shaped values block as secret-shaped audit notes. Follow-up reviews found accepted notes could omit required session/config or publication policy/rollback fields and the generated validation-summary/redaction-check lines; completion status now derives audit-emitted required fields from the proof schema, invalidates missing/empty/template-placeholder values, requires a zero-failed validation summary matching the proof plan check count, and requires `secret_redaction_check` to be passed/true. Accepted JSON records and audit notes also require parseable `validation_timestamp` values, and unorderable invalid/secret accepted markers block conservatively instead of being masked by older accepted evidence. This remains a status-only audit gate and does not mutate blocker state.
- Latest provider proof publication-summary update: accepted `external-publication-proof` JSON records and compact accepted audit notes now require `preflight_validation_report_validated_publish_channels` to be normalized, non-empty, supported, unique, and already expressed in canonical platform keys rather than aliases. Padded hand edits such as `linkedin, mastodon`, duplicate summaries such as `linkedin, linkedin`, alias summaries such as `twitter`, and blank comma padding such as `linkedin, ` no longer satisfy accepted external publication proof even when the destination channel itself matches after normalization. This is evidence-quality hardening only; it does not configure destination credentials, perform live publication, or clear the blocker.
- Latest provider proof product-run preflight update: accepted provider proof evidence now requires a captured `product-run.preflight.json` from `GET /api/runs/<uuid>` plus `validated_product_run_id` matching the command run id in the linked preflight-validation report. Proof records and compact audit notes must carry `product_run_preflight_artifact_id`; UUID shape alone is no longer enough. After Leibniz review, the validator also requires the captured file to match the real run response shape, so run-id-only JSON fails with `product_run_payload_schema_invalid`. Proof-readiness, OpenRouter voice boundary, publication boundary, browser tests, and the concrete `RUN-2026-05-20-NEXT` artifacts were refreshed, but that workspace remains blocked because it is still a non-UUID label and no live provider/publication proof was run.
- Latest provider proof closure-review status update: `provider-proof-closure-review-status` reads recorded closure-review audit notes without changing blocker state. It rechecks provider proof completion through `--proof-audit-target`, scans closure-review `--audit-target` notes separately, reports the latest approved or rejected review, and blocks missing, rejected, invalid, secret-shaped, or incomplete review coverage. After `Leibniz` found a hand-edited-note bypass, approved audit notes must now bind to the expected accepted proof list and expected review-requirement count from the completion packet. Only a latest valid approved review sets `blocker_state_update_allowed_after_review=true`; `state_change_allowed` stays false because blocker-state edits remain a separate reviewed step.
- Latest provider proof preflight validation update: `validate-provider-proof-preflight-artifacts` now checks captured provider-readiness, voice-runtime-readiness, and publish-readiness JSON files before proof records reference them. It validates expected run-scoped files, JSON object shape, and no token-shaped values without echoing secrets or changing blocker state; after `Leibniz` review, token-shaped `--preflight-dir` path segments fail closed and are redacted from `preflight_dir`, `expected_files`, issue fields, validated files, and artifact IDs. Provider proof plans, proof readiness, OpenRouter voice boundary, publication boundary, and proof-workspace READMEs now carry `preflight_artifact_id_fields` and `preflight_validation_commands` so operators can fill the required preflight artifact ID fields from validated local evidence. `validate-provider-proof-record` and `record-provider-proof-record` now accept `--preflight-validation-path`, accepted proof schemas require `preflight_validation_report_artifact_id`, and accepted records reject missing or mismatched preflight validation reports. Accepted audit notes also record `preflight_validation_report_status` plus `preflight_validation_report_matched_fields`, and completion status rejects accepted notes that omit this report-result summary.
- Latest provider proof blocker-state update note: `record-provider-proof-blocker-state-update` is the reviewed write-side bridge after closure approval. It reads proof records through `--proof-audit-target`, reads closure-review notes through `--closure-review-audit-target`, writes update audit notes through separate `--audit-target`, and refuses to write unless `provider-proof-closure-review-status` is `closure_review_approved`. After `Leibniz` review, payloads and notes explicitly keep `state_change_allowed=false`, approved status can preserve a separate `--blocker-update-audit-target` in generated next-action commands, and same-review replays return `already_recorded` by idempotency key instead of duplicating notes. The note records candidate blocker states and `goal_completion_claimed=false`, so it does not by itself complete the full active objective or bypass the real provider-backed voice/publication proof requirement.

## Required Preflight

For non-trivial Agent Studio work, read only this minimum set first:

1. [[../../Agent Studio MOC]]
2. [[../../01-work-tracking/Current Sprint]]
3. This note.
4. One subsystem wiki note relevant to the task.

For deep source-ingestion or book-note work, also read `system_design_vault/MOC.md` and `system_design_vault/01-sources/local-books-corpus.md`.

Only open raw notes when source evidence is needed. Only open output viewers when checking generated design state.

For ingestion work, also read [[autonomous-obsidian-ingestion-flow]] and maintain lifecycle/index status.

## Required Closeout

At the end of a meaningful slice:

1. Update the subsystem wiki note.
2. Update [[../../01-work-tracking/Current Sprint]].
3. Append one compact entry to [[../../log]].
4. Refresh JSON/HTML viewers only when the design state changed or the user asked for a viewer.

## Implementation Workflow Rule

- For every meaningful code slice, define focused tests first, implement one cohesive product/backend feature slice, run the full relevant validation set once, and then send that complete slice to `Leibniz` for review. Avoid token-wasting micro-patches. A second `Leibniz` pass is only required when the first review finds a real issue that was fixed.
- `Leibniz` review requests must be whole-slice, deep, and invariant-based. Send the complete changed-file set, tests, validation results, and critical invariants in one request, and explicitly ask it to inspect async stale-state risks, run/session guards, provider and secret boundaries, test gaps, bloat, and out-of-box behavior in one pass.
- Do not ask `Leibniz` for shallow checkpoint reviews. The main thread must wait until a coherent slice is ready, then provide one complete review packet and enough investigation budget for a consolidated senior review. `Leibniz` should spend the first pass finding stale-state, contract, UI, provider-boundary, test-design, and bloat issues together. If a later pass is needed, it should be only because new code was written to fix a review finding, not because the first request was underspecified or the first pass was too narrow.
- Current stricter review protocol also requires `Leibniz` to explicitly cover stale state, replay/idempotency, secret leakage/redaction bypasses, incorrect agent ownership/routing, durable-store consistency after partial failure, concurrency/retry behavior, critical test adequacy, and bloat in one consolidated pass.
- Latest process correction from the user: if `Leibniz` misses a stale-state or integration issue in a first review, treat that as a review-request quality failure unless the issue was introduced by the fix itself. Give `Leibniz` the full slice, surrounding contracts, tests, validation evidence, browser evidence when UI changed, and enough time to produce one senior consolidated review before making code changes from that review.

## Memory Quality Rules

- Do not let useful work stay only in chat.
- Do not treat generated output as source truth.
- Do not apply graph-neighbor project memories as policy until reviewed.
- Use labeled relevance when evaluating project-memory retrieval if the expected memory ids, tags, or wiki notes are known.
- Prefer precision for policy memory and coverage for source-backed content research.

## Fast Validation Set

- `python3 -m pytest -q`
- `python3 -m compileall -q src/all_about_llms`
- For no-skip backend confidence from a host context that can reach local Postgres and bind loopback: `LIVE_POSTGRES=1 python -m pytest -q -rs`. Latest evidence on 2026-05-19: `454 passed` with local Docker Postgres/pgvector enabled and no default infra skips.
- JSON viewer validation with `python3 -m json.tool`
- JavaScript syntax check for generated HTML scripts when a viewer or cockpit changes
- No-SQLite scan
- Screenshot-location scan excluding `screenshots/`, virtualenvs, git internals, and Playwright cache


## Provider Proof Record - provider-backed-live-voice-proof - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-21
- validation_timestamp: 2026-05-21T05:50:10Z
- proof_outcome: failed
- validation_status: valid_failed_record
- state_change_allowed: false
- proof_artifact_type: provider_backed_live_voice_proof_record
- validation_issues: none
- workspace_validation_report_artifact_id: workspace-validation-valid-workspace-2026-05-21
- preflight_validation_report_artifact_id: provider-backed-live-voice-preflight-valid-openrouter-livekit-2026-05-23
- product_run_preflight_artifact_id: product-run-preflight-190ae2f9-a74b-4a23-b39c-aaf2d636bd8e
- provider_readiness_preflight_artifact_id: provider-readiness-local-livekit-ready-2026-05-21
- voice_runtime_readiness_preflight_artifact_id: voice-runtime-readiness-openrouter-livekit-ready-2026-05-23
- runtime_health_ledger_artifact_id: runtime-health-ledger-ready-9-of-9
- voice_edge_benchmark_status: ready
- provider_smoke_ledger_artifact_id: pending-accepted-proof-record-capture
- realtime_voice_timing_ledger_artifact_id: pending-accepted-proof-record-capture
- realtime_provider: openrouter_livekit
- execute_live_calls: false
- realtime_session_id_or_livekit_room: not-created-preflight-blocked
- participant_identity: openrouter-kokoro-agent-startup-preflight-only
- runtime_configuration_snapshot_id: runtime-config-local-livekit-2026-05-21
- post_capture_validation_results: 7 recorded / 2 passed / 5 failed
- secret_redaction_check: passed


## Provider Proof Record - external-publication-proof - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-21
- validation_timestamp: 2026-05-21T05:54:08Z
- proof_outcome: failed
- validation_status: valid_failed_record
- state_change_allowed: false
- proof_artifact_type: external_publication_proof_record
- validation_issues: none
- workspace_validation_report_artifact_id: workspace-validation-valid-workspace-2026-05-21
- preflight_validation_report_artifact_id: external-publication-preflight-invalid-linkedin-credential-policy-2026-05-21
- product_run_preflight_artifact_id: product-run-preflight-190ae2f9-a74b-4a23-b39c-aaf2d636bd8e
- publish_readiness_preflight_artifact_id: publish-readiness-preflight-blocked-linkedin-credential-policy-2026-05-21
- publish_readiness_artifact_id: publish-readiness-blocked-linkedin-credential-policy-2026-05-21
- distribution_package_artifact_id: not-run-preflight-blocked-linkedin-credential-policy
- approved_artifact_snapshot_id: publication-fixture-artifact-41538595-9f06-41f2-8d4d-54ec0da24acb
- destination_channel: linkedin
- durable_platform_id_or_url: not-published-preflight-blocked
- policy_acknowledgement_artifact_id: not-completed-policy-review-required
- rollback_or_postcondition_artifact_id: not-created-preflight-blocked
- post_capture_validation_results: 6 recorded / 2 passed / 4 failed
- secret_redaction_check: passed


## Closure Review Boundary - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-21
- completion_status: blocked_by_latest_failed_proof_record
- closure_review_template_status: blocked_by_completion_status
- closure_review_status: blocked_by_completion_status
- blocker_state_update_status: blocked_by_closure_review_status
- blocker_state_update_note_recorded: false
- goal_completion_claimed: false
- state_change_allowed: false
- evidence_files: `closure-review-template.json`, `closure-review-status.json`, `blocker-state-update.json`
- next action: provider-backed OpenRouter LiveKit live-voice proof is already accepted for this UUID. Capture and record the remaining LinkedIn policy/credential/destination/rollback evidence, then rerun completion status before closure review.


## Operator Unblocker Checklist - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-21
- source: `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/operator-unblocker-checklist.md`
- generator: `uv run all-about-llms-admin provider-proof-operator-unblocker-checklist --run-id 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e --output-dir social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`
- purpose: compact current-run handoff for the two remaining real proof blockers.
- live_voice_inputs_needed: none; provider-backed live voice is accepted for the current OpenRouter DeepSeek + LiveKit + Kokoro path.
- publication_inputs_needed: `LINKEDIN_ACCESS_TOKEN` or `LINKEDIN_ACCESS_TOKEN_FILE`, LinkedIn policy acknowledgement, durable external destination proof, and rollback or postcondition evidence.
- boundary: the checklist does not configure secrets, run providers, approve closure review, update blocker state, or complete the goal.


## Current Blocker Matrix - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-21
- source: `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/current-blocker-matrix.json`
- generator: `uv run all-about-llms-admin provider-proof-current-blocker-matrix --run-id 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e --output-dir social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`
- purpose: machine-readable split between already-evidenced local readiness and remaining external provider/platform blockers.
- live_voice_remaining: accepted OpenRouter LiveKit proof record is available; no live-voice operator-input blocker remains.
- publication_remaining: LinkedIn credential, policy acknowledgement, durable external destination proof, and rollback or postcondition evidence.
- boundary: no secret values, no state change, and no completion claim.


## Proof Workspace README Handoff - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-21
- source: `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/README.md`
- update: current blocker matrix and operator checklist regeneration commands are now visible in the proof workspace README.
- refreshed_workspace_validation: `workspace-validation.json` is `valid_workspace` after the README update.
- boundary: handoff instruction update only; the remaining proof blocker is LinkedIn publication evidence.


## Refreshed Preflight Validation - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-23
- live_voice: refreshed `provider-backed-live-voice-proof.preflight-validation.json` is `valid_preflight_artifacts` for OpenRouter DeepSeek + LiveKit + Kokoro, and the accepted proof record is now recorded.
- publication: refreshed `external-publication-proof.preflight-validation.json` remains `invalid_preflight_artifacts`; blocker is still LinkedIn credential plus policy review.
- boundary: validation evidence only; objective remains active and incomplete.


## Refreshed Failed Record Validation - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-21
- live_voice_record_validation: `valid_failed_record`, `proof_outcome=failed`, `issue_codes=[]`, `state_change_allowed=false`.
- publication_record_validation: `valid_failed_record`, `proof_outcome=failed`, `issue_codes=[]`, `state_change_allowed=false`.
- caveat: linked workspace/preflight statuses are verified through separate refreshed reports, not echoed inside failed-record validation payloads.


## Propagated Downstream Status - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-21
- regenerated_artifacts: `completion-status.json`, `closure-review-template.json`, `closure-review-status.json`, `blocker-state-update.json`, `current-blocker-matrix.json`, `operator-unblocker-checklist.md`
- status: completion remains `blocked_by_latest_failed_proof_record`; closure remains blocked by completion status; blocker-state update remains no-write with `goal_completion_claimed=false`.
- boundary: current handoff evidence only; no accepted proof or objective completion.


## Current Blocker Matrix Audit-State Summary - 2026-05-21

- update: `provider-proof-current-blocker-matrix` now preserves non-empty completion-level audit blocker lists for incomplete audit coverage, secret-shaped accepted audit notes, and invalid accepted audit notes.
- update: if a completion-status file has per-proof audit-note blocker states but lacks the matching top-level audit blocker arrays, the matrix now derives the summary lists from the proof rows.
- verification: `UV_CACHE_DIR=/private/tmp/uv-cache uv run --extra voice pytest tests/test_provider_proof_plan_cli.py -q` passed with `198 passed`.
- latest_verification: provider-proof CLI suite passed with `199 passed`.
- boundary: superseded by the accepted live-voice proof; the current UUID active blocker is external publication proof plus closure review.


## Operator Checklist Credential Snapshot - 2026-05-21

- update: `operator-unblocker-checklist.md` now includes a Credential Snapshot step before retrying proof preflights after provider/platform inputs change.
- command: `uv run all-about-llms-admin blocker-credential-snapshot > social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/credential-snapshot.json`
- expected_retry_gate: both `provider-backed-live-voice-proof` and `external-publication-proof` should read `runtime_configuration_present_unverified` before retrying proof preflights.
- current_snapshot: superseded by later OpenRouter/LiveKit operator-input refresh and accepted live-voice proof; external publication remains blocked on LinkedIn credential, policy, destination, and rollback/postcondition evidence.
- update: current credential snapshot states now render directly inside `operator-unblocker-checklist.md` beside the expected retry gate.
- update: the checklist now also prints the credential snapshot `checked_at` value so stale snapshot state is visible.
- verification: provider-proof CLI suite passed with `201 passed`.
- boundary: current proof state is updated; live voice has accepted OpenRouter LiveKit proof, and publication still needs LinkedIn/external destination evidence plus accepted external publication proof capture/recheck.


## Operator Input Template - 2026-05-21

- artifact: `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/operator-inputs.template.env`
- purpose: no-secret placeholder template for the current OpenRouter/LiveKit live-voice inputs and the remaining LinkedIn publication inputs.
- update: `operator-unblocker-checklist.md` now links to this template before the credential snapshot refresh step.
- verification: provider-proof CLI suite passed with `202 passed`.
- boundary: placeholders only; no credential, endpoint, publication, rollback, or accepted proof was supplied.


## Operator Checklist Checkpoint Evidence - 2026-05-21

- update: `operator-unblocker-checklist.md` now surfaces existing long-running recovery artifacts `operator-checkpoint.json` and `checkpoints.list.json` when present in the UUID workspace.
- checkpoint_kind: `operator_preflight`
- update: checkpoint kind is now read from the recorded checkpoint artifact instead of hardcoded in the checklist generator.
- update: checkpoint id `cc9c7ff8-99d8-4656-9ef1-7412694602bb` and event cursor `105458` are now visible in the UUID operator checklist.
- verification: provider-proof CLI suite passed with `201 passed`.
- boundary: checkpoint visibility only; no provider proof, publication proof, closure approval, or blocker-state mutation occurred.


## Reusable Operator Input Template Generation - 2026-05-21

- update: `init-provider-proof-workspace` now writes `operator-inputs.template.env` for every concrete UUID workspace, refuses to overwrite an existing copy, and lists it in generated `README.md`.
- validation_update: `validate-provider-proof-workspace` now expects and validates `operator-inputs.template.env` beside both proof templates and `README.md`.
- proof_plan_update: `provider-proof-plan` now lists `operator-inputs.template.env` in `workspace_expected_files`; refreshed UUID and placeholder proof-plan artifacts carry the new expected-file list.
- verification: focused workspace/checklist regressions passed with `5 passed`; full provider-proof CLI suite passed with `202 passed`.
- boundary: reusable placeholder generation only; no live-voice accepted proof record, LinkedIn credential, publication destination, rollback evidence, closure approval, or blocker-state update was supplied.


## Operator Input Readiness Gate - 2026-05-21

- new_command: `uv run all-about-llms-admin provider-proof-operator-input-readiness --run-id 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e --input-path social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/operator-inputs.template.env`
- artifact: `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/operator-input-readiness.json`
- current_status: `blocked_by_operator_inputs` at aggregate level because publication fields remain blocked; live voice is `ready_for_credential_snapshot_refresh`
- current_issue_codes: publication-only `operator_input_secret_file_unavailable`, `operator_input_placeholder`
- handoff_update: generated `README.md` and `operator-unblocker-checklist.md` now tell operators to capture `operator-input-readiness.json` before refreshing credential snapshots and preflight reports.
- verification: focused readiness/workspace/checklist regressions passed with `5 passed`; full provider-proof CLI suite passed with `205 passed`.
- boundary: readiness check reports field names and states only; it does not print token file contents, endpoint URLs, durable publication URLs, or secret values, and it does not change blocker state.


## Operator Input Path Consumption - 2026-05-21

- update: `blocker-credential-snapshot` and `provider-proof-plan` now accept `--operator-input-path` and merge non-placeholder credential/file fields into the no-secret classifier without printing values.
- handoff_update: `operator-unblocker-checklist.md` now passes `--operator-input-path social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/operator-inputs.template.env` into both retry commands.
- handoff_update: generated `README.md` now also carries the same `--operator-input-path` credential snapshot and proof-plan refresh commands, preserving README/checklist command parity.
- handoff_update: generated `README.md` now includes a strict `--fail-on-blocked` operator input readiness command before credential snapshot refresh for automation.
- handoff_update: generated `README.md` now includes the guarded strict-first retry sequence before credential snapshot, proof-plan, and matrix refresh.
- handoff_update: generated `README.md` now includes required evidence after input unblock for both proof routes, matching checklist and matrix evidence contracts.
- handoff_update: `operator-unblocker-checklist.md` now also renders current `operator-input-readiness.json` status, checked_at, and issue codes so placeholder/missing-file blockers are visible without opening JSON.
- handoff_update: `operator-unblocker-checklist.md` now renders readiness blocked field names from `operator-input-readiness.json`, keeping operator handoff value-free but precise.
- handoff_update: `operator-unblocker-checklist.md` now renders per-proof operator-input readiness state, issue codes, and field groups, matching the matrix routing view for human handoff.
- handoff_update: `operator-unblocker-checklist.md` now renders per-proof next actions for live voice and external publication operator-input blockers.
- handoff_update: `operator-unblocker-checklist.md` now renders per-proof required evidence after unblock, matching the matrix evidence contract for live voice and publication.
- handoff_update: `operator-unblocker-checklist.md` now documents optional `--fail-on-blocked` readiness execution for automation that should stop before credential snapshot refresh when inputs are still blocked.
- handoff_update: `operator-unblocker-checklist.md` now renders the effective `--fail-on-blocked` exit code from `operator-input-readiness.json`, matching the machine-readable readiness and matrix handoffs.
- handoff_update: `operator-unblocker-checklist.md` now includes the exact strict `--fail-on-blocked` readiness capture command, not only prose guidance.
- handoff_update: `operator-unblocker-checklist.md` now includes a guarded retry sequence that runs strict readiness before credential snapshot, proof-plan, and matrix refresh.
- matrix_update: `current-blocker-matrix.json` now includes `operator_input_readiness` with status, checked_at, issue codes, blocked fields, and evidence ref for automation/review agents.
- matrix_update: `current-blocker-matrix.json` now groups operator-input readiness by proof, including exit policy, a strict readiness command, a guarded strict retry sequence, per-proof state, issue codes, blocked fields, grouped field categories, value-free next actions, and repo-relative retry commands for live voice versus external publication routing.
- matrix_update: each per-proof `operator_input_readiness.proofs[*]` row now also carries `guarded_next_action_commands`, so route-specific agents can consume a strict-first retry sequence without reading the top-level matrix field.
- matrix_update: each per-proof `operator_input_readiness.proofs[*]` row now carries `required_evidence_after_unblock`, tying operator input unblock to credential snapshot, proof-plan, preflight, and proof-record evidence.
- validation_update: operator input readiness now rejects local/example/test/invalid endpoint URLs and local/draft publication destinations; OpenRouter LiveKit local-dev URL is allowed for the current realtime path.
- validation_update: operator input readiness now rejects non-LinkedIn durable publication destinations with `operator_input_destination_channel_mismatch`, matching the current LinkedIn credential/policy blocker.
- validation_update: operator input readiness now rejects draft/local policy acknowledgement and rollback/postcondition artifact IDs with `operator_input_local_artifact_substitute`.
- validation_update: operator input readiness now rejects secret-shaped token file paths with `operator_input_secret_path_detected`; snapshot/proof-plan merging skips those file path values.
- validation_update: `provider-proof-operator-input-readiness --fail-on-blocked` now prints the same no-secret JSON and exits nonzero unless all proof inputs are ready for credential snapshot refresh.
- validation_update: `operator-input-readiness.json` now includes an `exit_policy` block documenting default and `--fail-on-blocked` exit behavior for automation.
- validation_update: `operator-input-readiness.json` and `current-blocker-matrix.json` now expose `effective_fail_on_blocked_exit_code`, so automation can use strict readiness without deriving exit behavior from status plus policy.
- validation_update: invalid operator input file payloads now include `next_action: fix_operator_input_file`, so strict automation can separate file repair from credential filling.
- regression_coverage: filled input-file tests prove OpenRouter/LiveKit and LinkedIn file inputs can drive `runtime_configuration_present_unverified` and `ready_for_preflight_capture` without serializing token contents or URLs.
- boundary: current UUID remains blocked by failed proof records and publication placeholders, not by live-voice operator inputs.


## Static Proof Packet Guarded Retry Chain - 2026-05-21

- static_surface_update: proof-readiness, OpenRouter voice boundary, and publication boundary HTML proof packets now export `guarded_next_action_commands` inside `operator_input_readiness`.
- static_surface_update: the rendered proof packet panel now shows the strict-first guarded retry sequence beside blocked fields and required evidence.
- regression_coverage: browser proof-packet regressions first failed on missing `guarded_next_action_commands`; after the static-surface update, `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- static_surface_update: the same proof packets now export and render `exit_policy`, including default exit code, strict blocked exit code, blocked statuses, and the ready status.
- regression_coverage: a follow-up browser regression first failed because static proof packets omitted `exit_policy`; after the update, `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- static_surface_update: static proof packets now also export and render readiness diagnostics: `checked_at`, `evidence_ref`, `issue_codes`, and per-proof field groups.
- regression_coverage: a follow-up browser regression first failed because static proof packets omitted those diagnostics; after the update, `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- static_surface_update: static proof packets now export `state` and report-only `next_action_commands` beside the guarded retry sequence.
- regression_coverage: a follow-up browser regression first failed because static proof packets omitted report-only retry commands; after the update, `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- static_surface_update: static proof packets now export and render stable `proof_id` values for live voice and external publication routing.
- regression_coverage: a follow-up browser regression first failed because static proof packets omitted `proof_id`; after the update, `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- static_surface_update: static proof packets now export and render `packet_schema_version: operator-proof-packet.v1` and `handoff_contract: value-free-operator-proof-handoff`.
- regression_coverage: a follow-up browser regression first failed because static proof packets omitted the schema contract; after the update, `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- static_surface_update: static proof packets now export and render value-free `source_artifacts` refs for `operator-input-readiness.json`, `current-blocker-matrix.json`, and `operator-inputs.template.env`.
- regression_coverage: a follow-up browser regression first failed because static proof packets omitted packet-level source artifacts; after the update, `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- static_surface_update: static proof packets now export and render `state_change_allowed: false` plus `state_change_guardrail: no_state_change_without_accepted_proof_and_closure_review`.
- regression_coverage: a follow-up browser regression first failed because static proof packets omitted the state-change guardrail; after the update, `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- static_surface_update: static proof packets now export and render `matrix_parity_ref` paths into `operator_input_readiness.proofs.<proof_id>`.
- regression_coverage: a follow-up browser regression first failed because static proof packets omitted matrix parity refs; after the update, `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- matrix_parity_update: proof-packet browser tests now compare static readiness fields against the current blocker matrix rows after normalizing concrete UUIDs to `<run-id>`.
- regression_coverage: the new matrix-parity regression first failed on `blocked_fields` order drift; static live-voice and publication packets now match the matrix row order and `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- matrix_parity_update: `matrix_parity_ref` now uses JSON Pointer paths such as `/operator_input_readiness/proofs/provider-backed-live-voice-proof`.
- regression_coverage: a follow-up browser regression first failed because matrix refs used dotted paths that were not cleanly resolvable with hyphenated proof ids; JSON Pointer refs now resolve directly and `tests/test_blocker_proof_packets_browser.py` passed with `2 passed`.
- matrix_update: `current-blocker-matrix.json` now emits `operator_proof_packets` for live voice and external publication, giving non-browser agents the same value-free handoff contract as the static proof packets.
- regression_coverage: a CLI regression first failed because the current blocker matrix omitted `operator_proof_packets`; after the generator update and UUID artifact refresh, `tests/test_provider_proof_plan_cli.py::test_concrete_current_blocker_matrix_report_is_current` passed with `1 passed`.
- verification_coverage: after the matrix packet update, the full provider-proof CLI suite passed with `215 passed`, the static proof-surface browser suite passed with `14 passed`, `compileall` passed for `tests src`, `git diff --check` passed, the touched-file trailing-whitespace scan was clean, and the generated/user-facing proof artifacts had no matches for token-shaped secrets, absolute workspace paths, or concrete publication URLs.
- status_packet_update: `current-proof-status.md` is now regenerated by `provider-proof-current-status`, with the README handoff listing the status command beside the matrix and operator checklist. A red exact-match regression first failed because the status packet had no generator; after the CLI update and UUID artifact refresh, the provider-proof CLI suite passed with `216 passed`.
- matrix_status_handoff_update: `current-blocker-matrix.json` now exposes `current_state_packets`, `current_state_packet_commands`, and `next_status_packet`, so non-browser agents can discover and regenerate `current-proof-status.md` without scraping the README. The red matrix regression first failed on missing `current_state_packets`; after the matrix refresh, the full provider-proof CLI suite passed with `216 passed`.
- static_surface_update: proof-readiness, OpenRouter voice boundary, and publication boundary proof packets now also export/render the current-state packet map and regeneration commands, including `current-proof-status.md`.
- regression_coverage: the browser proof-packet regression first failed because static packets omitted the current-state packet handoff; after the HTML export update, the focused proof-packet browser test passed with `2 passed` and the full static proof-surface browser suite passed with `14 passed`.
- status_packet_contract_update: `current-proof-status.md` now includes a `Current State Packet Contract` section listing `current_state_packets`, `current_state_packet_commands`, `next_status_packet`, and `next_operator_packet`, so the generated human-readable packet is also self-describing for agents. The red exact-match regression first failed on the missing section; after regeneration, the full provider-proof CLI suite passed with `216 passed`.
- retry_chain_update: operator-input retry chains now refresh `current-proof-status.md` after readiness, credential snapshot, proof plan, and current blocker matrix refreshes, preventing stale status packets after external operator inputs change.
- static_surface_update: proof-readiness, OpenRouter voice boundary, and publication boundary proof packets now include the same status refresh command in report-only and guarded retry sequences.
- regression_coverage: the CLI retry-sequence regression first failed on missing status refresh; after the generator and artifact update, the focused CLI regression passed with `2 passed`, the proof-packet browser regression passed with `2 passed`, the full static proof-surface browser suite passed with `14 passed`, and the full provider-proof CLI suite passed with `216 passed`.
- verification_coverage: `compileall` passed for `src tests`, `git diff --check` passed, the touched-file trailing-whitespace scan was clean, and the refined generated/static artifact secret scan found no token-shaped secrets.
- kanban_status_handoff_update: `Agent Studio Kanban.md` now links blocked live-voice and publication work to `current-proof-status.md`, and its done evidence names `current-blocker-matrix.json` plus `operator-unblocker-checklist.md`, so board-level continuation starts from generated current-state packets instead of the long sprint log.
- regression_coverage: the foundation vault contract first failed because Kanban did not mention `current-proof-status.md`; after the board update, `tests/test_foundation.py::test_obsidian_vault_contains_interactive_design_and_tracking_artifacts` passed with `1 passed`.
- vault_home_handoff_update: `agent-studio-vault-home.html` and `system_design_vault/agent-studio-system-design-home.html` now expose current proof packet cards linking `current-proof-status.md`, `current-blocker-matrix.json`, and `operator-unblocker-checklist.md`.
- regression_coverage: the vault-home browser checks first failed because both homes omitted `current-proof-status.md`; after the HTML update, `tests/test_vault_home_browser.py tests/test_system_design_vault_home_browser.py` passed with `2 passed`, and the focused foundation contract passed with `1 passed`.
- boundary: this synchronizes static review exports with the README/checklist/matrix handoff only; it does not supply accepted live-voice or LinkedIn publication proof records.


## System Design Viewer Current Proof Packet Handoff - 2026-05-21

- system_design_viewer_handoff_update: generated `agent-studio-system-design.json` and embedded `agent-studio-system-design-viewer.html` now expose `current-proof-status.md`, `current-blocker-matrix.json`, and `operator-unblocker-checklist.md` inside the Objective Completion Audit component.
- regression_coverage: the system-design viewer browser check and foundation projection check first failed on the missing current proof packet names; after the viewer projection update, each focused check passed with `1 passed`.
- boundary: this is an interactive review-discovery update only; LinkedIn publication proof remains blocked on real evidence.


## Operator Checklist Retry Refresh - 2026-05-21

- retry_chain_update: operator-input report-only and guarded retry chains now refresh `operator-unblocker-checklist.md` after `current-proof-status.md`, so the matrix, status packet, and human checklist stay aligned after operator inputs or proof artifacts change.
- static_surface_update: proof-readiness, OpenRouter voice boundary, and publication boundary HTML proof packets now export/render the same checklist refresh command in `next_action_commands` and `guarded_next_action_commands`.
- regression_coverage: focused CLI regressions first failed on the missing checklist refresh; after the generator/artifact/static-surface update, the two focused CLI checks passed and static proof-packet browser parity passed with `2 passed`.
- boundary: this improves handoff freshness only; LinkedIn publication proof still requires real evidence.


## Current Status Regeneration Order - 2026-05-21

- status_packet_update: `current-proof-status.md` now lists regeneration commands in the same order as the current-state command contract and retry chains: `current-blocker-matrix.json`, `current-proof-status.md`, then `operator-unblocker-checklist.md`.
- status_packet_update: `current-proof-status.md` now lists `Next Packets` in that same matrix -> status -> checklist order, matching `next_status_packet` before `next_operator_packet`.
- regression_coverage: a focused CLI regression first failed because the status packet shell block put the checklist before status; after the generator and UUID status refresh, the focused check passed with `1 passed` and the full provider-proof CLI suite passed with `216 passed`.
- regression_coverage: a follow-up focused CLI regression first failed because `Next Packets` still listed checklist before status; after the generator and UUID packet refresh, the focused check passed with `1 passed`, provider-proof CLI passed with `216 passed`, and foundation passed with `21 passed`.
- boundary: this is command-order consistency only; it does not alter proof state or unblock the external provider/publication evidence.


## Operator Checklist Closure Gate - 2026-05-21

- checklist_update: `operator-unblocker-checklist.md` now includes `closure-review-status.json` in `Current Gate`, so the compact human handoff mirrors completion, closure template, closure status, and blocker-state update status from the matrix/status packet.
- checklist_update: `operator-unblocker-checklist.md` now also exposes `state_change_allowed: False` and `goal_completion_claimed: False`, matching the status packet's no-write completion boundary.
- checklist_update: `operator-unblocker-checklist.md` now exposes completion issue codes and latest failed proof IDs from the blocker matrix, including `latest_proof_record_failed`, `provider-backed-live-voice-proof`, and `external-publication-proof`.
- status_packet_update: `current-proof-status.md` now also exposes the same completion issue code and latest failed proof IDs in `Current Gate`, aligning the status packet and checklist on the current completion blocker.
- packet_parity_update: both `current-proof-status.md` and `operator-unblocker-checklist.md` now expose `missing accepted proofs: none`, preserving the matrix distinction between absent accepted records and latest failed records.
- regression_coverage: focused CLI regression first failed because the checklist omitted `closure-review-status.json`; after the generator and UUID checklist refresh, focused checklist verification passed with `1 passed`, provider-proof CLI passed with `216 passed`, and foundation passed with `21 passed`.
- regression_coverage: follow-up focused CLI regression first failed because the checklist omitted the state-change and goal-completion booleans; after the generator and UUID checklist refresh, focused checklist verification passed with `1 passed`, provider-proof CLI passed with `216 passed`, and foundation passed with `21 passed`.
- regression_coverage: follow-up focused CLI regression first failed because the checklist omitted completion issue details; after the generator and UUID checklist refresh, focused checklist verification passed with `1 passed`, provider-proof CLI passed with `216 passed`, and foundation passed with `21 passed`.
- regression_coverage: follow-up focused CLI regression first failed because the status packet omitted completion issue details; after the generator and UUID status refresh, focused status verification passed with `1 passed`, provider-proof CLI passed with `216 passed`, and foundation passed with `21 passed`.
- regression_coverage: follow-up focused CLI regressions first failed because both human packets omitted `missing accepted proofs`; after the generator and UUID packet refresh, focused packet verification passed with `2 passed`, provider-proof CLI passed with `216 passed`, and foundation passed with `21 passed`.
- packet_parity_update: both human packets now also expose `completion evidence ref: completion-status.json` plus closure evidence refs for `closure-review-template.json`, `closure-review-status.json`, and `blocker-state-update.json`, matching the current blocker matrix provenance fields.
- regression_coverage: focused packet regressions first failed on the omitted evidence-ref labels; after the generator and UUID packet refresh, focused packet verification passed with `2 passed`, provider-proof CLI passed with `216 passed`, and compileall passed.
- static_surface_update: proof-readiness, OpenRouter voice boundary, and publication boundary HTML proof packets now export/render `completion_evidence_ref` and `closure_evidence_refs`, matching the current blocker matrix provenance fields.
- regression_coverage: focused Playwright proof-packet regression first failed on missing rendered provenance labels; after the static export update, focused browser verification passed with `2 passed`, provider-proof CLI passed with `216 passed`, and foundation passed with `21 passed`.
- static_surface_update: the same HTML proof packets now export/render `current_gate`, including completion status, closure statuses, state-change flags, issue codes, latest failed proofs, and missing accepted proofs.
- regression_coverage: focused Playwright proof-packet regression first failed on missing `Current gate`; after the static export update, focused browser verification passed with `2 passed`, provider-proof CLI passed with `216 passed`, and foundation passed with `21 passed`.
- system_design_viewer_update: the generated system-design viewer now exposes the Objective Completion Audit current proof gate inline, including the latest failed proof IDs and `state_change_allowed=false`.
- regression_coverage: focused Playwright viewer regression first failed on missing `Current proof gate`; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, and compileall passed.
- system_design_viewer_update: the viewer current proof gate now also renders completion and closure evidence refs: `completion-status.json`, `closure-review-template.json`, `closure-review-status.json`, and `blocker-state-update.json`.
- regression_coverage: focused Playwright viewer regression first failed on missing evidence refs; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, and compileall passed.
- system_design_viewer_update: the Objective Completion Audit viewer detail now renders current-state packet regeneration commands for the matrix, status packet, and operator checklist.
- regression_coverage: focused Playwright viewer regression first failed on the missing command block; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, and compileall passed.
- system_design_viewer_update: the Objective Completion Audit viewer detail now renders the remaining no-secret operator input blocker names for live voice and external publication.
- regression_coverage: focused Playwright viewer regression first failed on missing `Operator input blockers`; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, and compileall passed.
- system_design_viewer_update: the Objective Completion Audit viewer detail now renders required evidence after unblock for both remaining proof records.
- regression_coverage: focused Playwright viewer regression first failed on missing `Required evidence after unblock`; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, and compileall passed.
- system_design_viewer_update: the Objective Completion Audit viewer detail now renders the strict `provider-proof-operator-input-readiness --fail-on-blocked` command that writes `operator-input-readiness.json`, so operator rechecks are visible from the top-level architecture viewer.
- regression_coverage: focused Playwright viewer regression first failed on the missing readiness command section; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, compileall passed, and touched-file hygiene/secret-shape scans were clean.
- system_design_viewer_update: the Objective Completion Audit viewer detail now renders the full guarded operator retry sequence: strict readiness, credential snapshot, proof plan, current blocker matrix, current status packet, and operator checklist refresh.
- regression_coverage: focused Playwright viewer regression first failed on the missing guarded-sequence section; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, compileall passed, and touched-file hygiene/secret-shape scans were clean.
- system_design_viewer_update: the Objective Completion Audit viewer detail now renders the operator input readiness exit policy, including `default_exit_code=0`, `fail_on_blocked_exit_code=2`, blocked statuses, and `ready_for_credential_snapshot_refresh`.
- regression_coverage: focused Playwright viewer regression first failed on the missing exit-policy section; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, compileall passed, and touched-file hygiene/secret-shape scans were clean.
- system_design_viewer_update: the Objective Completion Audit viewer detail now renders operator input readiness diagnostics, including `checked_at`, `operator-input-readiness.json`, effective strict exit code, and current issue codes.
- regression_coverage: focused Playwright viewer regression first failed on the missing diagnostics section; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, compileall passed, and touched-file hygiene/secret-shape scans were clean.
- system_design_viewer_update: the Objective Completion Audit viewer detail now renders per-proof operator-input routing for live voice and external publication, including next actions, JSON Pointer matrix refs, issue codes, blocked fields, and field groups.
- regression_coverage: focused Playwright viewer regression first failed on the missing routing section; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, compileall passed, and touched-file hygiene/secret-shape scans were clean.
- system_design_viewer_update: the Objective Completion Audit viewer detail now renders operator-input source artifact refs for `current-blocker-matrix.json`, `operator-input-readiness.json`, and `operator-inputs.template.env`.
- regression_coverage: focused Playwright viewer regression first failed on the missing source-artifacts section; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, compileall passed, and touched-file hygiene/secret-shape scans were clean.
- system_design_viewer_update: the Objective Completion Audit viewer detail now renders the operator proof packet contract, including `operator-proof-packet.v1`, `value-free-operator-proof-handoff`, `state_change_allowed=false`, and the no-state-change guardrail.
- regression_coverage: focused Playwright viewer regression first failed on the missing contract section; after the viewer update, focused browser verification passed with `1 passed`, foundation passed with `21 passed`, compileall passed, and touched-file hygiene/secret-shape scans were clean.
- system_design_viewer_update: the Objective Completion Audit viewer now has a direct parity regression against `current-blocker-matrix.json` for operator-input command, guarded retry sequence, exit policy, diagnostics, source artifacts, and per-proof routing.
- regression_coverage: parity first failed because the viewer omitted empty `invalid_fields` and `missing_fields` field groups; after the projection update, the focused viewer module passed with `2 passed`, foundation passed with `21 passed`, compileall passed, and touched-file hygiene/secret-shape scans were clean.
- system_design_viewer_update: the Objective Completion Audit per-proof routes now carry and render route-level `next_action_commands` and `guarded_next_action_commands` from `current-blocker-matrix.json`, covering both report-only and strict-first operator retry paths.
- regression_coverage: parity and browser checks first failed on missing route command arrays/labels; after the projection/render update, the focused viewer module passed with `2 passed`.
- status_packet_update: `current-proof-status.md` now mirrors those route-level report-only and strict-first command lists under `Per-proof readiness`, so Markdown-first operators can refresh readiness, snapshot, proof plan, matrix, status, and checklist without switching to the viewer or raw matrix.
- regression_coverage: the concrete current-status regression first failed on missing per-proof route command labels; after the generator and UUID status refresh, the focused test passed with `1 passed`.
- checklist_update: `operator-unblocker-checklist.md` now mirrors the same per-proof `next_action_commands` and `guarded_next_action_commands` inside compact operator-input readiness, aligning matrix, viewer, status packet, and checklist handoffs.
- regression_coverage: the concrete checklist regression first failed on missing per-proof route command labels; after the generator and UUID checklist refresh, the focused checklist test passed with `1 passed`.
- readme_update: generated provider proof workspace `README.md` now mirrors per-proof route command lists for report-only and strict-first retries, so the command-source handoff matches matrix, viewer, status, and checklist surfaces.
- regression_coverage: the workspace README regression first failed on the missing per-proof route-command section; after the generator update and UUID README refresh, the focused workspace README test passed with `1 passed`.
- status_packet_update: `current-proof-status.md` now also renders per-proof `issue_codes` and `field_groups` beside route commands, keeping status-first diagnostics aligned with the matrix, viewer, and checklist.
- regression_coverage: the concrete current-status regression first failed on missing per-proof issue-code and field-group labels; after the generator and UUID status refresh, the focused status test passed with `1 passed`.
- readme_update: generated provider proof workspace `README.md` now also renders per-proof route `issue_codes` and `field_groups`, keeping the command-source handoff aligned with status, matrix, viewer, and checklist diagnostics.
- regression_coverage: the workspace README regression first failed on missing route diagnostics; after the generator update and UUID README refresh, the focused workspace README test passed with `1 passed`.
- regression_coverage: added a concrete UUID README freshness guard comparing the checked-in `README.md` against fresh generator output; the new focused guard passed with `1 passed`.
- operator_input_update: `operator-input-readiness.json` now includes top-level report-only and strict-first retry command arrays that preserve the exact filled operator-input file path through readiness, credential snapshot, proof-plan, matrix, status, and checklist refresh.
- regression_coverage: the focused readiness regression first failed on missing command arrays, then passed with `1 passed`; the UUID readiness artifact was refreshed and its freshness check passed.
- matrix_update: `current-blocker-matrix.json` now preserves the readiness artifact's `input_path` when rebuilding report-only and strict-first retry commands, including `<workspace-root>/...` normalization for executable command paths.
- regression_coverage: the focused matrix regression first failed because the matrix omitted top-level command arrays and fell back to `operator-inputs.template.env`; after the fix, focused checks passed with `2 passed` including UUID matrix freshness.
- checklist_update: `operator-unblocker-checklist.md` now consumes readiness command arrays for validate, strict gate, credential snapshot, proof-plan, matrix, status, and checklist refresh commands, so a filled `operator-inputs.local.env` path survives into the compact handoff.
- regression_coverage: the focused checklist regression first failed because the compact handoff omitted the filled input path; after the renderer update and UUID checklist refresh, focused checks passed with `2 passed`.
- status_packet_update: `current-proof-status.md` now renders top-level operator-input `next_action_commands` and `guarded_next_action_commands`, preserving filled input paths before the per-proof readiness section.
- regression_coverage: the focused status regression first failed on missing top-level command arrays; after the renderer update and UUID status refresh, focused checks passed with `2 passed`.
- system_design_viewer_update: the Objective Completion Audit projection now carries `operator_input_retry_sequence` from `current-blocker-matrix.json`, and the HTML detail renders `Report-only operator retry sequence` before the guarded retry sequence.
- regression_coverage: the viewer parity regression first failed on the missing report-only sequence field; after the JSON/HTML update, focused parity and browser checks passed.
- boundary: this is no-secret gate visibility only. Provider-backed live voice is now accepted; closure remains blocked until accepted LinkedIn publication proof, completion recheck, and closure review exist.
- kanban_update: `Agent Studio Kanban.md` now names the `Report-only operator-input retry chain` separately from the `Guarded operator-input retry chain` on the remaining live-voice and publication blockers.
- regression_coverage: the foundation vault contract first failed because the Kanban linked generated proof packets but omitted the retry-chain split; after the board update, focused foundation verification passed with `1 passed`.
- status_packet_update: `current-blocker-matrix.json` and `current-proof-status.md` now carry `gemma-audio-reasoning` as `blocked` rather than `unknown`, taking the status from `voice-runtime-readiness.preflight.json`.
- regression_coverage: a focused current-status regression first failed on the stale `unknown` label; after the renderer and packet refresh, matrix/status/checklist freshness checks passed with `3 passed`.
- status_packet_update: `current-blocker-matrix.json` and `current-proof-status.md` now also carry `linkedin-publication-readiness` as `blocked` rather than `unknown`, taking the status from `publish-readiness.preflight.json`.
- regression_coverage: a focused current-status regression first failed on the stale publication `unknown` label; after the renderer and packet refresh, matrix/status/checklist freshness checks passed with `3 passed`.
- checklist_update: `operator-unblocker-checklist.md` now carries proof-level blocker statuses for both live voice and publication, keeping the compact operator handoff aligned with `current-proof-status.md` and `current-blocker-matrix.json`.
- regression_coverage: the concrete checklist regression first failed on missing status lines; after the renderer update and UUID checklist refresh, focused checklist verification passed with `1 passed`.
- boundary: this is status visibility only; external live voice and publication proof evidence remain required.
- matrix_update: `current-blocker-matrix.json` now carries `proof_capture_commands_after_unblock` for both remaining proofs, covering preflight capture, validation capture, proof-record template, proof-record validation, and proof-record recording.
- status_packet_update: `current-proof-status.md` renders the same proof-capture command chains so Markdown-first and matrix-first agents share the same after-unblock path.
- regression_coverage: concrete matrix/status regressions first failed on missing proof-capture command fields; after the generator update and UUID packet refresh, focused packet verification passed with `3 passed`.
- boundary corrected 2026-05-24: this is command routing only; HF/Gemma audio is legacy/native optional for the current route, provider-backed OpenRouter + LiveKit + Kokoro live voice is accepted, and LinkedIn publication evidence is the active external blocker.
- operator_packet_update: each `operator_proof_packets` route now also carries the same proof-capture command chain plus a `proof_capture_matrix_ref` back to `/proofs/<proof-id>/proof_capture_commands_after_unblock`.
- regression_coverage: the concrete matrix regression first failed on missing packet-level capture fields; after the generator update and UUID matrix refresh, focused packet verification passed with `3 passed`.
- boundary: this keeps the A2A packet contract self-contained; it does not execute or accept proof.
- static_surface_update: proof-readiness, OpenRouter voice boundary, and publication boundary HTML packets now render packet-level `Proof capture commands after unblock`, keeping browser-first review aligned with the matrix packet contract.
- regression_coverage: focused browser checks first failed on missing visible proof-capture commands; after the static packet update, live voice and publication packet checks passed with `2 passed`.
- boundary: this is interactive handoff visibility only; external proof execution remains blocked.
- system_design_viewer_update: the Objective Completion Audit viewer now carries and renders route-level `proof_capture_matrix_ref` and `proof_capture_commands_after_unblock` for both remaining proof routes.
- regression_coverage: the system-design viewer parity regression first failed on missing proof-capture route fields; after the JSON and embedded HTML update, focused JSON/browser verification passed.
- boundary: this keeps architecture-level A2A review aligned with matrix/static packets; it does not execute provider or publication proof.
- checklist_update: the compact operator unblocker checklist now renders explicit `proof_capture_commands_after_unblock` blocks for live voice and publication, reusing the same post-unblock capture commands as the matrix/status/static/system-design surfaces.
- regression_coverage: the concrete checklist regression first failed with zero proof-capture command labels; after updating the renderer and refreshing the UUID checklist, focused checklist verification passed with `1 passed`.
- boundary: this is command-label parity only; external publication proof evidence remains blocked.
- publication_handoff_update: external-publication `proof_capture_commands_after_unblock` now uses `acknowledge_publish_channel_policy=true`, matching the fact that this command chain runs after the operator has supplied policy approval evidence.
- regression_coverage: matrix/status/checklist tests first failed on the stale `false` value; after refreshing UUID packets and updating static/system-design packet data, focused packet, system-design parity, and publication browser parity checks passed.
- boundary: this does not perform external publication or create accepted proof; it only prevents the after-unblock handoff from recapturing the known policy-review blocker.
- readme_update: generated provider proof workspace READMEs now render `proof_capture_commands_after_unblock` for live voice and publication, including the post-approval publication readiness capture with policy acknowledgement set to true.
- regression_coverage: focused README checks first failed with zero proof-capture sections; after generator and UUID README refresh, focused README verification passed with `2 passed`.
- boundary: this keeps the README command-source contract aligned; external proof evidence remains blocked.
- proof_plan_update: `provider-proof-plan` proof rows now carry `proof_capture_commands_after_unblock`, so plan-first A2A/review agents can follow the same post-unblock capture path without first opening generated packets.
- regression_coverage: focused proof-plan checks first failed on missing fields; after the payload update and proof-plan refresh, focused proof-plan verification passed with `2 passed`.
- boundary: this is routing metadata only; no external proof capture or accepted proof was created.
- static_proof_plan_update: proof-readiness, OpenRouter voice boundary, and publication boundary `proof_plan` exports now also carry `proof_capture_commands_after_unblock`, preserving parity with `provider-proof-plan` rows.
- regression_coverage: static parity first failed with missing proof-capture fields; after the HTML updates, focused parity passed with `4 passed`, affected browser suites passed with `8 passed`, and compileall passed for the touched test.
- boundary: this closes a browser export handoff gap only; external publication proof evidence remains blocked.
- static_proof_plan_render_update: the same static surfaces now render `proof_capture_commands_after_unblock` visibly inside `proof_plan`, so browser-first agents do not have to rely on the hidden export or the separate operator proof packet.
- regression_coverage: focused browser checks first failed because detail panes showed one `Proof capture commands after unblock` label instead of two; after the renderer update, affected browser suites passed with `8 passed` and adjacent static checks passed with `8 passed`.
- boundary: visibility only; no external proof execution or state change.
- system_design_viewer_update: Objective Completion Audit route details now show the human label `Proof capture commands after unblock` while preserving the raw `proof_capture_commands_after_unblock` key.
- regression_coverage: focused viewer coverage first failed on the missing human label; after the renderer update, `tests/test_system_design_viewer_browser.py` passed with `2 passed`.
- boundary: architecture-viewer readability only; external proof evidence remains blocked.
- status_packet_update: `current-proof-status.md` now renders per-proof `operator_proof_packet` metadata from `current-blocker-matrix.json`, including human label, packet schema version, handoff contract, no-secret handling, next packet refs, `must_capture`, and `store_in`.
- checklist_update: `operator-unblocker-checklist.md` now renders the same packet metadata for live voice and publication, so human operators see the A2A proof-packet contract without opening the raw matrix.
- regression_coverage: focused Markdown regressions first failed on missing `operator_proof_packet` sections; after the shared renderer and UUID packet refresh, focused checks passed with `2 passed`, full provider-proof CLI passed with `221 passed`, foundation passed with `21 passed`, compileall passed, and touched-file whitespace/secret scans plus `git diff --check` were clean.
- boundary: this is packet-contract visibility only; provider-backed live voice is already accepted, and external publication proof remains the active blocker.
- readme_update: generated proof workspace `README.md` now also renders per-proof `operator_proof_packet` metadata, matching current status/checklist handoffs and preserving the command-source contract for A2A/human operators.
- workspace_validation_update: the UUID live-voice proof template now includes `voice_agent_process_start_artifact_id`, and `workspace-validation.json` has been refreshed to `valid_workspace`.
- regression_coverage: README regressions first failed on missing packet sections and workspace validation first failed as `invalid_workspace`; after the generator/artifact updates, focused checks passed with `2 passed`, full provider-proof CLI passed with `222 passed`, foundation passed with `21 passed`, compileall passed, and touched-file whitespace/secret scans plus `git diff --check` were clean.
- boundary corrected 2026-05-24: workspace validity is restored for local proof capture readiness only; external HF/Gemma evidence is legacy/native optional for the current route, provider-backed OpenRouter + LiveKit + Kokoro live voice is accepted, and publication evidence remains unsatisfied.
- proof_plan_update: `proof-plan.json` proof rows now include self-contained `operator_proof_packet` payloads with label, schema version, handoff contract, no-state-change guardrail, secret handling, proof-capture commands, proof-record schema/required fields, and audit storage targets.
- regression_coverage: proof-plan regressions first failed on missing `operator_proof_packet`; after the payload update and refreshing both portable/current proof-plan artifacts, focused checks passed with `2 passed`, full provider-proof CLI passed with `223 passed`, foundation passed with `21 passed`, compileall passed, and touched-file whitespace/secret scans plus `git diff --check` were clean.
- boundary: this is plan-first A2A routing metadata only; it does not satisfy the external proof blockers.
- static_proof_plan_update: proof-readiness, OpenRouter voice boundary, and publication boundary exports now inject the compact `proof_plan.operator_proof_packet` projection, matching the CLI proof-plan packet while preserving the richer top-level current-matrix packet.
- regression_coverage: static proof-plan parity first failed on missing `proof_plan.operator_proof_packet`; after static export normalization, focused parity passed with `2 passed`, full snapshot consistency passed with `6 passed`, proof-packet browser coverage passed with `2 passed`, foundation passed with `21 passed`, and compileall passed for the touched test.
- boundary: this is browser/export parity only; provider-backed live voice is already accepted, and external publication proof remains the active blocker.
- system_design_viewer_update: Objective Completion Audit routes now include and render `proof_plan_operator_packet`, preserving the compact provider-proof-plan packet alongside the richer current-matrix `operator_proof_packet`.
- regression_coverage: system-design viewer parity first failed on missing `proof_plan_operator_packet`; after the JSON/HTML update, viewer checks passed with `2 passed`, foundation passed with `21 passed`, and compileall passed for the touched viewer test.
- static_surface_update: proof-readiness, OpenRouter voice boundary, and publication boundary packets now export and visibly render current-matrix operator input field contracts plus per-field readiness statuses.
- regression_coverage: non-browser static parity first failed on stale embedded packets; after refreshing static packets and adding browser assertions, `tests/test_provider_proof_plan_cli.py tests/test_blocker_snapshot_consistency.py tests/test_blocker_proof_packets_browser.py tests/test_foundation.py` passed with `253 passed`, compileall passed, and `git diff --check` was clean.
- boundary: this closes a browser/export parity gap only; provider-backed live voice is already accepted, and external publication proof remains the active blocker.
- system_design_viewer_update: Objective Completion Audit routes in `output/viewers/agent-studio-system-design.json` and the embedded HTML viewer now carry and render route-level operator-input `field_contracts` and `field_statuses`.
- regression_coverage: focused viewer parity first failed because the route maps omitted those fields; after refreshing the projection and renderer, viewer checks passed with `2 passed`, combined viewer/static/foundation checks passed with `32 passed`, provider-proof CLI passed with `223 passed`, compileall passed, and `git diff --check` was clean.
- boundary: this closes architecture-viewer handoff parity only; external publication proof remains required.
- readme_update: generated provider proof workspace READMEs now render `operator_input_readiness.field_statuses` inside each `operator_proof_packet`, including secret-file, endpoint, artifact, and external-destination value sources.
- regression_coverage: focused README coverage first failed on missing field statuses; after updating the generator and refreshing UUID/RUN-NEXT READMEs, focused README checks passed with `2 passed`, provider-proof CLI passed with `223 passed`, combined viewer/static/foundation checks passed with `32 passed`, compileall passed, and `git diff --check` was clean.
- boundary: this closes proof-workspace handoff parity only; no external proof execution or acceptance occurred.
- boundary: architecture-viewer packet parity only; no external proof evidence was created.
- operator_input_update: `operator-input-readiness.json` now exposes aggregate `configured_fields` plus missing, placeholder, invalid, and unavailable secret-file field groups for no-secret operator-input automation.
- matrix_update: `current-blocker-matrix.json` preserves those aggregate field groups so broad A2A/review agents can assess configured versus blocked inputs without proof-by-proof traversal.
- regression_coverage: focused readiness/matrix regressions first failed on missing aggregate fields; after the CLI update and UUID packet refresh, focused checks passed with `3 passed`, provider-proof CLI passed with `223 passed`, static/viewer parity passed with `9 passed`, compileall passed, and `git diff --check` was clean.
- boundary: this is aggregate readiness metadata only; provider-backed live voice is already accepted, and external publication proof remains the active blocker.
- markdown_packet_update: `current-proof-status.md` and `operator-unblocker-checklist.md` now render top-level configured fields and aggregate field groups before per-proof readiness routes.
- regression_coverage: focused Markdown regressions first failed on missing aggregate sections; after the renderer update and UUID packet refresh, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is text-first readiness parity only; accepted external proof records remain required.
- system_design_viewer_update: Objective Completion Audit diagnostics in the generated system-design JSON and HTML viewer now expose aggregate blocked fields, configured fields, and field groups before per-proof readiness routes.
- regression_coverage: focused viewer parity first failed on missing aggregate diagnostics; after refreshing the viewer projection, system-design viewer checks passed with `2 passed`, static/viewer parity passed with `9 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is architecture-viewer readiness parity only; provider-backed live voice is already accepted, and external publication proof remains the active blocker.
- readme_update: generated provider proof workspace READMEs now render top-level aggregate configured fields and grouped missing/placeholder/invalid/unavailable operator inputs before per-proof route commands.
- regression_coverage: README regressions first failed on missing aggregate readiness; after the generator update and refreshing UUID/RUN-NEXT READMEs plus validation artifacts, focused checks passed with `3 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is workspace-local no-secret handoff parity only; accepted external proof records remain required.
- matrix_update: `current-blocker-matrix.json` now preserves per-proof `configured_fields` from `operator-input-readiness.json` inside readiness proof rows and operator proof packets.
- static_viewer_update: proof-plan/status/checklist/README/static proof pages and the system-design viewer route projection now carry the same per-proof `configured_fields` handoff.
- regression_coverage: focused matrix/browser regressions first failed on missing `configured_fields`; after generator and artifact refresh, focused packet/viewer checks passed with `4 passed`, static/viewer suites passed with `11 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is no-secret field-name parity only; accepted external proof records remain required.
- matrix_update: readiness proof rows and operator proof packets now preserve per-proof `required_fields` beside blocked/configured fields.
- static_viewer_update: proof-plan/status/checklist/README/static proof pages and the system-design viewer route projection now expose per-proof `required_fields`, giving every handoff the required/configured/blocked input split.
- regression_coverage: focused matrix/browser/viewer regressions first failed on missing `required_fields`; after generator and artifact refresh, focused checks passed with `5 passed`, provider-proof CLI passed with `223 passed`, and static/viewer suites passed with `11 passed`.
- boundary: this is no-secret input-schema parity only; accepted external proof records remain required.
- markdown_packet_update: compact per-proof readiness sections in `current-proof-status.md` and `operator-unblocker-checklist.md` now render `required_fields` and `configured_fields` beside blocked fields, retry commands, field groups, and required post-unblock evidence.
- regression_coverage: focused Markdown regressions first failed on missing per-proof required/configured labels; after the renderer update and UUID packet refresh, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is text-first no-secret operator handoff parity only; external publication proof remains required.
- markdown_packet_update: aggregate operator-input readiness in `current-proof-status.md` and `operator-unblocker-checklist.md` now renders `required_fields` before configured fields and grouped diagnostics.
- regression_coverage: focused Markdown regressions first failed on missing aggregate required-field labels; after the renderer update and UUID packet refresh, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is aggregate no-secret Markdown parity only; external publication proof remains required.
- markdown_packet_update: aggregate operator-input readiness in `current-proof-status.md` and `operator-unblocker-checklist.md` now renders `field_contracts` and `field_statuses`, including state, issue code, value source, next action, and validation contract.
- regression_coverage: focused Markdown regressions first failed on missing aggregate contract/status sections; after the renderer update and UUID packet refresh, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is no-secret text handoff parity only; external publication proof remains required.
- markdown_packet_update: compact per-proof readiness routes in `current-proof-status.md` and `operator-unblocker-checklist.md` now render each proof's `field_contracts` and `field_statuses` before retry commands.
- regression_coverage: focused Markdown regressions first failed on missing per-proof contract/status sections; after the renderer update and UUID packet refresh, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is no-secret per-proof Markdown parity only; external publication proof remains required.
- readme_update: generated provider proof workspace READMEs now render aggregate `required_fields`, `field_contracts`, and `field_statuses` in the operator-input readiness block, matching the no-secret readiness metadata visible in status/checklist packets.
- regression_coverage: focused README regressions first failed on missing aggregate required/contract/status sections; after the generator update and refreshing UUID plus RUN-NEXT READMEs, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is command-source README handoff parity only; external publication proof remains required.
- readme_update: generated provider proof workspace READMEs now render per-proof route `required_fields`, `configured_fields`, `field_contracts`, and `field_statuses` before the retry command lists.
- regression_coverage: focused README regressions first failed on missing per-proof route field sections; after the generator update and refreshing UUID plus RUN-NEXT READMEs, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is command-source per-proof route parity only; external publication proof remains required.
- readme_update: generated provider proof workspace READMEs now render each route's `status`, `checked_at`, `evidence_ref`, `next_action`, and `effective_fail_on_blocked_exit_code` before route fields and retry commands.
- regression_coverage: focused README regressions first failed on missing route state metadata; after the generator update and refreshing UUID plus RUN-NEXT READMEs, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is command-source route-state parity only; external publication proof remains required.
- template_update: generated `operator-inputs.template.env` files now include no-secret `contract` and `value_source` comments for HF token file, Gemma endpoint, LinkedIn token file, policy acknowledgement artifact, durable destination, and rollback/postcondition artifact fields.
- regression_coverage: focused template regressions first failed on missing contract/value-source comments; after the generator update and refreshing UUID plus RUN-NEXT templates, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is operator input guidance only; external publication proof remains required.
- readme_update: generated provider proof workspace READMEs now render aggregate and per-proof route `blocked_fields` before required/configured field details and retry commands.
- regression_coverage: focused README regressions first failed on missing blocked-field sections; after the generator update and refreshing UUID plus RUN-NEXT READMEs, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is command-source blocked-field parity only; external publication proof remains required.
- template_update: generated `operator-inputs.template.env` files now include no-secret `template_state`, `issue_code`, and `next_action` comments for every remaining operator input field.
- regression_coverage: focused template regressions first failed on missing state/issue/action comments; after the generator update and refreshing UUID plus RUN-NEXT templates, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is operator-input template guidance only; external publication proof remains required.
- template_update: generated `operator-inputs.template.env` files now include no-secret `proof_id` and `proof_input_role` comments for every remaining operator input field.
- regression_coverage: focused template regressions first failed on missing proof ownership comments; after the generator update and refreshing UUID plus RUN-NEXT templates, focused checks passed with `2 passed`, and provider-proof CLI passed with `223 passed`.
- boundary: this is operator-input proof ownership guidance only; external publication proof remains required.
- readiness_update: `operator-input-readiness.json` now carries no-secret `proof_id` and `proof_input_role` inside field-status maps for every remaining operator input.
- static_viewer_update: current proof-plan, blocker matrix, Markdown packets, proof-readiness HTML, OpenRouter voice boundary, publication boundary, and the system-design viewer now preserve those machine-readable ownership fields; Markdown renders them as `owner_proof_id` and `owner_proof_input_role` to avoid colliding with packet identity.
- regression_coverage: focused readiness regressions first failed on missing proof ownership keys; after the CLI update and artifact refresh, focused checks passed with `2 passed`, provider-proof CLI passed with `223 passed`, and escalated static/viewer parity passed with `11 passed`.
- boundary: this is machine-readable no-secret input ownership only; external publication proof remains required.
- readiness_update: `operator-input-readiness.json` now exposes direct aggregate and per-proof `field_ownership` maps so A2A/review agents can route required fields without traversing `field_statuses`.
- static_viewer_update: current proof-plan, blocker matrix, Markdown packets, proof-readiness HTML, OpenRouter voice boundary, publication boundary, proof workspace README/validation, and system-design viewer exports were refreshed with the direct ownership maps.
- regression_coverage: focused readiness/viewer regressions first failed on missing direct ownership maps; after the CLI update and artifact refresh, focused checks passed, provider-proof CLI passed with `223 passed`, and escalated static/viewer parity passed with `11 passed`.
- boundary: this is direct no-secret routing metadata only; external publication proof remains required.
- current_continuation_update: after checking Leibniz/Cursor-vault status, stale test expectations were aligned to the current OpenRouter `deepseek/deepseek-v4-flash` + LiveKit + Kokoro route. Live-voice proof is accepted; do not reopen HF/Gemma/Gamma/MLX setup for current proof capture.
- verification_update: shell-only checks passed: frontend `npm run test:race` (`251 passed`), `uv run pytest tests/test_foundation.py -q` (`22 passed`), focused current UUID proof-packet CLI checks (`2 passed`), and Python compile checks for touched static/browser tests. Browser-rendered verification remains deferred until keyboard/input automation is confirmed stable.
- remaining_gates: provider-backed live voice has accepted timing/proof evidence with 8/8 timing stages measured. External publication still lacks LinkedIn credential, policy acknowledgement, durable destination, and rollback/postcondition evidence.
- runtime_caveat: the existing FastAPI process on `127.0.0.1:8000` is listening but currently does not complete `/health`; the Next dev proxy reports `ECONNRESET` for `/api/worker-scheduler-process`. A temporary isolated backend on `127.0.0.1:8001` returned `/health` successfully, so treat this as a stale or wedged shared process until the operator approves a restart.


## Provider Proof Record - provider-backed-live-voice-proof - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-23
- validation_timestamp: 2026-05-24T02:53:43Z
- proof_outcome: failed
- validation_status: valid_failed_record
- state_change_allowed: false
- proof_artifact_type: provider_backed_live_voice_proof_record
- validation_issues: none
- workspace_validation_report_artifact_id: workspace-validation.json
- preflight_validation_report_artifact_id: provider-backed-live-voice-proof.preflight-validation.json
- product_run_preflight_artifact_id: product-run.preflight.json
- provider_readiness_preflight_artifact_id: provider-readiness.preflight.json
- voice_runtime_readiness_preflight_artifact_id: voice-runtime-readiness.preflight.json
- voice_agent_process_start_artifact_id: voice-agent-process.start.json:pid-29685
- runtime_health_ledger_artifact_id: 93a723c3-01c4-4559-9cf5-cf52e4e3f287
- voice_edge_benchmark_status: ready
- provider_smoke_ledger_artifact_id: 9b371737-1344-4cfa-be2e-fcfc9cc30700
- realtime_voice_timing_ledger_artifact_id: 4437cf16-302c-401f-8b8c-7cd9032e6dca
- realtime_provider: openrouter_livekit
- execute_live_calls: true
- realtime_session_id_or_livekit_room: 89f7b584-6905-4e74-9210-08c28ba254e4
- participant_identity: openrouter-kokoro-agent
- runtime_configuration_snapshot_id: runtime-config-openrouter-livekit-2026-05-23
- post_capture_validation_results: 7 recorded / 5 passed / 2 failed
- secret_redaction_check: passed


## Provider Proof Record - provider-backed-live-voice-proof - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-23
- validation_timestamp: 2026-05-24T03:44:14Z
- proof_outcome: failed
- validation_status: valid_failed_record
- state_change_allowed: false
- proof_artifact_type: provider_backed_live_voice_proof_record
- validation_issues: none
- workspace_validation_report_artifact_id: workspace-validation.json
- preflight_validation_report_artifact_id: provider-backed-live-voice-proof.preflight-validation.json
- product_run_preflight_artifact_id: product-run.preflight.json
- provider_readiness_preflight_artifact_id: provider-readiness.preflight.json
- voice_runtime_readiness_preflight_artifact_id: voice-runtime-readiness.preflight.json
- voice_agent_process_start_artifact_id: voice-agent-process.start.json:pid-29685
- runtime_health_ledger_artifact_id: 93a723c3-01c4-4559-9cf5-cf52e4e3f287
- voice_edge_benchmark_status: ready
- provider_smoke_ledger_artifact_id: 94857bb9-c5eb-4174-8bc5-6687bd8befbe
- realtime_voice_timing_ledger_artifact_id: 3244077a-e689-4d26-9a9e-ff8c1ddb74df
- realtime_provider: openrouter_livekit
- execute_live_calls: true
- realtime_session_id_or_livekit_room: ebd43531-86e3-4af1-ade0-15ac8d7184bf
- participant_identity: openrouter-kokoro-agent
- runtime_configuration_snapshot_id: runtime-config-openrouter-livekit-2026-05-23
- post_capture_validation_results: 7 recorded / 6 passed / 1 failed
- secret_redaction_check: passed


## Provider Proof Record - provider-backed-live-voice-proof - 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e

- checked_at: 2026-05-24
- validation_timestamp: 2026-05-24T06:06:52Z
- proof_outcome: accepted
- validation_status: valid_accepted_record
- state_change_allowed: true
- proof_artifact_type: provider_backed_live_voice_proof_record
- validation_issues: none
- preflight_validation_report_status: valid_preflight_artifacts
- preflight_validation_report_matched_fields: all_required_fields_matched
- preflight_validation_report_validated_product_run_id: 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e
- preflight_validation_report_validated_runtime_checks: livekit-transport, livekit-agent-participant, voice-agent-backend-event-sink, openrouter-live-dialogue-reasoning, kokoro-tts, rust-voice-edge
- workspace_validation_report_status: valid_workspace
- workspace_validation_report_matched_fields: all_required_fields_matched
- workspace_validation_report_artifact_id: <workspace-root>/social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/workspace-validation.json
- preflight_validation_report_artifact_id: <workspace-root>/social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/provider-backed-live-voice-proof.preflight-validation.json
- product_run_preflight_artifact_id: <workspace-root>/social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/product-run.preflight.json
- provider_readiness_preflight_artifact_id: <workspace-root>/social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/provider-readiness.preflight.json
- voice_runtime_readiness_preflight_artifact_id: <workspace-root>/social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/voice-runtime-readiness.preflight.json
- voice_agent_process_start_artifact_id: <workspace-root>/social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/voice-agent-process.start.json
- runtime_health_ledger_artifact_id: 93a723c3-01c4-4559-9cf5-cf52e4e3f287
- voice_edge_benchmark_status: ready
- provider_smoke_ledger_artifact_id: 94857bb9-c5eb-4174-8bc5-6687bd8befbe
- livekit_voice_timing_capture_artifact_id: <workspace-root>/social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/livekit-voice-timing-capture.json
- realtime_voice_timing_ledger_artifact_id: 7e932381-4bf4-4206-a490-58d6a4ca7880
- realtime_provider: openrouter_livekit
- execute_live_calls: true
- realtime_session_id_or_livekit_room: ebd43531-86e3-4af1-ade0-15ac8d7184bf
- participant_identity: provider-smoke-creator
- runtime_configuration_snapshot_id: <workspace-root>/social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/credential-snapshot.json
- post_capture_validation_results: 7 recorded / 7 passed / 0 failed
- secret_redaction_check: passed

## Frontend OpenRouter Copy Handoff - 2026-05-24

- Current frontend default path is OpenRouter `deepseek/deepseek-v4-flash` + LiveKit + Kokoro. Voice setup, provider release-gate, no-ledger streaming proof, and captured-audio proof cards now render OpenRouter/Kokoro labels for the active path instead of defaulting to Gemma/Kokoro.
- Current OpenRouter voice release readiness is isolated from content research providers: missing Tavily/web-search or reranker setup is a publication/retrieval concern, not a live voice release blocker. Legacy/Gemma provider paths keep their existing selected-provider checks.
- Regression coverage added in `frontend/next-app/tests/voiceSetup.test.ts`, `frontend/next-app/tests/voiceProviderReadiness.test.ts`, and `frontend/next-app/tests/voiceProviderSmoke.test.ts`. The old stale-copy assertions failed first; follow-up regressions also failed on legacy Gemma/HF release blocking, Gemma captured-audio copy, Tavily blocking OpenRouter voice readiness, and Gemma/HF missing-runtime fallback copy. After the fix, `npm run test:race` passed with `259 passed`, and `npm run typecheck`, `npm run lint`, `npm run build`, `git diff --check`, and `uv run pytest tests/test_repo_workflow_ci.py -q` passed. CI/repo hygiene tracks the committed `uv.lock`.
- Browser render check on `http://127.0.0.1:3001/` showed no `data-nextjs-error`, visible OpenRouter/DeepSeek/Kokoro runtime copy, and no stale default Gemma/Gamma body copy. Screenshot capture through the Browser plugin timed out; do not treat the missing screenshot as a product failure.
- Current proof gate: provider-backed live voice is accepted; completion remains blocked only by external publication operator evidence for LinkedIn credential, policy acknowledgement artifact, durable publication destination, and rollback/postcondition artifact.

## Current Proof Packet Refresh - 2026-05-24

- The UUID proof workspace packets were refreshed from `operator-inputs.template.env` on 2026-05-24. `completion-status.json` still reports `blocked_by_latest_failed_proof_record`; accepted proofs remain `provider-backed-live-voice-proof`; latest failed proof remains `external-publication-proof`.
- Strict operator input readiness is still the right first gate for publication work. It exits `2` and names exactly four blocked fields: `LINKEDIN_ACCESS_TOKEN_FILE`, `LINKEDIN_POLICY_ACKNOWLEDGEMENT_ARTIFACT_ID`, `PUBLICATION_DURABLE_PLATFORM_ID_OR_URL`, and `PUBLICATION_ROLLBACK_OR_POSTCONDITION_ARTIFACT_ID`.
- Browser proof packets were aligned to the refreshed `checked_at=2026-05-24` state. Focused proof-packet browser regressions passed with `5 passed, 2 skipped`; no runtime/Cursor/LiveKit process was restarted.

## LinkedIn-Only Publication Proof Setup - 2026-05-24

- Current handoff: `external-publication-proof` is LinkedIn-only for this proof gate. Credential setup and proof-level credential classification require `LINKEDIN_ACCESS_TOKEN_FILE` or `LINKEDIN_ACCESS_TOKEN`; Instagram, X, and Substack remain outside this current strict proof blocker.
- Refreshed artifacts: `credential-snapshot.json`, `proof-plan.json`, proof workspace README/validation, proof readiness HTML, and publication boundary HTML were regenerated without secret values. The UUID workspace validates as `valid_workspace`; `RUN-2026-05-20-NEXT` remains `blocked_by_run_id`.
- Verification: proof/static suite `204 passed, 32 skipped`; stable CI Python slice `227 passed, 30 skipped`; ruff, frontend lint/typecheck/race tests/build, and `git diff --check` passed.
- Browser note: the in-app Browser runtime refused direct `file://` navigation by policy. Render is covered by local Playwright pytest browser tests that loaded and exercised both static HTML views.
- Remaining blocker: do not claim objective completion until the operator supplies LinkedIn credential-file proof, LinkedIn policy acknowledgement artifact, durable publication URL/platform id, and rollback/postcondition artifact. No Cursor, LiveKit, or backend process was restarted during this update.

## LinkedIn-Only Accepted Publication Validation - 2026-05-24

- Source: Leibniz review finding plus `src/all_about_llms/cli.py`, `tests/test_provider_proof_plan_cli.py`, proof-readiness HTML, and publication boundary HTML.
- Fix: accepted `external-publication-proof` records now require normalized `destination_channel=linkedin`, a durable LinkedIn destination URL/platform id, and linked preflight `validated_publish_channels` exactly equal to `linkedin`. Completion-status audit-note parsing enforces the same rule for hand-edited accepted proof notes.
- Follow-up: the generated proof handoff now says the same invariant directly. Provider-proof templates, `proof-plan.json`, proof-readiness HTML, and publication boundary HTML use the exact LinkedIn destination/preflight wording instead of the older generic destination/preflight-channel phrasing.
- Verification: generic Instagram/X accepted-record regressions failed first, then provider-proof CLI passed with `200 passed, 30 skipped`; proof/static suite passed with `205 passed, 32 skipped`; stable Python CI passed with `228 passed, 30 skipped`; ruff and `git diff --check` passed.
- Boundary: this closes the LinkedIn-only proof-quality gap only. External publication remains blocked by the four operator-owned fields: `LINKEDIN_ACCESS_TOKEN_FILE`, `LINKEDIN_POLICY_ACKNOWLEDGEMENT_ARTIFACT_ID`, `PUBLICATION_DURABLE_PLATFORM_ID_OR_URL`, and `PUBLICATION_ROLLBACK_OR_POSTCONDITION_ARTIFACT_ID`.

## CI/CD Verification And Lockfile Correction - 2026-05-24

- Current repo workflow uses `uv.lock` as the committed Python dependency lockfile. Keep local command-log artifacts out of commits.
- Latest verification after LinkedIn-only proof validation: full Python `789 passed, 49 skipped`; stable Python CI `228 passed, 30 skipped`; ruff passed; `git diff --check` passed; frontend lint/typecheck/race tests/build passed; Rust fmt and locked tests passed for `services/retrieval-ranker` and `services/voice-edge`.
- Local generated viewer/proof artifacts remain ignored by `.gitignore`; this is intentional to avoid pushing large generated state. Browser/render parity for those static files is covered by local Playwright pytest, not by committing image/PDF artifacts.
- Remaining critical blocker: external publication proof still needs LinkedIn credential-file evidence, policy acknowledgement artifact, durable LinkedIn destination URL/platform id, rollback/postcondition artifact, completion-status recheck, and closure review before any goal-complete claim.

## Provider Proof Status Parser Boundary Fix - 2026-05-24

- Source: `src/all_about_llms/cli.py`, `tests/test_provider_proof_plan_cli.py`, `provider-proof-completion-status` for run `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`.
- Fix: completion-status parsing now stops a `## Provider Proof Record ...` block at the next top-level Markdown `##` section, not only at the next provider-proof record. This prevents ordinary Obsidian handoff sections from being parsed as proof-record fields.
- Current status after rerun: accepted proofs are `provider-backed-live-voice-proof`; invalid accepted audit-note proofs are empty; latest failed proof remains `external-publication-proof`.
- Verification: provider-proof CLI suite passed with `201 passed, 30 skipped`; static proof/system-design browser render tests passed with `6 passed`; stable Python CI slice passed with `230 passed, 30 skipped`.
- Boundary: this is a parser/status correction only. It does not create publication evidence or change the external publication blocker.

## Root Agent Operating Contract - 2026-05-24

- Source: `AGENTS.md`, `tests/test_repo_workflow_ci.py`, branch GitHub Actions CI.
- Handoff: root `AGENTS.md` now captures the repo operating contract for future Codex/Cursor agents: PR-style branch naming, `uv.lock` as the committed dependency lockfile, local command-log artifacts as transient output that must stay untracked, no secrets or large artifacts, OpenRouter `deepseek/deepseek-v4-flash` + LiveKit + Kokoro as the active realtime path, and no Hugging Face/Gemma4/Gamma4/MLX active realtime default.
- Gate state: `provider-backed-live-voice-proof` remains accepted; `external-publication-proof` still requires `LINKEDIN_ACCESS_TOKEN_FILE`, `LINKEDIN_POLICY_ACKNOWLEDGEMENT_ARTIFACT_ID`, `PUBLICATION_DURABLE_PLATFORM_ID_OR_URL`, and `PUBLICATION_ROLLBACK_OR_POSTCONDITION_ARTIFACT_ID`.
- Verification: the handoff regression failed first because `AGENTS.md` was absent, then passed after the file was added. Branch CI was green at verification time. PR creation remains blocked by GitHub integration permission `403 Resource not accessible by integration`.

## PR Proof-Gate Handoff - 2026-05-24

- Source: `.github/pull_request_template.md`, `tests/test_repo_workflow_ci.py`, `docs/repo-workflow.md`, branch GitHub Actions CI.
- Handoff: the PR template now requires every manual PR to state `provider-backed-live-voice-proof` status, `external-publication-proof` status, OpenRouter `deepseek/deepseek-v4-flash` + LiveKit + Kokoro as the current accepted path, and whether the LinkedIn operator inputs remain blocked.
- Verification: the PR template regression failed first on the missing provider proof section, then passed after the template update. Branch-head CI was green at last live check. REST PR lookup still returns no open PR for `feature/livekit-voice-proof-capture` into `main`.
- Boundary: this advances PR/auto-merge handoff readiness only. It does not create LinkedIn credentials or accepted external publication evidence.

## Manual PR Handoff CLI - 2026-05-24

- Source: `src/all_about_llms/cli.py`, `tests/test_repo_workflow_ci.py`, `docs/repo-workflow.md`, branch GitHub Actions CI.
- Handoff: `uv run all-about-llms-admin provider-proof-pr-handoff --run-id 190ae2f9-a74b-4a23-b39c-aaf2d636bd8e --operator-input-path social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/operator-inputs.template.env --ci-url <latest-branch-head-ci-url> --head-sha <current-branch-head-sha>` emits a no-secret manual PR body from the current completion and operator-input gates after the placeholders are filled from the current branch head. It names the accepted OpenRouter/LiveKit/Kokoro live-voice proof, the failed external-publication proof, the four LinkedIn/publication blocked fields, and current CI evidence.
- Review finding fixed: Leibniz found that `--output-dir` could summarize one proof workspace while printing the default operator input path. The command now derives `operator-inputs.template.env` from `--output-dir` when no explicit `--operator-input-path` is supplied, and the verification command carries the same `--output-dir`.
- Review finding fixed: Leibniz found that documented manual PR commands claimed CI evidence without passing a CI URL or branch-head SHA. The documented commands now require `--ci-url <latest-branch-head-ci-url>` and `--head-sha <current-branch-head-sha>` placeholders so agents fill current branch evidence before pasting the generated body.
- Verification: repo workflow regression passed with `10 passed`; stable Python CI slice passed with `236 passed, 30 skipped`; branch-head remote CI was green for the current pushed head at last live check. Connector PR creation still returns GitHub `403 Resource not accessible by integration`, and REST lookup still shows no open PR.
- Boundary: this advances the PR fallback path only. External publication remains blocked by `LINKEDIN_ACCESS_TOKEN_FILE`, `LINKEDIN_POLICY_ACKNOWLEDGEMENT_ARTIFACT_ID`, `PUBLICATION_DURABLE_PLATFORM_ID_OR_URL`, and `PUBLICATION_ROLLBACK_OR_POSTCONDITION_ARTIFACT_ID`.

## OpenRouter LiveKit Vault Surface Rename - 2026-05-24

- Source: `agent_progress_vault/06-live-voice/openrouter-livekit-current-unblock-guide.md`, `social_media_optimiser/02-research/openrouter-livekit-voice-boundary-map.html`, `tests/test_repo_workflow_ci.py`, `tests/test_openrouter_livekit_voice_boundary_browser.py`.
- Handoff: the stale Gemma-named live-voice guide and review surface were replaced with OpenRouter/LiveKit/Kokoro-named paths. MOCs, vault homes, Kanban/audit references, system-design mirrors, and browser tests now point at the current OpenRouter LiveKit voice boundary and current unblock guide.
- Verification: stale-name regressions and handoff CI-evidence regressions failed first and then passed; focused repo/browser/foundation checks passed with `43 passed, 9 skipped`; stable Python CI script passed with `240 passed, 30 skipped`; ruff passed on the changed Python test surfaces.
- Boundary: this is naming and pickup-surface alignment only. It does not create the LinkedIn token file, policy acknowledgement artifact, durable publication URL/id, rollback/postcondition artifact, accepted external publication proof, or PR.

## External Publication Operator Runbook - 2026-05-24

- Source: `docs/external-publication-proof-runbook.md`, generated packet reference `social_media_optimiser/output/provider-proof/190ae2f9-a74b-4a23-b39c-aaf2d636bd8e/operator-unblocker-checklist.md`, and `tests/test_repo_workflow_ci.py`.
- Handoff: the remaining `external-publication-proof` path now has a committed no-secret operator runbook with the strict readiness gate, packet-refresh commands, publication preflight validation, proof-record validation/recording, completion-status recheck, closure review, and blocker-state update order.
- Current blocked fields remain `LINKEDIN_ACCESS_TOKEN_FILE`, `LINKEDIN_POLICY_ACKNOWLEDGEMENT_ARTIFACT_ID`, `PUBLICATION_DURABLE_PLATFORM_ID_OR_URL`, and `PUBLICATION_ROLLBACK_OR_POSTCONDITION_ARTIFACT_ID`.
- Verification: repo workflow guards passed with `16 passed`; stable Python CI script passed with `242 passed, 30 skipped`; ruff passed on `tests/test_repo_workflow_ci.py`; touched docs/vault surfaces had no OpenRouter, GitHub, Hugging Face, or LinkedIn token-shaped values.
- Boundary: this improves pickup clarity only. It does not supply LinkedIn credentials, publish externally, record an accepted publication proof, or mark the goal complete.

## Frontend README Current Route Guard - 2026-05-24

- Source: `frontend/next-app/README.md` and `tests/test_repo_workflow_ci.py`.
- Handoff: the Next app validation README now describes browser single-flight voice proof as OpenRouter DeepSeek V4 Flash + LiveKit + Kokoro provenance and OpenRouter/LiveKit/Kokoro preflight readiness. A repo workflow regression rejects the stale current-route phrases `Gemma 4 E4B/Kokoro provenance` and `LiveKit/Gemma/Kokoro preflight readiness`.
- Verification: the focused regression failed first on the stale README wording, then passed after the README update.
- Boundary: this is documentation and guardrail alignment only. It does not supply the LinkedIn publication proof inputs or create a GitHub PR; connector PR creation still returns GitHub permission `403`.

## External Publication Evidence Artifact Guard - 2026-05-24

- Source: `src/all_about_llms/cli.py`, `tests/test_provider_proof_plan_cli.py`, `provider-proof-completion-status`, and `provider-proof-operator-input-readiness`.
- Guardrail: accepted `external-publication-proof` record validation and completion-status audit-note parsing now reject local, draft, or bare placeholder evidence references in `policy_acknowledgement_artifact_id` and `rollback_or_postcondition_artifact_id` with `publication_artifact_local_substitute`. This prevents hand-built proof records or hand-edited Obsidian audit notes from bypassing the stricter operator-input artifact checks.
- Verification: the two new regressions failed first with `valid_accepted_record` / `required_proofs_accepted`; after the fix the targeted regressions passed, the provider-proof CLI module passed with `203 passed, 30 skipped`, and Ruff passed on the touched Python files.
- Current gate state: `provider-backed-live-voice-proof` remains accepted. `external-publication-proof` remains blocked by `LINKEDIN_ACCESS_TOKEN_FILE`, `LINKEDIN_POLICY_ACKNOWLEDGEMENT_ARTIFACT_ID`, `PUBLICATION_DURABLE_PLATFORM_ID_OR_URL`, and `PUBLICATION_ROLLBACK_OR_POSTCONDITION_ARTIFACT_ID`.
- Boundary: this is fail-closed proof validation only. It does not create LinkedIn credentials, policy acknowledgement, durable publication destination, rollback/postcondition evidence, closure review, or GitHub PR permissions.

## Manual PR Handoff Refresh - 2026-05-24

- Source: `provider-proof-pr-handoff`, latest branch-head GitHub Actions live check, and pushed feature branch.
- Handoff: automated PR creation still returns GitHub permission `403 Resource not accessible by integration`; the current manual compare URL is `https://github.com/DeconvFFT/Content-creator-optimizer/compare/main...feature/livekit-voice-proof-capture?expand=1`.
- Current generated PR body preserves OpenRouter `deepseek/deepseek-v4-flash` + LiveKit + Kokoro as the active realtime path, `provider-backed-live-voice-proof` as accepted, and `external-publication-proof` as blocked by the four LinkedIn/publication operator inputs.
- Merge boundary: exact branch-head SHA and CI URL must be regenerated with `provider-proof-pr-handoff --ci-url <latest-branch-head-ci-url> --head-sha <current-branch-head-sha>` when the manual PR is created. Do not rely on committed vault text for those exact values; documentation-only follow-up commits can make pinned values stale. Auto-merge cannot be enabled from this session until a PR exists and GitHub integration permissions allow PR mutation.

## Manual PR Evidence Guard - 2026-05-24

- Source: `provider-proof-pr-handoff`, `tests/test_repo_workflow_ci.py`, `agent_progress_vault/04-cross-vault-links/vault-sync-notes.md`, and `system_design_vault/04-agent-studio-implications/agent-studio-objective-completion-audit.md`.
- Handoff: the manual PR body generator now requires `--ci-url` and `--head-sha`; running it without current branch-head evidence exits with argparse error code `2` and prints no PR body. The CI URL must be a same-repo GitHub Actions run URL, and the head SHA must be a 40-character hex commit id. All committed runnable handoff commands now include `--run-id`, `--ci-url`, and `--head-sha`.
- Boundary: this tightens merge evidence only. It does not create a GitHub PR, enable auto-merge, or satisfy the external LinkedIn publication proof.
- Fresh repo integration check: GitHub connector PR creation still returns `403 Resource not accessible by integration`; REST lookup still shows no open PR for the feature branch. Public branch metadata reports `main` is not protected, while the branch-protection endpoint requires authenticated repo-admin access. Continue using the manual PR handoff until GitHub app permissions and repository rulesets are updated.

## External Publication Durable Artifact Guard - 2026-05-24

- Source: `src/all_about_llms/cli.py`, `tests/test_provider_proof_plan_cli.py`, `docs/external-publication-proof-runbook.md`, and current operator-readiness proof gate output.
- Handoff: `LINKEDIN_POLICY_ACKNOWLEDGEMENT_ARTIFACT_ID` and `PUBLICATION_ROLLBACK_OR_POSTCONDITION_ARTIFACT_ID` now fail closed when filled with generic bare strings such as `policy-artifact-1` or `rollback-artifact-1`. The accepted shapes are external non-local URLs, LinkedIn URNs with an ID suffix, whitelisted namespaced durable artifact ids, or bare artifact ids carrying a UUID.
- Boundary: this advances external-publication proof integrity only. The gate is still blocked until the operator supplies the LinkedIn token file, policy acknowledgement artifact, durable LinkedIn destination, and rollback/postcondition artifact.

## Accepted-Proof Handoff Suppression - 2026-05-24

- Source: `src/all_about_llms/cli.py`, `tests/test_provider_proof_plan_cli.py`, and the current UUID proof gate for run `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`.
- Handoff: `current-proof-status.md` and `operator-unblocker-checklist.md` now suppress proof-level operator packet, schema, capture, readiness-command, required-evidence, and credential-snapshot retry details for proofs whose current state is `accepted_proof_record_available`.
- Current effect: `provider-backed-live-voice-proof` stays closed as accepted. The compact handoff should only route operators toward `external-publication-proof` until LinkedIn credential, policy acknowledgement artifact, durable destination, and rollback/postcondition evidence are supplied.
- Boundary: this is report/handoff accuracy only. It does not create accepted external publication evidence, run closure review, create a PR, or change blocker state.

## PR Review And CI Evidence Drift Guard - 2026-05-24

- Source: `.github/pull_request_template.md`, `.github/CODEOWNERS`, `docs/repo-workflow.md`, `agent_progress_vault/00-session-log/2026-05-23-ship-readiness-audit.md`, and `tests/test_repo_workflow_ci.py`.
- Handoff: manual PRs now explicitly require CODEOWNERS review approval, and durable vault handoff surfaces must not pin exact branch-head CI run ids as current evidence. Regenerate `provider-proof-pr-handoff` with fresh `--ci-url` and `--head-sha` when opening or updating the PR.
- Boundary: this advances review/merge handoff reliability only. It does not create the GitHub PR, enable repository auto-merge, or satisfy `external-publication-proof`.

## External Publication Operator Input Example - 2026-05-24

- Source: `docs/external-publication-operator-inputs.example.env`, `docs/external-publication-proof-runbook.md`, and `tests/test_repo_workflow_ci.py`.
- Handoff: the committed example env contains only placeholder values for `LINKEDIN_ACCESS_TOKEN_FILE`, `LINKEDIN_POLICY_ACKNOWLEDGEMENT_ARTIFACT_ID`, `PUBLICATION_DURABLE_PLATFORM_ID_OR_URL`, and `PUBLICATION_ROLLBACK_OR_POSTCONDITION_ARTIFACT_ID`. Operators should copy those keys into the ignored UUID `operator-inputs.template.env` and replace values locally.
- Boundary: do not commit filled operator input files, token values, generated proof output, screenshots, PDFs, images, media, local DBs, or local command logs. This does not close `external-publication-proof`; it only lowers pickup friction.
