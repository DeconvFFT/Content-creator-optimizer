---
type: memory-log
project: agent-studio
status: active
updated: 2026-05-23
owners:
  - interactive-note-taking-agent
---

# Memory Log

## 2026-05-23

- Refreshed the live OpenRouter/LiveKit provider-smoke ledger for proof run `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e` after clearing stale Gemma/Gamma command surfaces. `provider-smoke-ledger.live-openrouter.json` is valid JSON and reports `status=passed`, `execute_live_calls=true`, ledger artifact `1c7bc39d-bfb7-4cf3-a6b9-b77637841e83`, event `105490`, selected realtime session `5a9b52ec-ad80-473e-8e24-f54831881e81`, OpenRouter `deepseek/deepseek-v4-flash` first text delta at 621.226 ms, Kokoro `hexgrad/Kokoro-82M` first audio chunk at 4657.059 ms, and end-to-end first-audio latency at 5278.295 ms. The Kokoro local streamer now supplies `repo_id="hexgrad/Kokoro-82M"` to keep warning text out of JSON ledgers. `realtime-voice-timing-ledger.json` remains `needs_more_evidence` with 1/8 stages measured; missing evidence covers audio track bridge, speech start, turn correlation, first text/audio, and barge-in stop. Accepted live-voice proof and external publication proof remain open.
- Refreshed the UUID proof workspace after the Cursor/OpenRouter LiveKit recovery check. `voice-runtime-readiness.preflight.json` is overall ready for the OpenRouter text-turn path plus LiveKit/Kokoro/Rust/agent checks, and `provider-backed-live-voice-proof.preflight-validation.json` is `valid_preflight_artifacts` for `livekit-transport`, `livekit-agent-participant`, `voice-agent-backend-event-sink`, `openrouter-live-dialogue-reasoning`, `kokoro-tts`, and `rust-voice-edge`. Regenerated the no-secret operator-input, credential snapshot, proof plan, current blocker matrix, current status, and operator checklist packets; they now show `OPENROUTER_API_KEY_FILE`, `OPENROUTER_LIVEKIT_URL`, `LIVEKIT_API_KEY_FILE`, and `LIVEKIT_API_SECRET_FILE` configured, no live-voice operator-input blockers, and blocker state `provider_backed_live_voice_preflight_ready_record_capture_needed`. External publication proof still needs LinkedIn credential, policy acknowledgement, durable destination, and rollback/postcondition evidence.
- Refreshed the UUID provider-proof operator-input state after Cursor/OpenRouter coordination. The no-secret packets now use OpenRouter (`deepseek/deepseek-v4-flash`) over LiveKit with Kokoro spoken output as the active realtime default; Hugging Face/Gamma/Gemma/MLX setup is legacy/native-audio background and is not the next live-dialogue blocker. External publication still needs LinkedIn credential, policy acknowledgement, durable destination, and rollback/postcondition evidence. The Cursor agent vault was synced to use `openrouter-live-dialogue-reasoning` for OpenRouter text-turn dialogue and to keep `gemma-audio-reasoning` reserved only for a future native Gemma audio proof path if deliberately reintroduced.
- Synced Cursor live-voice coordination status from `agent_progress_vault/06-live-voice/status-snapshot-2026-05-23.md` and `agent_progress_vault/06-live-voice/openrouter-livekit-live-dialogue.md`: OpenRouter text reasoning over LiveKit/Kokoro is now the current local live-dialogue path, while local MLX/Gemma multimodal on `:8080`/`:8090` and `GEMMA4_MULTIMODAL_ENDPOINT_URL` are no longer required for that path. This does not by itself create accepted product live-voice proof.
- Added the OpenRouter text-turn live-dialogue readiness contract. Voice runtime readiness now emits a distinct ready `openrouter-live-dialogue-reasoning` check for OpenRouter chat-completions routing without `GEMMA4_MULTIMODAL_ENDPOINT_URL`, keeps HF router/text-primary endpoints from satisfying native `gemma-audio-reasoning` proof, and emits both OpenRouter and native Gemma checks when both configurations are present. The Gemma/OpenRouter stream preflight header construction is fixed, provider-proof validation rejects OpenRouter-only readiness as native Gemma audio proof, and accepts native Gemma readiness with an additional OpenRouter dialogue check. Focused readiness coverage passed with `7 passed`, provider-proof boundary coverage passed with `28 passed`, broader readiness/provider coverage passed with `28 passed`, foundation passed with `22 passed`, and `compileall` passed. Leibniz fix-back review reported no material Critical/Important findings. This is product-runtime readiness alignment only; accepted live-voice proof still needs refreshed product-owned captures and proof-record acceptance.
- Added project-memory and publish-readiness durable event redaction. Failing-first API regressions showed project memory recording and publish-readiness feedback/check events could persist raw project memory text, sensitive metadata, and unsupported publish-channel strings containing Bearer/HF-shaped values. `ProjectMemoryWorkflow` and `PublishReadinessWorkflow` now sanitize those durable event payloads with `safe_realtime_metadata`, and Bearer redaction now catches lower-case bearer variants from normalized platform names. Focused validation passed with `2 passed`.
- Added feedback and memory durable event redaction. Failing-first API regressions showed feedback routing, revision-loop feedback capture, and direct memory recording persisted raw human feedback text, author strings, target artifact titles, memory content, and sensitive metadata into `feedback_recorded`, `feedback_routed`, and `memory_recorded` event payloads. Those event writes now use `safe_realtime_metadata`, preserving private stored feedback and memory content while redacting Bearer/HF/Tavily-shaped strings and dropping sensitive metadata keys. Focused validation passed with `3 passed`; broader feedback/routing/A2A coverage passed with `43 passed`.
- Added conversation-turn durable event redaction. Failing-first API regressions showed `conversation_turn_recorded` persisted raw token-shaped transcript text and sensitive metadata from direct turn recording, content workflow capture, and conversation router turns; event writes now use `_safe_realtime_metadata` or `safe_realtime_metadata`, preserving private stored turns and API responses while redacting Bearer/HF/Tavily strings and dropping sensitive keys such as `authorization`, `clientSecret`, `hfToken`, and `sessionToken`. Focused validation passed with `3 passed`.

## 2026-05-21

- Added A2A skill-invocation event redaction. The regression first failed on raw token-shaped task type strings in `agent_skill_invocation_recorded`; `public_a2a_skill_invocation_event_payload` now preserves skill-use observability while redacting token-shaped task strings.
- Active context now carries the current A2A discovery paragraph. Red foundation coverage first showed the current-state handoff omitted `agent-message-public-projection-v1`, `listRunMessages`, and `agentInbox`; it now matches the Agent Card, `/api/a2a`, and A2A map public projection contract.
- Both HLDs now record the public A2A projection discovery contract. Red foundation coverage first showed the project HLD and system-design HLD omitted the well-known Agent Card, `/api/a2a`, `projection=public`, `agent-message-public-projection-v1`, and supported read endpoints; both HLDs now match runtime discovery and the A2A map.
- The well-known Agent Card now advertises the public projection contract. Red API coverage first showed `/.well-known/agent-card.json` omitted the task-inspection policy; `x-agentStudio.publicProjection` now matches `/api/a2a` for supported read endpoints, unsupported mutation endpoints, and `agent-message-public-projection-v1`.
- The interactive A2A map now renders the public projection contract. Red browser/export coverage first showed the Public boundary detail/export omitted `projection=public`, `agent-message-public-projection-v1`, and supported projection endpoints; `agent-studio-a2a-map.html` now carries those fields for browser-first review.
- `/api/a2a` now advertises the public projection contract. Red API coverage first showed the discovery document listed task routes but omitted which read endpoints accepted `projection=public`; it now exposes `publicProjection` with supported read endpoints, unsupported mutation endpoints, and `agent-message-public-projection-v1`.
- A2A agent inbox polling now supports the safe public projection. Red API coverage first showed `/api/a2a/agents/{agent_id}/inbox?projection=public` returned private payloads with no projection marker; the route now reuses the single-message public projection helper and redacts sensitive keys plus token-shaped strings across per-agent inbox responses.
- Run-level A2A message lists now support the safe public projection. Red API coverage first showed `/api/runs/{run_id}/agent-messages?projection=public` returned private payloads with no projection marker; the route now reuses the single-message public projection helper, redacts sensitive keys and token-shaped strings across lists, and `/api/a2a` advertises `listRunMessages`.
- System-design Objective Completion Audit routes now render required evidence after unblock. Red viewer parity coverage first showed route-first reviewers had to leave the per-proof route to inspect `required_evidence_after_unblock`; the generated system-design JSON and visible HTML detail now carry route-local live voice and publication post-unblock evidence gates. This is architecture-review handoff visibility only; accepted external proof remains blocked.
- System-design Objective Completion Audit diagnostics now render operator-input readiness status. Red viewer parity coverage first showed the diagnostics omitted the matrix-owned top-level `status`; the generated system-design JSON and visible HTML detail now show `status=blocked_by_operator_inputs` beside aggregate field contracts and field statuses. This is diagnostics visibility only; accepted external proof remains blocked.
- Added proof-record schema metadata to the voice and publication boundary maps. Red browser/export coverage first showed the specialized boundary maps carried proof-plan schemas but their exported/rendered `operator_proof_packet` blocks lacked packet-level `proof_record_schema` and `proof_record_required_fields`; both maps now expose record type, allowed outcomes, state field, and required fields beside the proof-capture handoff. This is boundary-map review parity only; no accepted external proof was created.
- Added proof-record schema metadata to proof-readiness operator packets. Red browser/export coverage first showed `agent-studio-proof-readiness.html` rendered full proof-plan schemas but its `operator_proof_packet` export/detail still lacked packet-level `proof_record_schema` and `proof_record_required_fields`; live voice and publication packet blocks now include record type, allowed outcomes, state field, and required fields. This is central proof-readiness handoff parity only; no accepted external proof was created.
- Mirrored proof-record schema metadata into the generated system-design viewer. Red viewer parity coverage first showed Objective Completion Audit routes carried required fields but not `artifact_type`, `allowed_outcomes`, or `state_field`; `agent-studio-system-design.json` and the embedded HTML viewer data now include `proof_record_schema`, and the detail pane renders record type/outcome/state metadata beside required fields. This is interactive review parity only; no accepted external proof was created.
- Added proof-record schema metadata to generated proof workspace READMEs. Red README coverage first showed initialized workspaces exposed required fields but not `artifact_type`, `allowed_outcomes`, or `state_field`; the UUID and non-UUID README handoffs now include `proof_record_schema` for live voice and publication before the required-field lists. This keeps workspace-local operator review self-contained; no accepted external proof was created.
- Rendered proof-record schema metadata in human proof packets. Red status/checklist coverage first showed `current-proof-status.md` and `operator-unblocker-checklist.md` rendered required fields but not `artifact_type`, `allowed_outcomes`, or `state_field`; both refreshed packets now include `proof_record_schema` for live voice and publication beside the existing required-field lists. This is human operator/review handoff metadata only; no accepted external proof was created.
- Promoted full proof-record schema metadata into current operator proof packets. Red matrix coverage first showed `operator_proof_packets` exposed required fields but not `artifact_type`, `allowed_outcomes`, or `state_field`; the refreshed UUID matrix now includes `proof_record_schema` for both proof records while keeping `proof_record_required_fields`. This is packet handoff metadata only; no accepted external proof was created.
- Added proof-record required fields to proof workspace READMEs. Red README regressions first showed initialized proof workspaces carried proof-capture commands but not the accepted proof-record schema; generated READMEs now list `proof_record_required_fields` for both proof records, including `voice_agent_process_start_artifact_id` for live voice and destination/rollback fields for publication. The non-UUID RUN README also preserves the product-run gate and avoids preflight paths under the placeholder workspace label. This keeps the command-source workspace self-contained; no accepted external proof was created.
- Rendered proof-record required fields in human proof packets. Red status/checklist regressions first showed the machine-readable matrix carried `proof_record_required_fields` but `current-proof-status.md` and `operator-unblocker-checklist.md` omitted them; both refreshed markdown packets now show the accepted-record fields, including `voice_agent_process_start_artifact_id`, beside the proof-capture handoff. This is operator/review handoff clarity only; external proof remains blocked.
- Promoted proof-record required fields into operator proof packets. A red current-blocker-matrix regression first showed `operator_proof_packets` carried proof-capture commands but not the accepted proof-record schema; the generator and refreshed UUID matrix now include `proof_record_required_fields` for both proofs, including `voice_agent_process_start_artifact_id` for live voice and destination/rollback fields for publication. This is A2A/review handoff metadata only; accepted external proof remains blocked.
- Mirrored proof-record required fields into the generated system-design viewer. Red viewer regressions first showed the Objective Completion Audit route could expose proof-capture commands without the accepted proof-record schema; `agent-studio-system-design.json` and the embedded HTML viewer now carry/render `proof_record_required_fields`, including `voice_agent_process_start_artifact_id` for provider-backed live voice. Focused viewer verification passed. This is interactive review parity only; accepted OpenRouter LiveKit live-voice proof and publication proof remain blocked.
- Required managed voice-agent process-start linkage in accepted live-voice proof records. A red validation regression first showed an accepted provider-backed live voice record could omit `voice_agent_process_start_artifact_id`; the live voice proof schema, template, validator, generated proof packets, and static proof surfaces now require the managed process-start artifact before an accepted record can pass. This is proof-record hardening only; no accepted OpenRouter LiveKit live voice proof was created, and external provider credentials remain blocked.
- Added evidence-producing execution commands to the post-unblock proof-capture chain. A red provider-proof regression first showed `proof_capture_commands_after_unblock` jumped from preflight validation to proof-record template/validation/recording without running the actual live voice or publication evidence builders; the CLI plan, UUID current blocker matrix, proof-readiness page, OpenRouter LiveKit voice boundary map, publication boundary map, and system-design viewer now insert a managed `voice-agent-process/start` capture, runtime-health ledger, live provider-smoke ledger, realtime voice timing ledger, and distribution-package commands before proof-record template generation. Leibniz then found the raw `run-voice-agent --dev` command was long-running, so the sequential handoff now uses the managed process-start API instead. Focused provider and browser parity verification passed; this remains handoff accuracy only and does not create accepted external proof.
- Labeled the external publication blocker status in current proof packets. A red current-status regression first showed `current-proof-status.md` rendered `linkedin-publication-readiness` as `unknown` even though `publish-readiness.preflight.json` is `blocked`; `_provider_proof_publication_blocker` now carries the publish-readiness status into `current-blocker-matrix.json`, and the regenerated status packet shows `linkedin-publication-readiness` as `blocked`. Focused packet freshness checks passed with `3 passed`.
- Labeled the live voice runtime blocker status in current proof packets. A red current-status regression first showed `current-proof-status.md` rendered `gemma-audio-reasoning` as `unknown` even though `voice-runtime-readiness.preflight.json` marks that check `blocked`; `_provider_proof_voice_blocker` now carries the check status into `current-blocker-matrix.json`, and the regenerated status packet shows `gemma-audio-reasoning` as `blocked`. Focused packet freshness checks passed with `3 passed`.
- Added the operator-input retry-chain split to the vault-only Kanban. A red foundation contract first showed the board linked `current-proof-status.md`, `current-blocker-matrix.json`, and `operator-unblocker-checklist.md` but did not distinguish the report-only refresh chain from the guarded strict-first chain; `Agent Studio Kanban.md` now names both paths for the live-voice and publication blockers. Focused foundation verification passed with `1 passed`.
- Captured the first actual UUID-backed provider-proof product run. Local Postgres/Docker and the FastAPI backend produced run `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`; the bootstrap-to-workspace command initialized its proof workspace, workspace validation is `valid_workspace`, live preflight captures now exist for product-run, provider readiness, voice runtime, and publish readiness, operator checkpoint `cc9c7ff8-99d8-4656-9ef1-7412694602bb` was recorded, and runtime health is now `ready` with 9/9 checks ready. A local source-backed publication-readiness fixture also now exists with an approved guardrail audit and source-ledger coverage, moving publication preflight from missing-content blockers to LinkedIn credential plus policy-review blockers. This historical entry was superseded on 2026-05-23: the current live-dialogue path is OpenRouter `deepseek/deepseek-v4-flash` over LiveKit with Kokoro output, with no live-voice operator-input blockers; completion still needs accepted OpenRouter LiveKit proof-record capture/recheck plus external publication proof.
- Added product-run bootstrap to the provider-proof handoff. `provider-proof-plan` and the proof-readiness/Gemma voice/publication static exports now expose `product_run_bootstrap` with no-secret `POST /api/runs` capture and `run_id` extraction commands, and blocked run-id attempt gates show those commands. The concrete `RUN-2026-05-20-NEXT` README now tells operators to bootstrap a durable product run UUID before reinitializing a real proof workspace. Verification covered provider-proof CLI `182 passed`, touched browser/export consistency `12 passed` outside the sandbox, `compileall`, `git diff --check`, and Leibniz review with no Critical/Important findings. This is operator-handoff hardening only; live voice proof and external publication proof remain blocked.
- Replaced inline bootstrap run-id extraction with `validate-provider-proof-product-run-bootstrap`. The validator reads the captured `POST /api/runs` response, validates the full `RunState` shape, rejects run-id-only JSON, and emits next commands for the validated UUID workspace without changing state or printing secrets. Verification covered provider-proof CLI `184 passed`, touched browser/export consistency `12 passed` outside the sandbox, foundation `21 passed`, `compileall`, `git diff --check`, and Leibniz review with no Critical/Important findings.
- Added `init-provider-proof-workspace-from-bootstrap`, a direct bootstrap-to-workspace bridge. It reuses the full `RunState` bootstrap validation, initializes the UUID proof workspace under the selected output root, writes no files for invalid bootstrap JSON, and keeps state-change false. The workspace initializer now rejects regular-file and broken-symlink ancestors with `workspace_path_unwritable` instead of raising. Static proof exports now point the blocked run-id attempt gate at this command. Verification covered provider-proof CLI `189 passed` and Leibniz review with no Critical/Important findings after the path-boundary fix.
- Closed the Leibniz UUID-gate follow-up for provider proof. Non-UUID run ids now fail closed through proof template generation, proof-record validation, preflight validation, workspace init/validation, completion status, and the checked-in `RUN-2026-05-20-NEXT` workspace. The old workspace README is blocked warning text, command fields render `<run-id>`, and review returned no Critical or Important actionable findings. Final verification: provider proof tests `178 passed`, foundation `21 passed`, browser/static proof surfaces `14 passed` outside the sandbox after the expected Chromium Mach-port sandbox failure, `compileall` passed, and stale-command plus hygiene scans passed.
- Hardened accepted provider-backed live voice proof records against missing or non-canonical runtime-check evidence. Preflight validation reports now carry canonical `validated_runtime_checks`, accepted record validation and completion-status audit-note parsing require the exact five required runtime checks in generated order, and compact audit notes preserve `preflight_validation_report_validated_runtime_checks`. Proof-plan/static voice surfaces now state the runtime-check linkage requirement. This remains no-secret proof-quality hardening only, not live provider-backed audio proof.
- Hardened accepted external publication proof records against destination/preflight channel drift. `validate-provider-proof-record` now rejects accepted publication records with `destination_channel_missing_from_preflight` unless the normalized destination channel appears in the linked preflight-validation report's `validated_publish_channels`; compact proof audit notes now preserve that channel list so completion status applies the same rule to hand-edited accepted notes. Regenerated the concrete `RUN-2026-05-20-NEXT` proof workspace templates, README, workspace validation, and proof-record validation reports to match the current proof plan. This remains no-secret proof-quality hardening only, not live publication proof.

## 2026-05-20

- Kept closure-review proof-audit reads separate from review writes. `record-provider-proof-closure-review` now uses only `--proof-audit-target` or default proof audit targets for completion-status rechecks, while review `--audit-target` paths only select closure-review write destinations.
- Added `record-provider-proof-closure-review`, a no-secret audit bridge for closure-review records. It validates filled reviews, appends compact redacted audit notes, refuses invalid reviews without writing, preserves separate proof-audit targets for completion-status rechecks, and keeps blocker-state mutation separate.
- Allowed meaningful rejected closure-review records. `validate-provider-proof-closure-review` now treats a rejected review with at least one rejected requirement as valid while keeping blocker-state updates disallowed.
- Added `validate-provider-proof-closure-review`, a no-secret closure-review validator. It rechecks accepted completion status, validates reviewer decision, timestamp, accepted proof/source linkage, review requirements, redaction proof, and token-shaped values without changing blocker state; the closure-review template now points to this validation command.
- Preserved audit-target overrides in provider proof closure-review commands. Accepted completion status now emits `provider-proof-closure-review-template` with the same `--audit-target` overrides that proved completion, including shell-safe quoting for paths with spaces.
- Added `provider-proof-closure-review-template`, a no-secret closure-review template command. It emits a reviewer-fillable record only after completion status reaches `required_proofs_accepted`, blocks when accepted proof is missing, links from accepted completion status, and keeps `state_change_allowed=false`.
- Added closure-review handoff to provider proof completion status. When all required proof records are accepted, the command now emits `next_action=prepare_blocker_closure_review`, no state-changing commands, and a no-secret `closure_review_packet` for reviewer-approved blocker closure while preserving `blocker_state_change_allowed_by_this_command=false`.
- Added `validate-provider-proof-record`, a no-secret provider proof-record validator. The command validates captured proof JSON against the selected proof schema, run id, outcome, required post-capture validation results, and token-shaped value scan without changing blocker state or echoing secret values.
- Added `proof_artifact_schema` to provider proof plans. CLI and static exports now define stable accepted/failed proof record schemas for live voice and publication proof, including artifact type, allowed outcomes, blocker state field, and required no-secret fields.
- Added `success_recording_requirements` to provider proof plans. CLI and static exports now require passing live voice/publication proof to record run/check/validation time, artifact ids, passed validation checks, blocker-state update timing, and follow-up closure or linkage before blocker state can change.
- Added `failure_recording_requirements` to provider proof plans. CLI and static exports now require failed live voice/publication attempts to record run/check time, failed step, redacted provider or platform context, blocked-state preservation, and follow-up task creation without exposing secrets.
- Added `post_capture_validation_checks` to provider proof plans. CLI and static exports now state the checks to run after live voice or publication artifacts are captured, including live-call/provider flags, same-run/session linkage, required timing/evidence, destination ID/URL linkage, and no-secret captured artifacts.
- Added `proof_linkage_requirements` to provider proof plans. CLI and static exports now require accepted live voice proof to link provider smoke, timing, LiveKit/session evidence, and runtime configuration to the same run/session; publication proof must link publish-readiness, approved artifact, destination proof, and rollback/postcondition evidence to the same platform ID or URL.
- Added `rejected_substitutes` to provider proof plans. CLI and static exports now state which credential-only, rehearsal, non-live, generic-policy, and local-draft evidence cannot unblock live voice or publication proof.
- Added `command_run_id` to provider proof plans. CLI and static exports now expose the accepted or substituted run id separately from raw operator input.
- Added unsafe-run-id guarding to provider proof plans. Unsupported run-id text now becomes `unsafe_run_id`, keeps runtime attempt blocked, and is not emitted into generated operator command/preflight strings.
- Fixed visible `<run-id>` rendering in static proof-plan details. Browser tests now catch dropped placeholders, and the proof-readiness plus boundary-map renderers escape list text before insertion.
- Added `preflight_checks` to provider proof plans. The CLI and browser exports now require provider/runtime readiness and publication approval/policy checks before live calls or destination proof.
- Added a concrete-run-id guard to provider proof plans. Configured credential names with the default `<run-id>` placeholder now return `blocked_by_run_id`, and static proof-plan exports carry the same run-id guard fields.
- Added `record_proof_in` targets to provider proof plans. The CLI and browser exports now point accepted live voice/publication proof back to the project audit, active context, and system-design audit mirror without exposing secret values.
- Updated the wiki index to expose the proof-plan packet handoff. `tests/test_foundation.py` first failed because `wiki/_index.md` linked the objective audit without naming proof_plan packets; it now points reviewers to the `provider-proof-plan` handoff.
- Updated the project and system-design MOCs to expose the proof-plan packet handoff. `tests/test_foundation.py` first failed because the MOCs linked the blocker surfaces without naming the proof_plan packets; both MOCs now point reviewers to the `provider-proof-plan` handoff.
- Updated the project vault home to expose proof-plan packet coverage. The browser/foundation regressions first failed because the home only linked proof readiness; it now tells reviewers that proof readiness plus the Gemma voice and publication boundary maps carry `proof_plan` proof-plan packets.
- Refreshed the generated Agent Studio system-design viewer with proof-plan packet state. The browser/foundation regressions first failed because the generated Objective Completion Audit detail omitted `proof_plan` and `provider-proof-plan`; the standalone JSON and embedded viewer data now include the current proof-plan boundary.
- Updated the system-design-vault browser home to expose proof-plan packet coverage. The browser/foundation regressions first failed because the home only mentioned credential snapshots; it now tells architecture reviewers that proof readiness and the boundary maps preserve both `secret_values_printed=false` credential snapshots and `proof_plan` proof-plan packets.
- Propagated provider proof-plan packets into the Gemma voice and publication boundary maps. `tests/test_blocker_snapshot_consistency.py` first failed because the voice boundary export lacked `proof_plan`; both boundary maps now render and export proof-plan packets matching proof readiness.
- Added browser/CLI parity for the provider proof plan. `tests/test_blocker_snapshot_consistency.py` first failed because proof-readiness exports lacked `proof_plan`; `agent-studio-proof-readiness.html` now renders and exports proof-plan fields matching `_provider_proof_plan_payload` for both remaining blockers.
- Added `provider-proof-plan`, a no-secret operator packet for the remaining live voice and external publication blockers. The CLI combines current credential snapshot state with exact proof commands plus publication policy/disclosure capture requirements, makes no live calls, reads no secret file contents, and keeps configured credentials in `runtime_configuration_present_unverified` until runtime evidence exists.

## 2026-05-19

- Added browser-level proof that `Start runner` blocks source refresh while the background runner start request is in flight. The persistent browser single-flight smoke now starts the delayed runner request, clicks `Run web research` before completion, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. The focused browser proof passed immediately because the existing runner-start gate already blocked overlap. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof that `Media plan` blocks source refresh while media planning is in flight. The persistent browser single-flight smoke now starts a delayed media-plan request, clicks `Run web research` before completion, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. The focused browser proof passed immediately because the existing media-plan gate already blocked overlap. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof that `Growth package` blocks source refresh while packaging is in flight. The persistent browser single-flight smoke now starts a delayed growth-package request, clicks `Run web research` before completion, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. The focused browser proof passed immediately because the existing growth-package gate already blocked overlap. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof that `Generate` blocks source refresh while the conversation turn request is in flight. The persistent browser single-flight smoke now separates conversation request start from delayed completion, clicks `Run web research` during generation, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. The red browser run timed out on the new conversation-start signal until fixture timing was split correctly. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof that `Publish check` blocks source refresh while publish readiness is in flight. The persistent browser single-flight smoke now starts a delayed publish-readiness request, clicks `Run web research` before completion, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. The focused browser proof passed immediately because the existing publish-readiness gate already blocked overlap. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof that `Send revision` blocks source refresh while the revision loop request is in flight. The persistent browser single-flight smoke now separates revision request start from delayed completion, clicks `Run web research` during the revision, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. The red browser run timed out on the new revision-start signal until fixture timing was split correctly. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof that `Start always-on` blocks source refresh while the always-on launch request is in flight. The persistent browser single-flight smoke now separates launch request start from delayed completion, clicks `Run web research` during the launch, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. The red browser run timed out on the new launch-start signal until fixture timing was split correctly. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof that `Run pulse` blocks source refresh while the manual heartbeat is in flight. The persistent browser single-flight smoke now separates heartbeat request start from delayed completion, clicks `Run web research` during the heartbeat, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. The red browser run timed out on the new heartbeat-start signal until fixture timing was split correctly. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof that `Check due work` blocks source refresh while the manual scheduler pass is in flight. The persistent browser single-flight smoke now separates scheduler request start from delayed completion, clicks `Run web research` during the scheduler pass, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. The red browser run timed out on the new scheduler-start signal until fixture timing was split correctly. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof for always-on pulse and scheduler controls. The persistent browser single-flight smoke now rapidly double-clicks `Run pulse` after `Start always-on` and proves exactly one worker-profile heartbeat request plus creator-facing `Specialist pulse finished.` copy, then rapidly double-clicks `Check due work` and proves exactly one run-scoped scheduler request with `run_id`, `execution_mode=autonomous_pass`, and `max_profiles=10`. The red browser run timed out on the new heartbeat signal until fixture heartbeat/scheduler endpoints were modeled. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof for long-running stop controls. The persistent browser single-flight smoke now rapidly double-clicks `Stop runner` after the background runner starts and proves exactly one stop request plus creator-facing stopped copy, then rapidly double-clicks the always-on rail `Stop` control and proves exactly one worker-profile stop request plus creator-facing stopped copy. The red browser run timed out on the new runner-stop signal until fixture stop endpoints were modeled. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), hygiene checks, and `Leibniz` review.
- Added browser-level proof that Activity retry blocks source refresh while recovery is in flight. The persistent browser single-flight smoke now marks the targeted retry worker-cycle start for `Queue and run`, clicks `Run web research` during that delayed retry cycle, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts during or shortly after the retry. `Leibniz` first found the proof was anchored to retry authorization instead of the worker-cycle start; the final `retry_cycle_started` version passed re-review. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), and hygiene checks.
- Added browser-level proof that planned-agent execution blocks source refresh. The persistent browser single-flight smoke now separates `Run next steps` planned worker-cycle start from delayed completion, clicks `Run web research` while the planned cycle is still in flight, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. `Leibniz` reported no actionable findings. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), and hygiene checks.
- Added browser-level proof that work-plan planning blocks source refresh. The persistent browser single-flight smoke now separates `Suggest next step` request start from delayed completion, clicks `Run web research` while planning is still in flight, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts. `Leibniz` found the first version only checked the worker cycle; the final proof checks both mutation points. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), and hygiene checks.
- Added browser-level proof that source refresh blocks production packaging. The persistent browser single-flight smoke now separates source-refresh worker-cycle request start from delayed completion, reruns `Run web research`, clicks `Growth package` while the worker cycle is still in flight, and proves no growth-package request is sent. The red browser run first timed out on the new source-refresh start signal until fixture timing was split correctly. No product code change was needed because the existing source-refresh mutation gate already blocked overlap. `Leibniz` reported no actionable findings. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), and hygiene checks.
- Added browser-level production packaging proof for the creator app. The persistent browser single-flight smoke now rapidly double-clicks `Growth package` and `Media plan`, proving one growth-package POST with platform/audience/campaign/outreach metadata and one media-production POST with image/audio/video planning options. The smoke also separates request-start from delayed fixture completion and proves `Growth package` blocks `Media plan`, while `Media plan` blocks `Publish check`, during the in-flight window. No product code change was needed because the existing production action gate already blocked duplicate submits and overlap. `Leibniz` reported no actionable findings. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), and hygiene checks.
- Added provider-backed Join voice room browser proof and hardened blocked/stale provider session cleanup. The browser single-flight test now rapidly double-clicks `Join voice room` after mocked Gemma/Kokoro/LiveKit readiness and proves one provider-backed realtime-session request with `dry_run=false` plus one cleanup status update after blocked LiveKit join. `RealtimeVoicePanel` now blocks Join while a retained live session exists, ends stale provider sessions after stale start or stale post-join completion, and clears local active session state only after durable cleanup succeeds. `Leibniz` verified the prior stale-cleanup and hidden-cleanup-failure findings are resolved. Validation passed browser single-flight, frontend race (`248 passed`), typecheck, lint, foundation (`21 passed`), and hygiene checks.
- Hardened live voice Interrupt and Stop controls against duplicate realtime control calls. The red source-contract test showed the product voice panel lacked synchronous guards before LiveKit agent control, backend realtime control, disconnect, and end-session work. `RealtimeVoicePanel` now uses interrupt and stop action tokens, blocks duplicate submits before async control calls, requires ready active-session ownership before publishing interrupt controls, invalidates pending interrupts when Stop starts, and clears both guards on Start, run change, and unmount. `Leibniz` found the same-tick Stop-then-Interrupt stale LiveKit publish edge; the follow-up guard closes it. Validation passed the red/green source contract, full frontend race suite (`247 passed`), Next typecheck, Next lint, touched-file hygiene, and final `Leibniz` review with no material findings.
- Added a browser-openable Agent Studio index to the separate `system_design_vault`. `tests/test_system_design_vault_home_browser.py` opens `system_design_vault/agent-studio-system-design-home.html` in Chromium and proves it exposes the objective-audit mirror, Gemma/realtime sources, production canon, source map, and blocker copy while staying a planning-memory surface. `system_design_vault/MOC.md` now links the index.
- Added browser-level proof that the Agent Studio vault home routes to generated knowledge viewers. `tests/test_vault_home_browser.py` opens `agent-studio-vault-home.html`, follows the new Memory OS and System Design Viewer links, and verifies the destination headings/content. The home page now exposes generated Memory OS and system-design viewers alongside HLD/LLD, work tracker, and retrieval research links.
- Added browser-level proof for the Retrieval Quality Research HTML surface. `tests/test_retrieval_research_browser.py` opens `02-research/retrieval-quality-research.html` in Chromium, filters to the Graph research area, and proves only graph cards stay visible with the correct count and summary copy. The research artifact now has a small segmented filter for Retrieval, Reranking, Graph, and Evaluation cards.
- Added browser-level proof for the generated Agent Studio Memory OS viewer interaction. `tests/test_memory_os_viewer_browser.py` opens the static HTML viewer in Chromium, filters to the Output Memory tier, and proves the visible tier is also promoted into the detail pane with the generated-output authority question. The viewer now promotes the first visible memory tier when filtering hides the previous selection. `Leibniz` found stale embedded Memory OS JSON; the HTML payload now matches the standalone JSON, and a foundation regression asserts projection equality plus linked-note/provider-free rehearsal coverage.
- Added browser-level proof for the Agent Studio work tracker interaction. `tests/test_work_tracker_browser.py` opens the static tracker in Chromium, advances `Functional Content Studio Cockpit` from `in_progress` to `blocked`, and proves the exported tracker packet preserves the artifact id, updated item status, and requested planning action.
- Added browser-level proof for the Agent Studio HLD/LLD design-review surface. `tests/test_hld_lld_viewer_browser.py` opens the static design viewer in Chromium, saves a high-priority Retrieval comment from `Codex Browser Proof`, verifies the rendered comment references the Retrieval Intelligence Agent, and proves the exported comment packet preserves the artifact id, Obsidian source, target, priority, author, and routing action.
- Added browser-level proof for the generated Agent Studio system-design viewer interaction. `tests/test_system_design_viewer_browser.py` opens the static HTML viewer in Chromium, filters for `external publication proof`, and proves the visible `Objective Completion Audit` runtime component also becomes the selected detail pane with the `Do not call the goal complete` blocker copy. The viewer now promotes the first visible runtime component when filtering hides the previous selection.
- Added the objective completion audit to the generated Agent Studio system-design viewer projection. The viewer now includes an `Objective Completion Audit` component and current-focus reminder that provider-backed live voice proof and external publication proof stay blocked until direct runtime evidence exists. The viewer regression now requires these audit terms in the generated projection.
- Closed the `Leibniz` canonical-source cleanup for the objective-audit viewer projection. The generated system-design viewer now lists `01-work-tracking/Agent Studio Objective Completion Audit.md` in `canonical_sources`, and the foundation regression asserts that source attribution is present.
- Added an objective completion audit for the full active Agent Studio goal. The project-vault audit maps each objective requirement to current evidence, status, blockers, and next executable slices, while the system-design-vault mirror preserves source attribution and design implications. The audit keeps the goal active until provider-backed live voice proof and external publication proof have direct runtime evidence.
- Closed the `Leibniz` review cleanup for the generated viewer refresh. Active context no longer says the creator rail shows `Local scheduler` proof or scheduler controls; it now uses `Background runner` proof and Start/Stop background-runner controls. The viewer freshness regression now asserts the standalone JSON projection equals the HTML-embedded projection, not only matching a few strings.
- Refreshed the generated Agent Studio system-design viewer projection after the LLD background-runner contract change. `output/viewers/agent-studio-system-design.json` and the embedded `system-design-data` JSON inside the HTML viewer now carry `updated: 2026-05-19` plus an `Always-On Activity Rail` component that exposes `Always-on studio`, `Specialist pulse`, and `Background runner` as creator-facing language. Added a foundation regression so stale viewer projections fail.
- Synchronized the Activity rail background-runner language contract across both vault LLDs. The social-media optimiser LLD and system-design LLD now state that creator-visible process control shows `Background runner`, `Always-on studio`, and `Specialist pulse` language while internal scheduler/autopilot identifiers remain implementation details. Added a foundation contract so the old `creator Activity rail can show Local scheduler status` design drift fails.
- Hardened source refresh as a first-class same-run mutation gate. `sourceRefreshActionGateRef` now owns `Run web research`, `runMutationGate` includes `sourceRefresh`, and guarded same-run mutation handlers call `blockSameRunMutationIfBusy(...)` before mutating APIs while source refresh is in flight. Browser validation rapidly double-clicked `Run web research` and proved one message plus one worker cycle while showing blocked status; focused race/source-contract tests, typecheck, lint, browser single-flight smoke, whitespace checks, and `Leibniz` review passed with no actionable findings.
- Added browser-level single-flight interaction coverage for the actual creator app. The new `npm run test:browser-single-flight` path drives Chromium against the real Next app, mocks FastAPI responses at the browser boundary, delays the guarded API calls, rapidly double-clicks Generate, Suggest next step, Publish check, and Send revision, and asserts each path creates exactly one backend action.
- Added a compact Kanban handoff to `wiki/ops/active-codex-context.md`. Future continuation turns can read the compact context to locate the source board, latest completed board item, next board item, current provider/publishing blockers, and the `Leibniz` review trigger without scanning the full sprint log. The foundation vault contract now asserts the handoff exists.
- Added opt-in non-live publish-channel readiness smoke. Publish readiness now distinguishes missing channel credentials from policy-review acknowledgement for Instagram, LinkedIn, X, and Substack without posting externally, normalizes known platform aliases, blocks unsupported publish channels, and the creator app/cockpit show every non-secret channel check with env-name and next-action guidance. Follow-up review fixed direct selected `post` and `reel_script` artifacts so their platform metadata is checked, and added no-secret-echo regressions for readiness results and `publish_readiness_checked` events. `Leibniz` found no actionable findings after the direct-artifact and secret-surface fixes.
- Aligned the guardrails-review skill with the feedback outcome ledger. Guardrail agents now have explicit skill instructions to inspect `feedback_resolution_ledger`, distinguish accepted/revised/held/rejected feedback outcomes, surface failed linked task ids, and keep failed or canceled feedback-linked work held until retry or review. `Leibniz` found no actionable findings.
- Added feedback-loop outcome analytics to the feedback resolution ledger. Each feedback entry now carries an `accepted`, `revised`, `held`, or `rejected` outcome, ledger totals expose those buckets, and artifact/event payloads preserve the same summary for autonomous loops and cockpit review. Resolved feedback with pending, blocked, failed, or canceled linked tasks is still held, which keeps long-running worker loops honest about unfinished or failed specialist work.
- Hardened the `Plan next` action in the creator app against duplicate planning requests and cross-action overlap with `Run plan`. `handleBuildWorkPlan` now uses a synchronous `workPlanActionGateRef`, requires captured run version plus action token before refreshing or committing the work plan, blocks while `agentCycleInFlightRef` is active, and invalidates the gate on `New` or voice-run replacement. `handleRunWorkPlan` now rejects while `Plan next` is in flight before it acquires the agent-cycle lock. Added failing-first work-plan source contracts for the `Plan next` gate and `Plan next`/`Run plan` cross-action exclusion. `Leibniz` found the cross-race before this fix and then verified the fix with no actionable findings. Validation passed the focused frontend race command (`228 passed`), Next typecheck, Next lint, `git diff --check`, and touched-file trailing-whitespace scan.
- Extended the `Plan next` planning gate across same-run mutation entry points that can affect the same run while a work plan is being built. Manual continuation, live voice follow-up continuation, source refresh, Activity retry, composer submit, feedback actions, production actions, Autopilot profile controls, and local scheduler controls now reject while `workPlanActionGateRef` is in flight, so a stale plan cannot commit after new content/agent/source/retry/feedback/production/scheduler mutations. `Plan next` now uses `isRunMutationInFlight(runId)` to reject while those mutation gates or scheduler/heartbeat refs are active. `Leibniz` found the compose and feedback/production gaps before this central fix, then verified the broader fix with no actionable findings. Added failing-first source contracts covering agent-cycle, composer, and same-run mutation gate exclusion. Validation passed the focused frontend race command (`231 passed`), Next typecheck, Next lint, `git diff --check`, and touched-file trailing-whitespace scan.
- Centralized the `Plan next`/same-run mutation exclusion logic in `lib/state/runMutationGate.ts` so the safety rule is behavior-tested instead of only page-source-shaped. The helper covers work-plan planning, composer, production, feedback, Autopilot, local scheduler, heartbeat, and run-scoped scheduler in-flight state, while the page keeps thin wrappers around current refs. Added failing-first helper tests for idle, feedback/production/heartbeat, and run-scoped scheduler behavior, and widened the frontend test build to include `lib/state/**/*.ts`. Validation passed the focused frontend race command (`235 passed`), the work-plan source contract command (`235 passed`), Next typecheck, Next lint, `git diff --check`, and touched-file trailing-whitespace scan. `Leibniz` found no actionable findings.
- Hardened local background-runner process controls in the creator app against duplicate submits. Start runner and Stop runner now share a synchronous `localSchedulerActionGateRef`, require captured run version plus action token before committing status/refresh state, and invalidate the gate on `New` or voice-run replacement while preserving the existing worker-scheduler status-token stale-poll guard. Added a failing-first AgentRetry/page source contract proving both process handlers use the gate. Validation passed the focused frontend race command (`226 passed`), Next typecheck, Next lint, `git diff --check`, and touched-file trailing-whitespace scan. `Leibniz` found no actionable findings.
- Added a vault-only Agent Studio Kanban note as Obsidian-native planning state. The board tracks backlog, ready, in-progress, blocked, done, and review-watch items while explicitly preserving the product boundary: the creator app must not render this board. Foundation coverage now checks the board, MOC/wiki links, and no-Kanban drift in product/planning HTML plus core architecture notes.
- Hardened always-on launch/stop actions in the creator app against duplicate submits. Start always-on and Stop now share a synchronous `autopilotActionGateRef`, require both captured run version and action token before refreshing state, and invalidate the gate on `New` or voice-run replacement. Added a failing-first Activity/page source contract proving both handlers use the gate. Validation passed the focused frontend race command (`225 passed`), Next typecheck, Next lint, `git diff --check`, and touched-file trailing-whitespace scan. `Leibniz` found no actionable findings; heartbeat/scheduler guards remain separate.
- Hardened standalone interactive HTML run notes against secret leakage. `InteractiveRunNoteWorkflow` now sanitizes the full embedded `run-note-data` JSON state before writing shareable HTML, reusing the recursive realtime metadata redaction boundary for event payloads, artifact content, artifact provenance, and nested token-shaped strings. Note titles from request input or run goals now pass through the same token-string redactor before HTML rendering, `html_note` artifact persistence, and `artifact_recorded` event payloads. Added a failing-first API regression covering sensitive keys and Bearer/HF/Tavily-shaped strings in event payloads, artifact content/provenance, and note titles. Validation passed the focused redaction regression (`1 passed`), neighboring interactive/Obsidian note subset (`3 passed`), `compileall`, `git diff --check`, and touched-file trailing-whitespace scan. `Leibniz` found the title leak and then verified the fix with no actionable findings.
- Hardened feedback-loop actions in the creator app against duplicate submits. Revision feedback and feedback-gate resolve now share a synchronous `feedbackActionGateRef` before async API work, require both captured run version and action token before refreshing/committing state, and invalidate the gate on `New` or voice-run replacement. Added a failing-first DraftBoard/page source contract proving both handlers use the feedback gate. Validation passed the focused frontend race command (`224 passed`), Next typecheck, Next lint, `git diff --check`, and touched-file trailing-whitespace scan. `Leibniz` found no actionable findings; residual risk is that the guard coverage is source-contract based rather than a full interaction simulation.
- Hardened production actions in the creator app against rapid duplicate submits. Growth package, Media plan, and Publish check now share a synchronous `productionActionGateRef` before async API work, require both run version and action token before committing post-refresh state, and invalidate the gate on `New` or voice-run replacement. Added a failing-first production source contract proving all three handlers use the single-flight gate. Validation passed the focused frontend race command (`223 passed`), Next typecheck, Next lint, `git diff --check`, and touched-file trailing-whitespace scan. `Leibniz` found no actionable findings; residual risk is that the guard coverage is source-contract based rather than a full browser interaction simulation.
- Hardened stale busy-state cleanup in the creator app. `clearBusyForOwnedRunSnapshot` now requires both run id and run version before clearing a busy label, and stale async branches in the main Next app pass their captured run version into cleanup. This closes the no-run overlap where an old in-flight create/compose path could call `clearBusyForRun(undefined)` after `New` and clear a newer no-run action's busy state. `Leibniz` found a same-version no-run double-submit race in the composer before React propagated `busy`; fixed with a synchronous compose submit gate that uses an in-flight flag plus ownership token, invalidated by `New` and voice-run creation. Added regressions for stale no-run cleanup preserving newer no-run work, version-scoped cleanup clearing only the current owner, single-flight same-version no-run compose submits, out-of-order no-run compose completions committing only the latest owner, and page wiring of the compose guard. Validation passed the focused frontend race command (`222 passed`), Next typecheck, Next lint, `git diff --check`, and touched-file trailing-whitespace scan. `Leibniz` fix verification found no actionable findings; residual risk is the small source-shape page-wiring assertion, while helper-level tests cover the race.
- Replaced the real voice panel's positional runtime-readiness boolean tuple with named `VoiceReadinessRefreshOptions` exported from `lib/voice/runtimeReadiness`. Full Runtime preflight now uses one constant that explicitly includes LiveKit, agent, edge, hosted Kokoro, and hosted Gemma preflight, LiveKit process refresh uses a transport-only option, and passive refreshes are empty calls. Added a regression that directly asserts the exported option values and rejects future `refreshVoiceReadiness(true, ...)` / `false` call sites. Validation passed the failing-first frontend race command (`217 passed`), follow-up direct-option race command (`217 passed`), Next typecheck, Next lint, `git diff --check`, and touched-file trailing-whitespace scan. `Leibniz` found no actionable findings; residual risk is minor because the remaining source guard intentionally depends on the protected helper name `refreshVoiceReadiness`.
- Added explicit Gemma 4 E4B audio endpoint preflight to the real voice runtime readiness path. Runtime preflight now sends a minimal raw PCM audio probe through the production Gemma reasoner, checks for a streamed text delta, blocks on hosted provider failures, keeps passive readiness non-networked, redacts token-shaped exception strings before API/proof surfaces, and avoids leaking `HF_TOKEN`. The Next voice setup/preflight/start paths now pass `preflight_gemma=true`, and shared frontend readiness logic prevents passive Gemma endpoint configuration from counting as completed audio preflight. `Leibniz` found no actionable findings. Validation passed Gemma/readiness tests, full Python regression (`501 passed, 11 explicit infra skips`), frontend race suite (`216 passed`), Next typecheck/lint/build, `compileall`, touched-file whitespace scan, and redacted token-shape scan.
- Added hosted Kokoro TTS preflight to the real voice runtime readiness path and fixed the two `Leibniz` review blockers. Runtime preflight now calls the configured hosted Kokoro endpoint via the production streamer, checks for audio bytes, blocks on hosted provider failures, keeps passive readiness non-networked, redacts hosted Kokoro exception strings before API/proof surfaces, and avoids leaking `HF_TOKEN`. The Next voice setup/preflight/start paths now pass `preflight_tts=true`, and shared frontend readiness logic prevents passive hosted Kokoro configuration from counting as a completed synthesis preflight. Final `Leibniz` verification found no actionable findings. Validation passed focused hosted Kokoro/readiness tests, full Python regression (`499 passed, 11 explicit infra skips`), frontend race suite (`213 passed`), Next typecheck/lint/build, `compileall`, touched-file whitespace scan, and redacted token-shape scan.
- Added a failed-preflight recovery action for supervised local LiveKit. When the `livekit-transport` runtime check has no missing envs, preflight was performed, `configured_for_local_dev=true`, and the local LiveKit process is already running/starting/started, the setup resolver now maps the primary blocker to `Restart local LiveKit transport` / `restart_livekit` and calls the local process start endpoint with `forceRestart: true` instead of repeatedly running preflight. `Leibniz` caught overly broad remote LiveKit routing; the final fix keeps external/non-supervised LiveKit, remote/non-local LiveKit, and stopped supervised LiveKit on their normal paths. Final `Leibniz` verification found no actionable findings. Validation passed full frontend race suite (`209 passed`), Next typecheck/lint/build, touched-file whitespace scan, and `git diff --check`.
- Wired the local LiveKit dev bootstrap into the live voice setup resolver. The runtime checklist now maps a `livekit-transport` blocker missing `GEMMA4_REALTIME_LIVEKIT_URL`, `LIVEKIT_API_KEY`, and `LIVEKIT_API_SECRET` to `Use local LiveKit dev defaults` / `configure_local_livekit_dev` instead of only rerunning preflight, and the provider release gate gives the same action for a fully missing selected `gemma4-realtime` transport. `Resolve next` calls the existing safe local bootstrap endpoint, refreshes provider/runtime readiness, and records durable setup proof with env/file-env names only. `Leibniz` found that the resolver bypassed the direct-button busy guard and that proof metadata needed behavioral coverage; fixed with helper-backed regressions for action disablement and proof metadata. Final `Leibniz` verification found no actionable findings. Validation passed full frontend race suite (`206 passed`), Next typecheck/lint/build, touched-file whitespace scan, and `git diff --check`.
- Added explicit local LiveKit dev bootstrap to the real voice app. `POST /api/local-livekit-dev-config` writes the local dev LiveKit URL and LiveKit key/secret into ignored local config/secret files for local/dev/test only without echoing credential values, rejects production/pathless/non-dev existing config, checks current files inside the shared local setup lock, serializes with `/api/local-secret-files`, rolls back partial writes on failure, and removes dev credential values from readiness hints. The Next voice panel now shows `Use local LiveKit dev defaults` only when all LiveKit config values are missing and disables per-field saves during bootstrap. `Leibniz` found stale-file/concurrent-overwrite and partial-write risks plus a shared-lock gap; all were fixed with regressions and final review found no actionable issues. Validation passed the local LiveKit/config/secret readiness subset (`45 passed`), frontend race suite (`202 passed`), full backend regression (`496 passed, 11 explicit infra skips`), live Postgres/pgvector (`10 passed`), Next typecheck/lint/build, `compileall`, no-SQLite boundary test, high-entropy token scan, screenshot-location scan, touched-file whitespace scan, and `git diff --check`. Remaining voice blockers are real Gemma multimodal endpoint validity, Kokoro availability, LiveKit process/agent startup, captured microphone audio/timing evidence, and provider-backed live smoke.
- Added A2A discovery compatibility for the local multi-agent studio. `/.well-known/agent-card.json` now returns an A2A 0.3-shaped, secret-free Agent Card for the Agent Studio orchestrator with `HTTP+JSON` transport at `/api/a2a`, nine project skills, and `x-agentStudio` metadata for the 38 internal specialists while explicitly marking that this is not a full public A2A protocol server. `/api/a2a` links the existing durable HTTP routes for local task/message lifecycle, worker execution, cards, skills, retry, dependency repair, and collaboration graphs. Validation passed targeted A2A/foundation tests, `compileall`, lock check, full regression (`428 passed, 11 expected infra skips`), no-SQLite scan, generic secret-shaped token scan, and whitespace check.
- Added foundation-gate coverage for project skill source contracts. Foundation audit now includes `agent-skill-source-contracts`, reusing the skill-usage ledger parser so advertised skill cards fail release proof if their `SKILL.md` file, frontmatter id/description, usable `agents/openai.yaml` interface manifest, or agent mappings drift. Focused regressions cover the real passing project, an empty project-root failure, and a malformed manifest failure; validation passed targeted foundation/skill tests, `compileall`, full Python regression (`426 passed, 11 expected infra skips`), no-SQLite adapter scan, generic secret-shaped token scan, and whitespace check. `Leibniz` found the manifest-existence-only gap in the first pass and verified the interface-field fix with no actionable findings.
- Hardened long-running Gemma/Kokoro LiveKit session memory. The Python participant now applies the realtime context-pruning policy to the maintained `LiveKitVoiceAgentSessionState.history` after successful, failed, or canceled turns, replacing old raw-audio references with transcript/summary turns and recording `voice_session_history_pruned` proof. `RealtimeContextPruner` now reports `raw_audio_turns_after` from the final retained context-window slice, so durable proof matches actual retained history. Validation passed focused session-history regressions (`2 passed`), voice-agent/voice-edge suite (`76 passed, 1 expected loopback skip`), `compileall`, full Python regression (`424 passed, 11 expected infra skips`), no-SQLite scan, exact supplied-secret scan, screenshot-location scan excluding dependency folders, and `git diff --check`. `Leibniz` found and verified the retained-count proof fix.
- Hardened initial source grounding for first-run content creation. Web-search provider failures in `ContentStudioWorkflow` now persist redacted `provider_fallback` plus durable `web_research_blocked` proof, search seeds remain pending placeholders, and seed-backed claims stay `needs_review` instead of becoming supported evidence. The slice also directly covers Gemma provider fallback redaction before durable events or artifacts. Final validation passed focused regressions, focused content/source/guardrail suite, targeted source workflow checks, `compileall`, full Python regression (`422 passed, 11 expected infra skips`), no-SQLite scan, exact supplied-secret scan, screenshot-location scan excluding dependency folders, touched-file whitespace scan, and `git diff --check`. `Leibniz` closed its Gemma fallback coverage finding with no directly introduced regression.
- Tightened the real Gemma/Kokoro voice endpoint boundary. `GEMMA4_PRIMARY_ENDPOINT_URL` and HF router chat-completions now remain text/chat expert routes only; runtime readiness, provider smoke, and the LiveKit participant require a valid HTTP(S) `GEMMA4_MULTIMODAL_ENDPOINT_URL` for Gemma 4 E4B native-audio proof. Added primary-only and malformed multimodal endpoint regressions for readiness and provider smoke plus a LiveKit participant contract guard. Validation passed focused regressions, related voice/provider-smoke suite, `compileall`, full Python regression (`420 passed, 11 expected infra skips`), no-SQLite scan, exact supplied-secret scan, screenshot-location scan excluding dependency folders, forbidden README wording scan, and `git diff --check`. `Leibniz` verified the malformed-url and README fixes closed its findings with no directly introduced regression.
- Added product-facing local worker-scheduler process supervision for always-on Autopilot. FastAPI now exposes `/api/worker-scheduler-process` status/start/stop for a run-scoped `run-worker-scheduler --watch` process, the supervisor redacts provider/database secrets and reports run/mode/cadence/log tail, and the actual Next Activity rail shows compact `Local scheduler` state with Start/Stop controls gated on the newest active Autopilot profile. After `Leibniz` review, polling uses a control token so stale status responses cannot overwrite fresh Start/Stop results, and the process contract accepts the same `0.25s` cadence floor as worker profiles. Validation passed focused backend tests, `compileall`, Next typecheck/lint/build, frontend race suite, full backend regression, escalated live Postgres, escalated Rust loopback smoke, hygiene scans, and desktop/mobile browser smoke at `screenshots/next-product-app-worker-scheduler-process*.png`.
- Made live voice follow-up worker continuation message-lineage targeted. The product app now sends the exact `followup_task_message_id` plus `continue_message_lineage=true`, and the backend processes only that target and accepted child tasks created from processed lineage messages. Added stale older-message regressions for normal voice-to-content and provider-failure recovery, bounded external `message_ids` requests to `100` after `Leibniz` review, and verified focused tests, frontend suite, full backend regression, live Postgres, hygiene scans, browser console checks, and final `Leibniz` closure.
- Made retrieval intelligence and knowledge graph curation concrete in the autonomous worker loop. `retrieval-intelligence-agent` now builds retrieval-quality ledgers with deterministic reranker fallback and redacted provider-fallback proof, and opens bounded idempotent Web Research follow-ups when retrieval quality is not ready. `knowledge-graph-curator-agent` now records source/claim/artifact graph coverage and traversal proof plus `knowledge_graph_curated` events, and graph edges only use `supported_by` for covered claims while non-supported source-linked claims remain `candidate_evidence`. Architecture review now exposes `concrete_worker_executor_agent_ids` and `generic_fallback_only_agent_ids`, so default worker-list membership is no longer treated as sufficient coverage. Validation passed focused retrieval/graph/source-refresh fallback tests, the relevant foundation/retrieval/project-memory/architecture suite (`29 passed`), compileall, full backend regression (`395 passed, 11 expected infra skips`), no-SQLite scan, generic secret scan, screenshot-location scan, whitespace check, and `Leibniz` fix verification with no new actionable issues.
- Corrected the implementation review process: `Leibniz` must receive one complete whole-slice packet after focused tests and implementation are ready, and the first pass must explicitly inspect stale-state/race conditions, event ordering, cancellation and cleanup paths, provider and secret boundaries, test adequacy, bloat, and out-of-box behavior. Follow-up review is reserved for verifying fixes to real findings, not for compensating for an underspecified first request.

## 2026-05-18

- Hardened the HF router versus native-audio boundary for the actual Gemma/Kokoro voice path. Voice runtime readiness now keeps Gemma 4 E4B audio reasoning blocked when only the Hugging Face router chat-completions route is configured, records non-secret metadata that the router is text/chat only, and points to a dedicated `GEMMA4_MULTIMODAL_ENDPOINT_URL` for native audio. Provider smoke now emits the same router-specific blocker for Gemma/Kokoro first-audio proof, while the product voice setup checklist surfaces the concrete failed runtime check instead of only a generic runtime-blocked summary. Validation passed focused backend regressions, provider-smoke regression, frontend race suite (`145 passed`), default Python regression (`381 passed, 11 expected infra skips`), compileall, Next typecheck/lint/build, no-SQLite adapter scan, generic secret scan, screenshot-location scan, whitespace check, `git diff --check`, and final `Leibniz` no-actionable-findings review after closing its multimodal-router, metadata, and test-isolation findings.
- Proved the actual Next.js creator app Autopilot lifecycle against live FastAPI/Postgres on validation run `191f525c-4861-4144-afe5-b0a780c4b26f`. The product app created a durable content workflow, streamed live run events, started Autopilot, recorded idle scheduler and blocked heartbeat proof while the run waited for human feedback, then stopped the worker profile and persisted `worker_profile_stopped` event `#69730`. Browser evidence is `screenshots/autopilot-live-postgres-proof.png`. Also documented the local validation boundary: use `NEXT_API_PROXY_TARGET` for same-origin Next proxying; reserve `NEXT_PUBLIC_API_BASE_URL` for intentional direct browser API origins with CORS.
- Hardened live voice transcript promotion for the actual Gemma/Kokoro dialogue path. A pending `voice_user_turn_committed` user turn can now be promoted to a final transcript later without duplicating conversation turns; Postgres performs the promotion under the voice-turn advisory lock and returns `None` on duplicate final replays, preventing duplicate `voice_user_transcript_promoted` events. If the assistant follow-up task already exists, the backend patches only that task payload to `final_user_transcript` and `source_transcript_excerpt`, preserving claim/completion state. The live transcript reducer now remembers recent turn ids so stale prior-turn commits and speech-starts cannot repaint a newer active turn while valid new turns still work. Validation passed focused backend/frontend/live-Postgres regressions, `npm run test:race` (`144 passed`), Next typecheck/lint/build, default Python regression (`380 passed, 11 expected infra skips`), host-context live regression (`391 passed`), compileall, no-SQLite scan, generic secret scan, screenshot-location scan, whitespace check, `git diff --check`, and final `Leibniz` review with no actionable findings.
- Added a fresh captured-audio boundary for provider-backed Gemma/Kokoro live smoke. `ProviderSmokeRunRequest` now carries `max_voice_audio_artifact_age_seconds` with a 120-second default; session-bound live smoke rejects stale same-session microphone PCM as `session_audio_artifact_stale`, returns blocked without calling Gemma or Kokoro, and exposes stale count, latest stale age, turn id, and max-age evidence. The product voice proof UI now explains stale audio separately and directs the creator to speak again before rerunning live smoke. Validation passed focused stale-audio backend/frontend regressions, full frontend race tests (`143 passed`), Next typecheck/lint/build, default Python regression (`377 passed, 10 expected infra skips`), host-context live regression (`387 passed`), compileall, no-SQLite adapter scan, generic secret scan, screenshot-location scan, whitespace check, `git diff --check`, and one deep whole-slice `Leibniz` review with no actionable findings. Residual risk: freshness follows durable `ConversationTurn.created_at`, so manually seeded bad timestamps could misstate freshness.
- Captured the implementation workflow rule in Obsidian: for meaningful code slices, write focused tests first, implement one cohesive product/backend feature, run the relevant validation set once, and send the full slice to `Leibniz` for review. Avoid micro-patches; only rerun `Leibniz` after a real review finding is fixed.
- Made the live voice proof path executable instead of merely informational. The product voice panel now chooses the first concrete proof action and routes it through existing handlers: refresh provider readiness, run runtime preflight, join the LiveKit room, probe agent presence, run live Runtime smoke, or build the Timing ledger. The presence-probe action is run/session guarded after `Leibniz` found a stale repaint risk. Validation passed `npm run test:race` (`142 passed`), Next typecheck/lint/build, default Python regression (`376 passed, 10 expected infra skips`), host-context live regression (`386 passed`), compileall, no-SQLite scan, generic secret scan, screenshot-location scan, whitespace check, `git diff --check`, browser proof at `screenshots/live-voice-proof-path-action.png`, and final `Leibniz` no-actionable-findings review.
- Added a product-visible live voice proof path to the actual Gemma/Kokoro voice panel. The UI now separates provider-route readiness from true voice-to-voice proof and only marks the path ready when the provider release gate is ready, runtime smoke used captured microphone audio rather than synthetic fallback, Gemma/Kokoro streaming proof passed, and the realtime timing ledger is ready. Browser proof is `screenshots/live-voice-proof-path.png`. Validation passed `npm run test:race` (`141 passed`), Next typecheck/lint/build, default Python regression (`376 passed, 10 expected infra skips`), host-context live regression (`386 passed`), compileall, no-SQLite scan, generic secret scan, screenshot-location scan, whitespace check, `git diff --check`, and `Leibniz` review with no actionable findings.
- Added product-facing content-readiness preflight to the actual Next.js production controls. The app now derives readiness from publishable artifacts, accepted context evidence, claim traces, claim support, reviewer decisions, and feedback gates; no-content disables all production actions, blocked content disables platform/media packaging while keeping Publish check available, and `visual_brief` is treated as publishable creator content. `Leibniz` found and closed one source-backed bug: raw `source_ids` no longer count unless matched to accepted source/citation/artifact evidence. Validation passed `npm run test:race` (`137 passed`), Next typecheck/lint/build, default Python regression (`376 passed, 10 expected infra skips`), host-context live regression (`386 passed`), compileall, no-SQLite scan, generic secret scan, screenshot-location scan, whitespace check, `git diff --check`, browser render proof at `screenshots/content-readiness-preflight.png`, and final `Leibniz` no-actionable-findings review.
- Hardened product Autopilot profile selection for long-running runs with multiple autonomous profiles. Shared frontend helpers now pick the newest `autonomous_pass` profile deterministically, Activity and heartbeat evidence no longer borrow scheduler/heartbeat proof from an older active profile, idle scheduler proof must be newer than the selected profile, and auto wake wakes the newest due profile while still running when an older active profile is due. Validation passed `npm run test:race` (`129 passed`), Next typecheck/lint/build, default Python regression (`376 passed, 10 skipped` with expected live-env skips), host-context live regression (`386 passed`), compileall, no-SQLite scan, exact supplied-secret scan, screenshot-location scan excluding dependency folders, whitespace check, `git diff --check`, and final `Leibniz` no-actionable-findings review after fixing its stale idle-proof finding.
- Scoped the local always-on scheduler CLI. `all-about-llms-admin run-worker-scheduler --watch` now accepts `--run-id`, `--execution-mode worker_cycle|autonomous_pass`, and `--max-iterations`, so a local background worker can safely run only one studio run or only Autopilot profiles while preserving the existing global scheduler loop. Validation passed focused CLI tests, CLI help smoke, full default Python regression (`365 passed, 10 skipped`), full host-context backend regression (`375 passed`), live Postgres (`9 passed`), frontend tests/typecheck/lint/build, hygiene scans, and `Leibniz` review.
- Added product-app Autopilot auto wake for browser-open always-on runs. The Next app now checks the active autonomous profile every 5 seconds, runs the existing run-scoped scheduler only when the profile is due, avoids overlap with user actions and in-flight scheduler calls, suppresses repeated attempts for the same due window, and exposes an `Auto wake` toggle/status in the Activity panel. Validation passed focused auto-wake/Activity/schedule tests, full frontend tests (`115 passed`), Next typecheck/lint/build, full default Python regression (`363 passed, 10 skipped`), full host-context backend regression (`373 passed`), live Postgres (`9 passed`), hygiene scans, and `Leibniz` review.
- Added product-visible Autopilot schedule and lease observability to the creator app. `ActivityPanel` now shows whether the active/latest autonomous profile is due now, scheduled for the next heartbeat, currently holding a heartbeat lease, or not running; a small local clock keeps due/scheduled status fresh while the page remains open. Validation passed focused Activity/autopilot tests, Next typecheck/lint/build, full frontend tests (`109 passed`), full default Python regression (`363 passed, 10 skipped`), full host-context backend regression (`373 passed`), live Postgres (`9 passed`), hygiene scans, and `Leibniz` review with no actionable findings.
- Added provider-failure recovery awareness to the actual voice release gate. The Next voice panel now passes run artifacts into the provider-backed release gate; a newer `realtime_provider_failure_recovery` ledger blocks voice readiness even if an older live smoke result still sits in UI state, and readiness clears only after a newer same-session live provider smoke ledger passes.
- Verification: focused provider-release checks passed, full Python regression passed (`363 passed, 10 skipped`), Next race tests passed (`97 passed`), Next typecheck/lint/build passed, live Postgres regression passed with sandbox escalation for localhost TCP (`9 passed`), compileall and whitespace/secret/screenshot-location/SQLite-adapter scans were clean.
- Focused product voice failure continuation on the actual recovery agents. Failed `gemma_kokoro_voice_turn_failed` API responses now include `provider_failure_recovery` hints with `inference-systems-engineer`, `observability-agent`, and `agent-harness-engineer`, plus `followup_worker_use_gemma=false`; the Next voice panel uses those hints to run the targeted deterministic recovery cycle instead of the generic all-agent continuation. Normal assistant voice follow-ups remain on default worker routing.
- Verification: focused backend checks passed (`3 passed`), full Python regression passed (`363 passed, 10 skipped`), Next race tests passed (`95 passed`), Next typecheck/lint/build passed, live Postgres regression passed with sandbox escalation for localhost TCP (`9 passed`), compileall and whitespace/secret/screenshot-location/SQLite-adapter scans were clean.
- Added an end-to-end worker-cycle regression for realtime provider failure recovery. A failed `gemma_kokoro_voice_turn_failed` event now proves the whole chain: Inference Systems Engineer records the recovery provider-ops ledger, Observability Agent builds runtime health proof, Agent Harness Engineer records a checkpointed resume plan, no fake assistant turn is materialized, secret-shaped HF/Tavily/Bearer values stay redacted, and rerunning the same recovery cycle is idle without duplicate artifacts or messages.
- Verification: focused recovery chain passed (`1 passed`), related recovery/runtime/resume subset passed (`15 passed`), default Python regression passed (`363 passed, 10 skipped`), live Postgres regression passed with sandbox escalation for localhost TCP (`9 passed`), whitespace/secret/screenshot-location/SQLite-adapter scans were clean, and `Leibniz` reported no actionable findings.
- Answered the skipped-test concern by running the gated live Postgres/pgvector suite with `LIVE_POSTGRES=1`; Docker Postgres was already healthy, but sandboxed localhost access blocked the first attempt, so the live suite was rerun with host access.
- Repaired stale live integration expectations instead of accepting skips: live tests now assert provider-backed web research blocks when no search provider is configured, deterministic fallback written drafts remain non-publishable, work-plan pending counts include all routed support tasks, and conversational `hook` feedback revises the reel/script artifact only.
- Fixed the real Rust voice-edge HTTP sidecar smoke test to keep the persistent async HTTP client inside one `asyncio` loop for analyze plus cancel, matching the production client lifecycle.
- Verification: `env LIVE_POSTGRES=1 python3 -m pytest tests/test_live_postgres.py -q -rs` passed with `9 passed`; `python3 -m pytest tests/test_voice_edge.py::test_real_rust_voice_edge_http_sidecar_health_and_cancel_smoke -q -rs` passed with `1 passed`; full live regression `env LIVE_POSTGRES=1 python3 -m pytest -q -rs` passed with `335 passed` and no skipped tests; `Leibniz` reported no actionable findings.
- Hardened provider-backed web-search adapters so Tavily and SerpAPI HTTP status failures, network/request errors, invalid JSON, malformed success payloads, and malformed individual result records are converted into durable provider-boundary behavior instead of leaking raw `httpx`, validation, or shape errors through content generation or Web Research Agent flows. Mixed valid/invalid result lists keep the valid sources; non-empty provider result lists with no valid source records block cleanly.
- Fixed the `voice_agent` package import boundary by making the heavy LiveKit app exports lazy; provider/realtime modules can now import the voice-edge helpers without cycling through the provider factory.
- Focused validation: `python3 -m pytest tests/test_providers.py -q -rs` passed with `20 passed`; `python3 -m pytest tests/test_voice_agent.py tests/test_voice_edge.py tests/test_api_contracts.py::test_agent_worker_research_executor_records_blocked_provider_status_without_gemma -q -rs` passed with `53 passed, 1 skipped` where the skip is sandbox loopback binding; full live regression after the final SerpAPI review fix passed with `env LIVE_POSTGRES=1 python3 -m pytest -q -rs` -> `348 passed`; `Leibniz` final check reported no actionable findings.

## 2026-05-17

- Created the Obsidian three-tier memory structure: `raw/`, `wiki/`, `output/`, and `templates/`.
- Adopted vault-first memory behavior: raw evidence, wiki synthesis, output deliverables.
- Added Max Mitcham article source note as raw inspiration for the memory architecture.
- Added product and Codex working-memory wiki notes.
- Added JSON and HTML companion viewer for the Agent Studio memory operating system.
- Added runtime memory-promotion proposals through `POST /api/runs/{run_id}/obsidian-memory-promotion` and the Interactive Note-Taking Agent task `generate_obsidian_memory_promotion`.
- Added typed `user_preference` and `project_decision` project memory records through `POST /api/runs/{run_id}/project-memory` and the `record_project_memory` A2A task.
- Added context-packet memory health checks for stale, low-confidence, and conflicting project memories.
- Added autonomous-pass memory summary export into `output/reports/autonomous-memory-summaries/{run_id}/memory-summary.md` with durable artifact and event tracking.
- Verified the autonomous memory summary export with full regression and boundary scans.
- Added regression coverage proving always-on autonomous profile heartbeats refresh claim revision closure after writer, script, and editor follow-ups complete; verified with full regression and boundary scans.
- Added worker-profile controls for autonomous research refresh policy, research/retrieval block gates, and profile-level Obsidian memory-summary export; verified with full regression and boundary scans.
- Added always-on policy observability to heartbeat ledgers and scheduler events so configured profile policies and observed autonomous pass outcomes are auditable; verified with full regression and boundary scans.
- Added always-on retrieval-quality and memory-health metrics to heartbeat ledgers, heartbeat events, blocked-heartbeat events, and scheduler events; updated the Obsidian memory notes and companion viewer state; verified with focused tests, full suite, viewer JSON checks, and boundary scans.
- Added pgvector-backed project-memory retrieval with keyword scoring, high-precision graph expansion, durable retrieval ledgers, and `retrieve_project_memory` worker support; verified with focused tests, full suite, viewer JSON checks, and boundary scans.
- Wired project-memory retrieval digests into context packets, autonomous context artifacts, worker-profile heartbeat context artifacts, and resume plans; verified with focused tests, full suite, viewer JSON checks, and boundary scans.
- Added project-memory retrieval evaluation metrics for labeled and heuristic precision/recall, false positives, false negatives, repeated-query counts, and trend deltas; verified with focused tests, full suite, viewer JSON checks, and boundary scans.
- Added project-memory policy status to worker context summaries and Gemma prompt instructions, plus heartbeat/scheduler aggregates for project-memory precision risks, recall gaps, regressions, and no-seed profiles; verified with focused tests, full suite, viewer JSON checks, and boundary scans.
- Added always-on `project_memory_policy_confirmation` gates so risky memory-policy states create structured feedback, set runs to `waiting_for_human`, and block worker-profile execution before agents act on weak memory.
- Added a product cockpit Memory Policy panel and wiring from retrieval, context packets, resume plans, heartbeat ledgers, feedback gate metadata, and timeline event chips.
- Verified the cockpit memory-policy surface with focused tests, full regression, JavaScript syntax check, no-SQLite scan, screenshot folder scan, no planning-board drift, and browser render check.
- Added labeled-relevance controls to the cockpit Memory Policy panel so operators can send relevant memory ids, relevant tags, and relevant wiki notes into project-memory precision/recall evaluation.
- Verified the labeled-relevance cockpit controls with focused test, full regression, JavaScript syntax check, boundary scans, and browser render check.
- Confirmed the Max Mitcham article through Browser/web review, added an append-only raw-source addendum, created [[wiki/ops/active-codex-context]], and tightened the Codex Obsidian working-memory protocol so future work starts from compact vault context rather than broad chat replay.
- Added generated `output/viewers/agent-studio-system-design.json` and `output/viewers/agent-studio-system-design-viewer.html` as an inspection surface derived from Obsidian HLD, LLD, and memory notes.
- Added cockpit source-ledger drilldowns for accepted retrieval evidence, rejected/unaccepted sources, artifact claim coverage, claim-source verdicts, and accepted-source overlap; updated sprint, HLD, LLD, and product-memory notes.
- Added cockpit project-memory recording controls for user-confirmed decisions/preferences, including scope, confidence, tags, wiki targets, source artifact ids, cockpit provenance, and a recorded-memory result renderer.
- Added a provider-free cockpit demo run and `Load demo run` control that seeds source records, supported claims, draft artifacts, retrieval quality, source ledger, feedback gate, and project memory for end-to-end cockpit review.
- Verified the cockpit demo slice with full regression, compileall, viewer JSON checks, generated-viewer script checks, no-SQLite scan, screenshot folder scan, planning-boundary scan, and browser render checks. Docker Postgres could not be started because the local Docker daemon was not running.
- Captured provider-readiness reference docs and added a provider-backed smoke walkthrough to `/api/provider-readiness` plus cockpit rendering for smoke status, blockers, actions, and expected durable evidence.
- Added a durable cockpit walkthrough ledger endpoint and cockpit renderer to package runtime health, provider readiness, provider-free demo proof, source coverage, realtime smoke state, provider observability, and feedback-loop state for a run.
- Started Docker Postgres + pgvector, applied durable storage, seeded a live provider-free cockpit demo run, recorded checkpoint/model-routing/provider-ops/runtime-health/cockpit-walkthrough ledgers, and captured the raw run export. Live walkthrough is blocked only by missing provider credentials/realtime evidence and the open demo feedback gate.
- Validated the Obsidian memory OS and system-design viewers after the three-tier memory setup: JSON parsed, embedded scripts compiled, full regression passed, no-SQLite scan was clean, screenshots stayed under `screenshots/`, and browser render screenshots were saved for both viewers.
- Added provider-free realtime rehearsal mode for cockpit voice UX testing, while tightening cockpit walkthrough proof so only non-rehearsal sessions from the selected realtime provider satisfy provider-backed smoke.
- Ran provider-free realtime rehearsal against live Postgres run `f34c732f-1168-4582-86f7-8bd6efcd0b02`, recorded one `local_realtime_rehearsal` session, routed one voice turn, built the realtime dialogue ledger, and rebuilt the cockpit walkthrough. The latest walkthrough remains blocked as intended until provider credentials and a real selected-provider realtime session exist.
- Captured production agent system design sources in raw memory, synthesized [[wiki/concepts/production-agent-system-design-canon]], expanded foundation references to 18 records, and added Backend Platform, Frontend Experience, Scalability/Reliability, and Inference Systems specialists with system architecture, frontend engineering, and inference reliability skills.
- Validated the production design canon slice with focused contract tests, full regression (`176 passed, 9 skipped`), compileall, JSON viewer parsing, generated-viewer script checks, no board-reference scan, screenshot-folder scan, and no SQLite adapter-pattern scan.
- Added concrete worker routing and tests for the Backend Platform, Frontend Experience, Scalability/Reliability, and Inference Systems specialists.
- Created the separate `system_design_vault/` Obsidian workspace for source ingestion and system design, added the local DS interview prep books corpus inventory, seeded official/open source notes, added a PDF chapter-scaffold generator, and generated non-raw chapter scaffolds for five local user-provided books.
- Validated the current slice with focused worker tests, full regression (`180 passed, 9 skipped`), compileall, JSON parsing, HTML parse check, ASCII scan, no SQLite adapter-pattern scan, and screenshot-location scan.
- Gave `Feynman` standing autonomy over background ingestion, refreshed the `Designing Machine Learning Systems` source path, generated `Inference Engineering` scaffolds, added compact book-synthesis notes for five priority books, and fed the cross-source production Agent Studio canon back into the system-design-vault HLD/LLD, MOC, ingestion plan, and source-map viewer.
- Implemented the first runtime contract from the new design canon: provider-operation entries now expose latency class, provider/end-to-end latency, fallback reason, and smoke-proof status; realtime session and turn events distinguish provider-backed voice from rehearsal-only evidence. Verified with focused tests and full regression (`180 passed, 9 skipped`).
- Updated the product cockpit Provider Operations panel to show latency/smoke evidence as chips and compact rows, started the local FastAPI server with Docker Postgres, seeded demo run `dca8bfb5-e127-4569-8ba0-89e029ad556a`, and verified the cockpit provider-ops render through Playwright with screenshots stored under `screenshots/`.
- Added durable provider smoke at `POST /api/runs/{run_id}/provider-smoke` with default non-live config/blocker proof, optional live provider calls, source/realtime session evidence, and cockpit Provider smoke rendering with a Live smoke toggle.
- Wired provider smoke into autonomous passes, CLI, cockpit pass requests, provider operations, and context-packet latest-ledger summaries so long-running profiles carry provider smoke evidence forward automatically.
- Verified the autonomous provider-smoke path against live Postgres run `f3290d85-77f5-42b2-9d60-34c050151f94`; provider smoke artifact `328f031a-d8b0-4618-810a-1365be6c5df0` was blocked only by missing provider credentials and appeared in the autonomous context packet latest-ledger summary.
- Granted `Feynman` autonomous ownership of the ingestion lane, including the local `/Users/saumyamehta/DS interview prep/books` corpus, so the main implementation loop only supports ingestion on concrete blockers.
- Validated the direct provider-smoke CLI path on live Postgres run `f3290d85-77f5-42b2-9d60-34c050151f94`; it created provider smoke artifact `82d4c751-d33f-4b35-bf24-dc6ee735c5f2`, reported status `blocked`, and correctly surfaced missing Gemma/HF, OpenAI Realtime, and Tavily credentials while local durable-store, cockpit-demo, reranker, and imagegen-boundary checks remained available.
- Re-ran focused provider-smoke CLI/API checks and the full regression suite after the validation; latest result is `183 passed, 9 skipped`.
- Added a cockpit `Launch autopilot` path for the Worker Profile panel so an operator can create or reuse an autonomous profile, start it, run the first heartbeat, and see saved profiles after refresh without clicking through the lower-level profile plumbing. Verified with static cockpit coverage, JavaScript parse, Playwright render evidence, full regression, compileall, no SQLite adapter scan, screenshot-location scan, and touched-file ASCII scan.
- Read Ziwen's Codex knowledge-vault article through Browser article view, captured a raw source note, and updated the Obsidian operating flow with passive capture, provenance gates, material-reading requirements, lifecycle/index statuses, source-citation gates, and daily/weekly memory refinement loops.
- Implemented the first actual creator-facing Next.js product app surface under `frontend/next-app`, with responsive text/voice composition, browser speech capture, prompt starters, run restore, draft filtering, copy/export/revise controls, feedback resolution, source/claim evidence, and same-origin `/api` proxying to FastAPI. Added the Rust retrieval-ranker crate and Python `RERANKER_PROVIDER=rust` adapter. Verified with Next typecheck/lint/build, Rust tests, focused Python tests, FastAPI and Next proxy smoke calls, and Playwright screenshots under `screenshots/`.
- Extended the Next.js product app toward a natural conversational studio: added dialogue history, agent/event activity, and selectable realtime voice settings for local rehearsal, OpenAI Realtime, ElevenLabs, and Cartesia. Browser validation against run `54767754-ce5a-49f8-87e8-f6359635e8c7` rendered the new panels, and the slice passed Next typecheck/lint/build, full Python regression, no-SQLite scan, and screenshot-location scan.
- Added production controls to the Next.js product app for platform packaging, media planning, and publish-readiness checks. Smoke-tested those controls through the Next `/api` proxy on run `54767754-ce5a-49f8-87e8-f6359635e8c7`: platform package created 5 platform variants, media planning created image/audio/video artifacts, and publish readiness correctly blocked on source/review/guardrail issues. Browser proof saved as `screenshots/next-product-app-production-panel.png`.
- Researched and corrected the production live-voice architecture: default voice stack is now Gemma 4 E4B native audio understanding/reasoning plus Kokoro-82M TTS over LiveKit, with Pipecat optional for internal pipeline composition, Rust Axum/Tokio + Silero VAD for low-latency barge-in/buffer control, and Python asyncio for Gemma/Kokoro orchestration/context pruning. Added the Obsidian research note, updated HLD/LLD/decision/current-context notes, and patched backend/frontend contracts away from OpenAI-first or raw WebSocket-first voice.
- Implemented the next LiveKit/Gemma/Kokoro contract slice: realtime session responses now include a structured transport grant; session records carry room/participant identities, context-window policy, and transport-token presence without persisting token values; media-only live turns can be committed without transcript while routed turns require a finalized transcript; spoken response plans carry Gemma/Kokoro/audio ids; and interrupt controls explicitly request Gemma cancellation, Kokoro buffer clearing, and LiveKit audio stop.
- Added local LiveKit room-token minting for `gemma4_realtime` using standard JWT HS256 grants. Provider-backed readiness now requires `GEMMA4_REALTIME_LIVEKIT_URL`, `LIVEKIT_API_KEY`, and `LIVEKIT_API_SECRET`; tests decode the token grant and verify raw token/secret values stay out of durable event payloads and streams.
- Completed a deeper realtime voice and retrieval research pass. Updated Obsidian research/HLD/LLD notes with the production boundary: LiveKit as public media transport, Rust as internal realtime edge, Python as Gemma/Kokoro engine, Gemma 4 E4B as audio-understanding/reasoning text-output model, Kokoro as speech synthesis, and multi-stage retrieval with dense plus lexical plus web plus graph candidate fanout, fusion, reranking, and FP/FN evaluation.
- Wired the creator-facing Next.js voice panel to the actual LiveKit browser client. The panel now joins backend-issued LiveKit rooms, enables microphone publishing, attaches remote audio tracks, clears local playback on interrupt/disconnect, and reports blocked states for missing or unsafe transport grants. Focused regression and frontend lint/typecheck passed.
- Revalidated the LiveKit voice panel after the config-error copy cleanup: full regression passed (`192 passed, 9 skipped`), Next typecheck/lint/build passed, and Playwright rendered/clicked the voice panel. Screenshots are in `screenshots/next-livekit-voice-panel.png` and `screenshots/next-livekit-voice-error-state.png`.
- Added the internal Python Gemma/Kokoro voice-agent engine scaffold. It prunes old raw audio from context, streams Gemma text into Kokoro TTS fragments, publishes PCM chunks through a LiveKit publisher protocol, emits durable-safe events, and handles barge-in cancellation by clearing Kokoro buffers and stopping LiveKit response audio. Focused voice-agent/provider tests pass.
- Rechecked the realtime voice direction against official LiveKit, Hugging Face Gemma 4, Kokoro, Pipecat, HF Inference Endpoints, and Silero VAD docs. Updated the Obsidian research note, LLD, sprint state, and active context. Added the runnable Python LiveKit participant scaffold, concrete Gemma/Kokoro/LiveKit adapters, `all-about-llms-admin run-voice-agent`, optional voice dependencies, and Gemma multimodal request formatting that places audio before text while preserving custom endpoint attachments.
- Added the first Rust voice-edge contract core under `services/voice-edge`: typed JSON contracts, deterministic VAD test logic, bounded buffer events, barge-in detection, and cancellation acknowledgement actions for Gemma/Kokoro/LiveKit. Verified with `cargo test --offline`, `cargo fmt --check`, and focused Python voice/provider tests.
- Rechecked the LiveKit/Gemma/Kokoro/Rust voice path against current docs, then wired the Rust voice-edge CLI into the Python LiveKit participant with `RustVoiceEdgeClient`. The participant can now keep reading audio while assistant turns run, call the Rust edge for active-response barge-in detection, forward edge events over LiveKit data, and cancel active Gemma/Kokoro work from the cancellation acknowledgement. Focused voice-edge, voice-agent, and provider tests passed.
- Extended the LiveKit participant from max-window audio turns to Rust-edge VAD endpointing when `RUST_VOICE_EDGE_BINARY_PATH` is available. It now opens user turns from `voice_user_speech_started`, preserves configurable pre-roll, commits after configurable silence frames, keeps max-duration as a safety flush, and still cancels active Gemma/Kokoro work on barge-in.
- Added `voice-edge --jsonl` and `PersistentRustVoiceEdgeClient` so the LiveKit participant keeps one Rust edge process open for frame-by-frame VAD instead of spawning a process per audio frame. Rust JSONL integration and Python persistent-client tests pass.
- Added a local `benchmark-voice-edge` command and benchmark module for synthetic persistent-edge proof. It measures Rust JSONL bridge latency and reports silence false positives, missed speech starts, and missed barge-in cancellations. A local one-run benchmark against the compiled Rust binary returned `status=passed` with zero FP/FN-style misses.
- Rechecked the LiveKit/Gemma/Kokoro/Rust voice architecture with sidecar review, then promoted the local Rust voice-edge benchmark into runtime health as durable operator evidence. The runtime health ledger now records persistent JSONL process mode, latency summary, false-positive / missed-speech / missed-cancellation counts, and emits voice-edge benchmark pass/attention events. Realtime provider metadata now marks JSONL subprocess as current and Axum/Tokio/gRPC as target architecture.
- Added the LiveKit/Gemma/Kokoro timing-ledger slice: agent data-channel events now persist through `/voice-events`, the LiveKit participant emits `voice_user_turn_committed`, `realtime_voice_timing_ledger` artifacts require correlated session/turn/response proof, mismatched barge-in response ids remain incomplete, and `vault-sync-subagent` now runs every 5 minutes to keep the planning and system-design vaults aligned.
- Added SSE/OpenAI-compatible Gemma streaming support to the voice-agent HF adapter, with focused tests proving incremental deltas, multimodal audio payload shape, authorization headers, timeout wiring, and fallback behavior when streaming is disabled.
- Added Gemma/Kokoro voice-streaming metrics to provider smoke. The default `gemma4_realtime` path now blocks on missing HF/Kokoro streaming config and records Gemma TTFT, Kokoro first-audio, and end-to-end first-audio latency when live smoke is enabled.
- Added the Rust voice-edge Axum/Tokio HTTP sidecar around the existing typed contract. It preserves stdin and persistent JSONL modes, adds explicit CLI mode parsing, exposes health/analyze/cancel HTTP routes, keeps cancellation acknowledgements explicit, and has route/parser regression coverage. `Leibniz` reviewed the slice; its findings on CLI ambiguity, unknown-arg fallback, docs drift, and route coverage were fixed.
- Added the Python HTTP client path for the Rust voice-edge sidecar. `RUST_VOICE_EDGE_HTTP_URL` now opts the LiveKit participant into `RustVoiceEdgeHttpClient`; otherwise it uses the persistent JSONL subprocess. Both paths share request construction and cancellation parsing. HTTP falls back to JSONL when the binary is executable, recent-frame windows and cancelled-response ids are bounded, the HTTP client is reused, and the persistent JSONL subprocess no longer risks stderr backpressure.
- Closed the lifecycle leak found by `Leibniz`: LiveKit sessions now require shutdown-callback support and register cleanup that closes the voice-edge transport exactly once, preventing persistent Rust JSONL processes or HTTP clients from surviving session teardown. Cleanup exceptions are reported through the event loop and remain retryable.
- Removed the remaining startup-risk findings from `Leibniz`: HTTP sidecar mode now checks `/healthz` before voice-agent readiness and falls back to JSONL when available; project-local Rust binary paths are resolved from the project root rather than the current working directory.
- Added JSONL startup preflight for the default Rust voice-edge path and a real HTTP sidecar smoke test that launches the compiled sidecar when loopback binding is allowed. Local focused validation passed with the real-smoke test skipped only because this sandbox denies loopback binding.
- Added the product-facing voice runtime readiness endpoint and wired it into the Next voice panel. The app now shows non-secret LiveKit, Gemma/HF, Kokoro, Rust edge, and context-pruning readiness, and can run a Rust edge preflight before joining voice.
- Added backend participant readiness to the Gemma/Kokoro voice runtime contract. `/api/voice-runtime-readiness` now includes `livekit-agent-participant`; Runtime preflight sends `preflight_agent=true` to construct the LiveKit agent server without starting it, so the browser room join path is not considered enough unless the LiveKit Agents runtime and `all-about-llms-admin run-voice-agent` path are available. Browser evidence is `screenshots/next-product-app-voice-agent-readiness.png`.

## 2026-05-18

- Hardened the product-facing voice runtime readiness UI after `Leibniz` review. The Next voice panel now exposes only the implemented Gemma 4 E4B + Kokoro LiveKit path plus local transcript rehearsal, renders all five readiness checks, surfaces mount-time readiness failures, labels the action as Runtime preflight, separates blocked/degraded severity colors, and keeps Stop tied to an active session. Browser validation confirmed desktop/mobile rendering and a passing Rust persistent-JSONL preflight; screenshots are under `screenshots/`.
- Added the Next voice proof panel. It can build a dry Gemma/Kokoro voice provider-smoke ledger, opt into live smoke explicitly with confirmation before external calls, build the realtime voice timing ledger, and show blocked evidence directly in the creator app. Local browser checks confirmed runtime smoke blocks on missing HF/Kokoro config and timing ledger blocks on missing provider-backed LiveKit events; desktop/mobile screenshots are under `screenshots/`.
- Added explicit VAD backend selection to the Rust voice edge and Python bridge. `deterministic_energy` remained the first default effective backend, `silero_onnx` gained an honest request/fallback/no-fallback contract with metadata, Python passes backend/model-path/fallback settings to Rust, and later work linked real Silero inference behind that contract.
- Closed the `Leibniz` review findings on the VAD slice by tightening HTTP sidecar health-contract validation, preserving VAD config through the HTTP+JSONL fallback wrapper, and making readiness/live-agent/provider-session metadata separate requested, effective, fallback, runtime-transport, and preflight-transport state.
- Linked real Rust Silero ONNX inference in `services/voice-edge` through the `silero` crate. `silero_onnx` now runs the bundled model unless `vad_model_path` points to a custom ONNX file, `vad_probability_threshold` controls speech probability, and invalid model-path cases still exercise deterministic fallback or conservative no-speech behavior according to policy.
- Fixed the Silero realtime performance blocker from `Leibniz` review: the Rust edge now caches Silero ONNX sessions in a bounded process-wide pool keyed by model source and session id instead of loading the model on every analyze request. The cache normalizes custom-model path keys where possible, evicts custom pools with LRU-style ordering, rebuilds pools when configured pool size changes, and the Rust test suite covers same-session reuse, bounded custom-file growth, LRU refresh, and pool-size changes.
- Added recurrent Silero stream-state reuse inside the Rust voice-edge process. Each session-pool slot keeps an LRU-bounded `StreamState` cache keyed by stream id and sample rate, Python/HTTP config now exposes `vad_stream_state_cache_size`, docs/vault notes distinguish request/response transport from in-process recurrent VAD state, and the remaining Rust voice work is concurrency tuning plus the LiveKit-side media bridge.
- Hardened the recurrent-state path after `Leibniz` review: missing, empty, or whitespace stream ids now use one-shot local Silero state instead of shared recurrent cache keys, one-shot calls are round-robined across Silero session slots, failed Silero inference drops the affected stream state, and the Rust suite now covers recurrent partial-frame continuity, missing/empty-key isolation, and one-shot slot spreading. Final review found no blockers; remaining risk is per-slot inference lock latency under high concurrency.
- Extended `benchmark-voice-edge` with a concurrent stream probe, VAD backend/pool metadata, and `--http-url` sidecar support. Local evidence: deterministic JSONL 4x2 concurrency passed with `serialized_by_client=true` and p95 `1.045ms`; deterministic HTTP 4x2 passed with `serialized_by_client=false` and p95 `2.492ms`; Silero HTTP with tuned synthetic threshold `0.01` and amplitude `12000` passed 4x1 with p95 `32.964ms`. Default Silero threshold `0.5` did not classify the synthetic tone as speech, so real speech-corpus tuning remains required.
- Hardened the benchmark evidence after `Leibniz` review: transport process mode and serialization now come from voice-edge client capabilities, CLI benchmark counts reject zero/negative values, runtime health can benchmark HTTP with executable JSONL fallback when both are configured, and non-executable binary paths degrade cleanly instead of raising an internal assertion.
- Added real-audio stimulus support to the voice-edge benchmark. `benchmark-voice-edge --speech-wav` now loads local 16-bit PCM WAV speech, splits it into voice-edge frames with the configured frame duration, and uses it for speech-start, barge-in, and concurrency probes; runtime health can use the same fixture through `RUST_VOICE_EDGE_BENCHMARK_SPEECH_WAV_PATH` and cap length with `RUST_VOICE_EDGE_BENCHMARK_MAX_SPEECH_FRAMES`. This gives Silero tuning a real-corpus path instead of relying on synthetic tones.
- Added corpus and threshold-sweep support for voice-edge tuning. The benchmark accepts repeated `--speech-wav`, recursive case-insensitive `--speech-wav-dir`, and `--vad-probability-threshold-sweep`, and now returns corpus summaries plus threshold pass lists so Silero pool/threshold tuning can use small real-audio fixture sets instead of one-off runs. Bad threshold lists, missing/empty fixture directories, missing explicit fixtures, synthetic-only sweeps, and frame caps above 4096 fail fast.
- Added durable dialogue materialization for real LiveKit voice-agent events. The Gemma/Kokoro engine now emits completed assistant text, `/api/realtime-sessions/{id}/voice-events` turns user commit and assistant completion events into voice `ConversationTurn` records with advisory-lock-backed idempotence and partial unique indexes, malformed user turn ids are skipped, and the Next voice panel refreshes the run dialogue when new materialized turn ids are returned.
- Added Realtime Conversation Host follow-up creation for materialized assistant voice completions. Fresh `assistant_response_completed` events now create a deterministic insert-if-absent `summarize_realtime_turn_context` A2A task with source/response turn ids, response id, transcript excerpt, and a routing policy that keeps raw audio distinct from verified text; materialized-turn retries can recover a missing follow-up, while duplicate voice-agent events skip duplicate task and follow-up-event creation.
- Added downstream A2A fanout from Realtime Conversation Host briefs. The host now turns recommended handoffs into deterministic insert-if-absent child tasks, records created/skipped handoff ids on the brief artifact and event payload, and focused regression proves duplicate host retries do not duplicate specialist tasks.
- Added regression proof for the voice-to-content worker chain. A finalized Gemma/LiveKit voice transcript now runs through Realtime Conversation Host, Intent Router, Content Strategist, and downstream workers, proving deterministic seed post/reel/Substack drafts scoped to the content-strategy handoff plus writer, data, audio, video, and artifact outputs. Leibniz review drove hardening for seed insert-conflict reload, no-route voice handoffs with prior artifacts, direct existing-content target pinning, ambiguous same-format draft rejection, and explicit-only source/claim lineage for seed evidence. Intent Router route-plan artifacts and child tasks use deterministic insert-if-absent writes, Content Strategist child task creation uses deterministic insert-if-absent ids, and replay assertions prove duplicate route plans, duplicate seeds, and downstream tasks are suppressed.
- Wired the creator-facing Next.js app to bounded A2A continuation: submitted turns call the worker-cycle endpoint after routing, Activity now has a Run agents control, and regression coverage proves a product dialogue turn can continue into route plans, handoff-scoped seed drafts, and writer artifacts through `/api/a2a/workers/run-cycle`.
- Wired product live voice follow-ups into bounded A2A continuation. Persisted assistant voice completions that return `followup_task_message_id` now trigger the Next app to run the same specialist worker-cycle path, so voice-led dialogue can continue into agent work without the cockpit.
- Browser validation against live FastAPI/Postgres exposed a worker-cycle 500 in Postgres JSONB mutation queries for agent-message recovery/retry/repair metadata. Added explicit casts for those runtime parameters, added regression coverage, verified the CLI and HTTP worker-cycle paths return structured results, and saved the product-app screenshot at `screenshots/next-product-app-agent-continuation.png`.
- Addressed `Leibniz` follow-up risks for the worker-cycle slice: added a `Gemma workers` Activity toggle, made omitted API helper calls non-Gemma by default, added an in-flight guard against overlapping worker cycles, and cast direct agent-message `result` writes as JSONB.
- Fixed the final stale-run race reported by `Leibniz`: `New` is disabled while busy and `refreshRun` uses a run-version guard before committing fetched context, preventing late worker-cycle completions from restoring a cleared run.
- Added durable Gemma/Kokoro LiveKit participant presence proof. The API now reports fresh/stale/missing `gemma_kokoro_voice_agent_ready` evidence per run/session, the Python LiveKit agent answers frontend presence probes, the Next LiveKit client persists sender identity, probe-specific checks can bind to `probe_id`, and the product voice panel shows missing participant proof honestly until a real agent is observed. Verified with focused tests, full regression (`269 passed, 10 skipped`), Next typecheck/lint/build, compileall, no SQLite adapter scan, and desktop/mobile screenshots under `screenshots/`.
- Added the local Gemma/Kokoro LiveKit voice-agent process supervisor and product Start/Stop/status controls. The API exposes process status/start/stop, intentional stops are not reported as failures, stop/exit races return stable envelopes with last return codes and typed `last_error`, the product row surfaces that control error directly, the supervisor redacts known provider secrets from bounded log tails, the app still treats process state as separate from readiness and participant presence, and the mobile draft-board overflow found in browser validation is fixed. Verified with focused tests, full regression (`274 passed, 10 skipped`), Next typecheck/lint/build, compileall, no SQLite adapter scan, and desktop/mobile screenshots under `screenshots/`.
- Added creator-facing Autopilot controls to the Next Activity panel. The app now lists worker profiles for the current run, starts/reuses an `autonomous_pass` profile with a first heartbeat, stops active/running/started profiles, explicitly clears run-coupled state on New, resets the voice panel on run changes, disconnects LiveKit runtime on unmount, and guards late start/stop/follow-up responses with active-run/run-version checks so the product remains a live content studio rather than a cockpit. Verified with static product contract coverage, full regression (`274 passed, 10 skipped`), Next typecheck/lint/build, compileall, no SQLite adapter scan, and desktop/mobile screenshots under `screenshots/`.
- Closed the `Leibniz` stale-run follow-up on the product app. Compose, bounded worker continuation, manual refresh, Autopilot start/stop, revision, feedback, media, distribution, readiness, and LiveKit session-ready paths now carry explicit run-version/active-run ownership before writing state; stale branches clear busy state only when the same run still owns the UI; the voice panel invalidates in-flight Gemma/Kokoro LiveKit starts on stop/unmount/run replacement and ends stale sessions before they can reconnect. Re-verified with focused contract coverage, full Python regression (`274 passed, 10 skipped`), `compileall`, Next typecheck/lint/build, and a no SQLite adapter-pattern scan.
- `Leibniz` final pass found no blockers or wrong-run busy-state clear risk. The remaining non-blocking test gap is a true frontend async-overlap race test for New/run replacement during in-flight compose, revision, worker-cycle, or voice follow-up.
- Added a durable direct voice-event bridge for the real Gemma/Kokoro LiveKit path. Python-emitted voice-agent events now carry stable `voice_agent_event_uid` values, the LiveKit data channel still drives live captions, and a best-effort backend HTTP sink can persist the same event directly to FastAPI so always-on sessions do not depend on an open browser tab. The voice-events API deduplicates agent/browser submissions by UID before appending run events, materializing turns, or creating follow-up tasks, and duplicate browser responses recover existing materialized-turn/follow-up hints when the direct agent sink wins first. Verified with focused sink/dedupe tests, full frontend tests (`122 passed`), Next typecheck/lint/build, full default Python regression (`369 passed, 10 skipped`), host-context live regression (`379 passed`), and compileall.
- Added the first executable frontend race-policy test layer. The run-ownership predicates now live in `frontend/next-app/lib/state/runOwnership.ts`, the app uses `isRunOwnerCurrent` through `isActiveRunOwner`, `npm run test:race` covers stale version checks, wrong-run busy preservation, New during in-flight create cleanup, and a deferred old-run completion landing after a newer run takes over. Full regression is now `275 passed, 10 skipped`; `Leibniz` found no blockers and accepted the remaining browser/component lifecycle test as future hardening.
- Added product-facing Autopilot heartbeat evidence to the creator app. `ActivityPanel` now shows durable heartbeat proof from matching `worker_profile_heartbeat_ledger` artifacts, including state, task count, ledger id, blocked reasons, work-plan id, realtime dialogue status, and feedback resolution status, with event fallback only when the event matches the active/latest profile. Added executable evidence extraction tests, prevented cross-profile proof borrowing, verified desktop/mobile render screenshots under `screenshots/`, and passed full regression (`275 passed, 10 skipped`), Next typecheck/lint/build, no SQLite adapter-pattern scan, screenshot-location scan, and `Leibniz` review.
- Added a creator-app Autopilot `Heartbeat` action for active autonomous profiles. The frontend now calls the existing heartbeat endpoint, refreshes run context after completion, and renders Start/Heartbeat/Stop without exposing a planning board. Added server-rendered React coverage for the Activity panel so the Heartbeat control and durable proof text are tested from real component props. Verified with `npm run test:race` (`8` pass), focused tests, full regression (`275 passed, 10 skipped`), Next typecheck/lint/build, no SQLite adapter-pattern scan, and screenshot-location scan.
- Closed the `Leibniz` cleanup on the heartbeat action. Duplicate clicks now keep the UI in the explicit heartbeat busy state and show a visible already-running error instead of throwing, while stale-profile and run-ownership guards continue to block invalid endpoint calls. Re-verified with Next typecheck/lint/build, `npm run test:race`, focused contract tests, full regression (`275 passed, 10 skipped`), `compileall`, no SQLite adapter-pattern scan, and screenshot-location scan.
- Added a run-scoped product scheduler wake for Autopilot. The scheduler request can now filter due profiles by run id and execution mode, the creator app calls that filtered path for active-run `autonomous_pass` profiles only, and Activity shows `Run due` plus scheduler proof only when the durable scheduler event names the active/latest Autopilot profile. Autopilot evidence no longer renders without an Autopilot profile, and scheduler in-flight state is tracked per run. Verified with focused scheduler and product contract tests, `npm run test:race` (`9` pass), full regression (`276 passed, 10 skipped`), Next typecheck/lint/build, `compileall`, no SQLite adapter-pattern scan, screenshot-location scan, and browser render screenshot under `screenshots/`.
- Proved the product Autopilot lifecycle against live FastAPI/Postgres run `bcb1fd1a-419a-479b-8588-6e4b42723340`. The restored creator app showed active Autopilot, durable scheduler proof (`1` profile, `1` heartbeat, `29` tasks, event `#28844`), durable heartbeat proof, and then Stop changed the profile to `Stopped` with Start as the only remaining Autopilot control.
- Fixed the live defects found during that proof: the Autopilot side-rail layout now keeps the summary visible, `list_due_worker_profiles` casts optional Postgres filter parameters to `uuid` and `text`, and context packets now use a latest-event tail so product Activity receives recent worker scheduler events. Added regression coverage for latest-tail context packets and re-verified the run-scoped scheduler path.
- Validation for the live Autopilot proof slice passed: `npm run typecheck`, `npm run lint`, `npm run test:race` (`9` pass), `python3 -m compileall -q src/all_about_llms tests`, focused API scheduler/context tests, full `python3 -m pytest` (`278 passed, 10 skipped`), `npm run build`, no SQLite adapter-pattern scan, screenshot-location scan, and browser screenshot `screenshots/next-product-app-autopilot-live-postgres-proof.png`. `Leibniz` found no blockers after the added cursor-semantics regression; optional future hardening is a true Postgres integration test for the same cursor path.
- Made transcript rehearsal a usable creator-app voice-path proof. The Next voice panel now starts a dry-run `local_realtime_rehearsal` session from the `Transcript rehearsal` runtime option, marks the panel as `NOT PRODUCTION AUDIO`, routes a typed rehearsal transcript through `POST /api/realtime-sessions/{id}/turns`, refreshes the run, and continues specialist agents when the realtime follow-up task is returned.
- Live proof used FastAPI/Postgres run `250d7516-74f7-4299-946e-e592f41c01b9`: selected `Transcript rehearsal`, clicked `Start transcript rehearsal`, saw `Rehearsal ready`, routed the rehearsal turn, saw `Rehearsal turn routed` plus `Rehearsal follow-up queued`, and the run context reported 3 conversation turns, 64 agent messages, and 57 artifacts after continuation. Screenshot evidence is `screenshots/next-product-app-transcript-rehearsal-proof.png`.
- Hardened the rehearsal path after `Leibniz` review. Run refresh failures no longer skip the realtime follow-up continuation, follow-up continuation failures are isolated into warning events, and the rehearsal turn payload moved into `frontend/next-app/lib/voice/rehearsal.ts` with executable tests for non-production payload shape and empty-transcript rejection.
- Validation for this slice passed: Next typecheck/lint/build, `npm run test:race` (`11` pass), focused realtime rehearsal tests, full Python regression (`278 passed, 10 skipped`), `compileall`, no SQLite adapter-pattern scan, and screenshot-location scan. `Leibniz` found no blockers after cleanup. The key boundary remains explicit: transcript rehearsal proves realtime routing/handoff contracts, not LiveKit media, Gemma audio input, or Kokoro speech output.
- Tightened the real voice-to-voice start path in the creator app. `Join voice room` now treats the local Gemma/Kokoro LiveKit participant as part of the start sequence: it runs runtime preflight, starts the supervised local voice-agent process when stopped, blocks on failed starts, and continues only when an external participant is explicitly implied by disabled local supervision.
- Added local LiveKit dev bootstrap and surfaced voice readiness blockers in the product app. The local infra now has `docker compose --profile voice up -d livekit`, readiness metadata tells users how to run native or Compose LiveKit dev mode, and the Next voice panel displays per-check evidence, missing env vars, and first next action so blocked voice-to-voice setup is actionable from the app. The Runtime preflight path now performs a LiveKit RoomService connectivity check instead of treating non-empty credentials as enough proof.
- Closed the LiveKit readiness cleanup from `Leibniz`: bounded timeout validation, explicit path-prefix preflight support, query/fragment URL rejection before network calls, and metadata for the probed LiveKit path prefix. Final cleanup validation passed with focused preflight tests, `compileall`, Next typecheck, full Python regression (`283 passed, 10 skipped`), and a final `Leibniz` no-blocker review.
- Added local voice-audio artifact persistence for provider-backed Gemma/Kokoro turns. The LiveKit participant now writes bounded captured PCM turns under `ARTIFACTS_ROOT/voice-audio`, emits artifact URI/path/hash/byte metadata without raw audio in durable events, carries the same metadata into materialized conversation turns, and still sends inline base64 PCM to Gemma even when the turn has a local artifact URI for provenance. `Leibniz` cleanup added retention-day and cleanup-interval settings plus opportunistic expired-PCM cleanup. Final validation passed with focused voice/API/provider regressions, `compileall`, Next typecheck, full Python regression (`288 passed, 10 skipped`), no SQLite adapter scan, and screenshot-location scan.
- Provider smoke now uses captured voice artifacts for the live Gemma/Kokoro streaming proof. The voice-streaming step scans recent materialized user voice turns, resolves a safe local `voice-audio` artifact path, verifies SHA-256 when present, sends the captured PCM inline to Gemma, and records the artifact URI/path/hash/byte evidence on the provider-smoke event and ledger. Runs without a usable artifact still use the synthetic silence probe fallback. `Leibniz` review drove recency and IO-race hardening: Postgres and FakeStore now expose latest-first conversation-turn listing, fallback sorting uses `created_at` descending, and artifact stat/read races skip bad candidates instead of failing the smoke flow. Final validation passed with full regression (`288 passed, 10 skipped`), compileall, Next typecheck, no SQLite adapter scan, screenshot-location scan, and `Leibniz` no-blocker review.
- The creator-facing Next voice panel now renders captured-audio provider-smoke proof directly in the product UI. `frontend/next-app/lib/voice/providerSmoke.ts` converts the Gemma/Kokoro streaming step into captured/pending/synthetic proof text, and `RealtimeVoicePanel.tsx` shows artifact path, byte count, short SHA-256, and source turn id when live smoke used a persisted microphone PCM artifact.
- The creator-facing Next voice panel now renders realtime voice timing proof directly in the product UI. `frontend/next-app/lib/voice/timingLedger.ts` converts timing ledger stages and latest-turn metrics into compact UI rows so the app can show measured or missing LiveKit/Gemma/Kokoro/barge-in evidence without raw JSON inspection.
- `Leibniz` timing-proof cleanup was addressed: the voice panel invalidates stale smoke/timing proof requests on run changes and ignores stale completions through a shared run-bound request guard, timing latency formatting now flags negative backend values as invalid, and the frontend race suite includes an executable stale smoke/timing completion simulation.
- Added a local LiveKit dev-server supervisor lane. `LocalLiveKitDevServerSupervisor` can run native `livekit-server --dev` or supervised foreground `docker compose --profile voice up livekit`, FastAPI exposes `/api/local-livekit-process`, and the product voice panel can start/stop transport separately from the Gemma/Kokoro participant before runtime preflight. The docs distinguish this from manual detached Compose mode, and the supervisor cleanup now drains stdout reader tasks explicitly on successful stop.
- Aligned provider smoke with the runtime Kokoro boundary. The Gemma/Kokoro live voice-streaming smoke now accepts either hosted `KOKORO_TTS_ENDPOINT_URL` or a local Kokoro package, instantiates the local TTS streamer when no endpoint is configured, records `kokoro_provider` / `kokoro_transport` proof in the event and ledger, and keeps the HF endpoint path unchanged.
- Centralized Kokoro endpoint normalization across readiness, provider smoke, and the LiveKit runtime streamer, so blank or whitespace endpoint values consistently fall back to local Kokoro instead of pretending a hosted endpoint is configured. The product voice proof card now labels blocked and failed Gemma/Kokoro smoke explicitly instead of calling those states a synthetic fallback.
- Added product-visible Gemma/Kokoro transport proof. `RealtimeVoicePanel.tsx` now renders hosted-vs-local Kokoro transport plus Gemma TTFT, Kokoro first-audio, and end-to-end first-audio latency from the provider-smoke streaming step, and the frontend provider-smoke status types now enumerate known smoke statuses while keeping unknown runtime statuses unproven.
- Tightened the backend provider-smoke contract to match the product proof surface. `ProviderSmokeStepResult.status` and `ProviderSmokeRunResult.status` now use explicit `StrEnum` values, invalid backend statuses fail validation, and JSON responses still serialize statuses as strings for the frontend.
- Added an explicit Kokoro runtime route contract. Readiness and provider-smoke now both report whether Kokoro is using a hosted HF endpoint, a local package, or is missing, and blocked smoke ledgers carry the same route metadata so the product UI can show the missing TTS path without raw JSON inspection.
- Hardened the Kokoro runtime route after `Leibniz` review. Hosted Kokoro readiness now requires a valid `http(s)` endpoint with a host, malformed endpoint config is surfaced as explicit evidence/metadata, local Kokoro can be used as a visible fallback, and the product proof card labels malformed endpoint blockers instead of treating any non-empty string as hosted TTS readiness.
- Closed the Kokoro route fail-fast gap from `Leibniz`: the LiveKit runtime streamer and provider-smoke streamer now refuse to construct local Kokoro when the local package is unavailable, raising a clear configuration error from the shared route contract instead of deferring failure to the first TTS call.
- Added session-bound live voice smoke for the product Gemma/Kokoro path. The provider-smoke request can now reuse an existing LiveKit realtime session, require fresh `gemma_kokoro_voice_agent_ready` evidence for that session, read the latest event tail for long-running runs, and block before Gemma/Kokoro external calls when participant presence is missing or stale. The Next voice panel passes the active session id when live smoke is run from a joined room and warns when live smoke is not session-bound.
- Closed the `Leibniz` review on session-bound smoke: the Gemma/Kokoro smoke turn now uses the active LiveKit session id, room, and participant identity when presence proof is required, and presence lookup uses a targeted `gemma_kokoro_voice_agent_ready` event query so unrelated high-churn events do not hide fresh participant proof.
- Added the Postgres index for targeted voice-agent-ready event lookup: `idx_run_events_run_id_event_type_event_id` supports session-bound smoke presence checks on high-churn long-running runs.
- Strengthened session-bound live voice smoke to require captured user audio from the same realtime session. When `require_voice_agent_presence=true`, provider smoke no longer falls back to a synthetic probe; it blocks before Gemma/Kokoro calls until a persisted `voice-audio` artifact from the bound LiveKit session exists, and the passing proof records the session id on both the smoke turn and audio fixture.
- Closed the same-session audio false-negative cleanup: fixture lookup now scans a larger recent-turn window and can infer the session from the artifact path when older captured-audio records lack explicit `realtime_session_id` metadata, while still rejecting artifacts that do not match the requested LiveKit session.
- Tightened the legacy artifact compatibility fallback after review: path-based session matching now requires the requested realtime session id to appear as an exact path segment, so a path containing `other-<session-id>` cannot satisfy session-bound smoke.
- Improved the product live-smoke flow after the session-bound audio gate. The Next voice panel now sends a fresh LiveKit `voice_agent_presence_probe` and waits briefly for durable Gemma/Kokoro participant proof before requesting session-bound provider smoke, while still recording a blocked ledger if the backend does not observe proof. The proof card now distinguishes `session_audio_artifact_missing` from generic blocked smoke and tells the creator to speak into the active room before rerunning live smoke.
- Closed the follow-up async race from `Leibniz`: stopping a voice session now invalidates in-flight smoke and timing proof requests, the presence wait is cancellable, and generic presence refreshes plus LiveKit data-channel handlers check the active run/session before mutating the product UI.
- Added the creator-app realtime voice state projection. `liveStage.ts` maps LiveKit connection events and Gemma/Kokoro data-channel events into the required state machine: disconnected, connecting, connected, listening, thinking, speaking, interrupting, reconnecting, ended, and failed. The Next voice panel now renders a compact state strip with room, microphone, agent, and raw-audio context-policy signals so voice-to-voice progress is visible without reading the event log.
- Closed the `Leibniz` cleanup on the voice state strip: LiveKit runtime callbacks are now guarded by the active run and realtime session before mutating the visible state machine, and transient connection-state `disconnected` events no longer override an active reconnecting state before the final LiveKit disconnected event.
- Added product-visible interruption acknowledgement. `voice/cancellation.ts` turns interrupt-control requests, Rust `voice_edge_cancellation_acknowledged` events, and Gemma/Kokoro `gemma_kokoro_voice_turn_cancelled` events into a cancellation proof shown in the voice state strip, including Gemma cancel, Kokoro buffer clear, and LiveKit output stop evidence.
- Closed the `Leibniz` cancellation-proof cleanup: late control failures no longer downgrade an already acknowledged runtime cancellation, Stop resets stale cancellation projection, and output-stop evidence also recognizes the `drop_outbound_audio_packets` action naming variant.
- Tightened manual voice barge-in for the product app. `RealtimeVoicePanel` now clears local playback and publishes a LiveKit `voice_interrupt` control to the Gemma/Kokoro participant before recording the durable session-control event; the durable record stores the LiveKit control id or failure. The Python LiveKit participant now emits explicit `voice_manual_interrupt_received` or `voice_interrupt_no_active_response` acknowledgements so the UI can distinguish requested, agent-acknowledged, no-active-output, and fully stopped cancellation states.
- Closed the `Leibniz` trust-boundary blocker on manual LiveKit controls. Presence probes and interrupts now require topic `agent.voice.control`, explicit matching `run_id` and `realtime_session_id`, and the creator participant identity from the LiveKit session state before the Python participant emits ready or cancellation events. Added negative regressions for missing targets, wrong topics, wrong senders, and wrong sessions.
- Added stop-session output cancellation to the creator app. Stopping a provider-backed Gemma/Kokoro LiveKit session now clears local playback, sends a LiveKit `voice_interrupt` control to stop any active agent output, records a durable `stop_output` control with Gemma cancel / Kokoro clear / LiveKit stop actions, and then disconnects/ends the session. The shared control metadata is covered by frontend tests.
- Closed the `Leibniz` stop-output cleanup: `handleStop` now uses a stop-sequence plus run/session ownership guard before committing late UI state, and the API has a regression proving `stop_output` with `create_followup_task=false` records the cancellation contract without creating a control follow-up task.
- Added connected-session voice-agent liveness monitoring in the creator app. While a provider-backed LiveKit room is ready, the panel periodically refreshes durable Gemma/Kokoro participant presence, sends a bounded presence probe when proof is stale/missing/old, updates visible presence state, and guards the monitor with run/session ownership so stale checks cannot repaint a newer session.
- Added creator-facing LiveKit microphone control to the real voice panel. `RealtimeVoicePanel` now exposes Mute/Unmute only for active provider-backed rooms, calls `livekitRuntime.setMicrophonePublishing()`, keeps in-flight mic operations run/session plus sequence scoped so Stop/run changes invalidate late completions, updates the state strip from LiveKit local-track mute/unmute/publish/unpublish signals, and avoids duplicate success log entries for each toggle.
- Added live voice captions to the actual creator voice panel. Gemma `assistant_text_delta` events now carry safe text deltas, committed user voice events include transcript when available, and `RealtimeVoicePanel` projects user/agent captions from LiveKit data-channel events so the back-and-forth voice loop is visible before the refreshed durable conversation history lands. The caption reducer correlates same-session events by turn/response id so late old-response deltas cannot repaint a newer turn after barge-in.
- Added mixed-mode live dialogue to the actual Gemma/Kokoro product voice panel. A creator can now send typed text into the active provider-backed LiveKit room through the same `agent.voice.control` data channel as presence and interrupt controls; the Python LiveKit participant requires matching run id, realtime session id, participant identity, and topic before scheduling Gemma/Kokoro. Typed turns use raw UUID turn ids for durable materialization, reject empty direct transcript payloads, emit `voice_user_turn_committed` with `input_modality=text`, and materialize into the run history as text turns while assistant speech still returns through the same voice-agent event path. The UI form is disabled until the LiveKit room is joined and the Gemma/Kokoro agent participant has fresh presence proof, and it sends an interrupt control before typed input when the agent is already thinking or speaking.
- Added product-visible source ledger transparency. The creator app Source panel now shows claim support counts, source provenance badges, freshness/search rank/query metadata, published/retrieved timestamps, snippets, and explicit warnings when a source is only a search seed that still needs provider-backed web research before publish. This makes source-backed drafts auditable in the actual app rather than only through backend ledgers or planning notes.
- Added product-visible draft evidence visibility. Each generated draft card now shows source count, supported/review/unsupported claim counts, reviewer decision, revision count, and deduplicated linked-claim count so source and guardrail risk is visible at the point of review. Claim linkage prefers explicit artifact `claim_ids` from content/provenance before falling back to source overlap. Verified on a real local FastAPI/Postgres run `efa8b3a7-021c-47a8-ad01-1d20b812a460`; browser evidence is in `screenshots/next-product-app-draft-evidence-summary.png` and `screenshots/next-product-app-draft-evidence-summary-playwright.md`.
- Added product-visible source-refresh action. When the Source panel contains search-query seeds, the actual Next.js creator app now shows live-source/search-seed counts and a `Run web research` action. The action creates a targeted A2A `research_topic` task for `web-research-agent` when no runnable `research_topic` task exists, then runs only that worker without Gemma fallback. Review cleanup tightened runnable detection to task type plus `accepted`, `claimed`, and `in_progress` status so unrelated, failed, blocked, or human-waiting tasks cannot suppress a fresh research enqueue. Browser evidence is in `screenshots/next-product-app-source-refresh-action.png`.
- Added product-visible source-refresh status. The Web Research Agent now records an explicit `web_search_provider_blocked` result and `web_research_blocked` event when provider-backed search is not configured and Gemma fallback is disabled, while still building a research freshness ledger. The actual Source panel reads recent web-research task/event proof and shows `Research blocked` with the provider reason or `Research refreshed` with accepted-source count, so the creator can tell whether the source seed was replaced or the environment needs search credentials.
- Added source-refresh to claim-verification handoff. When provider-backed web research replaces weak/search-seed source dependencies and repaired claims need review, Web Research Agent now creates an idempotent `verify_source_refresh_claims` A2A task for Claim Verification Agent unless a runnable claim-verification task already exists. The product `Run web research` action now runs a bounded two-agent cycle for Web Research Agent and Claim Verification Agent, so refreshed sources and repaired claim dependencies can be rechecked in the same creator action instead of waiting for a separate manual worker pass.
- Closed source-refresh review cleanups from `Leibniz`: Web Research Agent now honors capped, deduped `search_queries` supplied by the product refresh action instead of searching only the first topic, reports skipped query count when the cap is reached, and the Source panel counts only unresolved search seeds so repaired historical seed records do not keep the refresh affordance visible after live sources replace them.
- Added a product voice launch checklist to the actual Gemma/Kokoro voice panel. The checklist derives the first blocking prerequisite across content run, LiveKit transport, runtime readiness, local Gemma/Kokoro agent process, joined room, and participant presence. `Check setup` refreshes LiveKit, agent process, runtime preflight, and participant proof together, and `Resolve next` invokes the matching next action so blocked voice-to-voice startup is visible and actionable without raw log inspection.
- Added durable voice setup proof for the product launch checklist. `POST /api/runs/{run_id}/voice-setup-proof` records each setup check or resolver attempt as a `voice_setup_proof` artifact plus `voice_setup_proof_recorded` timeline event, and the Next voice panel writes those proofs after `Check setup`, `Resolve next`, and guarded failure paths.
- Made `voice_setup_proof` artifacts readable in the actual creator app. The draft/artifact board now renders setup proofs as compact operational evidence with status, action, provider, transport, blocker, runtime readiness, LiveKit process, Gemma/Kokoro process, and captured-step count instead of dumping raw JSON into the product UI, and proof artifacts are copy/export-only rather than draft-revision targets.
- Separated creator content from operational proof in the product artifact board. The default artifact view now shows generated content artifacts first, adds a `Proofs` filter for ledgers/proofs, and restricts revision selection to content artifacts only so operational evidence remains browseable/copyable/exportable without being queued as draft edits.
- Closed the content/proof artifact-board review with `Leibniz`: page-level production actions now use content-filtered selected artifacts, non-content selections are ignored before revision/media/distribution/readiness calls, regression assertions lock that boundary, the filter aria label now matches artifact terminology, and `Leibniz` reported no blockers after validation.
- Added source-ledger drilldown to the actual product Source panel. The panel now consumes context `source_evidence`, summarizes accepted retrieval evidence coverage, and shows each source's accepted/not-accepted context status, rank, rerank score, reranker, quality/freshness, coverage topics, rerank reason, and precision/recall risks without requiring raw JSON inspection.
- Added the provider-backed voice release gate to the actual product voice panel. The UI now consumes `/api/provider-readiness`, combines selected Gemma/HF, Gemma/Kokoro realtime, selected web-search, reranker, runtime preflight, participant presence, and live provider-smoke status, and refuses to call the voice path ready until live Gemma/Kokoro provider smoke has provider-backed proof. The missing-env summary is scoped only to selected release-gate providers, so unused OpenAI/Cartesia/ElevenLabs providers do not pollute the Gemma/Kokoro path.
- Tightened the release gate after `Leibniz` review: current-session readiness now requires an active provider-backed LiveKit session, participant proof for that session, and provider smoke bound to that same session. Older durable run-level smoke remains useful evidence but cannot mark the current room ready.
- Added safer local secret-file paths for provider-backed Gemma and web search. `Settings` now supports `HF_TOKEN_FILE` and `TAVILY_API_KEY_FILE`, `.env.example` documents both routes, `.gitignore` ignores `.secrets/`, and no supplied token/key is written to tracked files or tool output. Direct env vars still override file values when explicitly set.
- Hardened the secret-file route after `Leibniz` review: direct HF/Tavily secret values are stripped and blank values fall back to the secret file instead of falsely counting as configured; missing or empty secret files do not throw and leave provider readiness blocked.
- Added product-visible secret-file diagnostics to provider readiness. The backend now includes non-secret file status records for selected HF/Tavily file paths, and the actual voice release gate renders `HF_TOKEN_FILE` / `TAVILY_API_KEY_FILE` as loaded, missing, empty, unreadable, or not configured without exposing values. Browser proof is in `screenshots/provider-release-gate-secret-files.png` and `screenshots/provider-release-gate-secret-files-detail.md`.
- Added Hugging Face router fallback for Gemma text/chat expert agents. Dedicated Gemma endpoint URLs still win when configured, but text expert work can use the official `https://router.huggingface.co/v1/chat/completions` route with model ids when only HF token auth is available. Realtime native-audio smoke remains stricter and still requires an audio-capable Gemma endpoint plus Kokoro proof.
- Hardened the HF router fallback after `Leibniz` review: malformed or blank router URLs no longer count as configured, provider readiness reports the router env as missing, and runtime endpoint selection ignores invalid router values instead of failing later inside the HTTP client.
- Hardened the Hugging Face Gemma expert provider boundary. The provider now supports injected HTTP transports for no-network regression tests, wraps HTTP status failures, network/request errors, invalid JSON, non-object JSON, and no-usable-content responses into `ProviderConfigurationError`, and blocks malformed success payloads instead of letting raw `httpx` or payload-shape errors leak into orchestration. `Leibniz` found and we fixed malformed `choices` shape leakage before closeout. Verified with focused provider tests (`28 passed`), Gemma/voice contract tests, compileall, default regression (`346 passed, 10 skipped`), live Postgres regression (`356 passed`), no-SQLite scan, secret scan, screenshot-location scan, and whitespace check.
- Hardened the realtime Gemma/Kokoro adapter provider boundary. Streaming Gemma calls now wrap HTTP status and request failures into `ProviderConfigurationError`, block streams that finish without usable text, block whitespace-only streams after `Leibniz` review, and support injected transports for no-network tests. Hosted Kokoro TTS now wraps HTTP/request failures, rejects invalid JSON, invalid base64 audio, and empty audio bodies, and keeps provider secrets out of errors. Verified with voice adapter tests (`46 passed`), focused provider/voice-edge/provider-smoke checks (`46 passed, 1 skipped` for sandbox loopback), compileall, default regression (`355 passed, 10 skipped`), live Postgres regression (`365 passed`), no-SQLite scan, secret scan, screenshot-location scan, and whitespace check.
- Added durable voice-turn failure handling for Gemma/Kokoro provider blockers. `GemmaKokoroLiveKitAgentEngine` now catches normalized provider errors, clears Kokoro/output buffers, stops LiveKit response audio, emits a safe `gemma_kokoro_voice_turn_failed` event with failure stage and counts, and returns a failed result instead of letting the LiveKit turn task crash. The LiveKit participant records the user turn with failure metadata and does not add a fake assistant response. Verified with voice-agent tests (`49 passed`), focused provider/voice-edge/provider-smoke checks (`46 passed, 1 skipped` for sandbox loopback), compileall, default regression (`358 passed, 10 skipped`), live Postgres regression (`368 passed`), no-SQLite scan, secret scan, screenshot-location scan, and whitespace check.
- Made failed Gemma/Kokoro voice turns visible at the product boundary. Persisted `gemma_kokoro_voice_turn_failed` events now feed the realtime voice timing ledger as a failed status with provider-fix next action, timing turn entries carry failure stage/reason/timing, the Next live voice state strip moves to `Failed`, and live captions show the failed stage/reason instead of leaving the creator in a stale thinking state. The voice-events API records failed events without materializing fake assistant turns or follow-up tasks. Verified with focused timing/API/voice tests (`54 passed`), Next race/UI projection tests (`92 passed`), Next typecheck/lint/build, compileall, default regression (`360 passed, 10 skipped`), live Postgres regression (`370 passed`), no-SQLite scan, secret scan, screenshot-location scan, and whitespace check.
- Routed failed Gemma/Kokoro voice turns into the autonomous A2A loop. `gemma_kokoro_voice_turn_failed` now creates an idempotent `review_realtime_provider_failure` task for `inference-systems-engineer` with redacted failure metadata, provider/session context, and required recovery action, while still avoiding fake assistant turn materialization. Duplicate failed events for the same response id do not create duplicate recovery tasks. `Leibniz` found and we fixed the event-ledger trust boundary, so shared realtime metadata recursively redacts bearer, HF, and Tavily-shaped secrets before durable event persistence as well as before follow-up task creation, including nested list string values. Verified with focused API/timing/voice tests (`54 passed`), realtime secret-redaction checks (`3 passed`), inference worker smoke (`1 passed`), Next projection tests (`92 passed`), Next typecheck/lint/build, compileall, default regression (`360 passed, 10 skipped`), live Postgres regression (`370 passed`), no-SQLite scan, secret scan, screenshot-location scan, and whitespace check.
- Made the queued provider-failure task executable instead of generic. Inference Systems Engineer now handles `review_realtime_provider_failure` by recording a `realtime_provider_failure_recovery` provider-operations ledger with failed component classification, required readiness/smoke/timing checks, blocked fallback policy, redacted metadata, and deterministic follow-up tasks for Observability Agent runtime health plus Agent Harness checkpoint/resume planning. `Leibniz` found and we fixed the manual-task context-summary leak, so project-memory fallback queries redact bearer/HF/Tavily-shaped strings before `context_packet_built` or recovery artifacts can persist them. Focused worker/voice tests passed (`57 passed`), compileall passed, secret scan and no-SQLite adapter/fallback scan were clean.
- Made provider-failure recovery visible in the actual product artifact board. `provider_operations_ledger` artifacts with `realtime_provider_failure_recovery` content now render as compact Proof cards showing blocked status, failed component, provider/transport, redacted failure reason, recovery-check count, required-action count, owner agents, and blocked fallback instead of raw JSON. Proof filtering keeps the recovery card out of revision selection, and browser evidence is saved at `screenshots/provider-recovery-proof-card.png`.
- Made provider-release blockers first-class product voice setup blockers. The launch checklist now consumes the provider release gate, so a ready LiveKit room plus fresh agent presence cannot show `ready` while selected Gemma/HF, realtime, web-search, reranker, provider-failure recovery, or live Gemma/Kokoro smoke checks still block release. `Resolve next` can refresh provider readiness for selected-provider blockers or run live Runtime smoke for recovery/smoke blockers, and the smoke action forces the explicit live-smoke confirmation path. Verified with focused voice setup tests (`102` pass), Next typecheck, full frontend tests (`102` pass), full Python regression (`363 passed, 10 skipped`), live Postgres regression (`9 passed`), lint/build, compileall, secret scan, screenshot-location scan, no-SQLite adapter scan, and whitespace check.
- Tightened live-smoke resolver proof after review. `Resolve next -> Run live smoke` now returns a structured smoke outcome, rebuilds the provider release gate from the new smoke result, persists a refreshed voice setup proof with the post-smoke checklist/blocker, and converts the just-built smoke result into provider-smoke-ledger-shaped evidence so provider-failure recovery clears immediately instead of waiting for React artifact props to refresh. Verified with focused release-gate/setup tests (`103` pass), Next typecheck, full frontend tests (`103` pass), full Python regression (`363 passed, 10 skipped`), live Postgres regression (`9 passed`), lint/build, compileall, secret scan, screenshot-location scan, no-SQLite adapter scan, whitespace check, and final `Leibniz` no-actionable-findings review.
- Proved and documented the no-skip backend regression path. Default Python still reports the expected infrastructure skips, but with local Postgres reachable and loopback binding allowed the host-context command `LIVE_POSTGRES=1 python -m pytest -q -rs` runs the live Postgres suite plus the real Rust voice-edge HTTP sidecar smoke with `373 passed` and no skips. README now documents this command and explains when the sidecar smoke can still skip in sandboxed contexts.
- Added run-scoped idle proof for the always-on worker scheduler. When the API/CLI/browser auto wake asks the scheduler to check one run and no profile is due, the backend now records a durable `worker_scheduler_pass_completed` event with `idle_reason=no_due_profiles`, requested run/mode, zero counters, and empty proof lists; unscoped global scheduler checks still stay quiet. The product Activity panel now recognizes that scoped idle event as Scheduler proof for the active Autopilot run, so the creator can see that the wake check ran even when no heartbeat was needed. Verified with focused scheduler/UI tests, full Python regression (`366 passed, 10 skipped`), full host-context regression (`376 passed`), full frontend tests (`116 passed`), Next typecheck/lint/build, compileall, no-SQLite scan, secret scan, screenshot-location scan, and whitespace check.
- Wired live run-event streaming into the actual Next creator app. The app now consumes `/api/runs/{run_id}/events/stream` via a fetch-based SSE parser so custom backend event names are not lost, merges new `RunEvent` records into `context.recent_events`, reconnects after disconnect, guards updates by run/version ownership, and shows compact `Live events` status in Activity. Verified with parser/render tests, full frontend tests (`119 passed`), full Python regression (`366 passed, 10 skipped`), host-context live regression (`376 passed`), Next typecheck/lint/build, compileall, no-SQLite scan, secret scan, screenshot-location scan, whitespace check, and browser smoke evidence at `screenshots/next-product-app-live-event-stream-smoke.md` plus `screenshots/next-product-app-live-event-stream-smoke.png`.
- Added debounced full context refresh from live run events. Streamed high-level durable mutation events now schedule a silent `getRunContext` plus `listWorkerProfiles` refresh so autonomous artifacts, agent messages, sources, claims, feedback, and worker profile changes appear without manual Refresh; high-frequency voice text/audio events are ignored, and refreshes preserve current busy/error state while remaining run/version guarded. `Leibniz` found and we fixed non-suffix durable task events (`agent_message_dependency_waiting`, `agent_message_recovered`, `agent_message_retry_exhausted`) so those also refresh task state. Verified with focused stream-refresh tests, full frontend tests (`122 passed`), full Python regression (`366 passed, 10 skipped`), host-context live regression (`376 passed`), Next typecheck/lint/build, compileall, no-SQLite scan, secret scan, screenshot-location scan, whitespace check, and browser smoke evidence at `screenshots/next-product-app-stream-refresh-smoke.md` plus `screenshots/next-product-app-stream-refresh-smoke.png`.
- Added direct voice-event persistence readiness proof for the Gemma/Kokoro LiveKit path. Runtime readiness now includes a non-required backend event-sink check with safe URL validation, bounded timeout validation, and optional FastAPI `/health` preflight; provider release and setup gates treat degraded optional persistence as usable while preserving degraded proof metadata. Verified with focused readiness tests, full frontend tests (`124 passed`), Next typecheck/lint/build, default Python regression (`374 passed, 10 skipped`), live Postgres regression (`384 passed`), compileall, and two `Leibniz` no-finding reviews.
- Hardened direct voice-event sink startup safety. The backend sink adapter now shares the readiness URL boundary, rejects unsafe origins before posting, and the LiveKit participant falls back to data-channel-only delivery if optional sink configuration is malformed. Verified with focused sink/runtime tests, full frontend tests (`124 passed`), Next typecheck/lint/build, default Python regression (`376 passed, 10 skipped`), live Postgres regression (`386 passed`), compileall, and `Leibniz` no-actionable-findings review.
- Added reranked provider-backed source refresh in the real creator app path. Web Research Agent now dedupes provider results with URL canonicalization, uses the configured reranker or deterministic fallback before recording `worker_web_search_result` sources, preserves original provider `search_rank`, records rerank rank/score/provider/reason in source metadata and web-research proof, redacts reranker and search-provider failure reasons, and the Source panel shows source-level rerank proof. Validation passed focused backend tests (`7 passed`), full Python regression (`385 passed, 11 expected infra skips`), frontend tests (`145 passed`), Next typecheck/lint/build, compileall, no-SQLite scan, generic secret scan, screenshot-location scan, whitespace check, and final `Leibniz` no-actionable-findings verification after fixes.
- Added accepted-evidence source-refresh closure. Successful provider-backed source refresh now builds a retrieval-quality ledger after source repair and before Claim Verification handoff, so the same creator action enforces accepted retrieval evidence rather than raw source ids. Zero-accepted ledgers remain strict gates, and retrieval-quality fallback is limited to reranker-call failures. Validation passed focused tests (`11 passed`), full Python regression (`387 passed, 11 expected infra skips`), frontend tests (`145 passed`), Next typecheck/lint, compileall, hygiene scans, and final `Leibniz` verification with no remaining actionable issues.
- Tightened the standing `Leibniz` review protocol after user feedback. Future review requests must be one complete, deep, whole-slice senior pass with enough investigation budget to catch stale-state, contract, UI, provider-boundary, test-design, and bloat issues together; follow-up reviews are for verifying fixes or reviewing newly written fix code only.
- Added durable LiveKit media-bridge proof to realtime voice timing. The Python participant now emits `voice_agent_media_bridge_ready` after audio-track subscription, the timing ledger requires `livekit_audio_track_bridge` before `ready`, and session scoping prevents older complete sessions or late bridge events from satisfying the current room. `Leibniz` found stale/late proof and cleanup risks in the first deep pass; both were fixed with regressions and verified closed. Validation passed focused timing/API/voice tests (`65 passed`), full backend regression (`391 passed, 11 expected infra skips`), compileall, no-SQLite scan, generic secret scan, screenshot-location scan, whitespace check, and `git diff --check`.
- Improved the product voice proof UI for media-bridge timing. Missing `livekit_audio_track_bridge` now renders as creator-facing bridge/action copy instead of raw `voice_agent_media_bridge_ready`, and failed timing ledgers prefer provider recovery over media-bridge retry even when backend recommended actions list the media bridge first. Validation passed frontend race tests (`149 passed`), Next typecheck/lint/build, hygiene scans, and `Leibniz` verification.
- Tightened live-stage projection for media-bridge readiness. `voice_agent_media_bridge_ready` now moves only `connected` to `listening`; late bridge events preserve `thinking`, `speaking`, and `interrupting`. Focused frontend tests, typecheck, lint, whitespace check, and `Leibniz` verification passed.
- Split growth-agent execution into distinct specialist artifacts. Platform Optimization now owns the canonical `social_package`; Influencer Strategy and Outreach now record separate retry-idempotent `growth_strategy` artifacts with provenance, source/claim dependencies, reviewer decisions, and product `Growth` filtering. `Leibniz` found and the implementation fixed package-initiator provenance, source-repair membership/content updates, and compact product rendering for `growth_strategy`. Validation passed targeted growth/source-repair tests (`3 passed`), full backend regression (`397 passed`, `11` expected infra skips), Next typecheck/lint, frontend race suite (`150 passed`), compileall, no-SQLite scan, generic secret scan, screenshot-location scan, whitespace check, and final `Leibniz` verification.
- Added and hardened the product-level Growth package action. The Next production panel now calls `POST /api/runs/{run_id}/growth-package`, which packages platform variants and materializes influencer/outreach strategy artifacts plus completed A2A message proof. `Leibniz` found stale same-source package reuse, first-call idempotency, missing-run status, and invalid-initiator issues in one deep pass; all were fixed with focused regressions. Validation passed focused growth/API regressions (`5 passed`), full backend regression (`401 passed`, `11` expected infra skips), frontend race suite (`150 passed`), Next typecheck/lint, compileall, no-SQLite scan, generic secret scan, screenshot-location scan, whitespace check, and final `Leibniz` no-actionable-findings verification.
- 2026-05-19: Hardened the product revision feedback loop. Revision feedback, routed A2A tasks, revised artifacts, review decisions, and gate events now use deterministic ids and idempotent proof events; Postgres has advisory-locked `append_event_if_absent` with live coverage. Replays after partial persistence repair missing proof events, explicit and legacy revision targets cannot revise operational artifacts, repeated legacy replays converge on original parent artifacts, and Gemma revision fallback errors are redacted before durable `provider_fallback` events. Final validation: backend `405 passed, 11` expected infra skips, live Postgres `10 passed`, compileall, frontend gates from the unchanged UI slice, no-SQLite adapter scan, generic secret scan, screenshot-location scan, whitespace check, and final `Leibniz` confirmation.
- 2026-05-19: Added the product `Plan next` work-plan action. The Next Activity panel calls the run work-plan API, renders compact next agent actions, restores saved `run_work_plan_v1` proof after refresh, and stays outside board-style project-management/planning-cockpit territory. `Leibniz` found stale live-vs-durable proof ordering and the inverse stale-artifact case; both are now covered by regressions. Final validation: frontend race suite `156 passed`, Next typecheck/lint/build, backend regression `405 passed, 11` expected infra skips, live Postgres `10 passed`, no-SQLite adapter scan, generic secret scan, screenshot-location scan, whitespace check, and `Leibniz` closure.
- 2026-05-19: Added product `Run plan` execution for work plans. The Activity rail now runs only the current work plan's recommended/owner specialists through the existing guarded worker-cycle path, with run mismatch and zero-item protections. `Leibniz` found a zero-item helper-contract gap; fixed with a regression. Final validation: frontend race suite `160 passed`, Next typecheck/lint/build, backend regression `405 passed, 11` expected infra skips, live Postgres `10 passed`, no-SQLite adapter scan, generic secret scan, screenshot-location scan, browser render check, and `Leibniz` closure.
- 2026-05-19: Made product `Run plan` materialize work before execution. The click now calls the work-plan endpoint with `create_followup_tasks=true`, stores the materialized plan, and then runs only the materialized plan's agents. `Leibniz` found a race where materialization happened before the worker-cycle lock; fixed by acquiring `agentCycleInFlightRef` before task materialization and using an internal lock-free cycle runner under that lock. Final validation: frontend race suite `164 passed`, Next typecheck/lint/build, backend regression `405 passed, 11` expected infra skips, live Postgres `10 passed`, no-SQLite adapter scan, generic secret scan, screenshot-location scan, and `Leibniz` closure.
- 2026-05-19: Closed the product plan-to-execution feedback loop. After `Run plan` materializes tasks and runs the targeted specialists, it now rebuilds a post-run next-action plan without creating another task batch, using `refresh_reason=creator_app_after_run_plan`, and replaces the Activity rail plan with that next state. The whole sequence remains under the same agent-cycle lock. Final validation: frontend race suite `165 passed`, Next typecheck/lint/build, backend regression `405 passed, 11` expected infra skips, live Postgres `10 passed`, no-SQLite adapter scan, generic secret scan, screenshot-location scan, and `Leibniz` no-actionable-findings review.
- 2026-05-19: Added compact work-plan execution proof to the product Activity rail. The rail now shows manual/execution/post-run-refresh labels plus created task, duplicate skip, open feedback, and routed feedback counters when present. Final validation: frontend race suite `167 passed`, Next typecheck/lint/build, backend regression `405 passed, 11` expected infra skips, live Postgres `10 passed`, browser render check, no-SQLite adapter scan, generic secret scan, screenshot-location scan, and `Leibniz` no-actionable-findings review.
- 2026-05-19: Added recent completed specialist outcomes to the product Activity rail. Completed A2A messages now render as a compact latest-four outcome strip with safe summaries, artifact-count and generation-mode fallback text, and no raw nested result JSON; pending tasks remain separate. Validation passed frontend race suite `168 passed`, Next typecheck/lint/build, backend regression `405 passed, 11` expected infra skips, browser render/console check, no-SQLite adapter scan, generic secret scan, screenshot-location scan, whitespace check, and `Leibniz` no-actionable-findings review.
- 2026-05-19: Added artifact navigation from recent specialist outcomes. Activity outcome cards now link produced artifact chips to stable DraftBoard targets, dedupe duplicate artifact ids across result fields, and keep revision selection unchanged. DraftBoard hash navigation reveals hidden target artifacts by switching to the right artifact filter before scrolling. `Leibniz` found and verified fixes for missing `created_artifact_ids` / singular `strategy_artifact_id` extraction and same-hash recovery after filter changes. Final validation passed frontend race suite `169 passed`, Next typecheck/lint/build, compileall, backend regression `405 passed, 11` expected infra skips, live Postgres `10 passed` after local-network permission, browser render/console check, no-SQLite adapter scan, generic secret scan, screenshot-location scan, vault forbidden-word scan, and whitespace check.
- 2026-05-19: Added failed/blocked task visibility plus targeted retry execution to the product Activity rail. Failed, blocked, and canceled specialist tasks now show in a latest-first `Needs attention` strip with redacted summaries and a guarded `Queue and run` action backed by the existing A2A retry endpoint plus a bounded message-targeted worker cycle. `Leibniz` found duplicate-submit, runtime secret-leak, misleading-label, and stale same-recipient inbox execution risks; fixed with pre-POST guards, button-level disabled state, redaction regressions, label correction, `message_ids` worker-cycle targeting, and backend refusal to fall back to unrelated inbox/default-roster work for target-message retries. Final validation passed frontend race suite `177 passed`, Next typecheck/lint/build, backend regression `407 passed, 11` expected infra skips, live Postgres `10 passed`, compileall, browser desktop/mobile render checks, no-SQLite scan, secret scan, screenshot-location scan, vault forbidden-word scan, and `git diff --check`.
- 2026-05-19: Added and hardened the public A2A discovery adapter. `/.well-known/agent-card.json` now exposes the local Agent Studio as an A2A 0.3 discovery-compatible HTTP+JSON surface without claiming full public A2A execution, and `/api/a2a` now advertises method-aware endpoint records. `Leibniz` found two contract risks in one deep pass: the old `messageStatus` name implied a GET lookup for a POST mutation, and the public card overclaimed streaming/state-history/raw audio-image modes. Fixed by renaming the advertised mutation to `updateMessageStatus`, validating every advertised endpoint method against OpenAPI, limiting public modes to JSON/text, and moving internal realtime media modes under `x-agentStudio`. Validation: focused A2A regressions `4 passed`, broader A2A/foundation set `8 passed`, full backend regression `430 passed, 11` expected infra skips, compileall, no-SQLite scan, generic secret scan, whitespace check, and final `Leibniz` no-actionable-findings verification.
- 2026-05-19: Added read-only A2A message lookup. `/api/a2a/messages/{message_id}` now returns the current durable `AgentMessage` without appending audit events or mutating task state, and `/api/a2a` advertises it as method-aware `getTask`. Regression coverage proves the lookup stays read-only before and after a status update, returns 404 for unknown ids, and every advertised endpoint method/path matches OpenAPI. Validation: focused route/discovery tests `4 passed`, broader A2A/foundation set `10 passed`, full backend regression `431 passed, 11` expected infra skips, compileall, no-SQLite scan, generic secret scan, whitespace check, and `Leibniz` no-actionable-findings review with the residual note that remote exposure needs auth/redaction for raw payload/result/error fields.
- 2026-05-19: Added redacted public projection for A2A task lookup. `GET /api/a2a/messages/{message_id}?projection=public` now returns `AgentMessagePublicProjection` with sanitized payload, result, error, and handoff trace while private/raw remains the default trusted-local projection. `Leibniz` found and verified a high-severity redaction gap: initial key matching missed camelCase and token-suffixed fields such as `apiKey`, `rawResponse`, `signedUrl`, and `session_token`; the fix normalizes camelCase/separators, compares compact key forms, and blocks sensitive suffixes. Validation: focused redaction/read-only tests `3 passed`, broader A2A/foundation set `12 passed`, full backend regression `433 passed, 11` expected infra skips, compileall, no-SQLite scan, generic secret scan, whitespace check, and final `Leibniz` no-actionable-findings verification.
- 2026-05-19: Made provider credentials local-secret-file first. `Settings` now defaults to ignored `.secrets/hf_token` and `.secrets/tavily_api_key`, `.env.example` points at those paths, provider readiness reports `loaded`, `missing`, `empty`, `unreadable`, or `direct_env` without exposing values, and direct env secrets remain explicit overrides. `Leibniz` found and verified fixes for two issues: `.secrets/README.md` was ignored by the old broad `.secrets/` rule, and one missing-HF-token readiness test used environment-dependent plain `Settings()`. Validation: focused provider-readiness tests `8 passed`, broader provider/foundation set `9 passed`, backend regression `434 passed, 11` expected infra skips, frontend typecheck/lint/race suite `181 passed`/build, compileall, no-SQLite scan, generic secret scan, whitespace check, and final `Leibniz` verification.
- 2026-05-19: Added non-secret provider credential guidance to the actual Gemma/Kokoro voice setup surface. The provider release gate now derives selected-provider `secretFileGuidance` for loaded, direct-env override, missing, not-configured, empty, unreadable, and fallback secret-file states, and the Next voice proof panel renders concrete setup actions without exposing token values or adding planning UI. Validation passed frontend race suite `184 passed`, Next typecheck/lint/build, browser smoke with screenshot `screenshots/voice-provider-secret-guidance.png` and zero new console warnings/errors, no-SQLite scan, generic secret scan, touched-file whitespace scan, screenshot-location scan, `git diff --check`, and a deep whole-slice `Leibniz` review with no actionable findings.
- 2026-05-19: Aligned backend provider-readiness `next_actions` with the local secret-file-first contract. Missing HF/Tavily secret values now use matching secret-file diagnostics for create-file, fill-empty-file, fix-unreadable-file, or direct-env/file-env setup guidance instead of stale `.env/process` copy; non-secret missing env values keep generic env guidance. `Leibniz` found environment-dependent tests in the first review, now fixed by clearing relevant env/file-env vars and disabling `.env` in the new regressions. Validation passed focused provider next-action tests, an env-contaminated reproduction run, broader provider-readiness tests `29 passed`, full backend regression `436 passed, 11` expected infra skips, compileall, frontend race suite `184 passed`, no-SQLite scan, generic secret scan, whitespace scan, `git diff --check`, and final `Leibniz` no-actionable-findings verification.
- 2026-05-19: Removed stale cockpit wording from shared provider-readiness `next_actions` consumed by the product app. Ready realtime providers now say to create the realtime session from the product voice panel, and generic provider fallback says provider-specific smoke step while the separate `/cockpit` demo and `cockpit_action` smoke-plan contracts remain intact. Validation passed the failing-first product-surface wording regression, targeted provider/foundation tests `17 passed`, full backend regression `437 passed, 11` expected infra skips, compileall, frontend race suite `184 passed`, no-SQLite scan, generic secret scan, screenshot-location scan, whitespace scan, `git diff --check`, and `Leibniz` no-actionable-findings review.
- 2026-05-19: Routed provider-smoke configuration blockers into deterministic A2A recovery. Configuration-failed smoke steps now create one secret-safe `review_provider_configuration_blockers` task with non-secret blocker/setup payload, specialist routing, and collaborator recommendations; the smoke result exposes `provider_configuration_followup_message_ids`. `Leibniz` found provider-exception secret leakage and orphan ledger references after artifact-write failure; fixed with source redaction, deterministic blocked-ledger artifact ids, idempotent artifact persistence, frontend type alignment, and regressions for redaction plus replay convergence. Validation passed provider-smoke tests `16 passed`, targeted provider/A2A/foundation tests `39 passed`, full backend regression `440 passed, 11` expected infra skips, compileall, frontend race/type tests `184 passed`, hygiene scans, `git diff --check`, and `Leibniz` fix verification with no actionable findings.
- 2026-05-19: Made `review_provider_configuration_blockers` executable by the autonomous worker loop. Inference Systems Engineer and Agent Harness Engineer now record deterministic `provider_configuration_recovery` ledgers, keep provider operation blocked until configuration is repaired and provider smoke is rerun, and create deterministic Observability runtime-health plus Agent Harness checkpoint/resume follow-ups. Failing-first regressions cover parent plus child execution, stale same-recipient targeting, replay after partial completion failure, web-search owner split, and non-patterned credential/authorization metadata redaction across result/artifact/children/events. Validation passed focused recovery tests `3 passed`, targeted recovery/A2A/provider tests `52 passed`, frontend race suite `184 passed`, compileall, host-context live Postgres full regression `454 passed` with no skips, hygiene scans, and final `Leibniz` no-actionable-findings verification.
- 2026-05-19: Made provider-configuration recovery proof visible in the creator app. DraftBoard now renders `provider_configuration_recovery` provider-operations ledgers as compact Proof cards with status, provider ids, provider-smoke transport, provider-configuration component, blocker reason, recovery counts, owners, and blocked fallback while keeping the artifacts non-selectable for revision and avoiding raw workflow JSON. Validation passed the failing-first UI regression, frontend race suite `185 passed`, Next lint/build, focused backend provider-smoke/recovery tests `19 passed`, host-context live Postgres full regression `454 passed`, hygiene scans, and `Leibniz` no-actionable-findings review.
- 2026-05-19: Made provider-configuration recovery enforce the product voice release gate. Newer `provider_configuration_recovery` ledgers now block voice readiness over stale older live-smoke proof, clear only after newer same-session provider-backed smoke, and surface as `Provider configuration recovery` in the setup checklist with the live-smoke resolver action. `Leibniz` found and verified the setup-label fix after the release-gate implementation. Validation passed frontend race suite `188 passed`, Next lint/build, focused backend provider-smoke/recovery tests `19 passed`, host-context live Postgres full regression `454 passed`, hygiene scans, and final `Leibniz` no-actionable-findings verification.
- 2026-05-19: Added and enforced the retrieval-diversity recall gate. Retrieval-quality ledgers now block `ready` when accepted evidence came from one query/retriever cluster or cannot prove query/retriever metadata; downstream gates withhold accepted evidence until source evidence is ready; clean accepted evidence can still support claim verification when only rejected candidates carry rerank precision risks; publish readiness records `retrieval_quality_not_ready`; Autopilot blocks non-ready retrieval quality; source-ledger snapshots surface non-ready retrieval status; and both worker web-search plus initial provider-backed content sources persist `retriever=web_search`. Validation passed focused diversity/metadata/downstream regressions, broader retrieval/source-refresh/project-memory/autonomous-pass/publish tests `20 passed`, frontend race suite `188 passed`, compileall, default backend `447 passed, 11 expected infra skips`, live Postgres `458 passed`, and hygiene scans. Process correction: `Leibniz` reviews must be deep whole-slice reviews with stale-state, replay/idempotency, provider-boundary, UI/backend contract, secret, test-design, and bloat checks in one consolidated first pass; final diversity-slice closure returned no actionable findings.
- 2026-05-19: Tightened retrieval-quality graph traversal against accepted evidence. Supported claim nodes and content artifact nodes now require their source dependencies to overlap accepted retrieval sources before graph coverage counts as covered; otherwise they become graph gaps and keep the ledger `needs_more_recall`, even when enough unrelated accepted sources exist elsewhere in the run. Accepted source evidence still exports for claim verification when the remaining non-ready reason is claim/artifact rewrite/verification, so the verifier can repair the graph instead of losing good evidence. `Leibniz` found a current-contract false-gap risk for artifacts using `content.claim_ids` or `provenance.claim_ids`; fixed with a defensive artifact claim-id extractor that also accepts `claim_dependency_ids`, dedupes valid UUIDs, and ignores malformed historical values. Validation passed the new failing-first accepted-evidence graph traversal regression, the contract-variant artifact claim-id regression, focused retrieval/claim/publish graph tests `16 passed`, compileall, default backend `449 passed, 11 expected infra skips`, host-context live Postgres `460 passed`, no-SQLite adapter scan, high-entropy token scan, screenshot-location scan, whitespace scan, and `Leibniz` fix verification with no actionable findings.
- 2026-05-19: Added contradiction precision review to retrieval quality. Candidate metadata can now mark contradiction status, explicit truthy `contradicts` flags, valid claim-level contradiction ids, or pending contradiction review; those sources receive contradiction precision risks, are excluded from accepted context, and keep the ledger in `needs_rerank` when enough clean accepted evidence remains. The ledger also recommends a contradictory-evidence review query, while publish readiness blocks on `retrieval_quality_not_ready` until the contradiction is reviewed. `Leibniz` found and the implementation fixed false-positive metadata parsing for string false values such as `false` / `0` and malformed claim ids. Validation passed the failing-first contradiction regression, false/malformed contradiction metadata regression, focused retrieval/claim/publish graph tests `18 passed`, compileall, default backend `451 passed, 11 expected infra skips`, host-context live Postgres `462 passed`, no-SQLite adapter scan, high-entropy token scan, screenshot-location scan, whitespace scan, and `Leibniz` fix verification with no actionable findings.
- 2026-05-19: Added accepted-source independence to retrieval quality. Query/retriever-diverse accepted evidence from one normalized host now records a `source_independence:host` graph gap, marks accepted candidates with `single_host_source_cluster`, recommends independent source corroboration, keeps the ledger in `needs_more_recall`, and withholds accepted retrieval evidence from downstream claim verification until an independently hosted accepted source exists. Validation passed the failing-first single-host regression, focused retrieval/claim/publish graph tests `19 passed`, compileall, default backend `452 passed, 11 expected infra skips`, host-context live Postgres `463 passed`, no-SQLite adapter scan, high-entropy token scan, screenshot-location scan, whitespace scan, and `Leibniz` whole-slice review with no actionable findings. Residual policy note: official/vendor-primary-source-only topics now require an explicit source-policy waiver if the product wants that behavior.
- 2026-05-19: Tightened accepted-source independence host normalization and closed the `Leibniz` first-pass findings. Retrieval quality now groups accepted evidence by normalized source identity rather than raw subdomain, so sibling subdomains such as `docs.vendor.com` and `blog.vendor.com` cannot pass as independent source hosts. The heuristic strips repeated `www.`, handles simple ports, canonicalizes IP literals, and treats common country-code second-level labels under two-letter TLDs as public-suffix components. `Leibniz` found the initial `co.nz` false-cluster and IPv4 partial-label bugs in one consolidated review; fixed regressions now prove unrelated `vendor-a.co.nz`/`vendor-b.co.nz` stay independent, same `vendor-example.co.nz` subdomains cluster, distinct IPv4 hosts stay independent, and same IPv4 with different ports clusters. Validation passed targeted source-independence tests `6 passed`, focused retrieval/claim/publish graph tests `18 passed`, compileall, default backend `457 passed, 11 expected infra skips`, host-context live Postgres `468 passed`, no-SQLite adapter scan, high-entropy token scan, screenshot-location scan, and whitespace scan. `Leibniz` closure review found no actionable findings; residual risk is the hand-rolled PSL approximation for unusual/private suffix rules.
- 2026-05-19: Moved primary provider-backed voice controls above diagnostics in the actual Next product app. `RealtimeVoicePanel` now shows `Join voice room`, mic toggle, `Interrupt`, and `Stop` directly after the live voice state strip and before captions/readiness/proof panels, backed by a new component-tree order regression. Validation passed frontend race suite `189 passed`, Next typecheck/lint/build, browser DOM ordering proof plus screenshot `screenshots/next-product-app-voice-primary-controls.png`, no-SQLite scan, high-entropy token scan, screenshot-location scan, and whitespace scan. Updated `Leibniz` review protocol so future reviews get one complete deep packet with full diff, tests, validation, stale-state risks, and browser evidence instead of narrow back-and-forth passes.
- 2026-05-19: Added and reviewed a voice-first run starter in the actual Next product app. Live Voice now renders before the text composer, and the no-run voice panel can create a durable voice-mode run from `Voice run goal` via the typed `POST /api/runs` client path. Review fixes changed the CTA to `Create voice run`, made selected-provider provenance truthful for Gemma/Kokoro versus transcript rehearsal, and added parent/child ref-level duplicate-submit guards plus a mounted guard for the run-id remount. Validation passed frontend race suite `195 passed`, Next typecheck/lint/build, browser proof plus screenshot `screenshots/next-product-app-voice-first-run-starter.png`, no-SQLite scan, high-entropy token scan, screenshot-location scan, whitespace scan, and no repo `caret-color` source matches for the known browser-injected hydration warning. `Leibniz` closure found no actionable findings; `Banach` marked the remaining diagnostics-collapse and larger-brief-field items as next UI slices, not blockers.
- 2026-05-19: Closed the no-run Live Voice starter UX follow-up. The starter now uses a 3-row `Voice run brief` textarea with matched visible/accessibility labeling, mobile stacks the brief and create button into one column, and runtime/readiness/proof diagnostics collapse behind `Setup details` until a durable run id exists. Validation passed focused voice-first UI regressions, frontend race runner `198 passed`, Next typecheck/lint/build, browser proof at `screenshots/next-product-app-voice-collapsed-diagnostics.png` and `screenshots/next-product-app-voice-collapsed-diagnostics-mobile.png`, and hygiene scans.
- 2026-05-19: Cleaned the right rail from operations-dashboard copy into creator-facing Studio flow language. `ActivityPanel` now renders `Studio flow`, `Continue specialists`, `Suggest next step`, `Gemma experts`, `Always-on studio`, `Background runner`, `Next studio steps`, `Live updates`, and `Recent specialist outputs`; parent live-update and auto-continue summaries now avoid raw event names and `Autopilot` copy. Validation passed frontend race runner `201 passed`, Next typecheck/lint/build, browser proof at `screenshots/next-product-app-studio-flow-rail.png`, and hygiene scans. `Leibniz` closure found no actionable findings; `Banach` found no UX blocker for this slice.
- 2026-05-19: Reduced Activity rail proof density by moving scheduler, always-on pulse, recent-event ids, and background-runner raw evidence behind `Technical proof` disclosures. Primary rail copy now stays creator-facing with `Background check`, `Specialist pulse`, `Last pulse`, `Pulse running`, specialist update counts, pulse counts, and cadence summaries; raw event ids, ledger ids, profile/heartbeat counts, lease holders, process pid, and autonomous-pass mode remain available for audit. `Banach` found no UX blocker; `Leibniz` found and the implementation fixed timeline `#event_id` and heartbeat/lease primary-copy leaks; both reviewers confirmed closure with no actionable findings. Validation passed focused ActivityPanel render suite `201 passed`, Next typecheck/lint/build, browser proof at `screenshots/next-product-app-studio-flow-technical-proof.png`, no-SQLite scan, high-entropy token file scan, screenshot-location scan, and whitespace scan.
- 2026-05-19: Hardened local LiveKit Gemma/Kokoro session startup when job metadata is absent. The first implementation bound run/session from trusted-looking control messages, but `Leibniz` found two high-severity gaps in one pass: audio-track turns bypassed the guard and first binding proof was forgeable by room participants. Fixed by minting a backend HMAC `control_binding_token` for provider-backed LiveKit sessions, exposing it only in the immediate transport grant, stripping it from durable metadata/events, requiring it for first bind, rejecting placeholder identities, dropping pre-bind or wrong-participant audio, and suppressing startup ready proof until identity is bound. Final `Leibniz` verification found no actionable findings. Validation passed voice-agent suite `66 passed`, related realtime/provider checks `9 passed, 25 deselected`, API session/presence checks `2 passed, 302 deselected`, foundation voice contract checks `2 passed, 17 deselected`, frontend race suite `201 passed`, `compileall`, Next typecheck/lint/build, hygiene scans, and `git diff --check`.
- 2026-05-19: Extended the Gemma/Kokoro LiveKit control-binding contract into provider smoke. Provider-smoke now preallocates the durable `realtime_session_id` before provider creation, uses that id for HMAC proof minting, verifies the raw returned binding token before durable recording, persists the same id, strips normalized binding-token/credential key variants and redacts nested list token strings from durable metadata/events, and blocks manually supplied `gemma4_realtime` sessions without `control_binding_token_issued` before selected realtime or session-bound voice smoke can pass. Added failing-first regressions for proof issuance/no token persistence, self-reported proof with missing or wrong-session tokens, recursive camelCase/suffix/list-string secret redaction, and stale manual session proof. `Leibniz` found the self-reported-proof trust bug, exact-key redaction gap, and list-string redaction gap; all are fixed, and final verification found no actionable findings. Validation passed provider-smoke tests `21 passed`, full default backend regression `467 passed, 11 explicit infra skips`, live Postgres/pgvector outside sandbox `10 passed`, real Rust voice-edge HTTP sidecar smoke outside sandbox `1 passed`, `compileall`, no-SQLite adapter scan, high-entropy token scan, screenshot-location scan, trailing-whitespace scan, and `git diff --check`.
- 2026-05-19: Hardened the main realtime API redaction boundary. Normal realtime-session creation and realtime session control now use normalized recursive metadata sanitization before durable persistence, covering camelCase/separator/suffix credential keys plus Bearer/HF/Tavily strings inside nested lists while preserving non-secret proof booleans and `token: None`. `Leibniz` found that provider-configuration failures still wrote raw exception text; fixed by redacting failure reasons before durable `realtime_session_configuration_failed` events and HTTP 503 details. Added failing-first regressions for leaky provider metadata, provider-configuration failure text, and leaky interrupt metadata. Validation passed focused realtime session/control/provider-smoke tests `28 passed`, full default backend regression `470 passed, 11 explicit infra skips`, live Postgres/pgvector outside sandbox `10 passed`, real Rust voice-edge HTTP sidecar smoke outside sandbox `1 passed`, `compileall`, no-SQLite adapter scan, high-entropy token scan, screenshot-location scan, trailing-whitespace scan, and `git diff --check`. `Leibniz` fix verification found no actionable findings; residual risk is non-token-shaped opaque secrets embedded in exception text, out of scope unless carried under sensitive metadata keys.
- 2026-05-19: Added voice-edge concurrency tuning to the benchmark and runtime-health proof. The benchmark now warms the selected Rust edge transport before measuring steady-state frames, evaluates single-request, concurrent-request, and batch-wall p95 latency against realtime targets, returns tuning status plus recommended `RUST_VOICE_EDGE_VAD_SESSION_POOL_SIZE` and `RUST_VOICE_EDGE_VAD_STREAM_STATE_CACHE_SIZE`, and treats persistent JSONL serialization as an HTTP-sidecar recommendation rather than a local-health failure. `Leibniz` found that HTTP-primary plus JSONL-fallback runs could skip concurrency p95 checks because the fallback wrapper reported serialization even when fallback was unused; fixed by recording fallback usage and deriving measured serialization from actual fallback events. Runtime health records tuning/fallback evidence and surfaces tuning recommendations as the operator action when latency misses targets. Validation passed voice-edge benchmark/runtime-health tests `21 passed`, focused voice-edge/voice-agent suite `102 passed, 1 expected loopback skip`, full default backend `474 passed, 11 explicit infra skips`, live Postgres/pgvector outside sandbox `10 passed`, real Rust voice-edge HTTP sidecar smoke outside sandbox `1 passed`, compileall, no-SQLite adapter scan, high-entropy token scan, screenshot-location scan, whitespace scan, and `git diff --check`. `Leibniz` fix verification found no actionable findings.
- 2026-05-19: Hardened source-side realtime voice-event redaction. Added shared `realtime_safety` helpers and applied them before LiveKit data-channel broadcast, direct backend voice-event POST, and forwarded Rust voice-edge events so Bearer/HF/Tavily-shaped strings plus normalized sensitive keys are stripped or redacted before leaving the voice worker. Added failing-first regressions for backend posting, LiveKit data broadcast, and voice-edge forwarding. Validation passed targeted redaction tests `3 passed`, broader voice/API subset `6 passed`, voice-agent suite `69 passed`, compileall, API redaction subset `2 passed`, no-SQLite boundary tests `3 passed`, full backend regression `477 passed, 11 explicit infra skips`, and live Postgres/pgvector outside sandbox `10 passed`. `Leibniz` found no actionable issues; residual risk remains opaque non-pattern secrets in non-sensitive field names.
- 2026-05-19: Added safe local secret-file setup to the actual creator voice panel. The product UI now renders password-only `Save locally` controls for missing/empty/unreadable configured `HF_TOKEN` and `TAVILY_API_KEY` files, while `POST /api/local-secret-files` writes only supported local/dev/test secret files atomically with file mode `0600`, tightens newly created directories plus default project `.secrets` to `0700`, preserves existing custom parent permissions, and never echoes secret values. `Leibniz` found and the implementation fixed the pathless `not_configured` save affordance, default-directory permission gap, and too-broad custom-parent chmod from the first fix. Browser proof is `screenshots/next-product-app-local-secret-save-run.png`. Validation passed focused backend/frontend tests, full backend regression `481 passed, 11 explicit infra skips`, live Postgres `10 passed`, frontend race suite `202 passed`, Next typecheck/lint/build, `compileall`, no-SQLite boundary tests, token scan, screenshot scan, whitespace scan, and `git diff --check`.
- 2026-05-19: Extended local secret-file setup to LiveKit credentials for the Gemma/Kokoro voice stack. `LIVEKIT_API_KEY_FILE` and `LIVEKIT_API_SECRET_FILE` now default to ignored `.secrets/` paths, load into settings when direct env values are absent, surface in provider readiness as non-secret diagnostics for `gemma4-realtime`, and can be saved through the same local-only product voice setup path without echoing values. `Leibniz` found one stale fallback warning copy; fixed and final review found no actionable issues. Validation passed LiveKit secret-file tests, provider-readiness subset `17 passed`, full backend regression `484 passed, 11 explicit infra skips`, live Postgres `10 passed`, frontend race suite `202 passed`, Next typecheck/lint/build, `compileall`, no-SQLite boundary tests, token scan, screenshot scan, whitespace scan, and `git diff --check`. Playwright transport was closed and full-screen capture was rejected for privacy, so no new screenshot was captured for this slice.
- 2026-05-19: Added and hardened local provider endpoint setup for Gemma/Kokoro voice configuration. `LOCAL_PROVIDER_CONFIG_FILE=.secrets/local_provider_config.json` can now hold `GEMMA4_MULTIMODAL_ENDPOINT_URL`, `GEMMA4_REALTIME_LIVEKIT_URL`, and `KOKORO_TTS_ENDPOINT_URL`; the creator voice setup panel exposes `Save endpoint` controls for those missing envs through local-only `POST /api/local-provider-config`. The endpoint validates URL shape through a shared helper, rejects credential/query/fragment-bearing URLs, ignores unsafe startup JSON values, drops unsafe managed sibling values while preserving unrelated local string keys, serializes overlapping saves, writes atomically with `0600`, updates readiness, and never echoes endpoint values. The frontend disables all endpoint save controls while any endpoint save is in flight. `Leibniz` completed a consolidated review with no actionable findings; residual gaps are process-local locking, malformed JSON returning setup error instead of auto-repair, and source-contract frontend coverage. Validation passed local-provider-config tests `7 passed`, provider setup subset `23 passed`, full backend regression `490 passed, 11 explicit infra skips`, live Postgres `10 passed`, frontend race suite `202 passed`, Next typecheck/lint/build, `compileall`, no-SQLite boundary test, token scan, screenshot scan, whitespace scan, and `git diff --check`.
- 2026-05-19: Tightened creator-visible always-on/background-runner copy in the parent Next app. Busy labels, completion summaries, fallback errors, raw backend result summaries, and the default profile name now use `Always-on studio`, `Specialist pulse`, `Creator always-on studio`, and `Background runner` language instead of visible `Autopilot` / `local scheduler` copy. `Leibniz` found the raw-summary leak; fixed with a parent-status sanitizer plus AppShell regression using mocked backend summaries. Validation passed frontend race runner `240 passed`, Next lint, focused vault foundation test, compileall, stale-string scan, and hygiene checks.
- 2026-05-19: Extended the browser single-flight smoke to cover always-on/background-runner status copy. The Chromium fixture now clicks `Start always-on` and `Start runner` against stale backend Autopilot/local-scheduler summaries and asserts the status strip shows `Always-on studio launch recorded`, `Creator always-on studio`, and `Background runner is running` while hiding raw implementation wording. Validation passed frontend race/source contracts `241 passed` and the browser single-flight smoke.
- 2026-05-19: Extended the persistent browser single-flight smoke to cover source refresh. The Chromium fixture now keeps an unresolved search-seed claim visible, rapidly double-clicks `Run web research`, and proves the browser creates one A2A source-refresh message plus one bounded Web Research / Claim Verification worker cycle. Validation passed frontend race/source contracts `241 passed` and the browser single-flight smoke.
- 2026-05-19: Extended the persistent browser single-flight smoke to cover work-plan execution. After `Suggest next step`, the Chromium fixture now rapidly double-clicks `Run next steps` and proves the browser creates one work-plan materialization, one bounded planned worker cycle, and one post-run next-action refresh. Validation passed frontend race/source contracts `241 passed` and the browser single-flight smoke.
- 2026-05-19: Added the Agent Studio skill matrix as a planning-only browser surface. `tests/test_skill_matrix_browser.py` opens `00-system-design/agent-studio-skill-matrix.html`, filters to `Guardrails`, and proves the visible detail plus exported JSON preserve `agent-studio-guardrails-review`, `feedback_resolution_ledger`, `guardrails-agent`, `critic-reviewer-agent`, and the source `SKILL.md` path. The vault home and MOC now link the matrix for review and vault-sync handoffs.
- 2026-05-19: Added the Agent Studio proof readiness surface for the two remaining completion blockers. `tests/test_proof_readiness_browser.py` opens `01-work-tracking/agent-studio-proof-readiness.html`, filters to `Live voice` and `Publication`, and proves exported JSON preserves `provider-backed-live-voice-proof`, `external-publication-proof`, blocked status, provider env/input names, `realtime_voice_timing_ledger`, publication credential names, and durable platform proof requirements without marking the objective complete.
- 2026-05-19: Added the Agent Studio A2A map as a planning-only browser surface. `tests/test_a2a_map_browser.py` opens `00-system-design/agent-studio-a2a-map.html`, filters to `Public boundary` and `Repair`, and proves exported JSON preserves the scoped discovery boundary, `/.well-known/agent-card.json`, `/api/a2a`, `fullA2AProtocolServer=false`, JSON/text HTTP+JSON scope, retry/dependency-repair routes, and `a2a_graph_blocked` evidence without claiming a full public A2A protocol server.
- 2026-05-19: Added the Agent Studio feedback loop map as a planning-only browser surface. `tests/test_feedback_loop_map_browser.py` opens `03-review-packets/agent-studio-feedback-loop-map.html`, filters to `Guardrails` and `Publish gate`, and proves exported JSON preserves `feedback_resolution_ledger`, accepted/revised/held/rejected outcomes, failed linked task ids, held-until-review policy, `publish_readiness_checked`, unsupported-claim blockers, and external-publication blocker copy.
- 2026-05-19: Added the Gemma voice boundary map as a planning-only browser surface. `tests/test_gemma_voice_boundary_browser.py` opens `02-research/gemma-voice-boundary-map.html`, filters to `Audio proof` and `HF text`, and proves exported JSON preserves `provider-backed-live-voice-proof`, `hf-router-text-expert-route`, HF router text expert routing, Gemma E4B audio understanding, Kokoro TTS, LiveKit transport, Rust edge timing evidence, and blocked same-session provider voice proof.
- 2026-05-19: Added the Agent Studio publication boundary map as a planning-only browser surface. `tests/test_publication_boundary_browser.py` opens `03-review-packets/agent-studio-publication-boundary-map.html`, filters to `External proof`, `Non-live smoke`, `Policy review`, and `Rollback`, and proves exported JSON preserves non-live channel smoke, destination credential names, exact policy approval, durable platform evidence, rollback proof, and blocked external publication proof without treating readiness smoke as real API publish proof.
- 2026-05-19: Extended the persistent creator-app browser single-flight smoke to cover Activity retry execution. The Chromium fixture now renders a failed specialist task in `Needs attention`, rapidly double-clicks `Queue and run`, and proves the browser creates exactly one retry authorization plus one targeted retry worker cycle for the retried message id.
- 2026-05-19: Extended the persistent creator-app browser single-flight smoke to cover the voice-first run starter. The Chromium fixture now waits for the no-run Live Voice panel to hydrate, rapidly double-clicks `Create voice run`, and proves the browser creates exactly one voice-mode `POST /api/runs` payload with `live_voice_panel`, `provider_backed_realtime=true`, `google/gemma-4-E4B-it`, and `hexgrad/Kokoro-82M` provenance, refreshes the same run context, hides the no-run starter, and binds the next Generate turn to that run.
- 2026-05-19: Extended the persistent creator-app browser single-flight smoke to cover Runtime preflight. The red browser run caught duplicate readiness requests after rapid clicks; `RealtimeVoicePanel` now uses keyed in-flight readiness promises for identical refresh options, preserves stronger full-preflight evidence over weaker same-epoch overlaps, bumps a readiness epoch after config/process mutations so lower-strength refreshes can update changed setup state, and the smoke proves one full preflight request carrying LiveKit, Gemma, Kokoro, Rust edge, and agent flags.
- 2026-05-19: Extended the persistent creator-app browser single-flight smoke to cover voice `Check setup`. The red browser run caught duplicate setup fanout after rapid clicks; `RealtimeVoicePanel` now uses a synchronous setup-check ref gate plus `settleVoiceSetupFanout` so early branch failures do not release the gate while other setup calls are still pending, and the smoke proves one durable voice setup proof after one LiveKit process refresh, one voice-agent process refresh, one provider-readiness refresh, one runtime preflight, and one voice-agent presence check. Validation passed browser single-flight, focused frontend race contracts (`244 passed`), typecheck, lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- 2026-05-19: Extended the persistent creator-app browser single-flight smoke to cover voice proof actions. The red browser run caught duplicate provider-smoke writes after rapid `Runtime smoke` clicks; `RealtimeVoicePanel` now uses a shared synchronous `voiceProofActionInFlightRef` before Runtime smoke and Timing ledger API work. The smoke proves one non-live provider-smoke request with `execute_live_calls=false`, one realtime timing-ledger request with `event_limit=500`, and cross-click blocking in both proof-action orders without treating either blocked fixture proof as provider-backed live voice readiness. Validation passed browser single-flight, focused frontend race contracts (`244 passed`), typecheck, lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- 2026-05-19: Extended the persistent creator-app browser single-flight smoke to cover transcript rehearsal start. The red browser run caught duplicate realtime-session creation after rapid `Start transcript rehearsal` clicks; `RealtimeVoicePanel` now uses a synchronous `startActionInFlightRef` before start-session API work. The smoke proves one dry-run realtime session request with `transport_framework=local_rehearsal` and `dry_run=true`, preserving transcript rehearsal as a local proof path rather than provider-backed live voice proof. Validation passed browser single-flight, focused frontend race contracts (`244 passed`), typecheck, lint, foundation (`21 passed`), touched-file hygiene, and `Leibniz` review with no actionable findings.
- 2026-05-19: Extended the persistent creator-app browser single-flight smoke to cover transcript rehearsal turn routing. The red browser run caught duplicate realtime-turn creation after rapid `Route rehearsal turn` clicks; `RealtimeVoicePanel` now uses `canRouteRehearsalTurn`, a synchronous `rehearsalTurnInFlightRef`, run/session/token completion ownership, and Start/Stop active-session invalidation before routing the rehearsal turn. The smoke proves one non-production realtime-turn request with `input_surface=voice_runtime_transcript_rehearsal`, `provider_backed_realtime=false`, and `rehearsal_only=true`. `Leibniz` found and the implementation fixed stale completion, stuck loading, and Stop-then-route stale POST edges. Validation passed browser single-flight, focused frontend race contracts (`245 passed`), typecheck, lint, foundation (`21 passed`), touched-file hygiene, and final `Leibniz` review with no actionable findings.
- 2026-05-19: Hardened mixed-mode live text-turn submission for the provider-backed Gemma/Kokoro room. The red source-contract run caught the missing synchronous duplicate-submit ref before `sendTranscriptTurn`; `Leibniz` then found the stale pre-send gap after the optional interrupt await. `RealtimeVoicePanel` now uses `liveTextTurnInFlightRef`, run/session/token ownership, invalidation on run change/Start/Stop/unmount, and a post-interrupt ownership recheck before publishing the LiveKit text turn. Validation passed focused frontend race contracts (`246 passed`), typecheck, lint, foundation (`21 passed`), touched-file hygiene, and final `Leibniz` review with no actionable findings.
- 2026-05-19: Hardened cockpit walkthrough feedback-loop proof. The red backend test caught that a routed demo feedback gate could be reported as optional/ready; `_feedback_loop_step` now treats `open` and `routed` feedback as unresolved, makes the feedback-loop step required, exposes open/routed/unresolved counts, carries `unresolved_feedback_items`, and links `feedback_routed`. Added endpoint proof that `/api/runs/{run_id}/cockpit-walkthrough-ledger` preserves those routed-gate semantics from store state through the API response. Validation passed focused cockpit walkthrough and feedback-ledger tests, compileall, foundation, touched-file hygiene, and `Leibniz` review with no material findings.
- 2026-05-19: Hardened cockpit walkthrough realtime provider-turn ownership. The red backend test caught that a prior rehearsal `realtime_turn_routed` event could still satisfy selected-provider turn evidence after a provider-backed session was added. `_realtime_smoke_step` now requires a routed/recorded turn event whose `realtime_session_id` matches an active, unexpired selected provider-backed session before clearing `provider_backed_realtime_turn:<provider>`, while keeping rehearsal turns as generic realtime evidence only. `Leibniz` found the active-but-expired session overclaim; ended sessions now emit `active_provider_backed_realtime_session:<provider>` and expired sessions emit `unexpired_provider_backed_realtime_session:<provider>`. Validation passed focused cockpit walkthrough/realtime-dialogue tests, compileall, foundation, touched-file hygiene, and `Leibniz` review with no material findings.
- 2026-05-19: Extended the persistent creator-app browser single-flight smoke to prove `Stop runner` blocks source refresh while the delayed background-runner stop request is in flight. The Chromium fixture now starts `Stop runner`, tries `Run web research` before stop completion, and proves neither the source-refresh A2A message nor worker cycle starts. No product code change was needed; validation passed browser single-flight, frontend race suite `248 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` review with no material findings.
- 2026-05-19: Extended the persistent creator-app browser single-flight smoke to prove always-on rail `Stop` blocks source refresh while the delayed worker-profile stop request is in flight. `Leibniz` caught the first version's timing-margin proof; the final fixture now has explicit stop request-start and completion events for both runner stop and always-on stop. Validation passed browser single-flight, frontend race suite `248 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` closure with no material findings.
- 2026-05-19: Hardened provider-backed `Join voice room` as a same-run mutation boundary. The red browser smoke proved source refresh could start while the LiveKit realtime-session grant was in flight; the parent app now owns `voiceSessionActionGateRef`, includes it in the shared run-mutation gate, and the voice panel starts/finishes that gate around transcript rehearsal and provider-backed voice session work. `New` invalidates the gate. Validation passed browser single-flight, frontend race suite `249 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` closure with no material findings.
- 2026-05-19: Hardened `Runtime smoke` as a same-run mutation boundary. The red browser smoke proved source refresh could start while the delayed provider-smoke proof write was in flight; the parent app now owns `voiceProofActionGateRef`, includes it in the shared run-mutation gate, and the voice panel starts/finishes that parent gate around Runtime smoke and Timing ledger proof writes. `New` invalidates the gate. Validation passed browser single-flight, frontend race suite `249 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` closure with no material findings.
- 2026-05-19: Extended the creator-app browser smoke to prove `Timing ledger` also blocks source refresh while the delayed realtime voice timing-ledger request is in flight. No production code change was needed because the parent `voiceProofActionGateRef` already covers both voice proof writes. Validation passed browser single-flight, frontend race suite `249 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` closure with no material findings.
- 2026-05-19: Hardened `Check setup` as a same-run mutation boundary. The red browser smoke proved source refresh could start while setup fanout was in flight; `RealtimeVoicePanel` now starts the parent voice-proof mutation gate before setup fanout, owns Check setup with an independent run/token sequence, clears setup state on run change/unmount, disables `Resolve next` while Check setup is active, and synchronously blocks same-tick Resolve clicks. Validation passed browser single-flight, frontend race suite `249 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` closure with no material findings.
- 2026-05-19: Hardened `Resolve next -> Refresh providers` as a same-run mutation boundary. Red browser smokes proved rapid resolver clicks created duplicate provider-readiness requests and that source refresh could start while provider refresh was in flight. `RealtimeVoicePanel` now owns resolver actions with a synchronous in-flight ref/loading state, holds the parent voice-proof mutation gate through provider refresh plus durable setup-proof write, and ignores stale resolver proof-failure callbacks. Validation passed browser single-flight, frontend race suite `249 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` closure with no material findings.
- 2026-05-19: Hardened the live voice proof path `Refresh provider readiness` action as a same-run mutation boundary. The red browser smoke proved source refresh could start while the proof-path provider-readiness request was in flight; `RealtimeVoicePanel` now owns proof-path actions with a synchronous in-flight ref/loading state, holds the parent voice-proof mutation gate for provider-readiness/runtime/presence checks, and applies provider-readiness results only while the proof-path ownership predicate is current. `Leibniz` found and verified closure for the stale provider-readiness apply gap. Validation passed browser single-flight, frontend race suite `249 passed`, Next typecheck, Next lint, foundation `21 passed`, and hygiene checks.
- 2026-05-19: Hardened the main provider panel `Provider readiness` button as a same-run mutation boundary. The corrected red browser smoke proved source refresh could start while the provider-panel readiness request was in flight; `RealtimeVoicePanel` now owns manual provider readiness refreshes with a synchronous in-flight ref, takes the ownership token before the no-run branch, uses the parent voice-proof mutation gate for run-bound refreshes, and applies provider-readiness results only while the provider-panel ownership predicate is current. `Leibniz` verified the prior no-run single-flight minor is closed. Validation passed browser single-flight, frontend race suite `249 passed`, Next typecheck, Next lint, foundation `21 passed`, and hygiene checks.
- 2026-05-19: Hardened local LiveKit and Gemma/Kokoro process controls as same-run mutation boundaries. Red browser smokes proved rapid `Stop LiveKit` created duplicate process-stop requests and resolver `Start LiveKit` could overlap source refresh; `RealtimeVoicePanel` now owns process actions with synchronous in-flight refs, gates direct process controls with the parent voice-proof mutation gate when a run exists, and wraps resolver `Start LiveKit`, `Restart LiveKit`, and `Start agent` in `runResolverParentMutation` through process mutation plus resolver proof write. Validation passed browser single-flight, frontend race suite `249 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` closure with no material findings.
- 2026-05-20: Hardened direct `Runtime preflight` as a same-run mutation boundary. The red browser smoke proved source refresh could start while the full provider preflight request was in flight; `RealtimeVoicePanel` now owns direct runtime preflight with a synchronous in-flight ref, holds the parent voice-proof mutation gate while a run exists, and passes `shouldApply: isCurrentRuntimePreflight` into `refreshVoiceReadiness` so stale success/failure events cannot repaint after run changes. Validation passed browser single-flight, frontend race suite `249 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` closure with no material findings.
- 2026-05-20: Hardened voice follow-up continuation as a same-run mutation boundary. The shared mutation gate now includes `agentCycleInFlight`, automatic voice follow-up continuation checks the same-run gate before running specialists, and the browser smoke routes a transcript rehearsal turn with a durable follow-up then proves `Run web research` cannot create a source-refresh A2A message or worker cycle while the follow-up worker cycle is in flight. Validation passed frontend race suite `250 passed`, browser single-flight, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings review.
- 2026-05-20: Hardened `Route rehearsal turn` as a same-run mutation boundary. The browser red proved source refresh could start while the transcript rehearsal realtime-turn request was delayed; the voice panel now holds the parent voice-run mutation gate through `sendRealtimeTurn` and run refresh, then releases before voice follow-up continuation takes its own agent-cycle gate. Validation passed browser single-flight, frontend race suite `250 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings review.
- 2026-05-20: Hardened direct `Use local LiveKit dev defaults` as a same-run mutation boundary. The browser red proved source refresh could start while local LiveKit dev config was delayed; the voice panel now holds the parent voice-proof mutation gate through config plus all-settled provider/runtime readiness refreshes, uses run-owned apply predicates, and prevents resolver proof writes for skipped or gate-denied config attempts. Validation passed browser single-flight, frontend race suite `250 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings fix-back review.
- 2026-05-20: Hardened direct provider endpoint `Save endpoint` as a same-run mutation boundary. The browser red proved source refresh could start while `/api/local-provider-config` was delayed; the voice panel now uses a synchronous provider-config save ref, parent voice-proof gate, run/token stale ownership, and all-settled provider/runtime readiness refreshes before release. Validation passed browser single-flight, frontend race suite `250 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings review.
- 2026-05-20: Hardened direct secret-file `Save locally` as a same-run mutation boundary. The browser red proved source refresh could start while `/api/local-secret-files` was delayed; the voice panel now uses a synchronous secret-file save ref, parent voice-proof gate, run/token stale ownership, and guarded provider-readiness refresh before release. `Leibniz` found a false save-failure event after refresh rejection, fixed by splitting write failure from refresh failure. Validation passed browser single-flight, frontend race suite `250 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and final `Leibniz` no-material-findings review.
- 2026-05-20: Hardened action-row `Stop` for live voice sessions as a same-run mutation boundary. The browser red proved source refresh could start while transcript rehearsal Stop was writing session status; the voice panel now acquires the parent voice-run mutation gate before local stop state changes and releases it after `endRealtimeSession`. Validation passed browser single-flight, frontend race suite `250 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings review.
- 2026-05-20: Hardened `Resolve next -> Run preflight` as a same-run mutation boundary. The browser red proved source refresh could start while resolver runtime preflight was delayed; the voice panel now wraps resolver preflight and durable setup-proof recording in the parent voice-proof mutation gate and uses `shouldApply: isCurrentSetupAction` for stale completions. Validation passed browser single-flight, frontend race suite `250 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings review.
- 2026-05-20: Hardened stale ownership for resolver provider readiness and live-proof runtime preflight. The source contract first failed because those refresh calls lacked `shouldApply` predicates; resolver provider refresh now uses `isCurrentSetupAction`, and live-proof runtime preflight now uses `isCurrentProofAction`, preventing stale run/session/action completions from repainting readiness. Validation passed browser single-flight, frontend race suite `250 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings review.
- 2026-05-20: Hardened `Check setup` readiness fanout stale ownership. The source contract first failed because setup-check runtime/provider readiness refreshes lacked `shouldApply` predicates; both fanout calls now use `isCurrentSetupCheck`, preventing stale setup completions from repainting readiness after a newer run/check owns the panel. Validation passed browser single-flight, frontend race suite `250 passed`, Next typecheck, Next lint, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings review.
- 2026-05-20: Extended the persistent creator-app browser smoke to prove feedback `Resolve` is a same-run mutation boundary. The fixture now seeds an open feedback gate, rapid-clicks `Resolve`, waits for `/api/feedback/{feedback_id}/resolve` to start, clicks `Run web research`, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts while feedback resolution is in flight. No product code change was needed because the existing feedback action gate already enforced the invariant. Validation passed browser single-flight, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings review.
- 2026-05-20: Extended the persistent creator-app browser smoke to prove manual `Continue specialists` is a same-run A2A worker-loop mutation boundary. The fixture now rapid-clicks the Activity rail continuation control, waits for the manual worker cycle to start, clicks `Run web research`, and proves neither the source-refresh A2A message nor source-refresh worker cycle starts while manual specialist continuation is in flight. No product code change was needed because the existing agent-cycle lock already enforced the invariant. Validation passed browser single-flight, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings review.
- 2026-05-20: Extended the persistent creator-app browser smoke to prove topbar `Refresh` and `New` cannot interfere while source-refresh worker-cycle work is in flight. The fixture now starts delayed `Run web research`, attempts the exact topbar Refresh control during the overlap window, proves no manual context-packet refresh request starts, then attempts topbar New and proves the active run remains selected. No product code change was needed because the existing busy state and shared mutation gate already enforced the invariant. Validation passed browser single-flight, foundation `21 passed`, hygiene checks, and `Leibniz` no-material-findings review.
- 2026-05-20: Recorded a non-secret credential blocker snapshot for live voice and publication proof. Current shell environment has no required live-provider/publication values loaded; the workspace only exposes `.env.example` placeholders for Gemma/Kokoro/HF/LiveKit key material, including `GEMMA4_REALTIME_LIVEKIT_URL`, and social publishing credentials; generic `LIVEKIT_URL` is absent. No secret values were printed, and the objective remains blocked on real provider/publishing configuration before live proof can be claimed.
- 2026-05-20: Added browser-verified credential snapshot export to `agent-studio-proof-readiness.html`. The Live voice and Publication blockers now render and export `credential_snapshot` with placeholder-only input names, `GEMMA4_REALTIME_LIVEKIT_URL`, generic `LIVEKIT_URL` absence for voice, no shell values loaded, and `secret_values_printed=false`. The focused browser test first failed on missing snapshot evidence, then passed after the planning-only HTML update.
- 2026-05-20: Propagated credential snapshot exports into the Gemma voice and publication boundary maps. `gemma-voice-boundary-map.html` now exports the snapshot on `provider-backed-live-voice-proof`; `agent-studio-publication-boundary-map.html` now exports the snapshot on `external-publication-proof`. The focused browser tests first failed on missing snapshot evidence, then passed after both planning-only maps rendered and exported the non-secret blocker state.
- 2026-05-20: Linked the updated blocker boundary maps from the system-design-vault home. `system_design_vault/agent-studio-system-design-home.html` now points to proof readiness, the Gemma voice boundary map, and the publication boundary map, and preserves credential snapshot export copy with `secret_values_printed=false`. Browser/foundation tests first failed on the missing links and then passed after the home update.
- 2026-05-20: Added cross-export credential snapshot attribution proof. `tests/test_blocker_snapshot_consistency.py` first failed because the Gemma voice and publication boundary exports did not preserve the original proof-readiness classifier source. Both boundary maps now export `source_snapshot: "non-secret local classifier"` while retaining `source: "agent-studio-proof-readiness"`, and the browser regression compares the shared blocker fields against the proof-readiness export to catch future drift.
- 2026-05-20: Added explicit proof acceptance gates to the remaining blocker surfaces. `tests/test_blocker_acceptance_gates_browser.py` first failed because proof readiness and the boundary maps did not render/export an acceptance gate. The Live voice and Publication blockers now export `proof_acceptance_gate`, state that credential presence is not proof, reject HF text routing, transcript rehearsal, non-live smoke, and local draft substitutes, and require same-session live voice or exact-destination publication evidence.
- 2026-05-20: Added operator proof packets to the remaining blocker surfaces. `tests/test_blocker_proof_packets_browser.py` first failed because proof readiness and the boundary maps did not render/export the concrete capture packet needed after credentials are configured. The Live voice and Publication blockers now export `operator_proof_packet` with runtime capture targets, publication disclosure capture, no-secret-printing constraints, and storage targets in the project audit, active context, and system-design mirror.
- 2026-05-20: Added a reusable no-secret blocker credential classifier. `tests/test_blocker_credential_classifier.py` first failed on the missing module, then `src/all_about_llms/orchestration/blocker_credentials.py` added `build_blocker_credential_snapshots` for live voice and external publication blockers. The classifier reports env names and booleans only, preserves placeholder-only, missing-configuration, and configured-unverified states with state-aware notes, supports `X_ACCESS_TOKEN`/`X_API_KEY` group semantics, and never echoes configured token or secret values.
- 2026-05-20: Added `blocker-credential-snapshot` for operator refresh of remaining blocker credential snapshots. `tests/test_blocker_credential_snapshot_cli.py` first failed on the missing CLI payload helper, then the command was added to emit no-secret JSON from `.env.example` placeholder names plus known process env-name presence. In this source checkout it runs as `PYTHONPATH=src python3 -m all_about_llms.cli blocker-credential-snapshot`; installed environments can use `all-about-llms-admin blocker-credential-snapshot`. Configured credentials remain `runtime_configuration_present_unverified` rather than completing live voice or publication proof.
- 2026-05-20: Added CLI-to-HTML credential snapshot consistency proof. `tests/test_blocker_snapshot_consistency.py` now compares proof-readiness snapshots against `_blocker_credential_snapshot_payload` for current `.env.example` placeholder state and verifies the Gemma voice and publication boundary maps propagate the same fields, including notes. The red browser test failed on missing `configured_inputs`; the review-tightened note assertion then caught stale Gemma voice boundary wording. Proof readiness and both boundary maps now export classifier-aligned snapshot fields for the current blocked state.
- 2026-05-20: Added no-secret secret-file presence support to blocker credential snapshots. `tests/test_blocker_credential_classifier.py` and `tests/test_blocker_credential_snapshot_cli.py` first failed because file-backed `HF_TOKEN`, `LIVEKIT_API_KEY`, and `LIVEKIT_API_SECRET` were ignored. The classifier and CLI now treat non-empty `HF_TOKEN_FILE`, `LIVEKIT_API_KEY_FILE`, and `LIVEKIT_API_SECRET_FILE` paths as configured file inputs without reading or printing contents, and static proof-readiness/boundary snapshots export empty file fields for the current blocked state.
- 2026-05-21: Hardened external publication preflight validation against duplicate, alias-duplicate, blank, or unsupported channel checks. Red CLI regressions first proved a ready `publish-readiness.preflight.json` with two `linkedin` checks, alias-equivalent `x` and `twitter` checks, or unsupported `mastodon` checks could still emit preflight artifact IDs; validation now normalizes publish-channel aliases, rejects unsupported normalized platforms against the product credential map, emits `publish_channel_duplicate_platform` or `publish_channel_platform_unsupported`, returns `invalid_preflight_artifacts`, and withholds artifact IDs. `Leibniz` then found blank platform values could still pass; the blank-platform regression proves those emit `publish_channel_platform_missing` and return no artifact IDs. Proof-plan requirements, proof readiness, publication boundary, the concrete proof workspace, and vault/system-design notes now state that publication preflight must include one normalized, non-empty supported `publish_channel_checks` entry per channel. This remains local proof-quality hardening only, not accepted external publication proof.
- 2026-05-21: Hardened accepted external publication proof summaries against padded, duplicate, alias-form, or blank publish-channel evidence. Red CLI regressions first proved `validated_publish_channels` values such as `["linkedin", "mastodon"]`, `["linkedin", "linkedin"]`, `["twitter"]`, `linkedin, mastodon`, `linkedin, linkedin`, `twitter`, `linkedin, `, and `, linkedin` could still satisfy destination linkage after normalization; JSON record validation now emits `preflight_validation_report_publish_channels_not_canonical`, and completion status treats matching compact audit notes as invalid accepted evidence. Focused regression passed with `8 passed`; full provider proof suite passed with `173 passed`; foundation passed with `21 passed`; compileall, `git diff --check`, and touched-file whitespace/control-character scan passed. `Leibniz` found the alias/blank-padding bypass and then verified the fix-back with no actionable findings. This remains proof-quality hardening only, not live external publication proof.
- 2026-05-21: Hardened provider proof plans against non-UUID run-id handoffs. Red regressions proved configured credentials plus `RUN-ID` could produce `ready_for_runtime_attempt`, and `Leibniz` found proof-record validation, completion status, and workspace README paths could still accept or emit non-UUID proof commands. The proof plan, record/template validation, preflight/workspace validation, completion status, and checked-in `RUN-2026-05-20-NEXT` reports now fail closed with `run_id_not_product_uuid`; proof command fields render `<run-id>` until a durable product run UUID is supplied. Provider proof-plan suite passed with `178 passed`, foundation passed with `21 passed`, and static/browser proof-surface parity passed outside the macOS sandbox with `14 passed`. This is operator-handoff hardening only, not a durable run, credential setup, or accepted proof.
- 2026-05-21: Hardened live voice preflight validation against duplicate runtime check IDs. A red CLI regression first proved a duplicated ready `livekit-transport` check could mask an earlier blocked check with the same ID; `validate-provider-proof-preflight-artifacts` now emits `voice_runtime_duplicate_check_id`, returns `invalid_preflight_artifacts`, and withholds preflight artifact IDs. `Leibniz` then found duplicate issue paths could echo raw token-shaped `check_id` values; a second red regression now proves those values are absent from serialized validation JSON. Proof-plan requirements, proof readiness, Gemma voice boundary, the concrete proof workspace, and vault/system-design notes state that required runtime checks must be present once and ready. This remains local proof-quality hardening only, not provider-backed live voice proof.
- 2026-05-20: Added a no-secret provider proof-record audit bridge. `tests/test_provider_proof_plan_cli.py` first failed on the missing `_record_provider_proof_record_payload`, then `record-provider-proof-record` was added to validate captured accepted/failed proof JSON and append compact redacted Markdown notes to configured vault audit targets while refusing invalid records or placeholder/unsafe run ids without writing. Provider proof plans and proof-readiness/Gemma voice/publication boundary exports now carry `record_commands` for the handoff. Validation passed provider proof tests `13 passed` and proof-plan static/browser consistency `15 passed`.
- 2026-05-20: Added no-secret provider proof-record templates. `tests/test_provider_proof_plan_cli.py` first failed on missing `_provider_proof_record_template_payload`, then `provider-proof-record-template` was added to emit product-UUID-only draft JSON records with required schema fields, post-capture validation keys, and no secret values. Placeholder/unsafe/non-UUID run ids return no record, and partially filled templates with remaining `<...>` sentinels cannot validate as accepted or failed proof. Proof-plan packets plus proof-readiness/Gemma voice/publication boundary exports now carry `template_commands`. Initial validation passed provider proof tests `17 passed` and proof-plan static/browser consistency `19 passed`.
- 2026-05-20: Added a no-secret provider proof workspace initializer. `tests/test_provider_proof_plan_cli.py` first failed on missing `_provider_proof_workspace_payload`, then `init-provider-proof-workspace` was added to write run-specific voice/publication `.template.json` files plus `README.md` capture instructions. Placeholder/unsafe run ids return `blocked_by_run_id` and write nothing. Leibniz review found reruns could overwrite partially filled proof files; the initializer now fails closed as `workspace_exists` with zero writes when target files already exist. This entry is superseded by the later durable product UUID gate: shell-safe labels such as `RUN-2026-05-20-NEXT` now remain blocked as `run_id_not_product_uuid`. Initial validation passed provider proof tests `19 passed`.
- 2026-05-20: Added a no-secret provider proof completion-status checker. `tests/test_provider_proof_plan_cli.py` first failed on missing `_provider_proof_completion_status_payload`, then `provider-proof-completion-status` was added to scan configured audit targets for valid accepted live voice/publication proof-record notes, report missing accepted proof records and missing/invalid audit targets, and avoid changing blocker state. The proof workspace README points operators to that check only for durable product UUID workspaces. Leibniz review found accepted-like substring matches and directory targets could mislead or crash; exact audit-field parsing and non-file target reporting now cover both cases. Later product UUID hardening makes non-UUID labels fail closed before completion can report accepted proof.
- 2026-05-20: Propagated provider proof completion-status commands into proof-plan exports. CLI and browser parity tests first failed because proof plans did not expose `completion_status_commands`; `provider-proof-plan`, proof readiness, and Gemma voice/publication boundary maps now carry and render `provider-proof-completion-status --run-id <run-id>` after template/record commands.
- 2026-05-20: Tightened provider proof completion-status target coverage. `tests/test_provider_proof_plan_cli.py` first failed because accepted proof notes in one audit target could report completion while another configured readable audit target had no matching notes; the checker now blocks as `blocked_by_incomplete_audit_target_coverage`, emits `audit_target_coverage_incomplete`, and reports per-proof `missing_source_targets`.
- 2026-05-20: Tightened provider proof completion-status latest-record semantics. `tests/test_provider_proof_plan_cli.py` first failed because an older accepted audit note still counted after a later failed note for the same proof/run; the checker now classifies the latest valid accepted/failed note per proof and readable target, blocks as `blocked_by_latest_failed_proof_record`, emits `latest_proof_record_failed`, and reports per-proof `failed_source_targets`.
- 2026-05-20: Anchored provider proof completion-status latest-record selection to validation timestamps. `tests/test_provider_proof_plan_cli.py` first failed because manual note reordering could make an older accepted note override a newer failed record; the checker now prefers parseable `validation_timestamp` values and uses file order only as a tie/fallback.
- 2026-05-20: Added self-audit fields to provider proof completion status. `tests/test_provider_proof_plan_cli.py` first failed because completion-status payloads did not expose required proofs or the state-change boundary; the checker now includes `required_proofs`, `completion_requirements`, `state_change_boundary`, and `blocker_state_change_allowed_by_this_command=false` so green proof status remains separate from blocker mutation.
- 2026-05-20: Required matching proof artifact types in provider proof completion status. `tests/test_provider_proof_plan_cli.py` first failed because accepted-looking audit notes with swapped `proof_artifact_type` values could satisfy completion; the checker now ignores accepted/failed audit notes unless `proof_artifact_type` matches the selected proof schema.
- 2026-05-20: Blocked secret-shaped audit notes in provider proof completion status. `tests/test_provider_proof_plan_cli.py` first failed because a manually edited accepted audit note containing token-shaped text could still satisfy completion; the checker now returns `blocked_by_secret_shaped_audit_note`, emits `audit_note_secret_shape_detected`, reports proof IDs/target paths only, and does not echo the matched value.
- 2026-05-20: Fixed provider proof completion-status precedence after Leibniz review. Regressions first failed because simultaneous failed or missing proof states could mask `blocked_by_secret_shaped_audit_note`; the checker now keeps secret-shaped audit notes as the top-level status while preserving `latest_proof_record_failed` and `accepted_proof_record_missing` issue codes.
- 2026-05-20: Required runtime-health voice-edge benchmark evidence in live voice proof plans. `tests/test_provider_proof_plan_cli.py` first failed because live voice proof commands skipped `build-runtime-health-ledger`; proof plans and static operator surfaces now require the same-run runtime health ledger, `voice-edge-local-benchmark` ready status, and proof-record fields for `runtime_health_ledger_artifact_id` plus `voice_edge_benchmark_status`.
- 2026-05-20: Tightened accepted live-voice proof-record consistency after Leibniz review. A regression first failed because `voice_edge_benchmark_status=failed` could pass if checklist fields said passed; accepted live-voice records now require `voice_edge_benchmark_status=ready`, `execute_live_calls=true`, and `realtime_provider=gemma4_realtime`.
- 2026-05-20: Blocked local/draft publication destinations from accepted external publication proof. `tests/test_provider_proof_plan_cli.py` first failed because accepted publication proof could use `file://`, localhost, or local draft-preview evidence as the durable destination; validation now emits `durable_destination_not_external` for those substitutes.
- 2026-05-20: Expanded local/internal destination rejection after Leibniz review. Publication proof validation now also rejects private/link-local/unspecified IP URLs plus home, relative, and Windows path-like destinations while preserving external URLs and opaque external platform IDs.
- 2026-05-20: Closed DNS-based internal publication destination bypass after Leibniz review. Publication proof validation now rejects dotless hosts and reserved/internal suffixes such as `.internal`, `.lan`, `.test`, `.invalid`, and `.example`; accepted fixtures now use `linkedin.com` rather than a reserved `.example` URL.
- 2026-05-20: Closed non-HTTP publication destination scheme bypass after Leibniz review. Accepted publication proof now rejects schemes such as `ftp`, `ssh`, `javascript`, `chrome`, and `mailto`, preserving only external `http(s)` URLs and explicit opaque IDs such as `urn:li:share:123`.
- 2026-05-20: Closed short draft/preview/internal publication destination marker bypass after Leibniz review. Accepted publication proof now rejects separator-aware `draft`, `preview`, `local`, and `internal` tokens in durable destination evidence, including external-looking preview/draft URL paths.
- 2026-05-20: Narrowed publication destination marker rejection after Leibniz review found false positives. External `http(s)` URLs can now contain `local` or `internal` in ordinary slugs, while explicit `/draft` and `/preview` paths plus opaque draft/preview/local/internal evidence remain blocked.
- 2026-05-20: Rejected cross-channel accepted publication proof records. `tests/test_provider_proof_plan_cli.py` first failed because accepted proof could claim one channel while pointing to another known platform URL; validation now emits `destination_channel_mismatch` for known-platform contradictions.
- 2026-05-20: Normalized repo-canonical publication channel aliases after Leibniz review. `instagram_post` and `instagram_reel` now compare as Instagram, and `x_thread` compares as X, so matching proof destinations no longer fail channel validation.
- 2026-05-20: Blocked invalid accepted audit-note fields in provider proof completion status. `tests/test_provider_proof_plan_cli.py` first failed because a hand-edited accepted publication audit note with `destination_channel=linkedin` and an Instagram durable URL could satisfy completion status; the checker now returns `blocked_by_invalid_accepted_audit_note`, emits `accepted_audit_note_invalid_fields`, reports proof-level `latest_record_has_invalid_fields` plus `invalid_source_targets`, and keeps blocker-state changes separate. Focused provider-proof validation passed with `36 passed`.
- 2026-05-20: Closed Leibniz duplicate-field bypass in provider proof completion status. New red regressions proved duplicate accepted-note fields could hide a later contradiction or secret-shaped value behind the first parsed key; audit-note parsing now keeps ordered entries for secret scanning, treats duplicate accepted-note fields as invalid evidence, and focused provider-proof validation passed with `38 passed`.
- 2026-05-20: Required schema-backed accepted audit-note fields in provider proof completion status. Leibniz found accepted Markdown notes could omit required live voice session/config fields or publication policy/rollback fields; completion status now derives required audit fields from the proof schema and blocks missing, empty, or template-placeholder values as invalid accepted evidence. Focused provider-proof validation passed with `39 passed`.
- 2026-05-20: Required validation-summary and redaction-check audit lines for accepted provider proof completion status. Leibniz found notes could claim `valid_accepted_record` while omitting the generated post-capture summary and redaction check; completion status now requires the expected zero-failed summary count and passed/true redaction check. Focused provider-proof validation passed with `40 passed`.
- 2026-05-20: Required parseable provider-proof validation timestamps. Leibniz found accepted JSON records and accepted audit notes could use `validation_timestamp: not-a-date`; JSON validation now emits `invalid_validation_timestamp`, completion status treats unparseable accepted audit timestamps as invalid evidence, and focused provider-proof validation passed with `42 passed`.
- 2026-05-20: Prevented malformed invalid/secret accepted audit notes from being masked by older accepted evidence. Leibniz found older parseable accepted notes could hide later hand-edited accepted notes with unparseable timestamps plus invalid fields or secret-shaped values; completion status now blocks such unorderable invalid/secret markers conservatively, and focused provider-proof validation passed with `44 passed`.
- 2026-05-20: Added next-action proof commands to provider proof completion status. A red regression showed blocked completion status lacked the immediate operator handoff; proof entries now include `next_action`, `next_action_commands`, and `record_proof_in` for template generation, validation, audit recording, and recheck. Leibniz found the run-id-blocked branch still omitted the fields; placeholder and unsafe run ids now preserve the shape with safe `<run-id>` commands. Focused provider-proof validation passed with `46 passed`.
- 2026-05-20: Blocked secret-shaped provider proof run ids before command generation. Leibniz found token-shaped run ids could match the safe character whitelist and be echoed into proof-plan/completion-status commands; run-id classification now treats secret-shaped values as `unsafe_run_id`, sanitizes completion-status `run_id` output, and focused provider-proof validation passed with `47 passed`.
- 2026-05-20: Sanitized secret-shaped run ids in provider proof workspace initialization. Leibniz found `init-provider-proof-workspace` could still echo token-shaped run ids while blocking; workspace payloads now use the safe command run id and focused provider-proof validation passed with `48 passed`.
- 2026-05-20: Added no-secret provider proof closure-review status. `tests/test_provider_proof_plan_cli.py` first failed on missing `_provider_proof_closure_review_status_payload`; `provider-proof-closure-review-status` now rechecks proof completion through `--proof-audit-target`, scans closure-review `--audit-target` notes separately, reports latest approved/rejected review status, and keeps `state_change_allowed=false`. `Leibniz` found a hand-edited-note bypass where approved status did not bind to the expected proof set or requirement count; the status validator now requires both, focused provider-proof validation passed with `62 passed`, and final `Leibniz` fix-back review found no actionable issues.
- 2026-05-20: Added no-secret provider proof blocker-state update notes. `tests/test_provider_proof_plan_cli.py` first failed on missing `_record_provider_proof_blocker_state_update_payload`; `record-provider-proof-blocker-state-update` now refuses writes until closure-review status is approved, keeps proof/review/write targets separate, appends reviewed blocker candidate notes, and records `goal_completion_claimed=false`. `Leibniz` found missing explicit `state_change_allowed=false`, missing generated write-target preservation, and duplicate append risk; payloads/notes now keep the state-change boundary explicit, approved status preserves `--blocker-update-audit-target`, same-review replays return `already_recorded`, focused provider-proof validation passed with `65 passed`, and final `Leibniz` fix-back review found no actionable issues.
- 2026-05-20: Completed provider proof workspace closeout README handoff. `tests/test_provider_proof_plan_cli.py` first failed because `init-provider-proof-workspace` README stopped at completion status; it now lists closure-review template, validation, recording, status, and blocker-state update note commands, while preserving the `goal_completion_claimed=false` boundary. Focused provider-proof validation passed with `65 passed`, and `Leibniz` reported no actionable findings.
- 2026-05-20: Propagated provider proof closeout commands into static proof-plan packets. CLI proof plans, proof readiness, and the Gemma voice/publication boundary maps now carry/render `closeout_commands` for completion status, closure-review template, closure-review validation, closure-review recording, closure-review status, and reviewed blocker-state update notes. Focused parity checks passed for proof-plan propagation (`2 passed`) and the three browser proof surfaces (`3 passed`).
- 2026-05-20: Closed Leibniz's closeout-command propagation review. Leibniz found the static parity tests did not directly pin the CLI source-of-truth `closeout_commands` order; `tests/test_provider_proof_plan_cli.py` now asserts the exact six-command closeout chain for both live voice and publication proof-plan entries. Focused provider proof validation passed with `65 passed`, and Leibniz fix-back review reported no actionable findings.
- 2026-05-20: Added composite blocker reasons to provider proof plans. CLI proof plans, proof readiness, and the Gemma voice/publication boundary maps now carry/render `blocking_reasons`, so `run_id_not_concrete` and `blocked_by_placeholder_only_configuration` remain visible together when both block a proof attempt. Focused CLI regressions passed (`2 passed`), proof-plan parity passed (`2 passed`), and targeted browser surface checks passed (`4 passed`).
- 2026-05-20: Closed Leibniz's composite blocker-reason review. Leibniz found the new CLI tests did not directly pin `run_id_unsafe` or `blocked_by_missing_configuration`; provider proof-plan tests now cover unsafe run ids with otherwise configured credentials and shell-safe run ids with missing required configuration. Focused provider proof validation passed with `67 passed`.
- 2026-05-20: Added no-secret credential setup requirements to provider proof plans. CLI proof plans, proof readiness, and the Gemma voice/publication boundary maps now carry/render `credential_setup_requirements`, including `HF_TOKEN_FILE or HF_TOKEN`, LiveKit file-backed secret alternatives, and publication channel credential names. Focused CLI proof passed, proof-plan parity passed, and targeted browser surface checks passed.
- 2026-05-20: Added ordered operator sequences to provider proof plans. CLI proof plans, proof readiness, and the Gemma voice/publication boundary maps now carry/render `operator_sequence`, tying credential setup, safe run-id replacement, preflight, evidence capture, proof-record validation/recording, completion status, closure review, and reviewed blocker-state update into one no-secret ordered handoff. Focused CLI, parity, and browser checks passed.
- 2026-05-20: Exposed proof workspace initialization in provider proof plans. CLI proof plans, proof readiness, and the Gemma voice/publication boundary maps now carry/render `workspace_commands` for `init-provider-proof-workspace --run-id <run-id> --output-dir social_media_optimiser/output/provider-proof/<run-id>`, and `operator_sequence` includes workspace initialization before preflight/capture. Focused CLI, parity, and browser checks passed.
- 2026-05-20: Added expected proof workspace files to provider proof plans. CLI proof plans, proof readiness, and the Gemma voice/publication boundary maps now carry/render `workspace_expected_files` for the live voice proof template, external publication proof template, and `README.md` created by `init-provider-proof-workspace`. Focused CLI, parity, and browser checks passed.
- 2026-05-20: Fixed blocked proof workspace next action. `init-provider-proof-workspace` with placeholder or unsafe run ids now returns `replace_run_id_and_initialize_workspace` and a sanitized rerun command for the same output directory instead of pointing to completion status. Focused workspace regressions passed.
- 2026-05-20: Added runnable no-secret preflight commands to provider proof plans. CLI proof plans, proof readiness, the Gemma voice boundary map, the publication boundary map, and proof-workspace READMEs now carry/render `preflight_commands` for provider readiness, full Gemma/Kokoro/LiveKit runtime readiness, and non-mutating publish-readiness policy-channel checks. Leibniz found the first publication command would default `open_feedback_gate=true`; the command now explicitly sets `open_feedback_gate=false` and `mark_run_completed_if_ready=false`.
- 2026-05-20: Added stable preflight evidence targets to provider proof plans. CLI proof plans, proof readiness, the Gemma voice boundary map, the publication boundary map, and proof-workspace READMEs now carry/render `preflight_output_files` and shell-safe `preflight_capture_commands` for provider readiness, voice runtime readiness, and publish readiness JSON. Leibniz found custom workspace READMEs still pointed to the default proof path; they now use the actual initialized `--output-dir`, including paths with spaces. Focused CLI, parity, and browser export checks passed.
- 2026-05-20: Required preflight evidence links in provider proof records. Live voice proof schemas now require `provider_readiness_preflight_artifact_id` and `voice_runtime_readiness_preflight_artifact_id`; external publication proof schemas now require `publish_readiness_preflight_artifact_id`. Provider proof CLI, parity, and browser export checks passed after updating fixtures and static proof surfaces.
- 2026-05-20: Added no-secret preflight artifact validation. `validate-provider-proof-preflight-artifacts` validates captured preflight JSON file presence, JSON object shape, and token-shaped values before proof records reference the files; provider proof plans, proof readiness, Gemma voice boundary, publication boundary, and proof-workspace READMEs now expose artifact-id fields plus validation commands. `Leibniz` found token-shaped `--preflight-dir` paths could echo through output fields; a failing-first regression now blocks and redacts those paths. Proof-record validation/recording now accepts `--preflight-validation-path`, accepted proof schemas require `preflight_validation_report_artifact_id`, and accepted records reject missing or mismatched preflight validation reports while failed records remain recordable even if a generated report path is missing. Accepted audit notes now include preflight report status and matched-field summary, and completion status rejects accepted notes missing that summary. Provider proof tests passed with `76 passed`; static parity `4 passed`; browser proof surfaces `8 passed`; foundation `21 passed`.
- 2026-05-20: Added explicit preflight validation report capture handoff. `provider-proof-plan`, proof readiness, the Gemma voice boundary map, the publication boundary map, and generated proof-workspace READMEs now expose run-scoped `preflight_validation_report_files` plus `preflight_validation_capture_commands`, and proof record commands use those paths for `--preflight-validation-path` instead of `<preflight-validation.json>`. Focused provider-proof tests passed with `76 passed`, static parity with `4 passed`, browser proof surfaces with `8 passed`, compileall, foundation with `21 passed`, and `Leibniz` found no actionable issues.
- 2026-05-20: Tightened provider proof preflight validation semantics. `validate-provider-proof-preflight-artifacts` now validates captured provider-readiness, voice-runtime-readiness, and publish-readiness files against structured schemas plus proof-specific readiness rules before emitting artifact IDs. Blocked Gemma realtime, blocked voice runtime checks, blocked publication readiness, missing publish-channel credentials, feedback-gate side effects, empty publication review states, and non-policy publication review issues stay invalid. After `Leibniz` review, `needs_review` publication preflight now passes only when the sole remaining issue is `publish_channel_policy_review_required` and `ready=false`; fix-back review found no actionable issues. Provider-proof tests passed with `81 passed`, foundation with `21 passed`, static parity with `4 passed`, and compileall passed.
- 2026-05-20: Exposed provider proof preflight validation requirements. CLI proof plans, proof readiness, the Gemma voice boundary map, the publication boundary map, and proof-workspace READMEs now carry/render `preflight_validation_requirements` beside validation commands and report paths. Operators can see the exact semantic live voice and publication preflight rules before running the validator. Provider-proof tests passed with `81 passed`, static parity with `4 passed`, browser proof surfaces with `8 passed`, foundation with `21 passed`, compileall, `git diff --check`, whitespace scan, and non-test token-shape scan passed. `Leibniz` found the first publication wording weaker than the validator; the fix added exact ready/needs-review status semantics, required channel checks, accepted policy statuses, and fix-back review found no actionable issues.
- 2026-05-20: Added a no-secret provider proof attempt gate. CLI proof plans, proof readiness, Gemma voice boundary, publication boundary, and proof-workspace READMEs now expose `attempt_gate`, which separates run-id/credential blockers from preflight-capture readiness, keeps proof commands blocked until valid preflight artifacts exist, and lists workspace/capture/validation commands only when preflight capture can start. Provider proof tests passed with `81 passed`, static parity with `4 passed`, browser proof surfaces with `8 passed`, foundation with `21 passed`, compileall, `git diff --check`, whitespace scan, and non-test token-shape scan passed. `Leibniz` found no actionable issues.
- 2026-05-20: Initialized concrete provider proof workspace `social_media_optimiser/output/provider-proof/RUN-2026-05-20-NEXT/` with both proof templates and README. The generated templates parse as JSON, the README carries credential setup requirements, current no-secret attempt gates, and record/closeout path, and the generated workspace token-shape scan found no matches. Both proof attempts remain blocked by placeholder credentials; no preflight or live proof command was run. Provider proof and foundation tests passed after adding the credential setup section, and `Leibniz` found no actionable issues.
- 2026-05-20: Removed the placeholder preflight-validation report path from proof-record operator commands. `provider-proof-record-template` and missing-proof `provider-proof-completion-status` now point each proof's validation and recording command to the run-scoped `*.preflight-validation.json` report path, matching `provider-proof-plan` and the generated proof-workspace README. Provider proof tests passed with `81 passed`, foundation with `21 passed`, compileall and hygiene scans passed, and `Leibniz` fix-back found no actionable issues.
- 2026-05-20: Added no-secret local provider config classification to provider proof plans. A red regression first proved `LOCAL_PROVIDER_CONFIG_FILE` endpoint URLs could make the live voice credential state ready while the proof-plan packet omitted the local-provider evidence fields; CLI proof plans, credential snapshots, proof readiness, Gemma voice boundary, publication boundary, and the concrete `RUN-2026-05-20-NEXT` artifacts now expose `configured_local_provider_inputs` and `local_provider_config_loaded` without serializing endpoint values. Focused CLI/classifier checks passed with `149 passed`; static parity passed with `4 passed`.
- 2026-05-20: Persisted live voice endpoint setup into `LOCAL_PROVIDER_CONFIG_FILE`. An executable regression first failed because the generated setup commands did not create `.secrets/local_provider_config.json`; the proof-plan handoff now validates Gemma multimodal, Kokoro TTS, and LiveKit URL values before writing the ignored local provider config file with `umask 077` and `chmod 600`. Leibniz found malformed or credential-bearing endpoint strings could be persisted; the fix now fails with env-name-only errors and leaves an existing config file unchanged. The concrete proof-plan, README, workspace-validation report, proof readiness, and Gemma voice boundary map were refreshed.
- 2026-05-20: Made live voice endpoint setup merge-safe for existing local provider configs. A red executable regression first showed the setup command dropped unmanaged keys in `LOCAL_PROVIDER_CONFIG_FILE`; it now preserves keys outside the managed Gemma/Kokoro/LiveKit endpoint set while replacing the three managed endpoint values.
- 2026-05-20: Added executable setup-plus-recheck coverage for provider proof setup commands. The regression runs generated live voice and publication credential setup snippets in a temp workspace, then rebuilds `provider-proof-plan` from `.env.example` and proves both proofs become `ready_for_runtime_attempt` through file/local-provider config without leaking token or endpoint values.
- 2026-05-20: Fixed stale source-research status in the product Source panel. Red regressions first showed an older blocked web-research message could keep the rail in `Research blocked` after a newer `web_research_completed` event recorded provider-backed sources, a malformed message timestamp could mask valid event evidence, and a higher-id malformed blocked event could mask a valid completed event; `sourceResearchStatus` now uses parsed message/event timestamp ordering, orders events by valid `created_at` before `event_id`, preserves newer active-message `Research queued` precedence, and disables `Run web research` while provider-backed research is already running. Focused SourcePanel race tests passed with `251 passed`.
- 2026-05-21: Hardened external publication proof preflight policy semantics. Red CLI regressions first proved `validate-provider-proof-preflight-artifacts` accepted contradictory payloads with overall publish readiness `ready` while channel `policy_status` was still `needs_review`, with ready channel-level blocking issues, with policy-review-only `needs_review` after every channel policy was already acknowledged, with duplicate policy-review blockers passing a set-based issue check, with non-policy channel blockers hidden under policy-review-only handoff, and with policy-review blockers attached to acknowledged channels; validation now requires acknowledged channel policies plus no channel blockers when status is `ready`, exact single policy-review blocker text and only policy-review channel blockers for the handoff, no policy-review blocker on acknowledged channels, and at least one pending channel policy for the policy-review-only path.
- 2026-05-20: Closed malformed existing `LOCAL_PROVIDER_CONFIG_FILE` tracebacks in generated setup commands. Red executable regressions first showed malformed JSON, non-UTF-8 bytes, and directory paths produced Python tracebacks; the command now emits only `Invalid existing LOCAL_PROVIDER_CONFIG_FILE`, leaves existing file state unchanged, and avoids printing existing config content or new endpoint/secret values.
- 2026-05-21: Required product-run preflight evidence for provider proofs. `provider-proof-plan`, preflight validation, proof-record validation/audit, proof-readiness, Gemma voice boundary, publication boundary, and concrete `RUN-2026-05-20-NEXT` artifacts now require `product-run.preflight.json` from `GET /api/runs/<uuid>` plus matching `validated_product_run_id` before accepted proof evidence can count. Leibniz found a run-id-only JSON bypass; the validator now requires the real `/api/runs/<uuid>` response shape and fails hand-written `{"run_id": ...}` captures with `product_run_payload_schema_invalid`. Validation passed provider proof suite `181 passed`, blocker snapshot consistency `4 passed` outside sandbox, static HTML browser suite `24 passed` outside sandbox, compileall over `src tests`, `git diff --check`, and Leibniz fix-back with no Critical or Important findings; no live provider or publication proof was run.
- 2026-05-21: Captured local LiveKit dev transport readiness for UUID proof workspace `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`. A temporary local LiveKit dev container plus FastAPI preflight with local dev URL/key/secret produced `voice-runtime-readiness.local-livekit.preflight.json`, and the canonical `voice-runtime-readiness.preflight.json` marked `livekit-transport` ready after `RoomService/ListRooms`. Superseded on 2026-05-23: current OpenRouter/LiveKit/Kokoro preflight is valid and live voice waits on accepted proof-record capture/recheck, not provider setup.
- 2026-05-21: Installed/used the locked `voice` extra to capture stronger local voice-runtime readiness for UUID proof workspace `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`. `voice-runtime-readiness.local-voice-extra.preflight.json` and the refreshed canonical `voice-runtime-readiness.preflight.json` now mark `livekit-agent-participant` ready after constructing the LiveKit participant server and `kokoro-tts` ready via the local Kokoro package, while preserving ready `livekit-transport`, `rust-voice-edge`, and context-pruning checks. `provider-backed-live-voice-proof.preflight-validation.json` was still `invalid_preflight_artifacts` because provider readiness was not ready and `gemma-audio-reasoning` still needed HF credential plus `GEMMA4_MULTIMODAL_ENDPOINT_URL`; this was local runtime readiness only, not accepted provider-backed Gemma audio proof. Superseded on 2026-05-23: current operator-input readiness now has OpenRouter and LiveKit inputs configured with no live-voice operator-input blocker; `GEMMA4_MULTIMODAL_ENDPOINT_URL` is legacy native-audio background.
- 2026-05-21: Refreshed provider readiness for UUID proof workspace `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e` under the same local LiveKit dev URL/key/secret used by the voice-runtime preflight. `provider-readiness.local-livekit.preflight.json` and the canonical `provider-readiness.preflight.json` now mark `gemma4-realtime` ready, and `provider-backed-live-voice-proof.preflight-validation.json` no longer reports `provider_readiness_gemma4_realtime_not_ready`; the remaining live-voice validation issue is `checks.gemma-audio-reasoning`. Superseded on 2026-05-23: `HF_TOKEN_FILE` is configured, so the native Gemma audio input blocker is now `GEMMA4_MULTIMODAL_ENDPOINT_URL`. This is still not accepted provider-backed live voice proof.
- 2026-05-21: Added `current-proof-status.md` to UUID proof workspace `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e` so operators can see the current blocker state beside the generated README without editing generated proof-plan handoff text. It records that local LiveKit dev provider readiness, LiveKit transport, LiveKit participant startup construction, local Kokoro, Rust edge, and context pruning are ready; only Gemma audio reasoning remains blocked for live voice, while external publication remains blocked by LinkedIn credential and policy review.
- 2026-05-21: Added no-secret `runtime-configuration-snapshot.local-livekit.json` to UUID proof workspace `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`. The snapshot reconciles the current proof evidence with the at-rest proof plan: local LiveKit readiness was captured through temporary process environment inputs, while no secret files or `LOCAL_PROVIDER_CONFIG_FILE` were persisted, so `proof-plan.json` correctly remains placeholder-only. The snapshot records configured input names only, ready local dependency checks, and the remaining missing Gemma audio inputs without endpoint, key, or token values.
- 2026-05-21: Recorded a no-secret failed `provider-backed-live-voice-proof` attempt for UUID proof workspace `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`. `provider-backed-live-voice-proof.failed-record.json` validates as `valid_failed_record`, `provider-backed-live-voice-proof.failed-record-audit.json` wrote the failed record to all configured audit targets with `state_change_allowed=false`, and refreshed `completion-status.json` now reports latest failed live-voice proof plus missing accepted external publication proof. The failed record preserved the then-current blocker: Gemma audio reasoning still needed HF credential plus `GEMMA4_MULTIMODAL_ENDPOINT_URL`.
- 2026-05-21: Recorded a no-secret failed `external-publication-proof` attempt for UUID proof workspace `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`. `external-publication-proof.failed-record.json` validates as `valid_failed_record`, `external-publication-proof.failed-record-audit.json` wrote the failed record to all configured audit targets with `state_change_allowed=false`, and refreshed `completion-status.json` now reports `blocked_by_latest_failed_proof_record` for both required proofs. The failed record preserves the real blocker: LinkedIn publication remains blocked by missing channel credential and policy review, with no durable external platform destination proof.
- 2026-05-21: Captured closure-review and blocker-state no-write evidence for UUID proof workspace `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`. `closure-review-template.json` and `closure-review-status.json` both remain `blocked_by_completion_status` because `completion-status.json` is `blocked_by_latest_failed_proof_record`; `blocker-state-update.json` remains `blocked_by_closure_review_status` with `blocker_state_update_note_recorded=false`, `goal_completion_claimed=false`, `written_targets=[]`, and `state_change_allowed=false`.
- 2026-05-21: Added compact `operator-unblocker-checklist.md` for UUID proof workspace `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`. The checklist originally narrowed the next real inputs to HF credential, `GEMMA4_MULTIMODAL_ENDPOINT_URL`, LinkedIn credential, LinkedIn policy acknowledgement, exact external destination proof, and rollback or postcondition evidence, while preserving the no-secret/no-state-change boundary. Superseded on 2026-05-23: `HF_TOKEN_FILE` is configured; the current OpenRouter live-voice operator inputs are configured; `GEMMA4_MULTIMODAL_ENDPOINT_URL` is legacy native-audio background.
- 2026-05-21: Added `provider-proof-operator-unblocker-checklist` CLI generation for the UUID proof operator checklist. The concrete `operator-unblocker-checklist.md` is now regenerated from the current blocker matrix and proof workspace artifacts, with focused regression coverage proving the checked-in handoff matches the fresh CLI output.
- 2026-05-21: Updated provider proof workspace README generation to include current-blocker handoff commands. The UUID workspace `README.md` now points operators to regenerate `current-blocker-matrix.json` and `operator-unblocker-checklist.md` after proof attempts, and focused regression coverage guards the generated README.
- 2026-05-21: Refreshed UUID workspace validation after the README handoff update. `workspace-validation.json` for `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e` reports `valid_workspace`, `issue_codes=[]`, and validates both proof templates plus `README.md`; this keeps the workspace report usable for later accepted proof records without changing blocker state.
- 2026-05-21: Refreshed UUID preflight-validation reports after the workspace validation update. Live voice then remained `invalid_preflight_artifacts` because `gemma-audio-reasoning` still needed HF credential plus `GEMMA4_MULTIMODAL_ENDPOINT_URL`, while local LiveKit/Kokoro/Rust checks remained ready. External publication remained `invalid_preflight_artifacts` because LinkedIn credential and policy review were still missing; no accepted proof or blocker-state update was recorded. Superseded on 2026-05-23: current OpenRouter/LiveKit/Kokoro preflight is valid and the remaining live-voice work is accepted proof-record capture/recheck.
- 2026-05-21: Refreshed UUID failed-record validation reports after the workspace/preflight refresh. Both `provider-backed-live-voice-proof.failed-record-validation.json` and `external-publication-proof.failed-record-validation.json` still report `valid_failed_record`, `proof_outcome=failed`, `issue_codes=[]`, and `state_change_allowed=false`; this preserves explicit no-state-change failed evidence, not accepted proof.
- 2026-05-21: Propagated refreshed failed-proof state through downstream UUID handoff artifacts. `completion-status.json` remains `blocked_by_latest_failed_proof_record`, closure review remains blocked by completion status, `blocker-state-update.json` remains no-write with `goal_completion_claimed=false`, and the blocker matrix plus operator checklist were regenerated from that state.
- 2026-05-21: Added machine-readable `current-blocker-matrix.json` for UUID proof workspace `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`. The matrix separates already-evidenced local readiness from the remaining external proof blockers: Gemma audio reasoning still needs HF token plus `GEMMA4_MULTIMODAL_ENDPOINT_URL`, and publication still needs LinkedIn credential, policy acknowledgement, durable external destination proof, and rollback or postcondition evidence.
- 2026-05-21: Added `provider-proof-current-blocker-matrix` CLI generation for the UUID proof blocker matrix. The concrete `current-blocker-matrix.json` is now regenerated from proof workspace artifacts instead of being hand-authored, with focused regression coverage proving the checked-in matrix matches the fresh CLI payload.
- 2026-05-21: Aligned the sprint and Kanban tracker with the refreshed UUID proof state. The blocked live-voice item now names only Gemma audio reasoning inputs as the current provider blocker because local LiveKit/Kokoro/Rust readiness is already evidenced; the publication blocker now names LinkedIn credential, policy review, durable destination, and rollback/postcondition evidence. The tracker also records that downstream completion/closure/blocker-state artifacts were regenerated and remain no-write.
- 2026-05-21: Made current-blocker handoff generators accepted-proof aware. Red regressions first showed future accepted proof records would still render stale Gemma audio and LinkedIn operator prompts; `provider-proof-current-blocker-matrix` now clears `remaining_blockers` for accepted proofs, and `provider-proof-operator-unblocker-checklist` switches to closure-review guidance instead of credential prompts. Provider proof tests passed with `193 passed`.
- 2026-05-21: Corrected accepted-proof evidence refs in the current blocker matrix. A red regression first showed accepted proof rows would still cite `*.failed-record*` files; accepted rows now cite `completion-status.json` plus `accepted_record_sources`, while current failed rows keep failed-record evidence.
- 2026-05-21: Bound current-blocker accepted state to per-proof completion status. A red regression first showed top-level `accepted_proofs` could clear blockers while completion status remained `blocked_by_incomplete_audit_target_coverage`; the matrix now keeps incomplete/invalid/secret accepted proof rows blocked until each proof status is `accepted_record_found`.
- 2026-05-21: Added checklist repair guidance for incomplete accepted-proof audit coverage. A red regression first showed partial audit coverage could reopen normal credential/preflight retry instructions; the checklist now names missing audit targets and directs operators to record the same validated accepted proof record there before rerunning completion status.
- 2026-05-21: Added checklist repair guidance for invalid accepted-proof audit notes. A red regression first showed invalid accepted audit notes could be treated as missing proof or normal provider retry work; the checklist now directs operators to record a corrected validated accepted proof record and rerun completion status.
- 2026-05-21: Corrected operator checklist accepted-proof state wording. A red regression first showed latest failed proof records were described as missing accepted records; `operator-unblocker-checklist.md` now reports latest failed records separately from missing and accepted states, and the UUID checklist was regenerated from the CLI.
- 2026-05-21: Completed the operator checklist closeout sequence. Red regressions first showed `operator-unblocker-checklist.md` skipped `validate-provider-proof-closure-review` and `record-provider-proof-closure-review`; the checklist now lists completion status, closure-review template, validation, recording, status, and blocker-state update in order, and the UUID checklist was regenerated from the CLI.
- 2026-05-21: Completed the operator checklist proof-record sequence. Red regressions first showed `operator-unblocker-checklist.md` jumped from preflight validation to completion status without proof-record template, validation, and recording commands; the checklist now includes those proof-record commands for live voice and external publication with repo-relative report paths, and the UUID checklist was regenerated from the CLI.
- 2026-05-21: Preserved audit-state blocker lists in the current blocker matrix. Red regressions first showed `provider-proof-current-blocker-matrix` dropped completion-level `incomplete_audit_target_proofs`, `secret_shaped_audit_note_proofs`, and `invalid_accepted_audit_note_proofs`; the matrix now includes those lists when non-empty while avoiding current UUID artifact churn, and the full provider-proof CLI suite passed with `198 passed`.
- 2026-05-21: Made current-blocker matrix audit summaries row-consistent. A red regression first showed per-proof secret/invalid accepted-note states could render in proof rows without matching top-level completion summary lists when the source completion file omitted those arrays; summary lists are now derived from proof row statuses while preserving explicit source entries, and the full provider-proof CLI suite passed with `199 passed`.
- 2026-05-21: Added credential snapshot refresh to the operator unblocker checklist. Red regressions first showed latest-failed and concrete UUID checklists omitted `blocker-credential-snapshot > .../credential-snapshot.json`; the checklist now tells operators to refresh no-secret credential state and proof plan after changing provider/platform inputs, the UUID checklist was regenerated, and the full provider-proof CLI suite passed with `199 passed`.
- 2026-05-21: Captured current UUID no-secret credential snapshot. A red concrete-artifact regression first showed `credential-snapshot.json` was missing for UUID `190ae2f9-a74b-4a23-b39c-aaf2d636bd8e`; the generated snapshot records both live voice and external publication as `blocked_by_placeholder_only_configuration`, and the full provider-proof CLI suite passed with `200 passed`.
- 2026-05-21: Clarified the credential snapshot retry gate in the operator unblocker checklist. Red regressions first showed the checklist did not name the expected snapshot state after credentials are supplied; it now requires both proof snapshots to read `runtime_configuration_present_unverified` before retrying preflight capture, and the full provider-proof CLI suite passed with `200 passed`.
- 2026-05-21: Surfaced current credential snapshot states in the operator unblocker checklist. A red concrete checklist regression first showed the current no-secret snapshot state was hidden in `credential-snapshot.json`; the checklist now renders both required proofs as `blocked_by_placeholder_only_configuration` until inputs are supplied, and the full provider-proof CLI suite passed with `201 passed`.
- 2026-05-21: Added credential snapshot freshness to the operator unblocker checklist. A red concrete checklist regression first showed the checklist omitted the current snapshot `checked_at`; it now renders the timestamp beside current snapshot states, and the full provider-proof CLI suite passed with `201 passed`.
- 2026-05-21: Added a no-secret operator input template to the UUID proof workspace. A red concrete artifact regression first showed `operator-inputs.template.env` was missing; the template now names placeholder fields for HF token file, Gemma multimodal endpoint, LinkedIn token file, policy acknowledgement, durable destination, and rollback/postcondition evidence, and the full provider-proof CLI suite passed with `202 passed`.
- 2026-05-21: Linked the operator input template from the UUID unblocker checklist. A red concrete checklist regression first showed the handoff did not mention `operator-inputs.template.env`; the generated checklist now points to it before credential snapshot refresh, and the full provider-proof CLI suite passed with `202 passed`.
- 2026-05-21: Surfaced checkpoint evidence in the operator unblocker checklist. A red concrete checklist regression first showed the UUID handoff omitted existing `operator-checkpoint.json` and `checkpoints.list.json`; the checklist now includes a `Checkpoint Evidence` section for `operator_preflight`, and the full provider-proof CLI suite passed with `200 passed`.
- 2026-05-21: Made operator checklist checkpoint kind artifact-backed. A red regression first showed the checkpoint section hardcoded `operator_preflight`; it now reads checkpoint kind from `operator-checkpoint.json` or falls back to `checkpoints.list.json`, and the full provider-proof CLI suite passed with `201 passed`.
- 2026-05-21: Added checkpoint id and event cursor to the operator unblocker checklist. Red regressions first showed recovery handoff omitted the checkpoint UUID and event cursor; the checklist now prints both from checkpoint artifacts, the UUID checklist was regenerated, and the full provider-proof CLI suite passed with `201 passed`.
- 2026-05-21: Promoted `operator-inputs.template.env` into reusable proof workspace initialization. A red workspace-init regression first showed new proof workspaces did not create the operator input template; `init-provider-proof-workspace` now writes it, refuses to overwrite it, includes it in `README.md`, and `validate-provider-proof-workspace` plus `provider-proof-plan` now treat it as an expected workspace file. The UUID and placeholder proof-plan artifacts were refreshed, focused workspace regressions passed with `5 passed`, and the full provider-proof CLI suite passed with `202 passed`.
- 2026-05-21: Added `provider-proof-operator-input-readiness` as a no-secret gate before credential snapshot refresh. Red regressions first showed there was no parser/status payload for a filled operator input file and the generated README omitted the readiness command; the new command reports field-level readiness without printing secret file contents, endpoint URLs, or durable publication URLs. The UUID workspace now includes `operator-input-readiness.json` with `blocked_by_operator_inputs`, `README.md` and `operator-unblocker-checklist.md` both include the capture command, and the full provider-proof CLI suite passed with `205 passed`.
- 2026-05-21: Wired filled operator input files into the retry classifiers. Red regressions first showed `blocker-credential-snapshot` and `provider-proof-plan` ignored `operator-inputs.template.env`; both commands now accept `--operator-input-path`, merge only non-placeholder credential/file fields, and keep output value-free. The UUID operator checklist now passes that path into snapshot and proof-plan refresh commands, while current status remains blocked because the UUID input file still contains placeholders.
- 2026-05-21: Restored README/checklist command parity for operator-input retries. A red README regression first showed generated proof workspace READMEs omitted the `--operator-input-path` snapshot/proof-plan refresh commands that the checklist used; README generation now includes the same retry chain, and the UUID `README.md` plus `workspace-validation.json` were refreshed.
- 2026-05-21: Surfaced current operator input readiness in the UUID checklist. A red concrete checklist regression first showed restart handoff omitted `operator-input-readiness.json` status; the checklist now prints the current status, checked_at, and issue codes, showing `blocked_by_operator_inputs` with missing secret-file evidence and placeholder fields.
- 2026-05-21: Hardened operator input readiness against substitute external evidence. A red regression first showed local/example Gemma endpoint URLs and local/draft publication destinations could pass the readiness gate; readiness now emits `operator_input_local_substitute`, and snapshot/proof-plan merging ignores substitute Gemma endpoint values instead of promoting them to runtime retry readiness.
- 2026-05-21: Required LinkedIn destinations in operator publication inputs. A red regression first showed an Instagram durable URL could make the LinkedIn publication proof input gate read ready; readiness now emits `operator_input_destination_channel_mismatch` unless `PUBLICATION_DURABLE_PLATFORM_ID_OR_URL` resolves to LinkedIn.
- 2026-05-21: Rejected draft/local policy and rollback artifact IDs in operator publication inputs. A red regression first showed local policy/rollback strings could mark the publication input gate ready; readiness now emits `operator_input_local_artifact_substitute` for those fields.
- 2026-05-21: Rejected secret-shaped token file paths in operator inputs. A red regression first showed a LinkedIn token file path containing `linkedin_secret` could mark the publication input gate ready; readiness now emits `operator_input_secret_path_detected`, and snapshot/proof-plan input merging skips those paths.
- 2026-05-21: Added blocked-field summaries to the operator unblocker checklist. A red concrete checklist regression first showed restart handoff listed readiness issue codes but not the affected input fields; the checklist now renders field names from `operator-input-readiness.json` while omitting values.
- 2026-05-21: Added operator-input readiness to the current blocker matrix. A red concrete matrix regression first showed `current-blocker-matrix.json` omitted the input readiness state that the checklist carried; the matrix now includes status, checked_at, issue codes, blocked fields, and `operator-input-readiness.json` as evidence.
- 2026-05-21: Grouped operator-input readiness by proof in the current blocker matrix. A red concrete matrix regression first showed `operator_input_readiness` exposed only a flat blocked field list; the matrix now includes per-proof state and value-free blocked fields for live voice and external publication follow-up.
- 2026-05-21: Added per-proof next actions to current blocker matrix operator-input readiness. A red concrete matrix regression first showed automation could see blocked fields but not the route-specific action; live voice now points to HF token file plus Gemma multimodal endpoint supply, and publication points to LinkedIn token, policy, destination, and rollback evidence supply.
- 2026-05-21: Added per-proof retry commands to current blocker matrix operator-input readiness. A red concrete matrix regression first showed automation had route-specific next actions but no command list; each proof now carries repo-relative commands to refresh operator input readiness, credential snapshot, proof plan, and the current blocker matrix after inputs change.
- 2026-05-21: Added grouped readiness field categories to current blocker matrix operator-input proof entries. A red concrete matrix regression first showed route agents had only flat blocked fields; each proof now carries missing, placeholder, invalid, and unavailable secret-file field groups without values.
- 2026-05-21: Added per-proof operator-input issue codes to the current blocker matrix. A red concrete matrix regression first showed route agents had field groups but still needed to parse `operator-input-readiness.json` for issue codes; each proof now carries deduplicated issue codes derived from its blocked fields.
- 2026-05-21: Mirrored per-proof operator-input routing into the operator unblocker checklist. A red concrete checklist regression first showed the Markdown handoff had only global readiness state; it now lists each proof's input readiness state, issue codes, and field groups without values.
- 2026-05-21: Added strict blocked-input exit support to `provider-proof-operator-input-readiness`. Red CLI regressions first showed `fail_on_blocked` had no effect; the command now prints the no-secret JSON payload and exits with code 2 unless all required proof inputs are ready, while ready inputs still exit normally. The operator checklist now documents the optional `--fail-on-blocked` mode.
- 2026-05-21: Exposed the strict readiness command in generated handoffs. Red README and matrix regressions first showed `--fail-on-blocked` existed in the CLI but was not discoverable from the command-source README or machine-readable blocker matrix; both now include repo-relative strict readiness commands.
- 2026-05-21: Added readiness exit-policy metadata to `operator-input-readiness.json`. A red concrete readiness regression first showed automation had to infer strict exit behavior from CLI docs; the artifact now records default exit code, blocked-input strict exit code, strict blocked statuses, and the ready status without exposing values.
- 2026-05-21: Mirrored operator-input exit policy into the current blocker matrix. A red concrete matrix regression first showed route agents still had to open `operator-input-readiness.json` to interpret strict exit behavior; `current-blocker-matrix.json` now carries the same exit policy beside the strict readiness command.
- 2026-05-21: Added a guarded strict retry sequence to the current blocker matrix. A red concrete matrix regression first showed agents had to manually combine the strict readiness command with later retry commands; `operator_input_readiness.guarded_next_action_commands` now starts with `--fail-on-blocked` and then lists snapshot, proof-plan, and matrix refresh commands.
- 2026-05-21: Added invalid operator-input file repair next action. A red CLI regression first showed strict readiness failures for missing input files lacked a route-specific next action; invalid input file payloads now emit `next_action: fix_operator_input_file`.
- 2026-05-21: Added effective strict exit-code metadata to operator input readiness and current blocker matrix. A red regression first showed agents had to derive the strict-mode exit code from policy plus status; both artifacts now expose `effective_fail_on_blocked_exit_code` without values.
- 2026-05-21: Mirrored effective strict exit-code metadata into the operator unblocker checklist. A red concrete checklist regression first showed the Markdown handoff still omitted the strict stop/continue code; it now renders the effective `--fail-on-blocked` exit code from readiness JSON.
- 2026-05-21: Added the exact strict readiness command to the operator unblocker checklist. A red concrete checklist regression first showed the handoff described `--fail-on-blocked` without a copyable command; the checklist now renders a strict capture command alongside the default readiness command.
- 2026-05-21: Added per-proof guarded retry commands to the current blocker matrix. A red concrete matrix regression first showed route-specific proof rows only had the unguarded retry chain; each operator-input proof row now carries strict-first `guarded_next_action_commands`.
- 2026-05-21: Added a guarded retry sequence to the operator unblocker checklist. A red concrete checklist regression first showed the Markdown handoff had a strict readiness command but not the full strict-first retry chain; it now renders strict readiness, credential snapshot, proof-plan, and matrix refresh commands together.
- 2026-05-21: Added per-proof operator-input next actions to the operator unblocker checklist. A red concrete checklist regression first showed the Markdown handoff named proof states and fields but not route actions; live voice and publication rows now show the value-free next action.
- 2026-05-21: Added required evidence after operator-input unblock to the current blocker matrix. A red concrete matrix regression first showed per-proof input rows stopped at fields/actions; they now name readiness, snapshot, proof-plan, preflight, and proof-record evidence required after unblock.
- 2026-05-21: Mirrored required evidence after operator-input unblock into the operator unblocker checklist. A red concrete checklist regression first showed human handoff omitted the evidence contract; each proof row now lists the value-free evidence required after inputs are supplied.
- 2026-05-21: Added guarded retry sequence to provider proof workspace README generation. A red workspace README regression first showed the command-source README had strict readiness and unguarded commands separately; it now renders the strict-first readiness, credential snapshot, proof-plan, and matrix refresh chain.
- 2026-05-21: Added required evidence after input unblock to provider proof workspace README generation. A red workspace README regression first showed README command handoff lacked the proof evidence contract; it now lists value-free required evidence for live voice and publication after inputs are supplied.
- 2026-05-21: Added operator-input readiness to static blocker-surface proof packets. A red browser regression first showed proof readiness and boundary-map exports stopped at must-capture evidence; the exported packets now carry value-free input status, next action, strict exit code, blocked fields, strict command, and required evidence after unblock.
- 2026-05-21: Re-aligned static proof-plan workspace expected files with CLI output. A browser consistency regression first showed HTML exports still omitted `operator-inputs.template.env`; proof-readiness and boundary-map proof plans now include it before `README.md`.
- 2026-05-21: Added guarded retry commands to static proof-packet exports. A red browser regression first showed exported static packets omitted `guarded_next_action_commands`; proof-readiness, Gemma voice boundary, and publication boundary packets now expose and render the strict-first readiness, credential snapshot, proof-plan, and matrix refresh sequence.
- 2026-05-21: Added strict exit policy to static proof-packet exports. A red browser regression first showed static packets omitted `exit_policy`; proof-readiness, Gemma voice boundary, and publication boundary packets now expose and render default exit code, strict blocked exit code, strict blocked statuses, and ready status.
- 2026-05-21: Added readiness diagnostics to static proof-packet exports. A red browser regression first showed static packets omitted `checked_at`, `evidence_ref`, `issue_codes`, and per-proof field groups; proof-readiness, Gemma voice boundary, and publication boundary packets now expose and render them.
- 2026-05-21: Added report-only retry commands to static proof-packet exports. A red browser regression first showed static packets omitted `next_action_commands`; proof-readiness, Gemma voice boundary, and publication boundary packets now expose and render the report-only retry sequence beside the guarded sequence.
- 2026-05-21: Added stable proof ids to static proof-packet exports. A red browser regression first showed static packets omitted `proof_id`; proof-readiness, Gemma voice boundary, and publication boundary packets now expose and render route ids for live voice and external publication.
- 2026-05-21: Added a versioned schema contract to static proof-packet exports. A red browser regression first showed packets omitted schema contract fields; proof-readiness, Gemma voice boundary, and publication boundary packets now expose and render `operator-proof-packet.v1` plus the value-free handoff contract.
- 2026-05-21: Added source artifact refs to static proof-packet exports. A red browser regression first showed packets omitted packet-level refs; proof-readiness, Gemma voice boundary, and publication boundary packets now expose and render readiness, matrix, and operator-input template refs.
- 2026-05-21: Added a no-state-change guardrail to static proof-packet exports. A red browser regression first showed packets omitted blocker-state permissions; proof-readiness, Gemma voice boundary, and publication boundary packets now expose and render `state_change_allowed: false` until accepted proof and closure review exist.
- 2026-05-21: Added matrix parity refs to static proof-packet exports. A red browser regression first showed packets omitted authoritative matrix row paths; proof-readiness, Gemma voice boundary, and publication boundary packets now expose and render `operator_input_readiness.proofs.<proof_id>` refs.
- 2026-05-21: Added proof-packet matrix parity regression. The browser test now compares static readiness fields to `current-blocker-matrix.json` with UUIDs normalized; the red run caught `blocked_fields` order drift, and static packets now match the matrix row order.
- 2026-05-21: Converted proof-packet matrix refs to JSON Pointer paths. A red browser regression first showed dotted refs were not unambiguous for hyphenated proof ids; proof-readiness, Gemma voice boundary, and publication boundary packets now expose `/operator_input_readiness/proofs/<proof-id>` refs.
- 2026-05-21: Emitted operator proof packets from the current blocker matrix. A red CLI regression first showed the machine matrix lacked route-level packet contracts; `current-blocker-matrix.json` now includes `operator_proof_packets` for live voice and external publication, and the UUID artifact was refreshed.
- 2026-05-21: Verified the matrix proof-packet layer end to end. Full provider-proof CLI regression passed with `215 passed`, static proof-surface browser regression passed with `14 passed`, `compileall` passed for `tests src`, `git diff --check` passed, the touched-file trailing-whitespace scan was clean, and generated/user-facing proof artifacts did not expose token-shaped secrets, absolute workspace paths, or concrete HF/LinkedIn publication URLs. Remaining completion blockers are unchanged: real provider-backed OpenRouter LiveKit live voice proof and real LinkedIn publication proof.
- 2026-05-21: Made `current-proof-status.md` a generated status packet. A red exact-match regression first showed the current proof status was hand-authored and had no generator; `provider-proof-current-status` now renders the no-secret Markdown status from current workspace artifacts, the UUID README lists the status regeneration command, the UUID workspace validation remains `valid_workspace`, and the full provider-proof CLI suite passed with `216 passed`.
- 2026-05-21: Exposed generated status packets from the machine blocker matrix. A red matrix regression first showed non-browser agents could find `operator-unblocker-checklist.md` but not `current-proof-status.md`; `current-blocker-matrix.json` now emits `current_state_packets`, `current_state_packet_commands`, and `next_status_packet`, and the full provider-proof CLI suite passed with `216 passed`.
- 2026-05-21: Propagated current-state packet handoff into static proof packets. A red browser regression first showed proof-readiness and boundary-map exports omitted `current-proof-status.md` discovery; proof-readiness, Gemma voice boundary, and publication boundary packets now render/export `current_state_packets`, `current_state_packet_commands`, `next_status_packet`, and `next_operator_packet`. Focused proof-packet browser coverage passed with `2 passed`; the full static proof-surface browser suite passed with `14 passed`.
- 2026-05-21: Made the generated proof status packet self-describing. A red exact-match regression first showed `current-proof-status.md` lacked the current-state packet contract; it now lists packet names, regeneration commands, `next_status_packet`, and `next_operator_packet` directly, and the full provider-proof CLI suite passed with `216 passed`.
- 2026-05-21: Added `current-proof-status.md` refresh to operator-input retry chains and static proof-packet retry exports. A red CLI regression first showed retry sequences stopped after matrix refresh; after generator, artifact, and HTML packet updates, focused CLI retry coverage passed with `2 passed`, focused proof-packet browser parity passed with `2 passed`, static proof-surface browser coverage passed with `14 passed`, provider-proof CLI coverage passed with `216 passed`, `compileall` passed, `git diff --check` passed, trailing-whitespace scan was clean, and the refined generated/static artifact secret scan found no token-shaped secrets. Remaining completion blockers are unchanged: real provider-backed OpenRouter LiveKit live voice proof and real LinkedIn publication proof.
- 2026-05-21: Linked Kanban blocker tracking to generated current proof packets. A red foundation vault contract first showed `Agent Studio Kanban.md` did not mention `current-proof-status.md`; the board now links blocked live-voice and publication items to `current-proof-status.md` and records `current-blocker-matrix.json` plus `operator-unblocker-checklist.md` as done evidence. Focused foundation verification passed with `1 passed`.
- 2026-05-21: Updated the active-context Kanban handoff to point at generated current proof packets. A red foundation contract first showed `wiki/ops/active-codex-context.md` had a stale Kanban handoff that omitted `current-proof-status.md`; it now points to `current-proof-status.md`, `current-blocker-matrix.json`, and `operator-unblocker-checklist.md` for the UUID proof workspace. Focused foundation verification passed with `1 passed`.
- 2026-05-21: Added current proof packet links to the main Obsidian MOC and wiki index. A red foundation contract first showed those entry points did not expose `current-proof-status.md`; `Agent Studio MOC.md` and `wiki/_index.md` now link `current-proof-status.md`, `current-blocker-matrix.json`, and `operator-unblocker-checklist.md` for the UUID proof workspace. Focused foundation verification passed with `1 passed`.
- 2026-05-21: Added current proof packet cards to the browser-openable project and system-design vault homes. Red browser/foundation checks first showed the homes omitted `current-proof-status.md`; both now link `current-proof-status.md`, `current-blocker-matrix.json`, and `operator-unblocker-checklist.md` for the UUID proof workspace. Focused browser verification passed with `2 passed`, and focused foundation verification passed with `1 passed`.
- 2026-05-21: Added current proof packet links to the system-design-vault MOC. A red foundation contract first showed `system_design_vault/MOC.md` omitted `current-proof-status.md`; it now links `current-proof-status.md`, `current-blocker-matrix.json`, and `operator-unblocker-checklist.md` for the UUID proof workspace. Focused foundation verification passed with `1 passed`.
- 2026-05-21: Exposed current proof packet names in the generated system-design viewer. Red browser/foundation checks first showed the Objective Completion Audit projection omitted `current-proof-status.md`; `agent-studio-system-design.json` and embedded viewer data now name the status, matrix, and operator checklist packets, and both focused checks passed with `1 passed`. External Gemma/HF and LinkedIn proof blockers remain unchanged.
- 2026-05-21: Added checklist refresh to operator-input retry chains. Red CLI regressions first showed retry commands refreshed readiness, snapshot, proof plan, matrix, and status but not `operator-unblocker-checklist.md`; generator output, UUID packets, and static proof-packet exports now refresh the checklist after status. Focused CLI checks passed with `2 passed`, and static proof-packet browser parity passed with `2 passed`.
- 2026-05-21: Aligned `current-proof-status.md` regeneration order with current-state retry chains. A red CLI regression first showed the status packet shell block regenerated checklist before status; the generator and UUID packet now use matrix -> status -> checklist, and provider-proof CLI coverage passed with `216 passed`.
- 2026-05-21: Aligned `current-proof-status.md` Next Packets order with the same matrix -> status -> checklist contract. A red CLI regression first showed checklist appeared before status; after the generator and UUID packet refresh, focused status, provider-proof CLI, and foundation checks passed.
- 2026-05-21: Added `closure-review-status.json` to the operator checklist Current Gate. A red CLI regression first showed the checklist omitted this gate; the generator and UUID checklist now show closure status beside completion, closure-template, and blocker-state update status. Focused checklist, provider-proof CLI, and foundation checks passed.
- 2026-05-21: Added state-change flags to the operator checklist Current Gate. A red CLI regression first showed the checklist omitted `state_change_allowed` and `goal_completion_claimed`; the generator and UUID checklist now show both as `False`. Focused checklist, provider-proof CLI, and foundation checks passed.
- 2026-05-21: Added completion issue codes and latest failed proof IDs to the operator checklist Current Gate. A red CLI regression first showed the checklist omitted `latest_proof_record_failed` and the failed proof ids; the generator and UUID checklist now expose them. Focused checklist, provider-proof CLI, and foundation checks passed.
- 2026-05-21: Added completion issue codes and latest failed proof IDs to the current proof status Current Gate. A red CLI regression first showed the status packet omitted `latest_proof_record_failed` and the failed proof ids; the generator and UUID status packet now expose them. Focused status, provider-proof CLI, and foundation checks passed.
- 2026-05-21: Added `missing accepted proofs` visibility to `current-proof-status.md` and `operator-unblocker-checklist.md`. Red CLI regressions first showed both human packets omitted the matrix field; both now show `none`, and focused packet, provider-proof CLI, and foundation checks passed.
- 2026-05-21: Added completion and closure evidence refs to `current-proof-status.md` and `operator-unblocker-checklist.md`. Red CLI regressions first showed both human packets omitted the matrix provenance labels; both now show `completion-status.json`, `closure-review-template.json`, `closure-review-status.json`, and `blocker-state-update.json`. Focused packet and provider-proof CLI checks passed.
- 2026-05-21: Propagated completion and closure evidence refs into static proof packets. Red Playwright coverage first showed proof-readiness/boundary-map packets omitted rendered provenance labels; proof-readiness, Gemma voice boundary, and publication boundary exports now match the matrix fields, and focused browser, provider-proof CLI, and foundation checks passed.
- 2026-05-21: Added inline current gate state to static proof packets. Red Playwright coverage first showed proof-readiness/boundary-map packets omitted `Current gate`; proof-readiness, Gemma voice boundary, and publication boundary exports now render completion/closure statuses, state-change flags, issue codes, latest failed proofs, and missing accepted proofs. Focused browser, provider-proof CLI, and foundation checks passed.
- 2026-05-21: Added inline current proof gate state to the generated system-design viewer. Red Playwright coverage first showed Objective Completion Audit only linked packet filenames; the viewer now renders `blocked_by_latest_failed_proof_record`, failed proof IDs, and `state_change_allowed=false`. Focused viewer and foundation checks passed.
- 2026-05-21: Added completion and closure evidence refs to the system-design viewer current proof gate. Red Playwright coverage first showed the viewer gate omitted `completion-status.json` and closure refs; the viewer now renders completion and closure provenance beside the gate state. Focused viewer and foundation checks passed.
- 2026-05-21: Added current-state packet regeneration commands to the system-design viewer. Red Playwright coverage first showed Objective Completion Audit omitted the command block; the viewer now renders matrix, status packet, and operator checklist refresh commands. Focused viewer and foundation checks passed.
- 2026-05-21: Added no-secret operator input blockers to the system-design viewer. Red Playwright coverage first showed Objective Completion Audit omitted the input fields; the viewer now renders HF/Gemma endpoint, LinkedIn token, policy, durable destination, and rollback/postcondition blocker names. Focused viewer and foundation checks passed.
- 2026-05-21: Added required evidence after unblock to the system-design viewer. Red Playwright coverage first showed Objective Completion Audit omitted proof-quality requirements; the viewer now renders live voice same-run Gemma/realtime evidence requirements and publication durable destination/rollback/redaction requirements. Focused viewer and foundation checks passed.
- 2026-05-21: Added the strict operator-input readiness command to the system-design viewer. Red Playwright coverage first showed Objective Completion Audit omitted the copyable `provider-proof-operator-input-readiness --fail-on-blocked` command; the viewer now renders it with the `operator-input-readiness.json` output target, and focused viewer/foundation/hygiene checks passed. External proof blockers remain unchanged.
- 2026-05-21: Added the guarded operator retry sequence to the system-design viewer. Red Playwright coverage first showed Objective Completion Audit omitted the strict-first chain; the viewer now renders readiness, credential snapshot, proof plan, blocker matrix, status, and checklist refresh commands. Focused viewer/foundation/hygiene checks passed.
- 2026-05-21: Added operator-input readiness exit policy to the system-design viewer. Red Playwright coverage first showed Objective Completion Audit omitted strict-mode exit semantics; the viewer now renders default exit code `0`, strict blocked exit code `2`, blocked statuses, and ready status. Focused viewer/foundation/hygiene checks passed.
- 2026-05-21: Added operator-input readiness diagnostics to the system-design viewer. Red Playwright coverage first showed Objective Completion Audit omitted current readiness checked date, evidence ref, effective strict exit code, and issue codes; the viewer now renders those no-secret diagnostics. Focused viewer/foundation/hygiene checks passed.
- 2026-05-21: Added per-proof operator-input routing to the system-design viewer. Red Playwright coverage first showed Objective Completion Audit omitted live-voice/publication route details; the viewer now renders next actions, matrix refs, issue codes, blocked fields, and field groups for each proof route. Focused viewer/foundation/hygiene checks passed.
- 2026-05-21: Added operator-input source artifact refs to the system-design viewer. Red Playwright coverage first showed Objective Completion Audit omitted explicit refs for the matrix, readiness report, and input template; the viewer now renders all three. Focused viewer/foundation/hygiene checks passed.
- 2026-05-21: Added the operator proof packet contract to the system-design viewer. Red Playwright coverage first showed Objective Completion Audit omitted packet schema, value-free handoff contract, and no-state-change guardrail; the viewer now renders those contract fields. Focused viewer/foundation/hygiene checks passed.
- 2026-05-21: Added a system-design viewer parity guard against `current-blocker-matrix.json`. The new regression first failed because the viewer omitted empty per-proof `invalid_fields` and `missing_fields` groups; the projection now matches the matrix routing field groups exactly, and focused viewer/foundation/hygiene checks passed.
- 2026-05-21: Added per-proof route command arrays to the system-design viewer. Parity and browser regressions first failed because Objective Completion Audit routes omitted `next_action_commands` and `guarded_next_action_commands`; the projection now mirrors those arrays from `current-blocker-matrix.json`, the viewer renders both labels, and the focused viewer module passed with `2 passed`.
- 2026-05-21: Added per-proof route command arrays to `current-proof-status.md`. The concrete status regression first failed because the Markdown per-proof readiness block omitted `next_action_commands` and `guarded_next_action_commands`; the generator now renders both command lists for live voice and publication routes, the UUID status packet was refreshed, and the focused current-status test passed with `1 passed`.
- 2026-05-21: Added per-proof route command arrays to `operator-unblocker-checklist.md`. The concrete checklist regression first failed because compact per-proof input readiness omitted `next_action_commands` and `guarded_next_action_commands`; the generator now renders both route command lists, the UUID checklist was refreshed, and the focused checklist test passed with `1 passed`.
- 2026-05-21: Added per-proof route command arrays to generated provider proof workspace READMEs. The workspace README regression first failed because the command-source README omitted a per-proof operator input route-command section; the generator now renders both report-only and strict-first route command lists, the UUID README was refreshed, and the focused workspace README test passed with `1 passed`.
- 2026-05-21: Added per-proof issue codes and field groups to `current-proof-status.md`. The concrete status regression first failed because per-proof readiness omitted route diagnostics; the generator now renders issue-code and field-group blocks beside route commands, the UUID status packet was refreshed, and the focused current-status test passed with `1 passed`.
- 2026-05-21: Added per-proof issue codes and field groups to generated provider proof workspace READMEs. The focused README regression first failed because the route-command section omitted no-secret diagnostics; the generator now renders issue-code and field-group blocks before route commands, the UUID README was refreshed, and the focused workspace README test passed with `1 passed`.
- 2026-05-21: Added a concrete UUID provider proof workspace README freshness guard. The new regression compares the checked-in UUID README to fresh generator output and passed with `1 passed`, protecting the command-source handoff from future drift.
- 2026-05-21: Added top-level retry command arrays to `operator-input-readiness.json`. The focused readiness regression first failed on missing `next_action_commands`; the payload now preserves the exact filled operator-input path through readiness, credential snapshot, proof-plan, matrix, status, and checklist refresh, the UUID readiness report was refreshed, and focused checks passed.
- 2026-05-21: Preserved filled operator-input paths in `current-blocker-matrix.json`. The focused matrix regression first failed because downstream commands could fall back to `operator-inputs.template.env`; the matrix now rebuilds retry commands from the readiness artifact's `input_path`, normalizes `<workspace-root>/...` paths, the UUID matrix was refreshed, and focused checks passed.
- 2026-05-21: Preserved filled operator-input paths in `operator-unblocker-checklist.md`. The focused checklist regression first failed because the compact handoff omitted the filled input path; the checklist now renders validate, strict gate, credential snapshot, proof-plan, matrix, status, and checklist refresh commands from readiness command arrays, the UUID checklist was refreshed, and focused checks passed.
- 2026-05-21: Added top-level retry command arrays to `current-proof-status.md`. The focused status regression first failed because the Operator Input Readiness section omitted report-only and strict-first command arrays; the status packet now renders both, the UUID status Markdown was refreshed, and focused checks passed.
- 2026-05-21: Added the report-only operator retry sequence to the generated system-design viewer. The viewer parity regression first failed on missing `operator_input_retry_sequence`; `agent-studio-system-design.json` and embedded viewer data now match the blocker matrix, the HTML detail renders `Report-only operator retry sequence`, and focused parity/browser checks passed.
- 2026-05-21: Added proof-level blocker status parity to `operator-unblocker-checklist.md`. The concrete checklist regression first failed on missing live-voice/publication status lines; the compact checklist now renders both as `blocked`, matching `current-proof-status.md` and `current-blocker-matrix.json` without exposing secrets or changing proof state.
- 2026-05-21: Added machine-readable proof-capture commands to `current-blocker-matrix.json` and rendered them in `current-proof-status.md`. Red concrete packet regressions first showed A2A agents had to scrape the checklist for after-unblock preflight/proof-record commands; the matrix/status packets now expose those command chains without secrets or state changes.
- 2026-05-21: Mirrored proof-capture commands into `operator_proof_packets`. A red concrete matrix regression first showed packet-first A2A agents still had to dereference the broader proof row; each packet now carries `proof_capture_commands_after_unblock` and `proof_capture_matrix_ref`.
- 2026-05-21: Mirrored packet-level proof-capture commands into the static proof readiness and boundary-map HTML surfaces. The browser regression first failed because rendered proof packets omitted `Proof capture commands after unblock`; after the update, live voice and publication packet browser checks passed with `2 passed`.
- 2026-05-21: Mirrored proof-capture commands into the generated system-design viewer's Objective Completion Audit routes. The route parity regression first failed on missing `proof_capture_matrix_ref` and `proof_capture_commands_after_unblock`; after updating the JSON and embedded HTML viewer, focused JSON and browser checks passed.
- 2026-05-21: Mirrored proof-capture commands into the compact operator unblocker checklist. The concrete checklist regression first failed with zero `proof_capture_commands_after_unblock` labels; after the renderer update and UUID checklist refresh, the focused checklist test passed with `1 passed`. This keeps matrix, status, static, viewer, and checklist handoffs aligned without changing proof state.
- 2026-05-21: Corrected external-publication post-unblock proof capture to acknowledge policy after the operator supplies approval. The focused matrix/status/checklist regressions first failed because `proof_capture_commands_after_unblock` still used `acknowledge_publish_channel_policy=false`; after updating the CLI, UUID packets, static proof packets, and system-design viewer, the focused packet/parity/browser checks passed. General pre-approval discovery commands still use `false`.
- 2026-05-21: Added `proof_capture_commands_after_unblock` to generated provider proof workspace READMEs. The focused README regressions first failed because the command-source README had zero proof-capture sections; after the generator update and UUID README refresh, focused README checks passed with `2 passed`. This keeps the README aligned with matrix/status/checklist/static/viewer proof-capture handoffs.
- 2026-05-21: Added `proof_capture_commands_after_unblock` directly to `provider-proof-plan` proof rows. The proof-plan regressions first failed on missing fields; after the payload update and proof-plan refresh, focused proof-plan checks passed with `2 passed`. This keeps plan-first agents aligned with README, matrix, status, checklist, static, and viewer handoffs.
- 2026-05-21: Added static proof-plan export parity for `proof_capture_commands_after_unblock`. The browser/CLI parity regression first failed because proof-readiness and propagated boundary-map `proof_plan` exports omitted the new field even though `provider-proof-plan` rows carried it; after updating proof-readiness, Gemma voice boundary, and publication boundary HTML, focused static parity passed with `4 passed` and affected browser suites passed with `8 passed`.
- 2026-05-21: Rendered static proof-plan capture chains in the visible detail panes. Browser regressions first failed because `proof_plan` exports carried `proof_capture_commands_after_unblock` but proof-readiness, Gemma voice boundary, and publication boundary details showed the label only once from the operator proof packet. The detail panes now show it in both the proof packet and proof plan, and focused plus adjacent browser checks passed.
- 2026-05-21: Made the system-design viewer proof-capture route label human-readable. The focused viewer regression first failed because Objective Completion Audit routes only rendered the raw `proof_capture_commands_after_unblock` key; the detail now shows `Proof capture commands after unblock` while preserving the raw key for machine reviewers, and the focused viewer suite passed with `2 passed`.
- 2026-05-21: Added review-watch escalation coverage to the Agent Studio feedback loop map. The red browser regression first failed because there was no `Review watch` filter; the map now filters and exports `leibniz-review-watch-escalation` with standing reviewer `019e3899-5ab3-7171-9d3c-32e7c57bbde7`, latest no-Critical/Important status, and the severity/files/next-action escalation contract while keeping no-finding heartbeats quiet. Follow-up vault sync updated Kanban, active context, objective audits, and the system-design HLD; external live voice and publication proof blockers remain unchanged.
- 2026-05-21: The system-design vault entry links now route reviewers to `Project HLD/LLD Review Surface` and `Project System Design Viewer`. Red foundation/browser checks first failed on the missing tracking and links; the vault index, system-design MOC, Kanban, active context, and sprint note now preserve comment export, generated viewer inspection, and source-truth boundaries without changing product UI.
- 2026-05-21: Added system-design source-map viewer browser proof. A red Chromium regression first showed `system-design-source-map.html` did not render its embedded data; the viewer now renders source records, source groups, policy lists, first-slice items, search, kind filter, and coverage-granularity filter, plus design implications, implication search, visible record counts, no-match states, and clickable source-note paths, and tracking/audit notes preserve the planning-memory boundary.
- 2026-05-21: Added project vault-home navigation to the system-design source-map viewer. Red foundation coverage first showed `agent-studio-vault-home.html` lacked `Open System Design Source Map Viewer`; after updating the home and MOC, Chromium follows the route to `Agent Studio Source Map` and verifies 175 source records plus 126 design implications.
- 2026-05-21: Refreshed review-watch heartbeat alignment. Leibniz reported no Critical/Important findings for the latest checked scope, and the Kanban now records `Review-watch heartbeat alignment current` in Review Watch instead of leaving the heartbeat alignment task open in Ready.
- 2026-05-21: Added A2A collaboration graph trace-note redaction. The focused API test first failed because graph trace edge metadata copied token-shaped handoff notes into both response and recorded artifact; Leibniz then found trace actor/action/status plus task-node `latest_handoff_action` also needed the same boundary. `a2a_graph.py` now reuses shared realtime redaction for those graph fields before response or artifact persistence.
- 2026-05-21: Added A2A accepted-message event redaction. The new regression first failed on raw `api_key` inside `/api/a2a/messages` `agent_message_accepted` event payloads; Leibniz then found token-shaped top-level `task_type` / `claimed_by_agent_id` also needed the same boundary. Accepted-message event writers now use the shared public A2A projection so durable A2A events preserve routing metadata while redacting sensitive keys and Bearer/HF/Tavily-shaped strings.
- 2026-05-21: Leibniz fix-back review for A2A accepted-message event redaction reported no Critical/Important findings. Residual risk is limited to future secret shapes not covered by `redact_realtime_string`.
- 2026-05-21: Added A2A status-event redaction. The new regression first failed on raw HF/Bearer-shaped task type and notes values in `agent_message_status_updated` events; API and worker status-event writers now use `public_a2a_status_event_payload` so durable status transitions preserve ids/statuses while redacting token-shaped strings.
- 2026-05-21: Leibniz review for A2A status-event redaction reported no Critical/Important findings. Residual risk is limited to adjacent non-status A2A events outside this slice.
- 2026-05-21: Added A2A retry/dependency event redaction. The new regression first failed on raw HF/Tavily/Bearer-shaped task type and reason values in `agent_message_retry_authorized` and `agent_message_dependencies_repaired` events; Leibniz found adjacent `agent_message_dependency_waiting` and `agent_message_retry_exhausted` leaks, and the fix-back regression first failed there too. Shared A2A control-event helpers now redact those strings while preserving retry counts and dependency ids.
- 2026-05-21: Leibniz fix-back review for A2A retry/dependency event redaction reported no Critical/Important findings. The prior adjacent worker-event issue is closed; retry/dependency control events preserve operational ids/counters/status while redacting token-shaped task, notes, and reason strings.
- 2026-05-21: Added A2A stale-recovery event redaction. The new regression first failed on raw HF/Bearer-shaped task type and previous claimant values in `agent_message_recovered`; `public_a2a_recovered_event_payload` now preserves stale-recovery ids/status/timing while redacting token-shaped task and claimant strings.
- 2026-05-21: Leibniz review for A2A stale-recovery event redaction reported no Critical/Important findings. Residual risk is limited to private `AgentMessage` state outside the public durable control-event boundary.
- 2026-05-21: Added A2A context-packet event redaction. The new regression first failed on raw HF/Bearer-shaped task type and project-memory query values in worker `context_packet_built`; Leibniz then found the `/api/runs/{run_id}/context-packet` writer and the endpoint regression first failed on raw project-memory query values. `public_a2a_context_packet_event_payload` now preserves context summary shape while redacting token-shaped event strings for both writers.
- 2026-05-21: Leibniz fix-back review for A2A context-packet event redaction reported no Critical/Important findings. Private worker context and endpoint response remain full-fidelity while durable `context_packet_built` event payloads are redacted.
- 2026-05-23: Repointed the live dialogue proof handoff to OpenRouter by default. Cursor/LiveKit recovery evidence shows `openrouter-livekit` selected, `voice-runtime-readiness.preflight.json` ready for OpenRouter DeepSeek V4 Flash + LiveKit + Kokoro, and the regenerated current packets have no live-voice operator-input blocker; Hugging Face and Gamma/Gemma are no longer the active realtime default. Completion remains blocked by accepted proof-record capture/recheck for live voice and by LinkedIn credential/policy/destination/rollback evidence for external publication.
- 2026-05-23: Removed superseded UUID auxiliary local-LiveKit/native-Gemma preflight JSON captures from the proof workspace after replacing the current path with OpenRouter LiveKit artifacts. The current canonical files are `provider-readiness.preflight.json`, `voice-runtime-readiness.preflight.json`, and `provider-backed-live-voice-proof.preflight-validation.json`; older `*.local-livekit*` and `*.local-voice-extra*` captures are no longer route inputs.
